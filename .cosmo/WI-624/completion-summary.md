What was done:
3-PR serial delivery routing all in-scope hardcoded English attribute/prop/dialog strings through i18n (findings F-063 / F-064 / F-066, ~270 strings total). PR-A #1008 (merged `2071b31e4`) — session + session-summary + session-transcript cluster, ~80 strings across 13 files. PR-B #1017 (merged `b61c0d79c`) — shelf-book / topic / pick-book / my-notes cluster, ~95 strings across 6 screens incl. the ratchet-invisible config-array surface (HUB_ITEMS, titleForKind, TEACHING_METHODS/COPY age variants, deriveStudyCTA, normalizeSessionType) converted to static-key helpers. PR-C #1033 (merged `934d95e22`, the final landing) — profiles, subscription, quiz, homework camera, create-profile, save-wizard, preview screens, misc components, plus the shepherd-approved absorptions: session-types.ts QUICK_CHIP_CONFIG / CONFIRMATION_BY_CHIP / reconnect-prompt constants converted to runtime key-resolving functions, and 7 template-literal accessibilityLabel interpolations converted to single interpolated keys (amendment 17, no fragment pairs). Children WI-655 / WI-656 ride this parent (swept Closed/Done at the PR-C merge).

What changed:
~55 mobile source files + en.json + 6 machine-translated locales (de/es/ja/nb/pl/pt) + source-baseline.json per PR. All keyed-lookup surfaces use static-key switch/ternary helpers taking `t` (or runtime `i18next.t` in hooks/class components/module functions) — zero dynamic key construction, therefore zero i18n-keep.ts KEEP_PATTERNS entries across all three PRs. `t` added to affected useCallback dependency arrays. Gate-forced copy rewords (documented per PR body): no-clinical-copy guard — "Mark this reply as incorrect"→"Flag this reply as inaccurate", "Wrong subject"→"Off subject" (PR-A); "Something went wrong"→"Something didn't load", "Generation failed"→"Content unavailable" (PR-B); "Decline mentor memory"→"Skip mentor memory" incl. key rename a11yDecline→a11ySkip (PR-C); de/es translation validators — "Retry loading session recap"→"Retry loading the recap", "Skip summary"→"Skip the summary" (PR-A), "photo library access"→"photo access" (PR-C). Out-of-scope leftovers deliberately untouched and flagged: plural-shaped strings (WI-625 surface, partially absorbed by their merged migration mid-stream), brand-fixed animation labels (AGENTS.md brand exception), auth screens (bundle scope), YYYY-MM-DD format token, pure-interpolation labels.

Verification:
Per-PR full local gates: check-i18n staleness/orphans/jsx-literals green; pnpm translate 6/6 locales; tsc green; eslint clean on touched files; full src/i18n jest suite (pl plural-category parity) green; full scripts/ jest suite green where en values changed (297 tests incl. the source-baseline deep-equality test); --findRelatedTests green per PR (334 / 136 / 1575 tests). CI green on the exact merged heads (2a592bb8d→PR-A, 5413e5f79→PR-B after the stale-baseline-hash fix, 83bc6789b→PR-C after the fold round); Claude review APPROVED on all final heads; every Codex/CodeRabbit/Claude finding fixed or dispositioned in-thread (one PR-C reviewer must-fix rejected with recorded rationale: deliberate reuse of the pre-existing translated key subscription.alerts.purchaseConfirmedTitle). Pre-PR adversarial review rounds per PR (max 1 round each; all valid findings fixed). The PR-B stale-baseline-hash CI failure was root-caused (commit-skill reword path wrote stale hashes; my 3-way union merge preserved them) and fixed via the sanctioned pnpm translate regeneration — the lesson is codified in executor-amendments amendment 15.

Caveats / Follow-ups: deferred forward-guard = WI-673 (attribute-literal ratchet extension, per-prop allow/deny model, seed baseline at docs/audit/2026-05-29-full-audit/workflow-1/proposed-baseline.json) — the tracked deferred-sweep record per the cross-cutting DoD; the remaining unswept attribute-literal surface (outside the three PR groups + the config-array/object-literal class these PRs exposed) is noted there; gate-forced copy rewords per PR are listed in each PR body; 3 pre-existing exhaustive-deps warnings in subscription.tsx (poll/mountedRef) pre-date this WI and were left untouched.
