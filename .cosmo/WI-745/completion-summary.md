What was done: Built the repo-local `tech-eduagent-db` skill codifying the MentoMate data-access contract that previously lived only in AGENTS.md prose. Authored as reduce-and-extend against the two existing tech skills — `tech/drizzle-atomicity` (atomic SQL, onConflictDoUpdate, transactions/locks) and `tech/neon-postgres` (connection/branching) are cited by reference rather than repeated.

What changed: Added `.agents/skills/tech/eduagent-db/SKILL.md` (master, trigger-only description) and the synced `.claude/skills/tech-eduagent-db/SKILL.md` (generated via `pnpm sync-skills`, `tech/` GROUP_DIR flattening). The skill extends the two cited skills with four repo-specific rules: (1) `createScopedRepository(profileId)` — when to use vs the parent-chain join pattern, with `apps/api/src/services/session/session-topic.ts` as canonical example (plus session-book.ts / session-subject.ts); (2) `profileId` write-protection as a hard WHERE-clause / ownership-through-parent-chain requirement; (3) migration rollback discipline — the `## Rollback` section requirement plus data-loss accounting; (4) the `db:push:dev` (dev-only) vs `drizzle-kit migrate` (stg/prod) split, including the "a worker deploy does not migrate Neon" rule.

Verification: All cited file:line references verified against the worktree before commit (repository.ts:72-77 createScopedRepository guard; session-topic.ts:21-51 parent-chain join; session-book.ts:36-44 ownership check and :106-122 markSessionFiled write; session-subject.ts:19-44). `pnpm sync-skills` confirmed the flattened copy was created. CI all green on PR #1170; pre-push validation passed (no TS files in delta). Operator approved skill content as-is (prg14-002 gate cleared) and squash-merged to main.

Caveats / Follow-ups: Two Codex P2 content-accuracy items (false "lint-enforced" write-ownership claim ~line 86; TOCTOU-prone child-write example ~line 126) deferred to the Stream 2 skill lean-pointer rework per operator ruling prg14-in-005.
