What was done: Enforced the error-classification boundary (finding F-110) in all 6 flagged mobile screens: every user-facing error display now routes through classifyApiError/formatApiError instead of per-screen instanceof checks. Landed via PR #943 (merged to main, claude review APPROVED with 0 findings after two triage rounds: round-1 SHOULD_FIX fixed + CONSIDER rejected with endpoint evidence; round-2 both SHOULD_FIX fixed and CONSIDER folded in).

What changed: progress/index.tsx (handleRefresh catch → formatApiError); progress/saved.tsx (delete handler + load-error display → formatApiError); dictation/complete.tsx (review-failed + record-result-failed catches → formatApiError); create-subject.tsx (isFirstCurriculumPreparingError: instanceof ConflictError → extractApiErrorCode === 'CONFLICT' plus the narrow curriculum-preparing message test, rawMessage extracted for discrimination only); session/index.tsx (sessionExpired: instanceof NotFoundError → classifyApiError(...).category === 'not-found'). Removed 3 orphaned i18n keys (progress.refreshFailed, progress.saved.errorNetwork, dictation.complete.couldNotSaveDictation) from en.json + 6 locales and regenerated source-baseline.json via scripts/rebuild-source-baseline.ts. New tests: progress/index.test.tsx (new file), session/index.test.tsx +1, create-subject.test.tsx +1, saved.test.tsx +2, dictation/complete.test.tsx +2.

Verification: Six F-110 regression tests, each red-green verified against the pre-fix/reverted code path (RED demonstrated, then GREEN). Full related suites pass (121/121 across 4 suites at final head; 163 related earlier rounds). Mobile tsc --build clean, lint 0 errors, pre-push gates green. CI green on PR #943 at final head 3760c2cab; sweep audit confirms zero remaining instanceof-based error classification in the 6 target screens (one pre-existing documented TODO(typed-error) message-pattern check in create-subject.tsx remains, not an F-110 violation).

Caveats / Follow-ups: The curriculum-preparing detection in create-subject.tsx still discriminates on the 409 CONFLICT message text because the API has no dedicated error code — TODO(typed-error) comments in create-subject.tsx mark both sites; a follow-up could add a CURRICULUM_PREPARING code API-side. GC6 legacy gc1-allow'd internal mocks in session/index.test.tsx remain documented burn-down backlog. The classifier maps 403 SUBJECT_INACTIVE/SUBJECT_PAUSED to 'not-found'; harmless for the transcript endpoint today (it only returns 200/404) but worth knowing if that endpoint grows new error codes.
