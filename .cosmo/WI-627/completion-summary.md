What was done:
Fixed the four date-handling findings of bundle WP-L12-dates-locale in one PR (#993). Formatting class (F-067/F-072): replaced all 3 remaining `toLocaleDateString('en-US', …)` hardcodes with locale-aware formatting driven by the active i18n locale — pure helpers now accept a `locale` parameter and callers pass `i18n?.language`, so dates render in the user's UI locale instead of US English everywhere. Day-boundary class (F-177/F-178): replaced UTC-based localDate computations (`toISOString().slice(0,10)` in both dictation screens; `completedAt.slice(0,10)` grouping in quiz history) with a new `toLocalDateString()` helper that uses local Date component getters. One deliberate documented convention: all client-side local-date strings use the DEVICE LOCAL timezone, never UTC slices.

What changed:
11 files in the main fix + 2 hardening rounds. New: `apps/mobile/src/lib/local-date.ts` (the helper) + `local-date.test.ts` (midnight-boundary tests). Modified: `lib/format-note-source.ts` (locale param), `child/[profileId]/session/[sessionId].tsx` (formatDate locale param), `topic/[topicId].tsx` (formatSessionDate + formatBookmarkSourceLine locale params), `shelf/[subjectId]/book/[bookId].tsx` (caller), `dictation/complete.tsx` + `dictation/review.tsx` (localDate via helper), `quiz/history.tsx` (local-date grouping key), `quiz/history.test.tsx` (F-178 boundary test), `test-utils/mock-i18n.ts` (i18n.language added to shared stub). Round 2 (review SHOULD FIX): made the F-178 screen test non-vacuous — asserts the exact Date passed to the helper AND the rendered section header shows the local day (Apr 29) with the UTC day (Apr 30) absent; spy delegates non-boundary calls to the real implementation. Round 3 (amendment-2 hygiene): stripped the finding-ID token and self-reference from source comments.

Verification:
Red-green at BOTH levels. Helper level: with the buggy `toISOString().slice(0,10)` body, the midnight-boundary test fails (received '2026-04-30', expected local '2026-04-29' for a simulated UTC-1 user) — restored fix passes 4/4. Screen level: temporarily reverted ONLY the quiz/history.tsx grouping line back to `completedAt.slice(0,10)` (helper kept) — the F-178 test fails ('Number of calls: 0' plus header mislabel); restored fix passes 8/8. Sweep-verified: 0 `en-US` toLocaleDateString hits, 0 UTC-slice localDate hits remaining in production code. 817 related tests / 42 suites green; tsc clean; i18n orphan-keys and jsx-literals checkers clean. Three CI rounds green on exact heads a2fe451a8 → 2cf58d893 → 7879f337f; Claude Code Review APPROVED; zero open Codex/CodeRabbit threads. Merged to main as 1f5574e631276d01146fa55634a918f25cefb3df (PR #993).

Caveats / Follow-ups:
Deliberate behavior change: streak/quiz day grouping shifts for users at non-UTC offsets near midnight — that is the CORRECT new behavior (previously days bucketed by UTC). Comment-hygiene round folded into the PR (no residue). Children WI-659/WI-660 are absorbed provenance swept with the merge. No deferred work.
