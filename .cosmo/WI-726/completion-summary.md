**What was done:** Moved `dispatchSessionCompletedEvent` out of `apps/api/src/routes/sessions.ts` into the service layer at `apps/api/src/services/session/session-filing-dispatch.ts` (peer of `dispatchClosePathAutoFileIfEligible`), resolving the G1/G5 route/service-boundary violation. No behavior change — pure structural move.

**What changed:** `session-filing-dispatch.ts` gains the function with an explicit `Promise<{ pipelineQueued: boolean }>` return type plus the imports it now needs (`getSessionCompletionContext`, `captureException`, `createLogger`); the core-send annotation and Inngest idempotency-key rationale are preserved verbatim. `routes/sessions.ts` drops the ~97-line local definition and the now-unused `getSessionCompletionContext` import, importing `dispatchSessionCompletedEvent` from the `../services/session` barrel; all 3 call sites (close, summary, summary/skip) resolve through the import. `services/session/index.ts` re-exports the function from the filing-dispatch block. `routes/sessions.test.ts` extends the existing `session-crud` gc1-allow mock block to also stub `getSessionCompletionContext` — the move exposed a test gap because the service file imports that helper directly from `./session-crud`, not through the barrel, so the barrel mock alone left 4 tests failing.

**Verification:** `pnpm exec nx run api:lint --skip-nx-cache` = 0 errors, 2 pre-existing warnings (G1/G5 clean, the core AC); `pnpm exec nx run api:test --skip-nx-cache` = 6781/6781 tests pass across 338 suites; `pnpm exec nx run api:typecheck --skip-nx-cache` clean; pre-push hook ran 1156 findRelatedTests — all green. PR #1152 squash-merged to main (landed `be9eb335f`); strict green gate passed, triage clean (0/0 findings, 0 inline).

**Caveats / Follow-ups:** none.
