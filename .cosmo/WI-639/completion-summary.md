What was done: Logged/escalated the silent-failure catch blocks and restored webhook error context for audit findings F-022, F-047, F-048, F-049 (PRG-15 / API Error Handling, first unit). Scope held to signal emission + error context only — no user-facing behavior change, no typed-error redesign (deferred to WI-640). Verified each audit site against current main first: several F-022 sites named in the 13-day-old audit were already fixed on main (billing.ts/family.ts timezone catches, revenuecat handleNonRenewingPurchase, session-exchange prior-summaries) and were recorded as already-fixed dispositions rather than re-worked. Landed via PR #948 (merge f54dd3c0).

What changed: apps/api/src/services/session/session-crud.ts — parseTopicIntentMatcherResponse bare catch now logs (event session.topic_intent_matcher.parse_error) + captureException, still returns null. apps/api/src/services/billing/family.ts — listFamilyMembers and downgradeAllFamilyProfiles subscription-not-found paths now emit logger.warn + captureException (billing escalation rule). apps/api/src/routes/dictation.ts — struggle-fetch catch logs (event dictation.struggle_fetch_failed) + captureException, degradation preserved. apps/api/src/services/consent.ts — resendConsent rollback catch logs with event consent.resend.rollback_failed, best-effort semantics preserved. apps/api/src/routes/stripe-webhook.ts — signature-verification catch logs structured reason (no secrets/PII; no per-event Sentry to avoid probe alert-storms). apps/api/src/routes/resend-webhook.ts — invalid-signature warn now carries structured context (event, webhookId, webhookTimestamp). Plus regression tests in the six matching test files.

Verification: 7 regression tests added (one per fixed path), each demonstrated RED against pre-fix code (spy received 0 calls / unstructured args) then GREEN after the fix — evidence recorded in commit messages. All six touched suites green (300 tests), api typecheck + lint clean, pre-push hooks passed. CI green on every head; Claude Code Review final verdict APPROVED (0 blocking); both interim CHANGES_REQUESTED rounds (billing captureException + tests; consent event field + misplaced gc1-allow comment) were fixed and re-validated. GC6: no new internal mocks introduced (new spy points use jest.spyOn); pre-existing gc1-allow mocks in touched test files recorded as deferral in commit messages (dictation 3, resend-webhook 2, stripe-webhook 5, family 1).

Caveats / Follow-ups: (a) The reviewer's non-blocking consider to add rate-limited Sentry escalation for repeated stripe-webhook signature failures was deliberately not folded in — shepherd is capturing it as an incidental WI. (b) CodeRabbit was rate-limited (org out of usage credits) for part of this PR's life and produced no review threads. (c) Absorbed-provenance children WI-642/WI-643 rode this PR and were swept by the shepherd at merge.
