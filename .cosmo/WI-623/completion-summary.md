REWORK REFRESH — adds the dod.cross_cutting_sweep guard (PR #1006) to the original summary; the work landed in TWO PRs: #997 82dc84c9f7f18ba830045d5f168c558b04b046b7 (functional sweep — Fixed In) and #1006 bec39899fbb53304b11dea7ebec54263402e8776 (forward-only guard).

What was done:
Two passes over the mobile component surface in PR #997, fixing F-052, F-070, F-055, F-057, F-058, plus a follow-up forward-only guard in PR #1006 closing the reviewer's dod.cross_cutting_sweep gap. Modal pass: added accessibilityViewIsModal to all 15 Modal instances across 14 files (the audit's "13 modals" undercounted — SessionModals.tsx holds 2), so VoiceOver focus is now trapped within every modal on iOS; Android back/dismiss already routed through onRequestClose everywhere. Semantics pass: (F-055) added accessibilityLabel via t() to 6 TextInputs that had only placeholder text — ProfileBasicsStep x4 (your name / your birth year / child's name / child's birth year), create-profile display-name, FeedbackSheet message; (F-057) added accessibilityRole="button" to 8 Pressable groups missing it — TopicSwitcherModal topic rows, ParkingLotModal save, library.tsx manage-modal status buttons (pause/archive x2/resume/restore/delete), MetricInfoDot backdrop, ProfileSwitcher backdrop, account.tsx language-picker backdrop + option rows (option rows also gained accessibilityState={selected}), create-profile date-picker done; (F-058) NudgeBanner badge View hidden from the a11y tree (accessibilityElementsHidden + importantForAccessibility) with the state surfaced on the parent Pressable's accessibilityLabel composed of sender + nudge message + count (Codex P2 round on #997: the initial label dropped the message text; fixed so SR users hear the full content). Also: dictation/playback backdrop hardcoded "Dismiss" routed to new t('common.dismiss'); NudgeUnreadModal dismiss button labeled. Guard (PR #1006): apps/mobile/src/components/modal-a11y-guard.test.ts — forward-only guard mirroring the persona-fossil-guard pattern (source-text scan over git ls-files); scans every production apps/mobile/src .tsx file for <Modal ...> JSX opening tags with quote-aware, brace-depth-aware tag extraction (a '>' inside a quoted attribute value or a prop expression cannot truncate the tag), requires an attribute-boundary prop token (quoted attribute values stripped first so the prop name inside another attribute's string value cannot satisfy the match), and rejects a literal accessibilityViewIsModal={false} opt-out — failing with a file:line list and a message citing the rule (modal must trap SR focus), not the WI.

What changed:
PR #997 components: SessionModals.tsx, NudgeUnreadModal.tsx, NudgeActionSheet.tsx, NudgeBanner.tsx (+ test), FeedbackSheet.tsx, LearnTogetherSheet.tsx, MetricInfoDot.tsx, ProfileSwitcher.tsx, TopicPickerSheet.tsx, profiles.tsx, create-profile.tsx, quiz/play.tsx, library.tsx, more/account.tsx, dictation/playback.tsx, save-wizard/ProfileBasicsStep.tsx. PR #1006: apps/mobile/src/components/modal-a11y-guard.test.ts (new file, 6 tests). i18n: 9 new keys in en.json — common.dismiss; saveWizard.yourNameLabel / yourBirthYearLabel / childNameLabel / childBirthYearLabel; nudge.banner.bannerLabel / bannerLabelSingle (sender + {{message}} + {{count}} composition, with no-count companion per the interpolation-fallback rule); plus common.goBackAction / tryAgainAction arrived via rebase union-merge. All keys machine-translated to all 7 locale files (de/es/ja/nb/pl/pt + en source); {{message}}/{{fromDisplayName}}/{{count}} placeholders programmatically verified present in every locale. No plural keys added. Rebase onto origin/main (WI-621 PR #985 + WI-627 PR #993 merged underneath) resolved as locale-JSON union merges.

Verification:
PR #997: two CI rounds green on exact heads — 4af335561 (initial) and 6ce3c8b5b (rebase + Codex P2 fix); Claude Code Review APPROVED both rounds; Codex P2 (banner label lost nudge message) fixed with in-thread SHA cite; component tests assert the label carries BOTH the message text and the count (badge case) and sender + message (single case); NudgeBanner suite 69/69; full related-test run 67 suites / 1406 tests green; full src/i18n locale-parity suite 19/19; i18n staleness + orphan-keys + jsx-literals checkers green; mobile typecheck green. Merged as 82dc84c9f7f18ba830045d5f168c558b04b046b7. PR #1006 (guard): three review rounds, every change red-green verified — round 1 (head 14b0281c6): with accessibilityViewIsModal temporarily stripped from NudgeUnreadModal.tsx the guard failed citing apps/mobile/src/components/nudge/NudgeUnreadModal.tsx:17, restored green 3/3; round 2 (head 011094710, Codex P2 + Claude consider): new ={false}-rejection test RED against the existence-only predicate (1 failed / 3 passed), GREEN after the tagViolates fix (4/4); round 3 (head 4a808d153, two CodeRabbit Majors): quoted-'>' tag-extraction test and string-value substring-rejection test both RED against the prior scanner (2 failed / 4 passed), GREEN after quote-state tracking + strip-then-boundary matching (6/6). Final gate APPROVED, zero open threads; merged as bec39899fbb53304b11dea7ebec54263402e8776. Sweep audit: rg '<Modal' apps/mobile/src -> 15 hits, all 15 carry accessibilityViewIsModal, now enforced forward-only by the guard.

Caveats / Follow-ups:
Roles/labels half (F-055/F-057/F-058) has NO mechanical guard — deliberately: whether a given Pressable needs accessibilityRole/accessibilityLabel is contextual judgment (backdrop dismiss targets vs list rows vs deliberate accessibilityRole="none" wrappers each have different correct annotations), so any scanner would be either trivially noisy or vacuous; per the Fix Development Rules "or equivalent" clause the modal guard + this documented reasoning satisfy the sweep obligation, and role/label coverage stays a review-time concern. Guard limitation (documented in its header): a non-literal accessibilityViewIsModal={someCondition} is accepted — no static judgment of expressions; a literal ={false} is rejected. Hardcoded EXISTING English label strings were deliberately left for WI-624 (WP-L12-label-prop-strings) per the bundle scope rule — specific sites spotted: TopicPickerSheet.tsx ("Choose a topic" title, "Close topic picker" backdrop label; no useTranslation at all), ProfileSwitcher.tsx ("Close profile switcher", ", active" suffix in menu-item labels), create-profile.tsx ("Close date picker", "Enter your child's name"/"Enter name" placeholders, "Child's birth date"/"Birth date" web-input label, "Select child's birth date"/"Select birth date"), profiles.tsx ("Profile name" input label, "Name" placeholder, "Save" button label), ProfileBasicsStep.tsx placeholders ("Your name", "Birth year (e.g. 1985)", "Their name or nickname", "Birth year"), sign-in.tsx ("Enter backup code"/"Enter 6-digit code" placeholders, "Verify" button, "Signing you in" ActivityIndicator label). No device VoiceOver/TalkBack verification was possible headless — accessibilityViewIsModal behavior is asserted via props/tests only; recommend a one-pass manual SR sweep when a device QA window opens.
