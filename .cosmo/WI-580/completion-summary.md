What was done: Closed F-076 (child's real first name sent to third-party LLM providers in every exchange). The exchange path no longer sends a real minor name to any LLM provider: two thin fail-closed gates were added, both driven by a new conservative shared age predicate `isUnambiguouslyAdult(birthYear, currentYear?)` promoted into `@eduagent/schemas` (`packages/schemas/src/age.ts`, next to `isAdultOwner`) — `birthYear < currentYear - 18`, i.e. the ambiguous birth-year boundary (learner may still be 17) is treated as minor. PR #900, 3 review rounds (Codex P1 boundary-conservatism accepted + fixed; Claude SHOULD FIX schemas-promotion accepted + fixed; both CONSIDERs folded as documentation), merged to main as 582201b0.

What changed: (1) `apps/api/src/services/session/session-exchange.ts` — new exported `resolvePromptLearnerName` with an explicit fail-closed ladder (must be owner, must have known birthYear, must be unambiguously adult) now gates `learnerName` in `prepareExchangeContext`; (2) `apps/api/src/services/exchange-prompts.ts` — defense-in-depth egress guard: `buildSystemPrompt` only interpolates a learner name when `isUnambiguouslyAdult(context.birthYear)` holds, so a future caller bypassing the construction gate still cannot leak a minor's name; (3) `packages/schemas/src/age.ts` — canonical home of the conservative predicate with docstring distinguishing it from the calendar-year `isAdultOwner`/`computeAgeBracket` (which are unchanged); (4) tests: new `session-exchange-pii.integration.test.ts`, unit matrices in `session-exchange.test.ts`, builder break tests in `exchanges.test.ts`, schemas tests in `age.test.ts`. Minors get a name-free prompt (no literal placeholder pseudo-name); the existing ANTI-FABRICATION block forbids the model inventing one.

Verification: Red-green break-test evidence at all three levels — (a) integration break test on the real exchange path (seeded profiles → `prepareExchangeContext` → `buildSystemPrompt`, the exact provider-bound string) verified RED against pre-fix code for the minor-owner and non-owner-child cases, then green after the fix, 4/4 green vs stg DB including the boundary-year owner case; (b) unit matrix for `resolvePromptLearnerName` (7 cases incl. boundary-as-minor and fail-closed on ownership/unknown birth year); (c) builder-level break tests (minor and boundary-year names passed by a caller are dropped; adult-owner personalization pinned as preserved). Schemas tests pin the intended divergence from `isAdultOwner` at the boundary. Full local gates green each round: schemas test+lint, api typecheck+lint, 51 related suites / 1469 tests, `pnpm eval:llm` Tier 1 exit 0 with zero snapshot drift. PR #900 CI green on head ad08b8c7f (API Quality Gate, main, changes, Playwright smoke, claude-review APPROVED, CodeRabbit pass); all reviewer findings triaged with in-thread dispositions.

Caveats / Follow-ups: (1) Accepted trade-off — 18-year-olds whose birthday has already passed this calendar year lose name personalization until Jan 1 (conservative boundary; birth-year-only data). (2) Intentional — adult non-owner profiles also get no name (fail-closed on ownership; no consent surface of their own). (3) Scope — this Item covers the LLM-provider prompt egress surface only; the sibling W3 units (WI-577 event payloads, WI-578 step state, WI-579 error logging) own their surfaces, and no shared scrubber module existed on main to adopt — if one lands, this surface can adopt it as a follow-up. (4) Gate-gap candidate worth capturing as a WI: the pre-commit eval-snapshot guard cannot pass for a verified zero-drift prompt change (requires a staged snapshot file that doesn't exist); its documented `--no-verify` escape was used three times in this PR with `pnpm eval:llm` verified exit-0/zero-drift each time and lint-staged run manually — the hook should learn a "harness ran, zero drift" receipt.
