What was done: Fixed F-163 — the learning-preferences screen was showing the parent's accommodation mode when viewed in child mode (`childProfileId` query param present). Swapped the unconditional `useLearnerProfile()` call to use `useChildLearnerProfile(childProfileId)` when in child mode, mirroring the pattern already established in `accommodation.tsx`. Added a TDD regression test that sets distinct parent (`short-burst`) and child (`none`) accommodation modes and asserts the child's mode is rendered.

What changed: `apps/mobile/src/app/(app)/more/learning-preferences.tsx` — import `useChildLearnerProfile`; replace single `useLearnerProfile()` call with conditional hook selection (`isChildMode ? childQuery : selfQuery`). `apps/mobile/src/app/(app)/more/learning-preferences.test.tsx` — add `childModeRoutes()` helper that registers separate `/learner-profile/child-1` and `/learner-profile` mock routes; add F-163 regression test under the existing child-mode describe block. Test waits for `findByText('None')` (child's rendered label) before asserting `Short-Burst` (parent's label) is absent.

Verification: TDD red-green confirmed — test written first, confirmed failing against the bug, then confirmed passing after fix. `pnpm exec tsc --noEmit` and `pnpm exec tsc --build` both clean. i18n orphan check + staleness check: no findings. All 6 tests in the suite pass; 37/37 in related `use-learner-profile.test` suite pass. PR #874 CI: 7/7 checks pass (`main`, `API Quality Gate`, `claude-review`, `Playwright web smoke`, `changes`, `CodeRabbit`, `run-smoke` skipped). Fixed commit: `0bf98b9fe`. One Codex P2 review finding (test assertion timing) addressed in follow-up commit `0bf98b9fe`; reply posted in PR thread.

Caveats / Follow-ups: None. The fix is surgical (2 files, no API/schema/i18n changes). The `useChildLearnerProfile` gating uses `isOwner === true` inside the hook, which prevents non-owners from accidentally fetching child profiles via this path.
