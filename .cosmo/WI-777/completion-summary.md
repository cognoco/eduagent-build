**What was done:** Closed WP-8's v2-path test-coverage gap for the three Inngest scan functions (`monthly-report-cron`, `daily-reminder-scan`, `review-due-scan`). The production v2 wiring itself had already landed on `main` via CUT-B2 (`9ab1076ec` daily-reminder-scan + review-due-scan; `8bf67fc54` monthly-report-cron) — both confirmed ancestors of `origin/main`, predating the WP-1 enumeration whose branch-detection missed the `isIdentityV2EnabledInStep()` helper. This WI added the missing forward-only guard tests so the v2 wiring is protected against regression before WP-FLAG drops the legacy tables.

**What changed (3 test files):** Added 6 v2-path wiring guard tests (test-only diff, no source change). Per scan: a flag-on test asserting the v2 branch reads the canonical model (`review-due-scan`/`daily-reminder-scan` root the query on `person` not `profiles`/`consentStates`; `monthly-report-cron` derives pairs from the `guardianship` edge not `familyLinks`) plus a flag-off test asserting the legacy path stays intact. Each sets/restores `IDENTITY_V2_ENABLED` in try/finally so the assertion is robust to ambient flag state. Files: `apps/api/src/inngest/functions/{daily-reminder-scan,review-due-scan,monthly-report-cron}.test.ts`.

**Verification:** Red-green confidence per scan — temporarily disabling each source v2 branch makes the flag-on test fail (legacy table read instead) while flag-off still passes, confirming the guards are non-vacuous; source restored with no source diff. All three suites green with the flag cleared (CI state): 91 unit tests (was 85); full inngest suite 66 suites / 941 tests green; `api:typecheck` green; `api:lint` green (0 errors); no new internal `jest.mock` (GC1/GC6 clean). PR #1187 merged via squash (`bfd16f0bb598152f0c8ac71a5c62d9ff65de4422`); Gate-1 cleared 4/4 required green, claude-review APPROVED, merged on UNSTABLE for the ambient run-smoke only (staging post-DROP, WP-9 — not PR-caused).

**Caveats / Follow-ups:** Source files were untouched (already correct on main); all three functions remain registered in `apps/api/src/inngest/index.ts` `functions[]` (cron/event dispatch verified); the `review-due-scan.integration.test.ts` legacy-table sibling and the local `.env.development.local` flag-on harness divergence from CI are WP-FLAG / env-hygiene concerns, not this WI's.
