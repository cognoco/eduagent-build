What was done:
Twinned `getUsageBreakdownForProfile` onto the CUT-B2 guardianship reader so the family-aggregate usage breakdown no longer reads the legacy `family_links` table on the v2 path. The v2 twin reads guardianship via `services/identity-v2/guardianship.ts` (no duplicated family-edge logic) joined with `usage_events`, dispatched at the billing route by the `IDENTITY_V2_ENABLED` flag exactly like the other billing-v2 seams, and is byte-identical to legacy when the flag is off. This is the last billing-REST function CUT-B3 (WI-693) left on legacy; it is a hard convergence precondition before the WI-586 flip. Delivered on PR #1141, squash-merged to main as `ef19b1eb0`.

What changed:
- apps/api/src/services/billing/billing-v2/family-usage-v2.ts (new): the v2 twin `getUsageBreakdownForProfileV2`. Enumerates org members via `person × membership` scoped to the subscription org (the v2 image of `profiles WHERE account_id = …`), aggregates `usage_events` in-process, and derives the family-edge booleans from the CUT-B2 guardianship reader. The post-review fix intersects the global guardian/charge ids with the non-archived same-org `orgMemberIds` set before deriving `hasChildLink`/`isChild` (lines 154-158), restoring byte-for-byte equivalence with the legacy account-scoped `family_links` predicate and closing a cross-org over-grant.
- apps/api/src/services/billing/billing-v2/index.ts: export the twin.
- apps/api/src/routes/billing.ts: flag-dispatch the call site via the same `v2 ? fnV2 : fn` ternary CUT-B3 uses for every other billing-v2 seam (replacing the `[CUT-B3 / WI-693] NOT dispatched` deferral comment).
- apps/api/src/routes/billing.test.ts: flag-off test asserting the LEGACY function is invoked (verified, not assumed).
- apps/api/src/services/billing/billing-v2/family-usage-v2.test.ts: v2 unit test.
- apps/api/src/services/billing/billing-v2/family-usage-v2.integration.test.ts: semantic-equivalence test seeding one real family into both stores and deep-equaling the v2 breakdown vs legacy across all four viewer perspectives, plus an adversarial seed (co-parent with an out-of-org guardianship edge and no in-org link) that isolates the org-scope fix.

Verification:
Red-green proven for the post-review org-scope fix: the adversarial cross-org integration seed FAILS with the intersection reverted and PASSES with it, while the other four equivalence cases stay green (5/5 against the staging DB). The v2 module imports no `familyLinks` — verified the v2 path no longer reads `family_links`. api typecheck + lint clean; v2 unit + billing route tests green; pre-commit + pre-push hooks passed. Codex P2 ("Filter guardianship edges to the subscription org") was triaged, confirmed real, and fixed in `d58893cab`; no new findings on the fix commit. Deterministic CI green on the merged head (main, Playwright web smoke, API Quality Gate, Merge completeness); claude-review advisory ran clean.

Caveats / Follow-ups: Flag-gated — `IDENTITY_V2_ENABLED='false'` in every deployed environment, so the v2 path is flag-off-inert and legacy remains the sole live store; no production behavior changes until the WI-586 convergence flip, for which this WI is now a hard `Blocked-by` precondition. No schema change, no migration. A full flag-on route test was intentionally not added (it would drag in the CUT-B1/B2 v2 identity middleware, a separate seam with its own tests); the flag-on path is proven at the service-unit + integration altitude instead, documented in the test comment.
