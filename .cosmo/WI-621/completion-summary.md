What was done:
WP-L12-jsx-strings delivered in full via 3 serial PRs off main (shepherd-ruled split): PR A #942 (merge 684495a2c) — auth + onboarding + shared components, 89 strings; PR B #961 (merge e382a95e6) — session + home + subscription + save-wizard, 138 strings; PR C #985 (merge 51452269c) — library + progress + my-notes + pick-book + quiz + topic/relearn + preview + child reports, 122 strings. The jsx-literals ratchet baseline burned 361 → 272 → 134 → 12. The 12 remaining entries are documented, justified retentions of genuinely non-translatable copy: 4 brand wordmark splits (ment/mate in AnimatedSplash + MentomateLogo), 4 dev-only seeding strings (app/dev-only/seed-*.tsx), the isE2E-gated "Pick from gallery (E2E)" affordance, the MentoMate speaker label (brand token), and 2 __DEV__-only debug strings ("Debug details (screenshot this)" in ErrorBoundary, "RUNG" in ChatShell). Findings resolved: F-062 (auth screens, fully — PR A), F-026 (mode-switch error row, fully — PR A), F-061/F-069 (multiline/aggregate hardcoded strings — across all 3 PRs), INV-1 (dispositioned as baseline burn-down per the slice ruling; the ratchet pre-existed).

What changed:
Per PR: JSX text-children literals routed through t(), screen-scoped en.json keys added, and all 6 target locales regenerated via pnpm translate in the same PR. Split-sentence fragment keys were consolidated into single full-sentence keys with interpolation/Trans across 5 families in PR A (Codex P2 + mandated sibling sweep: sentCodeTo ×2, offerBody, agreeToTerms, enterCodeAndPassword) and applied as a standing rule from PR B onward. PR C review rounds added: per-kind myNotes keys (replacing English-noun interpolation that produced hybrid-language copy), the finishedBody plural family, and a translate-pipeline fix so locale-specific CLDR plural variants survive — the family rule ("a target key with a plural suffix is legitimate iff en carries any member of the same base-key plural family") enforced at all 3 deletion/validation sites (diff-mode prune filter, write-path merge, validateTranslation extra_key), with 7 regression tests plus a live write-proof recorded on the PR. Polish _few/_many forms hand-authored for the 4 plural families this WP introduced (overdueTopicCount, daysOverdue, sessionCount, messageCount; finishedBody carries the full set), runtime-verified via i18next for counts 1/2/5/22 → one/few/many/few.

Verification:
Each PR passed the full local gate before push — check:i18n:jsx-literals (0 new), check:i18n:orphans, check:i18n staleness, keep-rot, mobile tsc, and jest --findRelatedTests on every touched file (PR A: 1388 pass; PR B: 528 pass; PR C: 476 pass; fix rounds: full apps/mobile/src/i18n suite 19/19 and scripts suite 42/42) — plus a pre-PR adversarial review per PR (verdicts clean). CI green on the exact merged heads (b6443ba0b / 13566d7cf / 287e67c2a); Claude review APPROVED on all three final heads (the 2 advisory claude-review reds were WI-378-class crashes whose reruns produced fresh APPROVED, not findings). Every Codex/CodeRabbit/Claude finding was either fixed with in-thread fix-SHA dispositions (PR A: split-sentence consolidation; PR C round 1: pl plurals + parity-test + count-param rename; round 2: per-kind myNotes + finishedBody; round 3: translate write-path fix with survival proof) or shepherd-dispositioned (titleForKind deferral, later escalated and fixed in round 2). Red-green evidence: the translate-pipeline regression tests' red state is the empirically recorded incident (the pl forms were deleted twice on PR #985's history — the guarded functions were newly extracted, so the historical double-deletion is the red evidence, per the PR comment).

Caveats / Follow-ups:
The 12-entry baseline is the floor (all justified retentions, not debt); 36 pre-existing _one/_other plural families still lack pl few/many → WI-625 (WP-L12-pluralization), which inherits the now-fixed pipeline rule + regression tests; the titleForKind/HUB_ITEMS English dynamic-dispatch surface (ratchet-invisible) → follow-up capture riding WI-624's review; the prune-only translate path is blocked for pl by 13 pre-existing glossary violations (sesja/biblioteka/quiz/dyktando/seria locked terms) — latent finding flagged for capture, related WI-625; JSX attribute literals (accessibilityLabel/placeholder/label/title) remain WI-624's surface by design.
