**What was done:** Fixed the P1 supportee self-unlink failure. Root cause: `requestSelfUnlink` in `apps/api/src/services/supportership-revocation.ts` wrote a support_link_ended visibility notice whose payload carried graceEndsAt, while `supportLinkEndedPayloadSchema` in `packages/schemas/src/visibility-contract.ts` requires graceDays — so the post-insert validation in `apps/api/src/services/visibility-moment-projections.ts` threw inside the revoke transaction, rolling back the entire revoke and failing the route. Per the ruled Option A, the schema-invalid synchronous notice write was deleted entirely; the Inngest function `apps/api/src/inngest/functions/supportership-revocation.ts` remains the sole producer of the notice (schema-correct payload, written after the grace window).

**What changed:** `apps/api/src/services/supportership-revocation.ts` — removed the createVisibilityNotice call from the transaction and the import it orphaned; the audit event and the RevocationNotice return value are unchanged. `apps/api/src/services/supportership-revocation.integration.test.ts` — added a happy-path integration test (real database, no internal mocks) asserting the supportee revoke resolves, sets supportership revokedAt plus contract status revoked, and writes NO synchronous support_link_ended notice; retired the now-unreachable notice-insert fault-injection variant and narrowed the failure-mode union accordingly.

**Verification:** Red-green proven: the new happy-path test fails on the pre-fix code (validation throw and rollback) and passes after the deletion — evidence recorded on PR #1854. All required CI checks were green on the final head; the automated review check ran and returned APPROVED with zero must-fix and zero should-fix findings; an independent adversarial review also returned no blocking findings. PR #1854 was squash-merged to main with merge state CLEAN.

**Caveats / Follow-ups:** Two optional hardening suggestions from review were declined as out-of-scope for this surgical fix (both pre-existing coverage gaps, not introduced by the diff: the ghost-edge missing-contract branch, and a rollback case for the write that now sits last in the transaction). Product behavior note: access cutoff remains immediate on revoke; the supporter notice arrives only after the grace window via Inngest — unchanged, by ruled design.
