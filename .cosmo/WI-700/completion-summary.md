What was done: Added schema/bounds validation at the route + DTO boundary for five security findings (F-142, F-158, F-166, F-179, F-180) so malformed or oversized input is rejected as 4xx before reaching handlers — never an unhandled 500, OOM, or CPU-DoS. Landed as one PR (#1111).

What changed: F-142 — questionResultSchema.answerGiven capped at .max(1000) and completeRoundInputSchema.results capped at .max(10) (max questions per round) in packages/schemas/src/quiz.ts. F-158 — parseHomeworkProblems now validates the untrusted deep-link payload with homeworkProblemSchema.array().safeParse() before trusting it (apps/mobile/src/components/homework/problem-cards.ts); import changed from type-only to value import. F-166 — zValidator('param', subjectIdParamSchema) added to GET/PUT/POST/PATCH /subjects/:id handlers that lacked UUID validation (DELETE already had it); all now use c.req.valid('param').id instead of c.req.param('id') (apps/api/src/routes/subjects.ts). F-179 — questionCheckInputSchema.answerGiven capped at .max(1000) to bound the O(m×n) Levenshtein routine in isGuessWhoFuzzyMatch. F-180 — dictationSentenceSchema.chunks and .chunksWithPunctuation capped at .max(100) in packages/schemas/src/dictation.ts.

Verification: New negative-path tests per finding assert 4xx/empty-fallback on malformed/oversized input — quiz.test.ts (F-142, F-179), dictation.test.ts (F-180), problem-cards.test.ts (F-158), subjects.test.ts (F-166, 4 cases across GET/PATCH/POST/PUT). Pre-existing subjects-language-setup.test.ts updated to use a valid UUID (its non-UUID 'subject-1' is now correctly rejected by the new guard). All touched suites green locally; typecheck green for schemas/api/mobile; pre-push validation passed (14 files). PR #1111 CI all-green required checks; squash-merged to main as 6fea5bc5.

Caveats / Follow-ups: Absorbed children WI-707 and WI-708 are provenance only and are bulk-closed by the shepherd when this WP closes; the only PR red was the advisory claude-review token-exhaustion non-run (not a code finding); no Codex/CodeRabbit findings.
