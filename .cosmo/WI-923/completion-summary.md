**What was done:** Scoped the `update-vocabulary-retention` subject lookup to the event `profileId`, added a focused regression test proving the subject lookup includes both `subjectId` and `profileId`, and restored unrelated `apps/mobile/eas.json` setup drift.

**What changed:** In `apps/api/src/inngest/functions/session-completed.ts`, the existing subject-id lookup now also requires `eq(subjects.profileId, profileId)`. In `apps/api/src/inngest/functions/session-completed.test.ts`, the mocked subject lookup is returned from the test helper and a regression test serializes the Drizzle where clause to verify the profile id is present.

**Verification:** Worker red proof: `pnpm exec jest src/inngest/functions/session-completed.test.ts --runInBand --no-coverage` failed before the fix because the new test expected the profile id in the subject lookup params but only the subject id was present. Worker green and post-restore checks passed the same focused Jest command with 1 suite / 109 tests. Worker lint `pnpm exec eslint apps/api/src/inngest/functions/session-completed.ts apps/api/src/inngest/functions/session-completed.test.ts` exited 0, and `pnpm exec tsc --noEmit` in `apps/api` exited 0.

**Caveats / Follow-ups:** Focused Jest output includes existing console warning/error noise and a `ts-jest` `esModuleInterop` warning, but the suite exits successfully. Targeted ESLint exits successfully with a pre-existing unused variable warning at `apps/api/src/inngest/functions/session-completed.test.ts:1793`. `apps/mobile/eas.json` was restored and is not part of this work item. No follow-up is required for this item.
