What was done: Broke the runtime import cycle between `apps/api/src/services/curriculum.ts` and `apps/api/src/services/language-curriculum.ts` by extracting `ensureDefaultBook` into a new module, `apps/api/src/services/curriculum-core.ts`. Both files now import it one-directionally from `curriculum-core`, and neither imports the other for that function.

What changed: New file `apps/api/src/services/curriculum-core.ts` holds `ensureDefaultBook` (the ~25-line pure DB upsert, with explicit `Promise<string>` return type, moved verbatim). `curriculum.ts` imports it from `./curriculum-core` and re-exports it for its existing in-module callers. `language-curriculum.ts` imports it from `./curriculum-core` (previously from `./curriculum`). The pre-existing one-directional `curriculum.ts -> language-curriculum.ts` import (for `regenerateLanguageCurriculum`) is unchanged.

Verification: Cycle-check via `npx madge --circular` over the three files reports 0 cycles among them (the 2 cycles madge still finds are in unrelated middleware files and pre-date this change). `api:typecheck` green; `api:lint` 0 errors (2 pre-existing warnings in unrelated files). `curriculum.test.ts` 106 tests pass; `language-curriculum.test.ts` 29 tests pass; pre-push hook ran 79 suites / 2191 tests, all green. No behavioral change — same function body, same public API surface preserved via re-export. Landed as squash-merge `f24f33d0d` (PR #1148); strict green gate passed, claude-review clean (0/0), CodeRabbit non-blocking only.

Caveats / Follow-ups: None — surgical extraction only; the 2 unrelated madge cycles in middleware (profile-scope/account/billing and profile-scope/identity-v2) are out of scope and untouched.
