What was done:
Fixed the three WP-E15 typed-error/classification findings at API boundaries (PRG-15 / API Error Handling workstream, third unit after WI-639 and WI-641). F-015 (raw Error → 500+Sentry on missing session): recordSystemPrompt, recordSessionEvent, and flagContent in session-crud.ts threw new Error('Session not found'), which matched no typed branch in the global onError handler; all three now throw NotFoundError('Session') from @eduagent/schemas → 404, no Sentry capture. F-016 (catch-all 422 + raw message echo): the vocabulary review route's catch block returned apiError(c, 422, VALIDATION_ERROR, err.message) for every non-VocabularyNotFoundError exception, masking transient DB errors as permanent validation failures and echoing internal messages to the client; the catch-all now re-throws so the global onError handler applies isTransientDatabaseError() classification (503 + Retry-After) and production message suppression. F-017 (JWKS shape unvalidated → wrongful 401 sign-out + poisoned cache): fetchJWKS and fetchJWKSForced cast (await res.json()) as JWKS with no shape validation, so a malformed upstream 200 missing keys[] later threw TypeError ('reading find') whose message missed auth.ts's infra-failure regex /fetch|JWKS|network|abort/i → 401 branch → wrongful sign-out, with the malformed payload cached for the 10-minute TTL; both functions now validate the keys array after res.json(), throw with 'JWKS' in the message (classified as infra → 503 + Retry-After), and never cache an invalid payload. The fix is response-shape validation only — no auth-flow shape change (operator-ratified constraint, verified by the shepherd's post-merge JWKS diff check). Review round: claude[bot] APPROVED with 2 CONSIDER findings, both accepted and fixed — a forced-refresh-path break test (fetchJWKSForced is module-private, driven through lookupJWKByKid's kid-miss re-fetch; RED-verified by temporarily neutering the forced-path validation) and a narrowed makeNullSessionDb stub wired only to the db surface getSession actually uses (query.learningSessions.findFirst via scoped repo) so a future Drizzle query-style migration fails loudly instead of silently passing.

What changed:
apps/api/src/services/session/session-crud.ts + session-crud.test.ts (F-015, 3 service-level NotFoundError tests). apps/api/src/routes/sessions.test.ts (3 route-level F-015 404 tests). apps/api/src/routes/vocabulary.ts + vocabulary.test.ts (F-016, propagation test replacing the old masking 422 test). apps/api/src/middleware/jwt.ts + jwt.test.ts (F-017, 2 fetchJWKS break tests + 1 forced-path break test). 2 commits on branch WI-640 (391dcd3d1 initial fixes, d6cecc62b review-round test hardening), squash-merged to main as c64327985 via PR #966. 7 files, 245 insertions / 15 deletions net. GC6 audit: jwt.test.ts and session-crud.test.ts carry zero internal mocks; sessions.test.ts (9) and vocabulary.test.ts (4) have pre-existing gc1-allow annotated internal mocks — burn-down deferred per the deferral escape, documented in the commit message.

Verification:
Red-green evidence via stash/revert technique: jwt.test.ts F-017 tests FAIL pre-fix / PASS post-fix; vocabulary.test.ts F-016 test FAILS pre-fix (422 returned) / PASSES post-fix; forced-path F-017 test RED-verified against temporarily neutered validation then GREEN restored. Local gates green in the worktree: full api:test (327 suites, 6628 tests), api:typecheck, api:lint, pre-commit and pre-push hooks. PR #966 CI fully green on both rounds (head d6cecc62b). Final claude[bot] review verdict APPROVED 0 MUST_FIX / 0 SHOULD_FIX / 0 CONSIDER; review threads clean. Shepherd's operator-flagged JWKS diff check passed: shape-validation-only, sanctioned error envelope, no auth-flow shape change. Merged to main at c64327985.

Caveats / Follow-ups:
CodeRabbit never substantively reviewed this PR — the org hit its review rate limit / usage credits both rounds, so its green check status reflects a skip, not a review; claude[bot] plus the shepherd gate were the effective reviews. The F-017 classification rides the existing isInfraFailure message-regex mechanism in auth.ts (the error message must contain the 'JWKS' substring) — fragile by construction and a candidate for a typed infra-error class in any future auth refactor. Deferred sweep: 9 further raw throw new Error('Session not found') sites in sibling services (session-summary.ts 2, session-exchange.ts 3, session-events.ts 2, homework-summary.ts 1, session-homework.ts 1) plus 2 in session-crud.ts (closeSession, getSessionCompletionContext) are outside F-015's route-surface scope and remain untyped; tracked as follow-up in the merge commit message. Provenance children WI-647/WI-648 were swept to Closed by the shepherd.
