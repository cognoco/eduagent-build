What was done: Built `listProfilesV2` — the v2 twin of legacy `listProfiles` — and wired `GET /v1/profiles` to read v2 behind `IDENTITY_V2_ENABLED`, with the legacy path intact until WP-FLAG. First code-half WP of PRG-06 and the mobile-app-launch gate; built TDD with a red-before-green IDOR break test per WP-1 enumeration §4.1.

What changed: `apps/api/src/services/identity-v2/profile-v2.ts` — added `listProfilesV2(db, organizationId): Promise<Profile[]>`, org-scoped via the `membership` join (the IDOR guard: `organizationId` is the caller's own resolved org, `account.id = organization.id`, never a request param; a person outside the caller's org has no membership row for it). Byte-identical `Profile` output: `isOwner` from `membership.roles @> '{admin}'`; `hasFamilyLinks`/`linkCreatedAt` from active `guardianship` edges; `consentStatus` via the behavior-preserving batched AnyBasis reducer; `hasPremiumLlm` derived false (§1.3, no v2 column). The guardianship read is DB-scoped to the org's persons (`or(inArray(guardian), inArray(charge))`) rather than a full-table scan (claude-review should-fix). `apps/api/src/routes/profiles.ts` — GET dispatches to `listProfilesV2(db, account.id)` flag-on, legacy `listProfiles` flag-off; pre-graph empty-list branch unchanged. Plus the two co-located test files.

Verification: Red-green-revert recorded — GREEN with the membership org-scope (8/8 integration incl. the cross-org IDOR break test); org-scope removed → RED (entire `person` table leaks, org B's owner appears in org A's list); restored → GREEN. A defense-in-depth test proves a cross-org guardianship edge never leaks a foreign guardian. Route suite 35/35 (incl. flag-on→v2 / flag-off→legacy dispatch). API typecheck clean; lint 0 errors; GC1 ratchet exit 0; pre-push `tsc --build` + 1092 tests green. PR #1185 merged to main via squash (`6d065ae6e`); Gate-1 4/4 required checks green, claude-review APPROVED after the rework.

Caveats / Follow-ups: The merged sibling `getActiveEdgesForPersons` (`services/identity-v2/guardianship.ts`, the export.ts twin) has the same full-table-scan + in-process-filter shape and warrants the identical DB-scoping fix in a later WP (left untouched here per surgical scope); `run-smoke` stays ambient-red (WP-9 staging post-DROP seed surface — not PR-caused, not a required check); `IDENTITY_V2_ENABLED` flag not removed (WP-FLAG owns removal); the "one active guardian per charge" invariant is service-enforced (not a DB constraint), so the charge `linkCreatedAt` "first edge wins" matches legacy `family_links` non-determinism on the (not-in-practice) multi-guardian case.
