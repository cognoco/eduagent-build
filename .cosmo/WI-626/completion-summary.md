What was done:
Fixed the 7-finding mobile logic/UX bug batch (Bundle #6, PRG-12 L10n & A11y Mobile workstream), merged to main as PR #959 (merge commit 0a7b07dbe). F-160 (sample-lesson buttons permanently disabled after navigating back): added useFocusEffect(useCallback(() => setSubmitting(false), [])) to preview/topic.tsx, mirroring the existing pattern in both.tsx and intent.tsx. F-161 (non-answer substring matching misclassifies substantive answers — locale false positives): replaced normalized.includes(token) with a boundary-guarded matcher, refined across four review rounds to its final shape — per-TOKEN script decision (hasCjkText(token) selects substring matching for CJK tokens only; Latin tokens keep the boundary guard regardless of the answer's other content) with both whitespace AND '?' as boundary characters (normalizeAnswer retains '?' so ?-suffixed non-answers like "idk? …" must still match). F-165 (masteryScore NaN guard): investigation confirmed it was ALREADY FIXED in commit 567df4a1 (BUG-801) with an existing regression test at child/[profileId]/topic/[topicId].test.tsx:146 — verified passing, no change needed. F-168 (subjectId route param not normalized for the array case): added Array.isArray guard with string|string[] typing in vocabulary/[subjectId].tsx, plus a note that an empty array degrades identically to a missing param. F-172 (recall-test double-submit race): added a shared anySubmissionInFlightRef both handlers check-and-set; adversarial review additionally caught that handleDontRemember's onError path bypassed releaseDontRememberBlock(), which after the shared gate would have permanently locked the screen — fixed and regression-tested. F-175 (sessionStorage write during render): moved rememberPendingAuthRedirect from the render phase into useEffect([redirectTarget, resolvedRedirectTarget]); also fixed the useRef initializer that invoked the storage writer on every render pass. F-123 (dormant web ChatShell voice controls): gated VoiceToggle behind {!isWebDormant && ...} and added && !isWebDormant to showVoicePlaybackControls; project-wide orphan grep confirmed no dangling refs. AC DEVIATION (deliberate, shepherd-approved): the bundle AC says "F-123 instance fully removed" — investigation showed ChatShell is a SHARED live component serving both mobile and the focused web instance; the dead thing is not an instance but the dormant (web + unfocused) render's voice-control exposure. That exposure is now gated off with both-polarity tests (controls absent when dormant, present when focused). Full removal of the voice controls would have broken live chat on mobile and focused web.

What changed:
7 commits on branch WI-626, merged via PR #959: c0e57aa13 (batch-1: F-160 F-161 F-168 F-172), 210b3a0d0 (batch-2: F-175 F-123), 217622f96 (F-172 error-path ref leak from adversarial review), 210569ee6 (F-161 CJK substring branch, Codex finding), 72a2370b3 (honest gc1-allow annotations + empty-array note), 78538e9b8 (F-161 per-token CJK decision + F-175 mount assertion, CodeRabbit findings), 4b4ddf55b (F-161 '?' boundary, operator-verified Claude consider). 12 files, ~430 insertions: apps/mobile/src/app/preview/topic.tsx+.test.tsx, apps/api/src/services/session/review-calibration.ts+.test.ts, apps/mobile/src/app/(app)/vocabulary/[subjectId].tsx+.test.tsx (new), apps/mobile/src/app/(app)/topic/recall-test.tsx+.test.tsx, apps/mobile/src/app/(auth)/_layout.tsx+.test.tsx, apps/mobile/src/components/session/ChatShell.tsx+.test.tsx. No new i18n keys (no fix forced a user-visible string). GC6 audit: no internal jest.mock relative imports in any touched test file.

Verification:
Per-fix red-green discipline: every fix's co-located regression test was demonstrated RED against the pre-fix code, then GREEN after — including two explicitly red-verified matcher regressions on review findings (the mixed-script test failed against the per-answer hasCjkText logic before 78538e9b8; the ?-suffix test failed against the whitespace-only boundary before 4b4ddf55b, both cases evaluating "substantive" pre-fix). Five CI rounds, all green, with the full finding chain addressed in sequence: round 1 adversarial review (F-172 error-path leak) → round 2 Codex (CJK fallback lost under word-boundary regex) → round 3 CodeRabbit (per-token vs per-answer script decision + F-175 mount assertion strengthening) → round 4 operator-verified Claude consider ('?' retained by normalizeAnswer defeats whitespace-only boundaries). Final suites green: review-calibration 10/10, recall-test 15/15, auth _layout 12/12, ChatShell, preview/topic, vocabulary suites all passing; api + mobile typecheck clean; pre-commit and pre-push hooks green on every push; all review threads dispositioned in-thread with fix SHAs (Codex r3396684287, CodeRabbit r3398085969 + r3398086254). CI green on final head 4b4ddf55b; Claude Code Review APPROVED; merged to main as PR #959, merge commit 0a7b07dbe2f75ae7681aaa0d0e41c763bab9c0e2. Children WI-644/WI-645 swept Closed/Done with the merge commit.

Caveats / Follow-ups:
F-161 changes matcher behavior semantics, not just bug-fixes it: non-answer tokens now require word/phrase boundaries (whitespace, '?', string edges) in non-CJK text instead of bare substring containment — answers that previously matched a token mid-word are now classified substantive (that is the fix), and any future token additions should be audited against the boundary semantics (CJK tokens get substring matching by design). The gc1-allow annotation wording on use-vocabulary/use-subjects was contested between contradictory Claude review rounds (round-2 prescribed the exact wording round-4 rejected); dispositioned by shepherd ruling in the PR #959 body at merge — leave as-is. F-168's test covers the useVocabulary path; useDeleteVocabulary consumes the same normalized variable (trivially correct, noted as a minor coverage gap, not a bug). F-175's render-to-effect migration adds one extra paint on redirectTarget change — imperceptible in the auth flow.
