What was done: Implemented CUT-B1 — the first reader/writer cutover unit of the Identity-Foundation application-cutover wave — as one reviewable PR (#1101), entirely flag-gated behind IDENTITY_V2_ENABLED (default 'false' in every deployed environment). Legacy remains the sole live store; the flag-off break test pins single-live-store argument 1. Builds on CUT-A (a52c4616d).

What changed: Typed-config flag plumbing (IDENTITY_V2_ENABLED + MAINTENANCE_READONLY + MAINTENANCE_BLOCK_INNGEST, eslint G4) + the flag-off break test (literal 'false' AND unset both select legacy; the JS-truthiness trap pinned RED→GREEN). Two-stage maintenance gate (maintenance.ts) mounted before auth/account in index.ts, exact-endpoint exemptions for /v1/health and (stage 1) /v1/inngest. resolveIdentityV2 (login→person→membership→organization, byte-identical Account shape, fails closed on !=1 membership). createIdentityGraph — the onboarding-completion 9-step transaction (org→person→login→reverse login_id wire→membership{admin,learner}→subscription plus-trial via computeTrialEndDate→subscription_payers→quota_pools); idempotent on login.clerk_user_id with a constraint-discriminating 23505 catch (names pinned from the 0108 migration) that distinguishes same-clerk idempotent replay from the BUG-411 different-clerk reclaim refusal; the BUG-411 email-reclaim guard with red-green evidence; pairwise calendar-valid birth dates. Consent-status READ module (consent-status-v2.ts) per §2.3a — basis-explicit resolver (basis required) + deliberately-named AnyBasis variants + batched; the full reducer (CONSENTED/WITHDRAWN/PENDING/REQUESTED, denied→WITHDRAWN, expired→PARENTAL_CONSENT_REQUESTED, direct-grant-no-request, current-row windowing for superseded-not-withdrawn, scan-side EXISTS with the (granted_at DESC, id DESC) tiebreak + adult notExists branch); isGdprProcessingAllowedV2. Byte-identical profileMeta v2. Account/profile-scope seams + the requireAccountMiddleware pre-graph allowlist + the graphless clerkIdentity context; POST /v1/profiles owner-bootstrap dispatch that never falls through to the legacy writer flag-on (idempotent replay returns the existing owner; child create 409s pending CUT-B2). Onboarding write twin with authorization folded into the UPDATE WHERE (atomic, TOCTOU-free). Shared helpers (ii)-(v) + person-scope twins (coaching-cards, snapshot-aggregation). 8 B1-row Inngest functions re-pointed via the step flag (incl. retry-safe GDPR rechecks). test-seed v2 core. Also fixed a real runtime bug the reducer integration test caught: the Neon driver returns min(granted_at) as an ISO string, coerced before .getTime().

Verification: 6739 API unit tests green (incl. new config / maintenance / account-seam pre-graph-allowlist / profile-v2 / V2-on daily-snapshot units); identity-v2 integration green (full §2.3a consent reducer incl. the same-timestamp tiebreak regression, BUG-411 sequential against the real DB, calendar + jurisdiction guards). tsc --build + api:lint clean (the single remaining lint warning is pre-existing in deletion.ts, untouched by this branch). All 13 PR #1101 review threads triaged — 8 correctness fixes with regression tests on the sensitive auth/consent paths, GC6 mock conversion, and one documented context rebuttal — replied in-thread and resolved (13/13, zero unresolved). Required + deterministic CI green on 620604d02 (API Quality Gate, Playwright web smoke, Merge completeness, CodeRabbit, changes). Operator independently verified A1/A2/A3/A5 in the diff and the gate state.

Caveats / Follow-ups: (1) 3 full-graph integration tests skip pre-M-REPOINT — quota_pools.subscription_id FK still targets legacy subscriptions until the WI-586 §4 step-6 re-point; flag-off in every deployed env means the v2 graph never runs pre-convergence, and the runnable subset (sequential BUG-411 + the consent reducer) is green now; (2) the A6 quota_pools FK sequencing coupling resolves at convergence (M-REPOINT lands in the same freeze window as the flag flip); (3) the claude-review advisory check crashed with no verdict (non-blocking — Codex/CodeRabbit carried the review and all 13 threads are resolved); (4) GC6 done in-place (the subject-retry-curriculum.test.ts ../helpers mock was converted to jest.requireActual with targeted overrides — no deferral). Single-live-store argument 1 is documented in the PR description.
