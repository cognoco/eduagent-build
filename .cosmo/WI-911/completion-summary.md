**What was done:** Fixed WI-911 (identity-v2 session supplementary cache key mismatch) by making all session static-context cache writebacks use the same identity-mode-aware cache key that populated the entry.
**What changed:** Threaded `identityV2Enabled` into supplementary cache lookup/writeback in `apps/api/src/services/session/session-cache.ts`, forwarded the flag from `apps/api/src/services/session/session-exchange.ts`, and added focused regression coverage in `apps/api/src/services/session/session-cache.test.ts` for identity-v2 supplementary writeback plus homework/book context legacy-key pollution.
**Verification:** Focused regression first failed because identity-v2 supplementary data was not written back and homework/book context touched the legacy key; after the fix `pnpm exec jest --config apps/api/jest.config.cjs --runInBand apps/api/src/services/session/session-cache.test.ts --no-coverage` passed with 12 tests. `pnpm exec eslint apps/api/src/services/session/session-cache.ts apps/api/src/services/session/session-cache.test.ts apps/api/src/services/session/session-exchange.ts` passed with known Nx project-graph warnings. `pnpm exec tsc --build apps/api/tsconfig.json --pretty false` passed. Related Jest via `--findRelatedTests` passed with 58 suites and 1468 tests. `git push origin HEAD:WI-911` completed and remote `WI-911` matches the local commit.
**Caveats / Follow-ups:** Related Jest emitted existing ts-jest and service logger console warnings, but validation exited successfully. Cosmo complete has not been run per coordinator instruction.
