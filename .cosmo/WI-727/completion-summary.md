What was done: Fixed the drifted retry-filing cap in apps/api/src/routes/filing.ts. The route's post-claim retry-count gate (line 94) hardcoded the literal 3 while services/session/session-crud.ts already derived the cap from FILING_CONFIG.maxRetries. The two agreed today (config value = 3) but the route could not follow a future config change — latent drift. Applied the operator-approved minimum: imported FILING_CONFIG into the route and replaced the literal with FILING_CONFIG.maxRetries at the single divergent site.

What changed: apps/api/src/routes/filing.ts — added `import { FILING_CONFIG } from '../config/filing'` and replaced `(fresh.filingRetryCount ?? 0) >= 3` with `(fresh.filingRetryCount ?? 0) >= FILING_CONFIG.maxRetries`. apps/api/src/routes/filing.test.ts — added a config-sourced cap regression test that asserts a 429 when filingRetryCount equals FILING_CONFIG.maxRetries. No config values changed; no cap logic moved into the data-access layer.

Verification: Red/green regression discipline. RED — with the route reverted to the hardcoded `>= 3` and FILING_CONFIG.maxRetries temporarily set to 2, the test (filingRetryCount=2) expected 429 but got 409 (route still on the literal). GREEN — with the fix applied and config restored to 3, the test passes. Full filing.test.ts suite green (22/22). Pre-push hook ran tsc --build plus 868 tests across 26 related suites — all green. api typecheck and lint clean (only 2 pre-existing warnings in unrelated files). CI on PR #1150 passed the strict green gate; claude-review 0/0. Squash-merged to main as ab70b7e3f.

Caveats / Follow-ups: Route-level cap now config-driven; the data-access atomic claim predicate (claimSessionForFilingRetry still hardcodes lt(filingRetryCount, 3)) remains hardcoded per the operator-deferred separate retry-logic scope, tracked as a follow-up WI.
