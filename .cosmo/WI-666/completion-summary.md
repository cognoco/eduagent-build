What was done:
All three findings of WP-S13-config-correctness remediated as independent surgical fixes on one branch, landed via PR #1045 (merge commit f0d122de5). F-174: the cooldown CAS claim in review-calibration-grade now precedes the paid LLM grade step — the old single persist step was split into a slim claim-cooldown-slot (timestamps only, re-entrant under at-least-once step execution via lastReviewedAt === eventAt) and finalize-retention-update (SM-2 fields, card-id + profileId write protection retained). F-162: the memory-facts-backfill self-reinvoke cursor now anchors on the longest successful prefix (just before the first errored profile) instead of the last row of the run, so errored profiles are re-included in the next run's window; a livelock guard stops the chain with structured Sentry escalation (cursor_stuck_no_progress) when the run made zero successful progress. F-094: the 12 module-level env-binding singletons in inngest/helpers.ts were replaced with one AsyncLocalStorage<EnvBindings> store entered by the middleware per invocation (onFunctionRun body + beforeMemoization/beforeExecution), matching the existing stepDatabaseScope isolation model; getters keep their process.env fallback; orphaned per-key setters removed.

What changed:
apps/api/src/inngest/review-calibration-grade.ts + test (step reorder, claim re-entrancy, red-green test asserting no LLM step on failed claim); apps/api/src/inngest/functions/memory-facts-backfill.ts + test (firstErroredOffset through step returns, prefix-anchored cursor, livelock guard; three new tests: mid-slice error, tail error, livelock); apps/api/src/inngest/helpers.ts (ALS EnvBindings store, singleton removal), apps/api/src/inngest/client.ts (middleware builds bindings + enters context), apps/api/src/inngest/helpers.test.ts (concurrent-invocation isolation tests), apps/api/src/inngest/functions/account-security-notification.test.ts (switched from removed setters to process.env fallback). Commits 17a76dbda, 677cc345f, ba09fe740, f238ce6b3.

Verification:
Red-green evidence per finding (F-174 grade-before-claim reproduced RED; F-162 mid-slice/tail/livelock all RED pre-fix, GREEN post-fix). Full src/inngest suite 919 tests green; tsc --noEmit clean; nx run api:lint clean. Adversarial review pre-PR: 1 round, claim re-entrancy finding fixed in-code. PR #1045 CI fully green on head f238ce6b3 (API Quality Gate, main, changes, claude-review, Playwright web smoke; ota-update/run-smoke skipped as expected). Claude Code Review APPROVED (0 blocking); CodeRabbit completed with 0 actionable inline comments; triage dispositions posted in-thread. Merged as f0d122de5.

Caveats / Follow-ups:
(1) F-174 partial-state trade (documented in PR description): if the function permanently exhausts retries between a successful claim and finalize, the cooldown slot is consumed with SM-2 fields unchanged — accepted; Inngest retries cover the common case and the alternative wastes a paid LLM call on every lost claim. (2) The old-step-name mock entries ('persist-retention-update') in review-calibration-grade.test.ts are a deliberate regression safety net keyed on the pre-fix step name, not stale cruft — accepted-as-is at review triage; do not "clean them up". (3) Sibling sweep outcome: memory-facts-embed-backfill already implements the safe-prefix cursor (BUG-366 lastSafeAdvanceId + no-progress halt) and filing-stranded-backfill is structurally unaffected (no per-row catch; a dispatch failure fails the run before the cursor advances) — no deferred sweep needed. (4) Deploy transition: runs in flight across the step rename (persist-retention-update → claim-cooldown-slot/finalize-retention-update) re-run the renamed steps once; standard Inngest step-graph change, documented in the PR description.
