**What was done:** Resolved the unresolved accessibility review finding the reviewer flagged in WI-1132's landed source. The WI-1132 feature (Subjects-richness — status grouping, urgency sort, book count, skeleton) already landed on main via #1634 (f41b342b1); this fix-forward addresses the outstanding finding: every `SubjectsBrowse` row carried the identical `accessibilityLabel={t('subjectsBrowse.openSubject')}`, so screen readers announced all rows the same.

**What changed:** `apps/mobile/src/components/subjects/SubjectsBrowse.tsx` — each row now gets a subject-specific accessible name via the interpolated key `subjectsBrowse.openSubjectNamed` ("Open {{subject}}"), with a no-variable fallback (`openSubject`) when the name is empty (repo variable-interpolation fallback rule). Added `openSubjectNamed` to `en.json` + 6 UI locales, regenerated the translate `source-baseline.json`, and added a regression test asserting distinct per-row accessible names. Landed via fix-forward PR #1669 (squash aa94a68a1a5c8fd33f7fd39d4d719e95359de4d9).

**Verification:** PR #1669: all required checks SUCCESS (incl. the `scripts/* tests` translate-baseline check after regen), claude-review VERDICT APPROVED (0 must-fix / 0 should-fix / 0 consider), mergeStateStatus CLEAN. The new Fixed-In (aa94a68a1a5c8fd33f7fd39d4d719e95359de4d9) carries a green `main` rollup, resolving the prior closure-verification bounce (the earlier Fixed-In f41b342b1 had a red post-merge `main` from the since-fixed ambient @inngest-admin gap).

**Caveats / Follow-ups:** Feature content was already on main via #1634; this PR is solely the a11y label fix + its locale key, regression test, and baseline regen. None outstanding.
