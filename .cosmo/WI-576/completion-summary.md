What was done: WP-W2-consent-deletion — consent authority + account-isolated deletion + fail-closed central age-gate, delivered TDD red-green in worktree .worktrees/WI-576, landed via PR #888 (merged 400f2644048, branch head 537535c42). AC1 [F-093]: deleteProfileIfConsentWithdrawn gained an optional parentProfileId param carrying an SQL account guard (AND profiles.account_id = (SELECT account_id FROM profiles WHERE id = parentProfileId)) — the same defence the archive branch already had (BUG-662 asymmetry); the consent-revocation Inngest function now passes archiveDecision.ownerProfileId so the delete step is account-guarded end to end. AC3 [F-029-sem]: checkConsentRequired / checkConsentRequiredFromDate widened to birthYear: number | null | undefined and fail CLOSED (required=true, consentType=GDPR, belowMinimumAge=true) on null/undefined/0 — previously NaN comparisons silently yielded "consent not required" for unknown age. Review fix round (537535c42): GC6 conversion — removed the internal mock of services/deletion in consent-revocation.test.ts; the real deleteProfileIfConsentWithdrawn now runs against the mocked @eduagent/database boundary and all 6 assertion sites verify the DELETE SQL (guard text + bound ownerProfileId) via findProfileDeleteSql(); plus a NULL-subquery fail-safe comment in deletion.ts (missing parent → guard blocks delete, caller returns 'restored').

What changed: apps/api/src/services/deletion.ts (parentProfileId param + SQL account guard + fail-safe comment); apps/api/src/inngest/functions/consent-revocation.ts (passes ownerProfileId to delete step); apps/api/src/services/consent.ts (fail-closed widened gates); apps/api/src/services/consent.test.ts ([F-029-sem][BREAK] null/undefined/0 fail-closed tests for both gate functions); apps/api/src/services/deletion.integration.test.ts ([F-093][BREAK] cross-account deletion rejected + same-account happy path + backward-compat); apps/api/src/inngest/functions/consent-revocation.test.ts (GC6 unmock + SQL-level account-guard assertions). 6 files, two commits (26ee97626 implementation, 537535c42 review fix round).

Verification: AC1/AC3 break tests red-green per TDD. Targeted suites post-rebase: 114 suites / 2867 tests green. Integration gate (pnpm exec nx test:integration api, Doppler dev DB): 52 suites / 400 tests green including the new [F-093][BREAK] cross-account test. AC4 — WI-549 regression break-tests F-118 / F-122 / F-130 / F-145 ran and passed inside those same suites (deletion.integration.test.ts F-122 restore-race atomicity; consent middleware + onboarding F-130/F-145; F-118 in the integration run). Post-GC6 fix round: 42 suites / 1090 related tests green. PR #888 CI fully green on head 537535c42 (6/6 checks); Claude Code Review APPROVED, 0 blocking / 1 consider; CodeRabbit pass. Review findings dispositioned in-thread (SHOULD FIX GC6 fixed; CONSIDER comment adopted). Main-checkout WIP was fully ported to the worktree early in the run and main restored (shepherd-verified clean).

Caveats / Follow-ups: parentProfileId on deleteProfileIfConsentWithdrawn is optional for backward compat — callers outside the consent-revocation flow still delete unguarded (intentional; covered by the backward-compat test). The remaining requireActual pattern-a mocks in consent-revocation.test.ts (consent, notifications, settings, notices, client) are sanctioned conversions, not bare internal mocks — GC6 sweep audit in commit 537535c42. Provenance children WI-602/603 swept Closed/Done by shepherd with Fixed In 537535c42.
