**What was done:** Fixed two GitHub Actions workflow correctness/security defects from the PRG-14 audit clear-out (F-151, F-157), then — after a reviewer bounce for a missing Type=Bug regression guard — added a durable, red-green-revert-verified regression guard. Two landed commits: the fix `bf569d725` (PR #1172) and the guard `f00473e22` (PR #1178).

**What changed:**
- F-151 (`.github/workflows/e2e-ci.yml`, commit `bf569d725`): deleted the unreachable analyze-step branch whose `${{ github.event.workflow_run.pull_requests[0].base.ref }}` interpolation into a shell variable was a latent script-injection sink. The earlier `exit 0` already handles `workflow_run + pull_request`, so the branch was dead; the surviving non-pull_request path diffs against `HEAD~1`.
- F-157 (`.github/workflows/e2e-web.yml`, commit `bf569d725`): reworked the always-green required gate. The required `Playwright web smoke` check is an honest, documented pass-through (reports the required name, exits 0, never gates on `run-smoke`); the real secret-backed smoke (`run-smoke`) runs for trusted web-surface PRs and is surfaced as its own non-required ADVISORY check — implementing the AC's "real smoke registered optional-only".
- Regression guard (`scripts/e2e-ci-injection-and-smoke-gate.test.ts`, commit `f00473e22`): new 10-test structural guard in the existing `scripts/` workflow-test harness (same yaml-parse style as `e2e-web-cleanup.test.ts`; no fixtures, no mocks).

**Verification:** Durable regression guard `scripts/e2e-ci-injection-and-smoke-gate.test.ts` (10 tests) directly answers the bounce. Red-green-revert evidence (workflows reverted to the pre-fix YAML via `git show bf569d725^:.github/workflows/...`): GREEN 10/10 on fixed main → RED on the pre-fix YAML with exactly the 3 meaningful invariants firing (2 F-151 injection-sink guards + the F-157 "no gate step branches its exit on `needs.run-smoke.result`" guard) → GREEN 10/10 on restore; workflows confirmed byte-identical to HEAD after restore. Two Codex P2 coverage hardenings folded in: (1) the F-151 matcher catches bracketed access to `workflow_run` itself (`github.event['workflow_run'].pull_requests…`) across all four dot/bracket notations, with a case asserting benign `workflow_run` fields (head_sha/event/conclusion) are not matched; (2) the F-157 gate scan covers EVERY gate `run:` step + its env mapping (not just the first), with a synthetic case proving a hard gate hidden in a later step is detected. CI on PR #1178: all required checks green (required `Playwright web smoke` pass-through green at 4s); both P2 threads disposed in-thread (👍 + fix reply). On the original PR #1172: all required checks green; advisory `run-smoke` red as designed; F-151 sink confirmed removed via grep.

**Caveats / Follow-ups:** the advisory `run-smoke` check is red on web-surface PRs because `DOPPLER_TOKEN_STG` is not provisioned as a GitHub Actions secret (every staging seed call 403s and auth.setup times out) — the secret-backed Playwright smoke is therefore structurally un-runnable in CI today and is deliberately surfaced advisory-only per the AC (the required `Playwright web smoke` check is an intentional pass-through, NOT the original F-157 always-green-by-accident bug); promoting the smoke to a hard required gate is a SEPARATE future effort, out of scope for this WI, blocked on (1) provisioning `DOPPLER_TOKEN_STG` and (2) stabilizing the suite, after which the gate logic can flip to mirror the real run-smoke outcome and the F-157 guard's exit-branching assertion would be updated accordingly.
