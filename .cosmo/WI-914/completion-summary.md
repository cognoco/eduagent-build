**What was done:** Fixed WI-914 (extractFirstJsonArray fence handling) by making `apps/api/src/services/llm/extract-json.ts` return the first parse-valid JSON array instead of trusting the first fenced block or bracket-balanced placeholder.
**What changed:** Added regression coverage in `apps/api/src/services/llm/extract-json.test.ts` for a non-array fenced block before the real array and a bracket placeholder before a valid array; changed array extraction to strip only whole-response fences, walk balanced array candidates, and JSON-parse candidates before returning them.
**Verification:** `pnpm exec jest --config apps/api/jest.config.cjs --runInBand apps/api/src/services/llm/extract-json.test.ts --no-coverage` passed; `pnpm exec eslint apps/api/src/services/llm/extract-json.ts apps/api/src/services/llm/extract-json.test.ts` passed with the known Nx project-graph warning; `pnpm exec tsc --build apps/api/tsconfig.json --pretty false` passed; `git push origin HEAD:WI-914` passed after pre-push validation.
**Caveats / Follow-ups:** Pre-push ran Tier-1 eval because `apps/api/src/services/llm/extract-json.ts` is under `services/llm`; generated snapshot drift was not part of the fix and was restored before completion.
