What was done:
Swept all 11 remaining raw `Error('Session not found')` throw sites in apps/api/src to the typed `NotFoundError('Session')` pattern landed in PR #966 (WI-640), so every route-reachable missing-session path now maps to 404 via the global onError handler instead of 500. Each converted site gained explicit regression coverage asserting `rejects.toBeInstanceOf(NotFoundError)`. Landed via PR #1036 (merged to main, 2 commits: the sweep + the review-fix batch covering the last two sites).

What changed:
Source (6 files): session-summary.ts (skipSummary, submitSummary), session-crud.ts (closeSession, getSessionCompletionContext), session-exchange.ts (persistChallengeRoundState — also exported for unit testing, checkExchangeLimit, prepareExchangeContext), session-events.ts (setSessionInputMode: both the findFirst-null and UPDATE…RETURNING-null guards), session-homework.ts (syncHomeworkState), homework-summary.ts (extractHomeworkSummary). Tests (6 files): 7 existing string-match assertions updated to typed instanceof checks; 6 new regression tests added (closeSession, getSessionCompletionContext, prepareExchangeContext, persistChallengeRoundState, setSessionInputMode RETURNING-null, extractHomeworkSummary).

Verification:
Sweep grep-clean: `rg "Error\('Session not found" apps/api/src --type ts` (excluding tests) → 0 hits. Red-green evidence recorded for the regression tests: verified RED against raw-Error throws (session-summary, session-crud via subagent; session-events RETURNING-null and persistChallengeRoundState via direct revert/restore, jest logs 1781254804/1781254855), GREEN with the fix. Pre-push gates ran 57 suites / 1517 tests green; PR #1036 CI green 6/6; final Claude Code Review verdict APPROVED (0 must-fix / 0 should-fix / 1 consider); no unresolved CodeRabbit/Codex threads. Inngest fence (per AC): closeSession is reachable from the session-stale-cleanup Inngest cron via closeStaleSessions — NotFoundError extends Error, no Inngest handler does instanceof NotFoundError, retry semantics confirmed unchanged (independently re-verified at merge), so the site converted plainly; no sites needed preservation.

Caveats / Follow-ups:
(a) The Challenge-Round site's original message context suffix ("…while persisting Challenge Round state") was dropped in favor of the uniform typed `NotFoundError('Session')` message — provenance remains available via the enclosing function (persistChallengeRoundState) in stack traces. (b) This item is not workstream-attached, so no autonomous reviewer will pick it up — it rests at Reviewing pending a manual /cosmo:review.
