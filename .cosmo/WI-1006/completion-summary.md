**What was done:**
Implemented v2 Stripe webhook parity for top-up credit re-attribution when a subscription tier change crosses the quota-model boundary. Read the v2 Stripe handler, v1 Stripe parity path, v2 tier attribution helper, v2 RevenueCat attribution path, v2 subscription race-fence core, database lock/find helpers, and the legacy/v2 integration seed patterns before patching.

**What changed:**
`handleSubscriptionEventV2` and `handleSubscriptionDeletedV2` now lock the previous v2 subscription tier inside the transaction, re-attribute active top-up credits inside the same transaction when moving between shared-pool and per-profile tiers, and emit the existing `emitTopUpCreditsReattributedMetric` outside the transaction when credits move. Added a focused v2 integration regression test covering pro/family expiry to free, plus to family, plus to plus no-op, and deleted-with-no-credits no-op.

**Verification:**
Red/green proof: the new focused v2 integration test first failed on the intended symptom after schema-tolerant seeding: pro-to-free left `top_up_credits.profileId` null, and plus-to-family left owner credits non-null. After the implementation, the same test passed: `pnpm exec jest --config apps/api/jest.integration.config.cjs --testMatch '**/apps/api/src/services/billing/billing-v2/stripe-webhook-handler-v2.integration.test.ts' --runInBand --no-coverage --forceExit` (4 passed). Also passed: `pnpm exec jest --config apps/api/jest.config.cjs apps/api/src/services/billing/billing-v2/stripe-webhook-handler-v2.test.ts --runInBand --no-coverage` (4 passed), `pnpm exec tsc --build apps/api/tsconfig.json --pretty false`, `pnpm exec eslint apps/api/src/services/billing/billing-v2/stripe-webhook-handler-v2.ts apps/api/src/services/billing/billing-v2/stripe-webhook-handler-v2.integration.test.ts`, and `git diff --check`.

**Caveats / Follow-ups:**
The adjacent `subscription-core-v2.integration.test.ts` was attempted but currently fails before reaching the code under test because its existing seed still assumes the legacy `accounts` table exists; the current integration DB reports `relation "accounts" does not exist`. The new WI-1006 regression test was written to tolerate both the pre-drop freeze-window schema and the post-legacy-drop schema. Integration runs also emit existing logger warnings for unconfigured Stripe pricing and use `--forceExit` because of the known Jest open-handle behavior.
