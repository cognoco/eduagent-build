Completion summary — WI-578 (WP-W3-pii-step-state)

What was done:
Removed minor-PII from the memoized Inngest step returns of all 6 audited findings (F-075, F-085, F-086, F-087, F-088, F-089) via the reference-and-rehydrate pattern: each offending step now memoizes opaque references only (ids, snapshot-date anchors, booleans, counters) and the consuming steps rehydrate names/topics/birth-year/email from the DB at use time. Delivered as PR #933 (merged to main, merge commit 73f0a15e0) through 4 review rounds: round 1 batched a Claude should-fix (scoped-repo read) + 2 Codex P2s (snapshot-pinned weekly rehydration; malformed-JSONB fail-safe); round 3 batched a Claude should-fix (owner-scoped pending-notice read) + 3 CodeRabbit minors + 2 considers; round 4 was an operator-fenced pure relocation of the weekly digest builder into services/. Also executed the agreed cross-PR consolidation: WI-579's summarizeRawPayload folded into the canonical scrubber home, and added a belt-and-braces step-return scrub middleware mirroring WI-577's event-payload ratchet.

What changed:
- apps/api/src/inngest/functions/progress-summary.ts (F-075/F-087): gather-context drops childName + knowledge inventory; generation and persistence merged into one step so the LLM summary (parent-facing minor PII) never crosses a step boundary; WI-82 consent re-check retained before the LLM call AND before the upsert.
- apps/api/src/inngest/functions/weekly-progress-push.ts + new apps/api/src/services/weekly-digest.ts (F-085): prepare memoizes childDigests [{childProfileId, snapshotDate}] + booleans only; per-child digest math lives in services/weekly-digest.ts (buildChildWeeklyDigestLine), called by prepare (persist + eligibility) and re-called by the send steps to rebuild content in-step, re-pinned to the prepare-time snapshot anchor on delayed retries; parent email re-read inside the email step.
- apps/api/src/inngest/functions/monthly-report-cron.ts (F-086): generate step drops childDisplayName + struggleTopics; push/email steps rehydrate displayName (blank-name fallback) and struggles via the new scoped listStruggleTopicNames helper (services/learner-profile.ts, topic names trimmed, malformed entries fail safe).
- apps/api/src/inngest/functions/consent-revocation.ts + services/notices.ts (F-088): load-child-profile returns an existence marker; choose-final-action rehydrates birthYear in-step; archive-branch steps rehydrate the name in-step; the delete step captures the name pre-delete into the pendingNotices row and memoizes only the opaque notice id; notify-parent rehydrates via getPendingNoticeChildName (owner-scoped WHERE); recordPendingNotice returns the notice id; notice-insert failure after a successful delete is Sentry-escalated, never thrown; old memoized boolean delete-shape tolerated for in-flight runs.
- apps/api/src/inngest/functions/session-completed.ts (F-089): struggle notifications sent inside the analyze-learner-profile step (per-notification try/catch + Sentry) so topics never round-trip through step state; step memoizes shape-only counters; notify-struggle outcome synthesized from them; delivery semantics on mid-step crash documented (idempotent applyAnalysis merge + 24h per-type dedup).
- packages/schemas/src/pii-scrub.ts + apps/api/src/inngest/client.ts: INNGEST_PII_STEP_KEYS denylist (childName, childDisplayName, childSummaries, struggleLines, struggleTopics) + onFunctionRun→transformOutput middleware (scrubStepOutput) scrubbing every memoized step return and function output, escalating any hit to Sentry. Consolidation: summarizeRawPayload/RawPayloadSummary moved from apps/api/src/services/pii-scrub.ts (deleted) into the canonical packages/schemas/src/pii-scrub.ts; 11 observe-handler imports rebarreled; guard test updated.

Verification:
- Break test per function, each demonstrated RED against pre-fix code via stash-revert and GREEN after: progress-summary (1 RED), monthly-report-cron (1 RED), consent-revocation (2 RED), session-completed (2 RED), weekly-progress-push (1 RED). Test names carry the finding IDs (e.g. "memoized step-state PII break test [F-086]").
- Snapshot-pin regression test: "re-pins a delayed retry to the memoized snapshotDate when a newer snapshot exists" (weekly-progress-push.test.ts).
- Full api unit suite green in-worktree (323 suites / 6563 tests at round 1; affected suites re-run green every round — weekly 25, monthly 58, learner-profile 121, session-completed 99, consent-revocation 26, email-digest 31, progress-summary 9, client 15, schemas 14); api tsc + lint + schemas lint/test green each round.
- PR #933: all CI checks green on final head cca679bb9; Claude review APPROVED (2 non-blocking considers); zero unresolved threads (round-1 Codex threads resolved with in-thread evidence; CodeRabbit minors replied in-thread citing commits); merged to main as 73f0a15e0. Provenance children WI-606/607 closed by the shepherd with Fixed In = cca679bb9.

Caveats / Follow-ups:
- GC6 deferral record: no new internal mocks added (one notifications stub added with explicit gc1-allow rationale); pre-existing gc1-allow'd pattern-a mocks retained in touched test files — weekly-progress-push.test.ts (9), session-completed.test.ts (21), email-digest-channel.test.ts (9), monthly-report-cron.test.ts (9), consent-revocation.test.ts (6), progress-summary.test.ts (5), learner-profile.test.ts (1), client.test.ts (1); tracked in the GC6 burn-down backlog (AGENTS.md Code Quality Guards), recorded in the round-1/round-3 commit messages.
- Weekly digest content is recomputed in each send step (≤3x per parent per week) — deliberate cost to keep content out of step state.
- In-flight-run tolerance shims (consent-revocation boolean delete-shape, load-child-profile truthy marker) can be removed once all pre-deploy runs have drained (~8 days after deploy given the 7-day grace sleep).
- parentEmail deliberately excluded from INNGEST_PII_STEP_KEYS: consent-reminders legitimately memoizes it in its non-idempotent token-mint steps — follow-up WI-637 (parent email memoized in consent-reminders token-mint step state).
- Key-based scrubbing cannot catch bare-string step returns — follow-up WI-638 (auto-file-session memoizes the full session transcript string in its fetch-transcript step, P2).
