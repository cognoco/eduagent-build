DOC: docs/plans/2026-06-08-note-correctness-and-challenge-draft.md (2026-06-08, 18K)

CLAIMS: (per-arm, 1 line each)
1. Arm 1 (T1-T8, checkmark): whole `note_correctness` table + LLM grading service + Inngest fn + dispatch + read-side + mobile marks is unbuilt.
2. T9: `validateNoteDraft` (lexical hallucination guard) is unwired into the Challenge-Round draft path — "no production code path calls validateNoteDraft today."
3. T10: no `source` param on `createNoteForSession`, no `POST /notes/from-challenge-draft` route.
4. T11: no session-screen "save your answers as your note?" prompt.
5. T12: no note-correctness nudge Inngest fn; this task explicitly **supersedes** the concept-capture spec's item 6 ("do not build those" — the spec's deferred T12/T13).
6. Plan-level: whole thing is **parked** behind the identity-foundation `profiles`→`person` rename (still pending as of the 2026-06-27 mechanism-correction banner).

TECH VALIDITY: per broken assumption, file:line
- **T1/T2/T7/T8 (Arm 1, checkmark) confirmed fully unbuilt.** No `note-correctness.ts` schema module in `packages/database/src/schema/`; no `source` column, no `topicNoteSourceEnum` on `topic_notes` (`packages/database/src/schema/notes.ts`); zero hits for `topicNoteSourceEnum` / `'challenge_draft'` enum value anywhere in `packages/database`, `packages/schemas`, or `apps/api/src`. Claim holds fully.
- **T9's "zero production callers" claim is STALE and WRONG as currently written — this is the safety-relevant finding.** `validateNoteDraft` (`apps/api/src/services/challenge-round/note-draft.ts:143`) is in fact **wired and actively hardened** in production: imported and called at `apps/api/src/services/session/session-exchange.ts:153,634` inside `buildValidatedDraft()`, which gates what `draftedNote` content is ever surfaced to the learner as a Challenge-Round note suggestion. Git history shows this wiring is not stale/abandoned — it was introduced same-day as this plan (`eb11ff2bf`, "concept-mastery capture + parked note-mark plans", 2026-06-08) and has been **hardened twice since**: `eaafb390b` [BUG-483] (2026-06-21, "guard uses DB-verified content for current-turn answer") and `696494e7f`/`595ca02e7` [WI-1056] (2026-06-25, "make verifiedEventContents required... force compile-time enforcement at every call site"). The stale premise is the comment at `apps/api/src/services/notes.ts:240-246` itself ("no production code path calls validateNoteDraft today") — that comment was accurate only for the `createNoteForSession` persistence path, never for the draft-*building* path, and has not been updated since the guard was wired one layer up. **The guard is not dead; it protects the exact draft the learner sees before any save decision.** What's actually missing is narrower: T10's dedicated persistence route/source-tagging, not the guard wiring itself.
- **T10/T11 status is more advanced than the plan assumes — this is a second stale-premise finding.** No `POST /notes/from-challenge-draft` route exists (T10 confirmed unbuilt as claimed) — `createNoteForSession` is called only from `apps/api/src/services/session/session-summary.ts:277` (the manual/reflection save path), never from a challenge-round-specific caller. **But T11's "save your answers as your note?" UX already exists**: `apps/mobile/src/app/(app)/session/index.tsx:1417-1426` renders `<DraftedNoteReview onSave={handleSaveDraftedNote} onSkip={handleSkipDraftedNote}>` whenever `draftedNote` is set (i.e., whenever `buildValidatedDraft` — guarded by `validateNoteDraft` — produced a savable draft). `handleSaveDraftedNote` (`session/index.tsx:1270-1291`) calls `challengeRoundActions.saveNote(content)` → `apps/mobile/src/hooks/use-challenge-round.ts:127-137` → the **generic** `useCreateNote` mutation (`use-notes.ts:187`), i.e. the plain `POST /notes` path, not a dedicated challenge-draft route. Net effect: a learner **can already** save a challenge-round-derived draft as a note today, validated by the (live, hardened) lexical-overlap guard — it just lands as an indistinguishable `manual` note with no `source:'challenge_draft'` tag, no MentoMate-logo marking (that column/enum doesn't exist — Arm 2's schema is what's actually missing, not the save UX).
- **T12 (nudge) confirmed fully unbuilt**, consistent with claim: no `note-correctness-nudge.ts`, no `app/notes.correctness-nudge-due` event, anywhere in `apps/api/src/inngest`.
- Plan-level parked-status confirmed current: this session independently re-verified (via row 9's concept-capture check) that `WI-586`'s `m-repoint`/`m-drop` remain inert in `apps/api/drizzle/_freeze-only/` and `meta/_journal.json` still ends at `0123` — the parking premise has not changed since 2026-06-27.

IMPLEMENTED: per claim — none/partial/complete/superseded, file:line
1. none — Arm 1 (T1-T8) = part of WI-1491 umbrella.
2. **superseded-by-drift, not "none"** — T9's dead-guard framing is factually wrong today; the guard is wired and hardened at `session-exchange.ts:634`. WI-1490 as titled ("dead safety guard") misdescribes current reality.
3. **partial** — T10: no dedicated route/source-tag (unbuilt), but the save-flow it would formalize already works end-to-end via the generic notes path.
4. **complete (via the generic path, not the plan's designed route)** — T11: `DraftedNoteReview` UI + save/skip handlers exist and function today (`session/index.tsx:1417-1426`, `1270-1296`).
5. none — T12 = WI-1455 (shared with row 9).
6. n/a — plan-level parking claim confirmed still accurate.

CANDIDATE WIs: each with fate: adopt / merge-into-<WI> / kill (+reason); WI-1455 spans rows 8+9, propose its single home
- **WI-1490** (validateNoteDraft dead-guard, typed Bug) — **kill as titled, re-scope or fold into WI-1491.** The premise ("dead safety guard," zero callers) is stale/incorrect: the guard is live, tested, and was hardened twice in the three weeks after this plan was written (BUG-483, WI-1056). Closing this as a standalone safety Bug would misrepresent the system as having an unwired safety gap when it does not. What remains genuinely open — tagging challenge-draft-derived notes distinctly from manual ones (T2/T10's `source` column + dedicated route) — is Arm 1/2 schema work, i.e. part of WI-1491, not a standalone dead-guard fix. **Do not close this WI silently**; it needs an explicit correction note (the "dead guard" framing is safety-adjacent messaging and should not just quietly disappear) before folding into WI-1491.
- **WI-1491** (T1-T13 umbrella) — **adopt, but re-scope down.** T9 is already effectively done (guard wired) — drop it from the umbrella's remaining scope. T11 is already done via the generic path — the umbrella's real remaining surface is: Arm 1 (T1,T2,T4-T8: schema + grading service + Inngest fn + read-side + mobile marks) and T10 (dedicated `from-challenge-draft` route + `source` tagging so the already-working save flow gets the correct provenance mark instead of masquerading as manual). This is meaningfully smaller than the plan's stated 13-task scope. Still correctly parked behind the identity-foundation rename per the plan's own stale-but-still-valid banner.
- **WI-1455** (nudge, shared row 8/9) — **home: row 8 (note-correctness plan), NOT row 9.** The note-correctness plan's own T12 (Phase A3) explicitly states it "supersedes the concept-capture plan's deferred T12/T13 (do not build those)" — the note-correctness doc is the authoritative design owner for the nudge; the concept-capture spec's item 6 is deliberately superseded prose, not a second live spec. Recommend: **merge-into-WI-1491** as its Phase-A3 sub-task (do not track as a fully independent WI) since it shares the exact same parked-behind-identity-rename gate and the same non-core `safeSend()`/dedup infrastructure as the rest of the umbrella; keep only if Phase 2 wants nudge-specific sequencing independent of Arm 1's schema work.

VERDICT: partially-implemented
The plan is correctly **parked** at the document level (identity-rename gate genuinely unresolved, confirmed independently this session). But the candidate-level picture the register's pre-bucket assumed ("T1-T13 umbrella; validateNoteDraft dead-guard is a live Bug") is wrong in two material ways: (1) the "dead guard" is not dead — it's live and hardened, making WI-1490 a stale-premise Bug, not a real gap; (2) T11's save UX already ships via the generic path, meaning the umbrella's true remaining scope is Arm 1 schema/grading + T10's provenance tagging, not 13 fresh tasks.

MVP RECOMMENDATION: in / out / finish-or-hide vs north star (Config T V2 shell on Google Play, RevenueCat Plus-only, proven V1 fallback)
- **OUT for MVP, as a whole plan**: correctly gated behind the identity-foundation rename, which is itself pre-MVP-launch infrastructure work, not user-facing. Arm 1 (LLM-graded checkmark) and the source-tagged logo are polish on top of an already-functional save-your-draft-as-a-note flow — nice-to-have provenance marking, not core value.
- **Different category — flag, do not fold into the "out" bucket silently:** WI-1490 is titled as a SAFETY guard gap, which is a different risk class than feature-completeness gaps. Since the underlying claim is factually stale (guard is wired), there is **no live safety gap to prioritize** — but this must be explicitly ruled and documented (not just quietly dropped), because a future auditor re-reading the plan/WI title without this session's code check would reasonably re-flag it as urgent. Recommend a one-line correction on WI-1490 itself before any close/merge action.
- **finish-or-hide note:** the already-shipped `DraftedNoteReview` save-as-manual-note flow is currently silently mislabeling challenge-derived content as ordinary manual notes (no `source` field exists to say otherwise). This is not a burning MVP gap, but it's worth a Phase-2 flag: is a challenge-derived note masquerading as manually-written acceptable indefinitely, or does it need the T10 provenance fix before general release regardless of Arm 1's LLM-grading half?

CONFIDENCE: high + up to 3 decidable Zuzka questions
High confidence — the T9/T11 findings are based on direct file:line reads of the current call graph (session-exchange.ts, use-challenge-round.ts, session/index.tsx) plus git-log confirmation of two post-plan hardening commits, not inference from the plan text.
1. Should WI-1490 be closed/re-titled now (guard confirmed wired+hardened), or does the correction need a formal ruling first given its safety framing?
2. Confirm WI-1455's single home as row 8 / merge-into-WI-1491 (per the note-correctness plan's own explicit supersession of the concept-capture spec's item 6) — any objection to dropping it from row 9's candidate set?
3. Given `DraftedNoteReview` already lets learners save challenge-round drafts as ordinary (unlabeled) notes today, is the missing `source:'challenge_draft'` provenance tag (T2/T10) worth pulling forward independently of the rest of Arm 1's LLM-grading work, or does it wait for the full umbrella post-rename?
