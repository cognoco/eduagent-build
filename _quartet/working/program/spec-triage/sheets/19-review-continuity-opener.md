DOC: docs/plans/2026-06-27-review-continuity-opener-simulation-harness.md (2026-06-27, 26K); parent spec docs/_archive/specs/Done/2026-06-08-memory-task-review-continuity.md (archived Done)
CLAIMS:
- C1: Build `buildReviewContinuityOpener(context)` — a pure, deterministic prompt-builder that swaps the review-mode calibration-question line for a continuity-framed opener when a `ReviewContinuityContext` is present, honoring EU-1 (verbatim-or-gesture), EU-2 (consent-gated degrade), EU-4 (no false recency / no weak-prior anchoring), and a product no-struggle-framing rule (R-tone).
- C2: Land it flag-gated (`REVIEW_CONTINUITY_OPENER_ENABLED`, default `'false'`), unwired in production — explicitly NOT gated by the production DB assembler or the EU-3 retention decision, both scoped out.
- C3: Build a DB-free, two-independent-model simulation harness (Tier-1 snapshot + Tier-2 live judge) to pre-screen — not prove — mentor-LLM obedience to the builder's instructions, using `assertTwoModelGuard` for real model independence.
- C4: Ship a known-bad/known-good calibration corpus so the judge's flags can be promoted from `qualityWarning` to `qualityError` once demonstrated reliable.

TECH VALIDITY: no broken assumptions — this is a recently-authored (2026-06-27), narrow, self-contained slice; no architecture drift to check since it postdates the current shell/routing state it depends on (`exchange-prompts.ts` composite block, `assertTwoModelGuard`, LLM routing).

IMPLEMENTED — all claims: complete, matching the plan's explicit scope.
- C1: `apps/api/src/services/review-continuity/opener-context.ts` (schema), `opener.ts` (`buildReviewContinuityOpener`) both exist. Wired into `apps/api/src/services/exchange-prompts.ts:21` (import) and `:946` (`sections.push(buildReviewContinuityOpener(continuityContext))`).
- C2: `apps/api/src/config.ts:214` — `REVIEW_CONTINUITY_OPENER_ENABLED: z.enum(['true','false']).default('false')`; `:358` — `isReviewContinuityOpenerEnabled()` helper present, matches plan's mirror-of-`isLlmRoutingV2Enabled` pattern. Still default-off as designed; no production DB assembler exists (confirmed: `continuityContext` is only ever supplied by the eval harness/tests, no `retrieval_events` read in production code) — the plan's explicit "out of scope" boundary is intact, not silently expanded.
- C3: `apps/api/eval-llm/fixtures/review-continuity.ts`, `apps/api/eval-llm/runner/opener-faithfulness-judge.ts`, `apps/api/eval-llm/flows/review-continuity-opener.ts` all exist; flow registered in `apps/api/eval-llm/index.ts:88`. Committed Tier-1 snapshots present at `apps/api/eval-llm/snapshots/review-continuity-opener/` (e.g. `11yo-czech-animals__recap-only.md`, `12yo-dinosaurs__injection-verbatim.md`, `13yo-spanish-beginner__verbatim-missing-blank.md`) — T8's "commit snapshots" done-when is satisfied.
- C4: calibration corpus is part of `opener-faithfulness-judge.ts` per T6's done-when; not independently re-verified beyond file existence (out of scope for a doc-level audit per row-16-style architecture pass; this row got item-level treatment since the register flagged it as a status-mapping task).

CANDIDATE WIs:
- WI-1437 (Surface milestone_reached moments as NowCards in Mentor /now stack): kill-the-provenance-link, not the item. Confirmed by direct grep of the plan doc (`rg -in "milestone|nowcard|now.stack|now-feed"` → zero hits): this plan is entirely about the review-continuity opener/simulation harness and never mentions milestones or NowCards. The register's "1437?" provenance guess (row 19, "possible candidate") is a misfile — WI-1437 is a standalone, unrelated finding (likely from the 2026-07-03 test-coverage/code audit, same as the unmapped WI-1399-1416 block). Recommend: detach from row 19 in the consolidation pass; re-file as unmapped/standalone. Separately, on its own merits: the underlying claim is real — `apps/api/src/services/now-feed.ts:809` routes `kind === 'milestone_reached'` straight to a `journal` deep link rather than surfacing a dedicated Now card, and `NowCardKind`/`nowDeepLinkRouteSchema` (`packages/schemas/src/now-feed.ts`) has no milestone-specific card type — so the ask is technically valid, just not sourced from this doc.

VERDICT: valid — fully implemented as scoped; the plan's own "What this does NOT unblock" section (prod A-B flip, DB assembler, real-staging calibration) is honestly still open and correctly not claimed as done.

MVP RECOMMENDATION: in (already shipped, flag-off, zero runtime risk) — this is inert infra behind a default-off flag, same accepted pattern as `LLM_ROUTING_V2_ENABLED`; nothing to gate for V2 launch. The remaining work (table slice: `retrieval_events` migration + DB assembler + EU-2 consent wiring + flag flip) is NOT part of this plan's scope and is a separate, larger decision (needs EU-3 retention ruling first) — track it as a distinct follow-on item, not as unfinished work on this row.

CONFIDENCE: high — every file the plan names exists, is wired per the plan's exact call-sites (`exchange-prompts.ts:946`, `config.ts:214/358`), and committed snapshots confirm T8 ran. The WI-1437 non-relation is a direct grep-negative, not an inference.
1. Should the table slice (retrieval_events + DB assembler + EU-2 consent gate + flag flip) be scheduled as a follow-on WI now, or deferred until after the EU-3 retention decision is ruled?
2. Confirm: should WI-1437 (milestone NowCards) be re-filed as unmapped/standalone rather than tracked under row 19?
3. None outstanding on this row's core disposition — it's a confirm-only close.
