DOC: docs/specs/2026-06-08-concept-capture-layer-design.md (2026-06-08, 25K)

CLAIMS: (per-scope-item, 1 line each)
1. Item 1-2 (write side): additive `concepts` + `concept_mastery` tables + capture write at Challenge-Round evaluation — status per the spec's own 2026-06-27 banner: built, gated `CONCEPT_CAPTURE_ENABLED=false`, tables not live pending the identity-rename gate.
2. Item 3 (star on a note, read side): derived three-state star (not-yet-assessed / verified / tutor-has-additions) via `useConceptMasterySignals`, currently the only host is the V1 library book reader.
3. Item 4 (tutor correction on recall): surface `needs_deepening_topics.correction` beside the note, neutral topic-level framing — status per spec banner: wired.
4. Item 5 (concept-targeted review): narrow due-topic recall to open weak concepts instead of the whole topic — explicitly flagged "not built" in the spec's own status banner.
5. Item 6 (note-correctness notification + sooner review): nudge on non-solid live concept, dedup vs. review-due — explicitly flagged "not built," and the sibling note-correctness plan's T12 explicitly supersedes this item ("do not build those").
6. Whole spec parked behind the same `profiles`→`person` identity rename as the note-correctness plan (same-gate sibling, MMT-ADR-0017 amends architecture.md in lockstep).

TECH VALIDITY: per broken assumption, file:line
- **Write side (items 1-2) confirmed built and flag-gated as claimed.** Schema exists: `packages/database/src/schema/concept-mastery.ts` (+ its own test file). Flag references confirmed in `apps/api/src/services/concept-capture.ts`, `apps/api/src/services/session/session-exchange.ts`, and `apps/api/src/services/database-rls-coverage.ts` — consistent with "wired, flag-off" rather than "unbuilt." This session did not re-verify the flag's literal current value, only that the gating code paths exist (per the register's instruction to reuse Found-In verification for code-level checks already dated 2026-07-03).
- **Item 3 (star, read side) confirmed wired**, consistent with claim: `useConceptMasterySignals` is consumed in `apps/mobile/src/hooks/use-notes.ts`, `apps/mobile/src/app/(app)/topic/[topicId].tsx`, and `apps/mobile/src/app/(app)/shelf/[subjectId]/book/[bookId].tsx:46,580` — the **sole callsite is the V1 book-reader** (WI-1456's premise). Confirmed **absent** from both V2-shell-adjacent note surfaces the spec/WI-1456 name as replacement candidates: zero `useConceptMasterySignals` hits in `apps/mobile/src/components/subject-hub/SubjectHubNotesSection.tsx` or anywhere under `apps/mobile/src/app/(app)/journal/`. Claim holds precisely.
- **Item 5 (concept-targeted review) confirmed unbuilt** — no due-topic review service narrows recall scope by `needs_deepening_topics`; searched `apps/api/src/services` for a session-review/recall-scoping module referencing `needs_deepening_topics` and found none beyond the existing weak-concept-write path (unchanged, topic-grained today).
- **Item 6 (note-correctness nudge) confirmed unbuilt**, and correctly cross-referenced: no `note-correctness-nudge.ts`, no `notes.correctness-nudge-due` event anywhere in `apps/api/src`. This is the exact same absence independently confirmed in row 8's sheet for the note-correctness plan's T12, which explicitly claims to supersede this spec item. Both docs agree the nudge is a single unbuilt piece of work with one design owner (the note-correctness plan), not two.
- **Plan-level parking claim independently re-verified current**: `apps/api/drizzle/_freeze-only/` still holds `m-repoint`/`m-drop` inert; `apps/api/drizzle/meta/_journal.json` chain still ends at `0123`; `packages/database/src/schema/profiles.ts` remains the live FK target. The spec's 2026-06-27 mechanism-correction banner (rename is the WI-586/`MMT-ADR-0020` catalog-driven `m-repoint` cutover, not the `MMT-ADR-0012` baseline-collapse the original spec assumed) is itself accurate and matches the identical correction on the sibling note-correctness plan — same gate, same current state, not yet cleared.

IMPLEMENTED: per claim — none/partial/complete/superseded, file:line
1. **complete but flag-gated** — items 1-2, write side. No candidate WI (correctly — nothing to fix, it's built and intentionally dark pending the rename).
2. **partial** — item 3, star: works, but only on the surface that V2 nav is retiring. WI-1456.
3. **complete** — item 4, tutor correction on recall. No candidate WI (correctly — nothing open).
4. none — item 5. WI-1454.
5. none — item 6. WI-1455 (home = row 8, see that sheet; do not duplicate ownership here).

CANDIDATE WIs: each with fate: adopt / merge-into-<WI> / kill (+reason); WI-1455 spans rows 8+9, propose its single home
- **WI-1454** (concept-targeted review, item 5) — **adopt, but hold pending flag flip.** Genuinely unbuilt, fully specced, and — critically — has **no dependency on the parked `concept_mastery` table**: the spec itself notes item 5 "leans on existing `needs_deepening_topics` data; `concept_mastery` is not required for it" (spec line 149). This makes it the one item in this whole row that is **not** identity-rename-gated and could theoretically ship independently of the write-side flag flip. Worth flagging for earlier sequencing than the rest of the row.
- **WI-1455** (note-correctness nudge, item 6) — **home: row 8, not row 9.** See sheet 08 for full reasoning: the note-correctness plan's own T12 explicitly supersedes this spec's item 6 ("do not build those"), making the note-correctness plan the single design owner. Recommend merge-into-WI-1491 (the note-correctness umbrella) rather than tracking as a row-9-owned item. Do not duplicate scope between rows.
- **WI-1456** (re-home the concept-mastery star to a V2 surface) — **adopt, but this is a placement decision, not a build task, until product rules on it.** Tech-confirmed: the star is invisible under V2 nav today because its sole host (V1 book reader) isn't reachable once V2 is default. No spec defines the V2 home (Subject Hub notes list vs. Journal merged archive) — this is squarely a product/design call, not an engineering unknown. Low implementation cost once decided (reuse `useConceptMasterySignals` verbatim against a new host component).

VERDICT: partially-implemented
Write side (items 1-2) is genuinely complete and correctly dark behind a flag pending the identity rename — no action needed there. Read side is a mixed bag: item 3 (star) and item 4 (tutor correction) are built; items 5 (targeted review) and 6 (nudge) are cleanly unbuilt as claimed. The register's pre-bucket ("write-side shipped, read-side items live") is accurate and needs no correction, unlike row 8 where two of the three candidates rested on stale premises.

MVP RECOMMENDATION: in / out / finish-or-hide vs north star (Config T V2 shell on Google Play, RevenueCat Plus-only, proven V1 fallback)
- **finish-or-hide, priority item: WI-1456.** This is the one genuinely time-sensitive item in the row — the star feature **currently works** for users on V1, but silently disappears the moment V2 becomes the default nav shell (no re-spec, no flag check, just an orphaned callsite). Shipping V2 to Google Play without resolving this either (a) quietly regresses a working feature with no user-facing signal, or (b) needs an explicit product ruling that the star is acceptably V1-only until a later release. Burden of proof: since this is a *regression risk on cutover* rather than a *net-new feature request*, it deserves a placement decision before V2 general release, even if the actual re-wiring is trivial once decided.
- **OUT for MVP: WI-1454** (concept-targeted review). Real, fully-specced, low-risk, and notably **not** blocked by the identity rename — but it's a recall-quality refinement (narrows what a due-topic review focuses on), not a launch blocker. Fine to sequence post-V2-launch; flag its rename-independence for Phase 2 in case there's appetite to pull it forward opportunistically.
- **OUT for MVP: WI-1455** (nudge) — tracked under row 8's WI-1491 umbrella; same disposition as that row (polish behind the identity-rename gate, not core value).

CONFIDENCE: high + up to 3 decidable Zuzka questions
High confidence on all claims — spot-checked against current source for both write-side flag references and read-side callsite absence/presence; cross-verified the item-6/note-correctness-plan overlap directly against row 8's independent check for consistency (both sessions found the identical unbuilt state).
1. **V2 star placement (WI-1456):** Subject Hub notes list, Journal merged archive, or both — and does the V1 book-reader instance stay live until S6 retirement, or get pulled the moment V2 is default? This blocks WI-1456's actual implementation.
2. Should WI-1456 be treated as a **regression-prevention** item (must land before/with V2 general release) rather than a normal enhancement, given the feature works today and silently vanishes on cutover with no flag or fallback?
3. Confirm: is WI-1454 (concept-targeted review) worth pulling into an earlier milestone specifically because it's rename-independent, or should it stay sequenced with the rest of the concept-capture backlog regardless?
