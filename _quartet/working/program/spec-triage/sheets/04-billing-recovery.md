DOC: docs/plans/2026-05-31-billing-recovery-learner-capacity.md (2026-05-31, 27K)

CLAIMS: (per-task, 1 line each)
1. T1 — `app/payment.failed` is currently log-only observe; needs to become a real push+email+in-app-banner flow with a new `payment_failed` EmailType.
2. T2 — no past-due banner exists on the home/account surface; owners need a grace-deadline-aware banner with defined precedence vs. the child-cap banner.
3. T3 — child-cap parent alerts write a DB row but never actually push; dedup is per-calendar-day not per-quota-window, and the child-facing "sent" copy overclaims delivery.
4. T4 — no owner→child top-up allocation exists; `purchaseTopUpCredits`/`consumeOwnerTopUpCredit` are owner-locked at three layers, and this is **hard-blocked on `learn-1`** (non-owner child 403s every learning write via `assertNotProxyMode`).
5. T5 — the `ParentHomeScreen` child-cap banner is dismiss-only; no "allocate credits vs. buy more" parent action exists.
6. T6 — no structured observability exists for billing-alert / child-cap-alert / allocation-consumed failure paths; the repo's silent-recovery-in-billing ban is currently unmet here.

TECH VALIDITY: per broken assumption, file:line (checked against current subscription/entitlement services + RevenueCat integration state)
- T1 confirmed current: `apps/api/src/inngest/functions/payment-failed-observe.ts:1,16` — still tagged `@inngest-admin: no-db (logging observer)`, comment still says "a real retry/notify/dunning strategy is [TODO]." No `sendPushNotification`/`sendEmail` call in the function. Claim holds.
- T2 confirmed current: zero occurrences of `past_due`/`pastDue` anywhere in `apps/mobile/src/components/home/ParentHomeScreen.tsx`. No past-due banner exists. Claim holds.
- T3 partially stale: `notify-parent-child-cap-hit.ts:35` still only returns `{status:'recorded', inserted: result.inserted}` — no push call, so the "no real push" half of the claim holds. But the **honesty-copy fix the plan calls for is already shipped**: `en.json:3343,3348` — `sentBody: "We let your parent know!"`, `quotaSentBody: "We let your parent know you hit your question limit."` — this is already a records-an-ask framing, not the delivery-confirmed "Your parent has been notified" the plan describes as the bug (EU-HIGH-3). The plan's file:line citations for this copy are stale/pre-fixed; the push-fan-out gap itself is not.
- T4's core premise re-verified against **current** (not plan-era) code, which has moved to a `billing-v2` path since 2026-05-31: legacy `purchaseTopUpCredits` was deleted (WI-1239/779-strip, `apps/api/src/services/billing/top-up.ts:12-17`); the owner-lock behavior the plan describes now lives in `apps/api/src/services/billing/billing-v2/top-up-v2.ts:73-79` (`buyerProfileId` forced to `findOwnerPersonId(...)`, returns `null` on mismatch for per-profile tiers) — same defect, new file. `metering.ts:836-848` (tagged `[F-135]`) still gates top-up decrement to `profileRole !== 'owner'` → `0`. **The HIGH-1 blocker is independently reconfirmed current**: `assertNotProxyMode` (`apps/api/src/middleware/proxy-guard.ts:58`) still throws 403 on `profileMeta.isOwner === false`, and is still called at the top of every session-write handler (`apps/api/src/routes/sessions.ts:268,300,340,353,433,454,484,514,633,706,1246,1317,1338,1357,1375,1395,1423,1460,1507,1528` — 20 call sites, same shape as the plan's citation). WI-1477's "blocked on learn-1" flag is correct and still true today.
- T5 confirmed current: `ParentHomeScreen.tsx:162-173` — the only action on the child-cap notification card is a dismiss `Pressable`; no allocate/purchase action exists.
- T6: no independent re-check beyond Found-In; plausible given T1/T3's confirmed log-only state (nothing to instrument yet).
- **Cross-cutting note on WI-1328 (RevenueCat MVP monetization, P1/WS-35):** `apps/mobile/src/lib/revenuecat.ts:17-25,38-55` confirms purchases are currently **off by design** in production — `getRevenueCatApiKey()` returns `''` when env keys are unset, and `configureRevenueCat()` reports (Sentry in prod) "purchases are disabled in this build" rather than crashing. Per `_quartet/working/lanes/v2-finalization/_state/shepherd-live-state.md:40`, the prod binary today ships **empty RC keys by design** (Test Store app only), and WI-1328 phase 5 is the tracked re-hardening once real keys land. `PRODUCT_TIER_MAP` (`revenuecat-shared.ts:61-76`) still carries all 7 tiered products (plus/family/pro × monthly/yearly, +Android suffixes) intact — Option A's product set is unchanged. **Practical read: no user can be a paying subscriber in production yet**, so today there is no live past-due or child-cap-exhaustion user to protect. This plan's relevance is gated on WI-1328 landing, not on its own merits.

IMPLEMENTED: per claim — none/partial/complete/superseded, file:line
1. none — WI-1479 is T6, not T1; T1 has no direct candidate in this row's 5 (T1/T2 were the "do-now" carve per the plan's own header but only T2 got a WI — T1 appears to have been dropped from extraction). Flag as a gap below.
2. none — WI-1475.
3. **partial** — copy-honesty half shipped (see above); push-fan-out half not — WI-1476.
4. none, and blocked — WI-1477.
5. none — WI-1478.
6. none — WI-1479.

CANDIDATE WIs: each with fate: adopt / merge-into-<WI> / kill (+reason)
- WI-1475 (T2, past-due banner) — adopt, but re-spec as a V2 NowCard per the register's own note; `ParentHomeScreen` is the legacy/V0 family-home surface (per `home.tsx:165`, gated on `navigationContract.home.screen === 'FamilyHome'`), not confirmed as the V1/V2 target. Do not build against the legacy screen without confirming which shell it must land in.
- WI-1476 (T3, child-cap fan-out) — adopt. Copy-honesty half already done narrows scope to: gate push on `inserted===true`, move dedup to `resetsAt` window, add the actual push call. Genuinely a **Bug** (typed correctly) — the repo's silent-recovery-in-billing ban is currently violated by the log-only/no-push path masquerading as "notified."
- WI-1477 (T0+T4, child-allocated top-up) — kill for now / re-open only after `learn-1` resolves. Confirmed still blocked today (proxy-guard unchanged); building T4 ahead of `learn-1` produces spendable credits a child cannot actually use (403 on every write). Re-file once `learn-1` lands.
- WI-1478 (T5, parent action from banner) — merge-into-WI-1477 (same dependency chain; T5 explicitly depends on T4 in the plan). Kill/park alongside T4 until `learn-1` resolves.
- WI-1479 (T6, observability) — adopt but scope to whatever of T1/T2/T3 actually ships; don't build instrumentation for T4/T5 paths that are parked.

**Gap flagged:** T1 (payment-failed → real notification) has no corresponding candidate WI in this row's 5, despite being explicitly the do-now, non-identity-coupled, small-effort slice per the plan's own PARKED header ("T1 + T2 = non-identity, DO-NOW slice... key it on the paying account"). This looks like an extraction miss, not a deliberate drop — Phase 2 consolidation should confirm whether T1 needs its own capture.

VERDICT: needs-product-ruling
Dominant: the plan is **valid** on T2/T3 (small, real gaps, tech-confirmed current), **partially-implemented** on T3's copy half, and **correctly-blocked** on T4/T5 pending `learn-1` — none of that has changed since 2026-05-31/06-08. The overriding factor is sequencing, not correctness: this whole row's urgency is gated on WI-1328 (RevenueCat going live) landing first, since production purchases are currently off. Route the T2/T3/T6 do-now slice for MVP timing alongside WI-1328, not independently.

MVP RECOMMENDATION: in / out / finish-or-hide vs north star (Config T V2 shell on Google Play, RevenueCat Plus-only purchases, proven V1 fallback)
The instructions are right that billing-recovery UX for paying users sits closer to MVP than most rows — a paying parent hitting a silent cap **is** a revenue/trust defect, not a nice-to-have. But "in" only becomes true the moment RC purchases actually go live (WI-1328); shipping V2 to Google Play with RC keys still empty means zero paying users exist to protect on day one.
- **IN, sequenced with WI-1328 (not independently gated):** WI-1475 (past-due banner, re-specced as NowCard) + WI-1476 (honest child-cap push, scope-narrowed) + WI-1479 (observability for whichever ships). These are the plan's own do-now carve, small, non-identity-coupled, and directly protect the first cohort of real payers. Land in the same milestone window as WI-1328 phase 5 (RC key re-hardening), not before (nothing to protect) and not much after (a payer with no recovery path in week one is the exact defect this plan exists to prevent).
- **OUT for MVP, revisit post-`learn-1`:** WI-1477 (T0+T4 child-allocated top-up) and WI-1478 (T5 parent action). Genuinely blocked, not a scope call — building them now produces unusable inventory (credits a child can't spend). Confirm `learn-1`'s resolution status in the identity-foundation roadmap before re-opening.
- **Flag for Phase 2:** the missing T1 candidate (payment-failed → user-facing notification) — this is arguably the single highest-leverage item in the whole plan (a payer who silently loses access and never finds out) and currently has no tracked WI at all in this row.

CONFIDENCE: high/med/low + up to 3 decidable Zuzka questions
Confidence: **high** on tech validity (5 of 6 tasks spot-checked directly against current code, including the billing-v2 migration the plan predates); **medium** on MVP sequencing (depends on WI-1328's actual launch timeline, which this auditor didn't verify beyond the shepherd-state note).
1. Is WI-1328 (RevenueCat MVP monetization, empty prod keys today) scheduled to land before or in the same milestone as the V2 Google Play publish — i.e., does billing-recovery need to be ready for day-one paying users, or is there a grace window?
2. Is `learn-1` (non-owner child 403 on every learning write) tracked and scheduled anywhere in the identity-foundation roadmap, or is it currently an unscheduled blocker that should itself get a WI?
3. Confirm/deny: should a WI-1480 (T1, payment-failed → real notification) be captured now, given it's the plan's own top-priority do-now item and currently has zero tracked candidate?
