DOC: docs/specs/2026-06-09-mentor-is-the-app-shell-redesign.md (2026-06-09, 103K, 612 lines) + docs/plans/v2-plan/ (S0-S6 phase plans, `00-STATE-OF-PLAY.md`) + docs/plans/v2-dossier/ (00-07, screen/trigger maps) (2026-06-13 to 2026-07-02)

CLAIMS:
- Spec §13 lists 7 open decisions gating specific phases, not the whole spec; item 1 is "V0-preservation constraint retirement — when does `MODE_NAV_V0_ENABLED` no-regress constraint formally retire? Owner: product (Zuzana). Blocks S6 only."
- S6 cutover plan (`v2-plan/2026-06-10-s6-cutover-deletions.md`) is DEFERRED, marked "must never be started by an agent autonomously," gated on (a) P3 eval coverage [MET], (b) the §13.1 product ruling [NOT MADE], (c) lost-V1-flow heirs live [BLOCKED — S4 cold-start/co-learning + S5 linking-ceremony gaps], plus a mandatory human irreversibility confirmation.
- v2-dossier 06 "Concrete Progress Ownership Split (WI-1172)" table names 3 legacy-Progress-only signals with no V2 home: CEFR vocabulary browser, full milestone history list, live global engagement glance (streak/sessions/minutes/recall-queue) — all marked `OPEN`, explicitly "tracked for the WI-1174 retirement-gate audit," and explicitly out of WI-1175's 7 publish-critical prompts.
- v2-dossier 07 "Current Gaps" table: visibility ceremony (`/link/new`) "depends on upstream anchors... remaining work is wiring S4 cold-start/Approve anchors into `/link/new`."
- v2-dossier 06 line 56 (Visibility ceremony row) still reads appeal affordance as "in-flight rework (WI-1171)"; 07 line 190 still lists "Support hub is still mostly list/placeholder UI" as an open gap.
- v2-dossier 06 has an internal contradiction: line 34 says Journal's `JournalSectionId` has no Practice section in code; line 46 (Quiz/Dictation/Practice row) still lists "Journal practice section" as a discovery surface.

TECH VALIDITY:
- No broken assumptions found — spec §13 and the S6 plan's gates are accurately describing current code state as of today. Confirmed live: `apps/mobile/src/app/(app)/_layout.tsx:72,101` lists `link/new` only as a `HIDDEN_TAB_ROUTES` entry; repo-wide grep for `router.push` to `/link/new` returns zero hits — no product-UI trigger reaches it, exactly as 07's gap row claims. Ruled out the obvious alternative mechanism: `apps/mobile/src/components/support/**` has zero references to `visibility/links`, `createLink`, or `invite`/`Invite`, and repo-wide grep for callers of `POST /visibility/links` other than `link/new.tsx` itself returns zero hits — there is no second linking affordance anywhere in the V2 support-hub tree. (The legacy "Add child" flow — `app/profiles.tsx`, `create-profile.tsx`, `more/index.tsx` — is a different mechanism: adding a child to one's own account, not credentialized/managed supporter-to-supportee linking.) So this is not "one ceremony screen unwired while linking works another way" — in the V2 shell as it stands today, a supporter genuinely cannot start a new share request from any screen.
- WI-1171 (appeal affordance) and WI-1170 (support hub) are actually Closed and merged per `docs/plans/2026-06-30-v2-publish-readiness-canonical-plan.md` lines 200-234 (PR #1751, `AppealButton.tsx` merged) — so dossier 06:56 and 07:190 are stale relative to that later, more-current doc.

IMPLEMENTED:
- S6 V0/V1 retirement ruling (WI-1440's subject): none. Product ruling not made; execution explicitly forbidden pre-ruling. Spec §13 item 1 + S6 plan Gate (b), `v2-plan/2026-06-10-s6-cutover-deletions.md:15-26,42`.
- Link-ceremony forward anchors (WI-1393): partial — `/link/new.tsx` and `/link/[contractId].tsx` exist and are tested (06:56), but zero forward navigation trigger exists anywhere in mobile source; user-visible effect is that a supporter cannot start a share request from any screen today.
- CEFR vocabulary re-home (WI-1394): none — legacy `progress/vocabulary.tsx` still the only home; confirmed by grep, 06:138.
- Milestone history re-home (WI-1395): none — legacy `progress/milestones.tsx` still the only home for the full paged list (Journal only surfaces milestones as they cross the feed); 06:139.
- Engagement glance re-home (WI-1396): none as a live chip — underlying values ARE captured in report snapshots (`packages/schemas/src/snapshots.ts:30-48`), so data isn't lost, but no current-value glance exists outside legacy `ProgressStatsChips.tsx`; 06:140.
- Dossier staleness (WI-1397): partial — confirmed 2 concrete stale claims (06:56 appeal-affordance "in-flight", 07:190 support-hub "list/placeholder") both superseded by the canonical plan's 2026-07-02 reconciliation; plus one internal 06 contradiction (Journal practice section, line 34 vs line 46). Not yet independently re-verified: the WI title's specific "person-scope" claim.

CANDIDATE WIs:
- WI-1440 (S6 ruling execution): merge-into-pointer — it duplicates existing gate tracking (spec §13.1 + S6 cutover plan's own Gate (b), `v2-plan/2026-06-10-s6-cutover-deletions.md:42`), which is the more precise target since Gate (b) IS the ruling-tracking mechanism; WI-1174 is a separate, sequential audit-prep item ("does not build new UI," 06:143-145) and shouldn't absorb this. Keep WI-1440 open as a blocked/pointer item referencing Gate (b), not an executable task, until product rules.
- WI-1393 (link-ceremony forward anchors): adopt, elevate — real, code-confirmed gap, and confirmed (not just inferred) that no alternate linking mechanism exists anywhere in the V2 support-hub tree; this is stronger than the dossier's own "OPEN, S4/S5 residual work" framing implies.
- WI-1394 (CEFR vocabulary browser re-home): adopt, backlog — confirmed real, but dossier and WI-1175 both agree it's non-publish-blocking.
- WI-1395 (milestone history re-home): adopt, backlog — same disposition as 1394.
- WI-1396 (engagement glance re-home): adopt, backlog — same disposition as 1394; note underlying data is not at risk, only the always-current chip.
- WI-1397 (dossier doc staleness): adopt — confirmed stale content exists (06:56, 07:190) and overlaps with the canonical plan's own "Recommended follow-up actions" #1 (docs/plans/2026-06-30-v2-publish-readiness-canonical-plan.md:249-254); WI-1397 is the right executing ticket for that already-identified doc fix, not a duplicate to kill.

VERDICT: valid

MVP RECOMMENDATION: WI-1440 out (S6/V0-retirement is explicitly out of scope for WS-28 publish readiness per the canonical plan's own scope list — "Executing S6 deletions or flipping V2 to production default" is listed Out of scope). WI-1393 finish — verified (see TECH VALIDITY) that no alternate linking affordance exists in the V2 support-hub tree, so this is not a cosmetic gap: a supporter cannot start any new share request in the shipped shell. Supporter V2 is the plan's own "critical publish gap" (row 18 doc); recommend treating this as a publish blocker or having WI-1175 explicitly disclose that new-supporter linking is unreachable in V2 today, rather than leaving it framed as ordinary S4/S5 residual work. WI-1394/1395/1396 out (non-publish-blocking per WI-1175's own 7-prompt list; legacy routes stay live as fallback). WI-1397 in (cheap, low-risk doc-accuracy fix; already recommended by the canonical plan review).

CONFIDENCE: high. Decidable questions for Product (Zuzka): (1) Is `/link/new` unreachability from any V2 screen acceptable for MVP publish, given Supporter V2 is called the critical publish gap? (2) Should WI-1394/1395/1396 be explicitly deferred with an owner/date now, or left silently OPEN in the dossier until WI-1174's audit closes? (3) Does the §13.1 V0-retirement ruling have a target date, or is it intentionally unscheduled pending post-MVP telemetry?
