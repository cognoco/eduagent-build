DOC: docs/plans/2026-05-31-resumable-practice-state.md (2026-05-31, 32K)

CLAIMS: 3-6 bullets
1. Quiz and dictation practice are lost entirely on app-kill; no persistence layer exists (`apps/mobile/src/lib/practice-recovery.ts` does not exist).
2. The quiz quit modal's terminal action lied — "Save and finish" / "Save progress and finish round" implied preservation while actually finalizing a partial round as final (gap `practice-4`).
3. Building a full persistence engine (quiz-pointer + dictation-sentence-array markers, CoachBand slot, sign-out cleanup, break tests) is expensive scaffold work that should be **deferred** post-launch; only the honest-copy fix (`T0`/`T3-lite`) is recommended pre-launch.
4. `T0` (rename the modal copy to state the true consequence) is a ~1hr, zero-persistence, zero-new-surface fix that resolves the harmful half of `practice-4` on its own.
5. If/when the engine is built, it must slot into the home `CoachBand` priority stack (single-slot) AND provide a `practice/index.tsx` fallback affordance, since CoachBand can only show one resume source at a time.
6. Quiz resume is "mostly a server-reconstruction problem" (server already records every answer via `useCheckAnswer`/`useCompleteRound`) while dictation resume requires persisting LLM-generated sentence objects locally (no server record).

TECH VALIDITY: per broken assumption — esp. pre-V2 surface assumptions vs the live V2 shell, file:line
- **Claim 1 (zero resume infra) is still true, and independently reconfirmed.** `find apps/mobile/src/lib -iname "*practice-recovery*"` returns nothing — the module still does not exist.
- **Claim 5's target (`CoachBand`/`LearnerScreen.tsx`) is a dead-in-V2 surface**, same finding as row 3: `apps/mobile/src/hooks/use-navigation-contract.ts:22` — `V2_TABS = new Set(['mentor', 'subjects', 'journal'])` excludes `home`; under `MODE_NAV_V2_ENABLED` the entire `CoachBand`/`LearnerScreen` stack this plan's T5 targets is unreachable via the tab bar. A V2-era engine cannot "slot into the CoachBand priority stack" as literally specified — it would need a `NowCard` kind instead.
- **The V2 shell already has a server-driven resume mechanism, but it only covers tutoring sessions, not quiz/dictation.** `apps/mobile/src/app/(app)/mentor.tsx:12-27` renders `NowCardStack` fed by `useNowFeed`, which includes an `unfinished_session` card kind. Server-side, `apps/api/src/services/now-feed.ts:494-521` (`collectUnfinishedSessionCandidates`) queries `learningSessions` where `status = 'active'` and produces a `session.resume` deep link — this is the *tutoring session* resume equivalent of the old `session-recovery.ts` marker, re-platformed onto the server/NowCard model. **It does not read quiz rounds or dictation state at all** (confirmed: no reference to quiz/dictation tables in `now-feed.ts`'s candidate collectors). So the specific gap this plan targets (`practice-1`/`practice-2`: quiz/dictation app-kill loss) is untouched by the V2 shell and remains exactly as described.
- **Claim 6 (quiz = server-reconstruction, dictation = must persist locally) is architecturally unaffected by V2** — this is an API/data-layer fact (`useCheckAnswer` posts per-answer, LLM-generated dictation sentences aren't reproducible), independent of which shell renders the resume affordance. Still valid.
- **Task/claim 2's premise (dishonest copy) is now false — it was fixed independently of this plan.** See IMPLEMENTED below.

IMPLEMENTED: per claim — none/partial/complete/superseded, file:line
1. **confirmed still true (none built)** — `practice-recovery.ts` absent; `sign-out-cleanup.ts` has no `practice` key reference (confirmed via `rg`, zero hits).
2/4. **complete — `T0` has already shipped**, independently of this plan. `apps/mobile/src/i18n/locales/en.json:1138-1149`: `pauseHere` → "End this round?", `pauseBody` → "Your answers so far become your final score — unanswered questions are left blank.", `saveAndFinish` → "End round now", `saveAndFinishLabel` → "Submit your answers so far as final". Git provenance: `af91a3e18` ("Copy wins: honest quiz-quit modal…", PR #1543, 2026-06-27, message cites `[BUG-892]`) — this landed via the bug tracker, not this plan, but the resulting code is word-for-word the plan's own recommended `T0` copy (compare plan lines 90-95 to the shipped strings — an exact match). `play.tsx:1219,1223` still wires these same i18n keys to `handleSaveAndQuit`, confirming the button still finalizes (not truly resumable) — consistent with `T0`-only, not the full `T3` two-action modal.
3. **partial — the plan's own recommendation was followed**, whether by design or coincidence: the cheap copy fix shipped, the expensive engine (T1/T2/T4/T5/T6) did not.
5. **none built** — no CoachBand practice-marker wiring, no `practice/index.tsx` resume affordance found.
6. **not applicable to implementation status** — this is an architecture note, not a task; still accurate as analysis.

CANDIDATE WIs: each with fate: adopt / merge-into-<WI> / kill (+reason)
- WI-1489 (umbrella: engine + V2 resume surface) — **kill as filed; split if reopened.** As a single umbrella it conflates (a) a now-moot honest-copy ask (already shipped as `af91a3e18`, so close that slice as superseded) with (b) a genuinely deferred, unbuilt persistence engine that the plan's own 80/20 analysis says should wait for post-launch telemetry, and whose target UI surface (`CoachBand`) is dead in V2 regardless. Recommend: close WI-1489 with a note citing `af91a3e18` for the shipped half, and do not open a replacement WI for the engine half unless post-launch signal (support tickets, telemetry) shows learners actually losing in-progress quiz/dictation work to app-kill — per the plan's own stated trigger condition.

VERDICT: valid | partially-implemented | superseded | obsolete | needs-product-ruling
**Partially-implemented (the recommended slice already shipped) and the remaining scope is correctly deferred, but its build target needs updating for V2.** The plan's central judgment call — "ship the honest copy now, defer the engine" — was executed exactly as recommended, just without this plan getting credit/closure for it. The undone remainder (the persistence engine) is still validly deferred per the plan's own 80/20 logic (zero users, unmeasured pain, expensive scaffold) — nothing about V2 changes that judgment. What V2 *does* change is the mechanical target: `CoachBand`/`LearnerScreen.tsx` is gone from the reachable tab set, so any future engine build must target a `NowCard` kind (e.g. a new `unfinished_practice` card feeding off `NowCardStack`) and the server-side `now-feed.ts` collector pattern already established for `unfinished_session`, not the old client-only SecureStore/AsyncStorage marker + CoachBand-slot design as literally specified.

MVP RECOMMENDATION: in / out / finish-or-hide vs north star (Config T V2 shell on Google Play, RevenueCat Plus-only, proven V1 fallback); burden of proof on inclusion
- OUT (already done, no action needed): the honest-copy fix — verify closure only.
- OUT (fails burden of proof for MVP): the full persistence engine (quiz/dictation app-kill resume). Zero users pre-launch, no telemetry signal possible yet, and the correct build target (NowCard-based, server-driven) isn't even the one this plan specifies — building it now risks a second throwaway, same as the plan's own identity-foundation-coupling concern for the product-continuity row. Revisit post-launch per the plan's stated trigger (support/telemetry evidence of real loss).

CONFIDENCE: high/med/low + up to 3 decidable Zuzka questions
Confidence: **high** — both the "already shipped" finding (exact string match + git blame) and the "V2 doesn't cover quiz/dictation" finding (direct read of `now-feed.ts`'s candidate collectors) are primary-source-verified, not inferred.
1. Should WI-1489 be closed now citing `af91a3e18` as the shipped honest-copy slice, with the engine explicitly deferred (not silently dropped)?
2. If/when the engine is later greenlit, should it be re-specced from scratch as a `NowCard`/`now-feed.ts` server-driven design (matching how `unfinished_session` now works) rather than resurrecting the CoachBand/SecureStore design in this plan?
3. Is there already a support/telemetry signal (even pre-launch, from internal dogfooding) of learners losing in-progress quiz/dictation work to app-kill that would justify pulling the engine into scope now instead of waiting for post-launch data?
