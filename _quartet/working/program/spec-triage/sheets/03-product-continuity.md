DOC: docs/plans/2026-05-31-product-continuity-low-hanging-fruit.md (2026-05-31, 31K)

CLAIMS: 7 tasks phased A(do-now)/B(identity-blocked)/C(guarded), all pre-V2
1. Task 1 — Learner Home coach band should become a specific "best next step" card with `body`/`ctaLabel` and a resolver module (`learner-next-step.ts`); replace "starting to fade" with positive copy.
2. Task 2 — Session summary should get a "Saved from this session" receipt card (positive-only, omit skipped/empty rows).
3. Task 3 — My Notes hub should show recent previews instead of empty-feeling count cards; flagged as identity/proxy-blocked (privacy gate needed).
4. Task 4 — Recap detail/list should surface `nextTopicTitle`/`nextTopicReason` "Coming up" fields for mentors; flagged for an unguarded-LLM-text-to-parent risk.
5. Task 5 — Parent nudges (`NudgeActionSheet`) should become context-aware (`recommendedTemplate`, `contextLine`), gated behind identity/proxy rework.
6. Task 6 — Learner-facing retention copy should drop deficit words ("fading", "weak") in favor of constructive phrasing.
7. Task 7 — Defer `ChatShell`'s mount-time microphone permission request until an intentional voice action.
8. Doc's own phasing (2026-06-09 review): only Phase A (Tasks 1+6, copy-only) is recommended pre-launch; B (2,3,5) parked on the identity-foundation clean-cut; C (4,7) optional/guarded.

TECH VALIDITY: per broken assumption — esp. pre-V2 surface assumptions vs the live V2 shell, file:line
- **All 7 target files are pre-V2 shell.** The doc's File Map targets `LearnerScreen.tsx`, `CoachBand.tsx`, `ParentHomeScreen.tsx`, `NudgeActionSheet.tsx`, `recaps/*`, `my-notes/*` — none of these live under the V2 tab set. `apps/mobile/src/hooks/use-navigation-contract.ts:22` — `V2_TABS = new Set(['mentor', 'subjects', 'journal'])`; `home` is absent. When `FEATURE_FLAGS.MODE_NAV_V2_ENABLED` is on (`use-navigation-contract.ts:185,194`), the entire `home` tab — and therefore `LearnerScreen`/`CoachBand`/`ParentHomeScreen` — is unreachable via the tab bar. Tasks 1, 2 (partially — session-summary is a pushed route, not a tab, but is reached from `home`/session flows), 5 target a hidden-in-V2 surface.
- **V2's actual equivalent of Task 1 is the NowCard stack, and it already covers the same ground differently.** `apps/mobile/src/app/(app)/mentor.tsx:12-27` renders `NowCardStack` fed by `useNowFeed`; card kinds `unfinished_session`, `retention_due`, `parked_item`, `needs_deepening`, `challenge_ready` (`apps/mobile/src/components/mentor/NowCard.tsx:31-56`) are a richer, multi-card, server-driven replacement for the old single-slot `CoachBand` priority stack. A V2-era Task 1 would mean writing card copy/body text for `NowCard`, not building `learner-next-step.ts` against `CoachBand`.
- **Task 4's target (recaps) is superseded by journal in V2.** `apps/mobile/src/app/(app)/journal/index.tsx` + `JournalTabView.tsx` (uses `useJournalRecaps`, `hooks/use-journal-recaps.ts:15`) is the V2 tab; `recaps/index.tsx` / `recaps/[recapId].tsx` still exist and are still referenced by `ParentHomeScreen.tsx:55` (`useRecaps`) and `notification-tap-navigation.ts:45`, but `JournalTabView.tsx` has **zero** references to `nextTopicTitle`/`nextTopicReason` — confirmed via `rg` (no hits). Building Task 4 against `recaps/*` builds into the legacy V1 family surface, not the V2 journal surface actually shipped to guardians on V2.
- **Task 3's target (`my-notes`) is orphaned in current navigation.** `rg "'/my-notes` across `apps/mobile/src` (excluding its own directory) returns zero hits — no in-app push source found to `/my-notes` from any reachable screen today, V1 or V2. The route file still exists (`apps/mobile/src/app/(app)/my-notes/index.tsx`) but may only be reachable by direct deep link, if at all.
- **Task 5's target (`NudgeActionSheet`) is a guardian/proxy surface** — `ParentHomeScreen` is V1-tab-gated the same way as Task 1/4; V2's guardian "recaps" replacement is the `journal` tab, and nudge-sending has no visible V2-era equivalent found in this pass.
- **Task 7's premise is still live and unaffected by V2**: `apps/mobile/src/components/session/ChatShell.tsx:381-383` — `useEffect(() => { void requestMicrophonePermission(); }, [requestMicrophonePermission])` fires unconditionally on mount, confirming the plan's claim is current. `ChatShell` is a session-flow component reachable from both V1 and V2 shells, so this task is genuinely surface-independent.

IMPLEMENTED: per claim — none/partial/complete/superseded, file:line
1. **partial** — copy-only half shipped, structural half not. Commit `af91a3e18` ("Copy wins: honest quiz-quit modal, positive retention framing, warm first-session greeting", #1543, 2026-06-27) changed `home.learner.coachBandRevisitTopic` from "starting to fade" to "ready for a quick refresh" and `intentActions.practice.subtitle`. But `CoachBand.tsx:14-20` still has no `body`/`ctaLabel` props (headline-only interface), and no `learner-next-step.ts` resolver exists anywhere in `apps/mobile/src` (confirmed via `find`). Done-when criteria "action-specific CTA labels" and "reason line" are unmet.
2. **none** — `apps/mobile/src/app/session-summary/_view-models/session-summary-artifacts.ts` does not exist; no receipt-card evidence found.
3. **none** — `my-notes/index.tsx:1-50` confirmed still uses only `useProfileSessions` (session count), no `useAllNotes`/`useBookmarks` preview calls found via `rg`.
4. **none** — no `RecapComingUpCard` component found; `recaps/[recapId].tsx`/`index.tsx` not modified per this task's shape.
5. **none** — `NudgeActionSheet.tsx` has no `contextLine`/`recommendedTemplate` props (confirmed via `rg`, zero hits).
6. **complete for the two cited visible strings, per commit `af91a3e18`** (same commit as Task 1's copy half) — `coachBandRevisitTopic` and the practice-subtitle string were both softened and translated across 6 locales per the commit message. The doc's own scoped claim ("only *visible* deficit strings are `en.json:159` and `LearnerScreen.tsx:367`") is now resolved for those two sites specifically.
7. **none** — `ChatShell.tsx:381-383` still requests mic permission on mount, exactly as the plan describes.

CANDIDATE WIs: each with fate: adopt / merge-into-<WI> / kill (+reason)
- WI-1480 (T1, best-next-step NowCard) — **adopt, but re-spec against NowCard, not CoachBand.** The copy half is done; the remaining work (a reasoned, action-specific card) should be built as a `NowCard` variant/copy improvement, not a `CoachBand` resolver — the old target is a dead-in-V2 surface. Kill the literal "re-specced as a V2 NowCard" framing's implication that this is small; it is a re-scope, not a port.
- WI-1481 (T2, saved-receipt card) — **kill for now / defer.** Targets `session-summary/[sessionId].tsx`, which is reachable from both shells, so it isn't dead — but it's an unbuilt Phase-A-adjacent nice-to-have with a documented "reads as failure scorecard" trap and zero users to validate against. Not launch-blocking.
- WI-1482 (T3, My Notes previews + proxy privacy gate) — **kill.** Target route has no confirmed in-app entry point in the current codebase (V1 or V2); building preview logic plus a privacy gate for an orphaned screen is work with no verified user path. Re-open only if `my-notes` is confirmed reachable from a live V2 surface.
- WI-1483 (T4, recap "coming up") — **kill / needs-product-ruling on target surface.** Target (`recaps/*`) is the pre-V2 guardian surface; V2 ships `journal` instead, which has no equivalent field wiring. Re-file against `JournalTabView.tsx` + `useJournalRecaps` if this is still wanted, with the doc's own unguarded-LLM-text-to-parent finding (Task 4 review finding) carried forward.
- WI-1484 (T5, context-aware parent nudges) — **kill.** Depends on the same pre-V2 `ParentHomeScreen`/`NudgeActionSheet` pairing and the identity-foundation clean-cut the plan itself parks this behind; no V2 nudge-sending surface found to re-target it to.
- WI-1485 (T6, deficit-language copy check) — **kill — already done for the plan's own cited scope.** Both visible strings named in the plan (`en.json:159`/`coachBandRevisitTopic`, `LearnerScreen.tsx:367`) are fixed per `af91a3e18`. If a broader `rg` sweep for other deficit words is still wanted, re-file as a small standalone audit item, not this WI.
- WI-1486 (T7, mic-permission deferral) — **adopt.** Confirmed still-live, surface-independent (not V1/V2-coupled), small, behavior-sensitive but well-specified with a red-green test requirement already in the plan.

VERDICT: valid | partially-implemented | superseded | obsolete | needs-product-ruling
**Partially-implemented, and partially superseded by the V2 NowCard stack.** The plan's own recommended Phase A slice (Tasks 1+6 copy layer) is ~50% shipped (copy done, structure not) via an out-of-band commit the plan never references. Phase B tasks (2, 3, 5) target surfaces that are either hidden-in-V2 (`home` tab) or orphaned (`my-notes`) — worse than "identity-blocked," several are now **surface-obsolete**, not merely parked. Task 4 targets a surface (`recaps`) V2 replaced with `journal`. Task 7 is unaffected by V2 and remains simply unbuilt.

MVP RECOMMENDATION: in / out / finish-or-hide vs north star (Config T V2 shell on Google Play, RevenueCat Plus-only, proven V1 fallback); burden of proof on inclusion
- IN: WI-1486 (T7, mic deferral) — small, surface-independent, still-live UX bug-adjacent friction; clears burden of proof easily.
- IN, reduced scope: WI-1480 (T1) — only the remaining structural half (reason line / action-specific CTA), re-targeted at `NowCard` copy, not `CoachBand`. The copy win already shipped; don't re-litigate it.
- OUT / already done: WI-1485 (T6) — close as superseded, cite `af91a3e18`.
- OUT (fails burden of proof — dead or wrong-shell targets): WI-1481, WI-1482, WI-1484 — no verified V2 reachability or user path; re-file post-launch against the actual live surface if a real gap resurfaces from telemetry/support.
- NEEDS-PRODUCT-RULING: WI-1483 (T4) — is a guardian "coming up next" hint still wanted on the V2 `journal` tab? If yes, this is new work against `JournalTabView`, not a resume of the old task; if no, close.

CONFIDENCE: high/med/low + up to 3 decidable Zuzka questions
Confidence: **high** on tech validity for the V2-surface-hiding claim (`V2_TABS` set is unambiguous) and on Tasks 1/6/7's implementation status (direct file/commit evidence); **medium** on whether `my-notes`/`recaps` are truly unreachable in production today vs. reachable via a code path this pass didn't find (e.g. a deep link or web-only route).
1. Is the V2 `journal` tab meant to eventually carry a guardian "coming up next" hint (old Task 4's intent), or was that idea dropped when `recaps` → `journal` happened?
2. Is `my-notes` still a live destination anywhere (deep link, notification, web), or is it dead code that should be deleted rather than triaged as a feature gap?
3. Does the partially-shipped Task 1 copy fix (commit `af91a3e18`) count as "close enough" for MVP, or is the reasoned/action-specific card still wanted before launch?
