<!-- TEMPLATE — first-run blank. Copy to activation-queue.md (strip `.template`), swap «PLACEHOLDER», delete this comment. Shape: ../../library/activation-queue.md. -->

# «PROGRAM NAME» — Activation Queue

> **Gate-ordered, not date-ordered.** Each entry is a condition on a named event, never a calendar
> position. **Full forward view** — every Initiative appears, including "much later" ones (a
> near-term-only queue can't be reconciled against the roster and is wrong by construction). Shape:
> `../../library/activation-queue.md`.

`PRG-NN · gate condition(s) · blast-radius class · notes`

| Initiative | Gate condition(s) | Blast-radius | Notes |
|---|---|---|---|
| PRG-01 | «e.g. pipeline-proven on the machinery + attention budget + «operator/product gate»» | «in-radius / out-of-radius» | «» |
| … | | | |

**Standard gates (all must clear):** blast-radius class · pipeline-**proven** (a few WIs cleanly
through claim→execute→review→close, not "first Initiative finished") · attention budget (honest
human-capacity call) · plus any named operator/product gates. Gates are **conditions, not edges**;
cross-Initiative gates reference **exported boundary nodes only**.
