# Execution Tracker — PRG-33 · Mobile UX & Navigation (WS-33)

> Lane substance for the `mobile-ux-nav` lane. Process lives in `roles/shepherd-protocol.md`;
> this file holds the lane's delivery state. Disposable by construction — a fresh shepherd
> pointed here loses only warm cache. Pointers, never copies (live WI state = Cosmo).

## Charter
Improve MentoMate mobile UX and navigation coherence — CTA/button consistency, subject/curriculum
empty-state and Back-navigation correctness, and study↔family entry-surface regressions. "Done" =
every WI in WS-33 Closed via the review gate, with no shell/tab regressions across the shipped
nav flag states (V0-off legacy, V0-on, V1).

## Canon authority
- **Nav gating / tab shapes:** `apps/mobile/src/lib/navigation-contract.ts` (+ `legacy-navigation-contract.ts`).
- **Shell matrix (audience × flag state):** `docs/flows/mobile-app-flow-inventory.md` → "Navigation shell matrix".
- **Target shell direction:** `docs/specs/2026-06-09-mentor-is-the-app-shell-redesign.md` (V2).
- **Cross-stack Back/`router.push` rules:** AGENTS.md → "Repo-Specific Guardrails" (push the full ancestor chain; `unstable_settings.initialRouteName`).
- **Hard constraint:** no regression to any shipped nav flag state until the V0-retirement ruling (mentor-is-the-app spec §13). Back-navigation and empty-state changes must be verified against all three shell states.
- **Lane review invariant:** canon (navigation-contract + shell matrix) wins over any source plan that diverges from it.

## How to use
Fresh shepherd: read `roles/shepherd-protocol.md`, then this tracker, then `roles/executor/executor-protocol.md`.
The lane is **gated** (see Launch gate) — the workstream is On hold and 6 of 8 WIs are unrefined.
Do **not** dispatch execution-class work until the operator releases the hold via an inbox `directive`.
Until then: prime-and-hold — orient, arm the inbox watcher, and (if the operator directs refinement)
drive Captured→Ready (triage/refine) so the slice reaches DoR.

## Pointers
- **Cosmo Workstream:** WS-33 "Mobile UX & Navigation" — page id `3918bce9-1f7c-81ae-97c1-d15ad8951beb` (Status: **On hold**).
- **Workstreams data source:** `08b3ab36-709d-44af-b78c-5e9f74f6e745`.
- **Work Items DB (data source):** `36fd1119-9955-4684-8bfe-deb145e6a21f` (from `zdx-config.yaml`).
- **Clacks channel:** `_quartet/working/lanes/mobile-ux-nav/_state/{inbox,outbox}.jsonl`.
- **Substrate operating rules:** repo `AGENTS.md`; `_quartet/planning-rules.md`.

## Units / slice (8 WIs — DoR-refined + sequenced 2026-07-02; verified in Cosmo via re-read + Stage watcher)

| WI | Name | Stage | Exec path | Effort | Order |
|---|---|---|---|---|---|
| WI-1204 | Keep homework capture bottom actions above system navigation | Ready | Assisted | — | 100 |
| WI-1208 | Keep pick-book Back inside the Subjects shell | Ready | Assisted | S | 200 |
| WI-1212 | Use book-flip animation for subject curriculum preparation | Ready | Assisted | XS | 300 |
| WI-1210 | Align empty subject states with visible curriculum state | Ready | Assisted | M | 400 |
| WI-1209 | Return subject-hub empty-state Back to Subjects, not Home | Ready | Assisted | S | 500 |
| WI-1248 | Route remaining inline CTA buttons through shared Button (WI-1081 tail) | Ready | Assisted | L | 600 |
| WI-1142 | Add Study→Family switch-CTA regression coverage (BRIDGE-04 path) | Ready | Assisted | S | 700 |
| WI-1184 | Verify/fix child subject route wedging Chrome walkthrough | Refining | Assisted | — | 800 |

> Refinement content: `_state/refinement-proposal.md` (researcher, file:line evidence) as amended by
> shepherd adjudication (below). All 6 Captured → Ready passed the mechanical DoR gate; WI-1184 held
> at Refining (no root cause to author until repro obtained). `Effort` set by the refine executor
> (new mandatory DoR field), shepherd-confirmed.

## Sequence & dependencies (adjudicated — evidence overrode the advisory WP hypothesis)
- **NO Work Package.** The advisory "1208/1209/1210 = one shell surface → one WP" hypothesis was
  **refuted** by file-level evidence — the three touch **disjoint files** (1208
  `pick-book/[subjectId].tsx`; 1209 `subject-hub/[subjectId]/index.tsx` `goBack`; 1210
  `apps/api/src/services/subject.ts` + `use-subject-hub.ts`). Kept as three separate Items.
- **Fully parallel** (disjoint files → isolated worktrees, zero merge risk): WI-1204, WI-1208,
  WI-1212, WI-1210, WI-1248.
- **Soft-serial** WI-1210 → WI-1209: conflicts only if 1210 adds a *new* `emptyKind` value (its AC
  forbids that — precedence-correction only) → parallel-safe in practice; merge 1210 first for safety.
- **WI-1142** — test-only, Jest/Playwright-heavy → execute via **branch checkout, not worktree**
  (repo worktree-Jest-haste pathology). Independent.
- **WI-1248** — rolling **multi-PR** Button sweep by screen-area; independent/low-risk; track as
  background, not a single unit.
- **WI-1184** — repro-blocked, file-disjoint, **blocks nothing** — parked last (800).

## Supervision / escalations
- **Nav changes are regression-sensitive:** every Back/shell/tab change verified against all three
  flag states (V0-off legacy / V0-on / V1). No destructive DB steps in this lane.
- **WI-1208 likely ALREADY FIXED** (`handleBack` already replaces to `/(app)/shelf/[subjectId]`,
  predates WI capture ~6-7 wks) → AC is verify-first; deliverable is a regression test, not a blind
  fix. Don't let a builder "fix" a non-bug.
- **WI-1209 scope constraint:** localize to the empty-state call site — do **not** change the shared
  `goBackOrReplace` helper semantics (blast radius = regression risk to other flag states).
- **WI-1210 behavior rule:** never render "generating/Building curriculum" copy for a non-generating
  empty state; correct precedence within existing `emptyKind` values; microcopy drafted in exec
  (operator may tweak in review) — non-blocking.
- **WI-1184 plan:** attempt the live staging Chrome/CDP walkthrough via a qa/browse executor
  (doppler `-c stg`) when reached; if unobtainable → surface `blocked` (ORION trigger (b)).
- **Tooling note (cross-lane):** `cosmo:triage` (`triage.ts`) auto-detects its judge client via Unix
  `which` → ENOENT-crashes on Windows; workaround `--judge-provider claude` on every triage call.
  Flagged to ORION (affects any Windows agent).

## ⚑ POST-COMPACTION ANCHOR (updated 2026-07-04 ~17:05Z — WI-1393 MERGE-READY (Gate-1 CLEAN); HOLDING for ORION go + operator merge-hold lift; read first)
> **CURRENT TRUTH (supersedes any stale "in triage" / "in flight" / "building" lines below):** **WI-1393 — V2 supporter-link forward anchors; PUBLISH-BLOCKER** (Cosmo page `3928bce9-1f7c-814e-9117-d21b6b21c1b7`) is **Stage=Executing, MERGE-READY**. **PR #1907** head **`be88d661c`** — CI **9/9 green**, claude-review APPROVED (0/0), CodeRabbit 0 MUST/0 SHOULD (its re-flagged dup-a11y-label = verified STALE FALSE-POSITIVE, mutually-exclusive render guards). One fix round already removed 2 avoidable internal test-mocks (verified from source). Merge-ready packet = outbox **mobile-ux-nav-50**. **GATED ON BOTH: (a) ORION Gate-1 go, (b) operator merge-HOLD (~14:40Z) lift — do NOT merge until BOTH clear.** ON GO: `gh pr merge 1907 --squash --delete-branch` → resume builder `a78fd137c48dc2127` → `/cosmo:execute complete --fixed-in <squash-sha>` (→ Reviewing, then separate review/QA-to-Close gate). Builder is STOOD DOWN (resume only for new findings). Option-2 (cross-account invite) already captured = **WI-1580** (Feature/P2, WS-33, Captured — leave for later triage/refine). The 9 prior WIs stay **CLOSED** (WI-1142/1184/1204/1208/1209/1210/1212/1283/1317); WI-1248 re-homed → WS-34; **WI-904** (Backlog) OPERATOR-HANDLED — do NOT touch/force. **Do NOT declare WS-33 graduation** until a full Cosmo query shows every item Closed/deferred-out (now gated on WI-1393 landing + WI-1580 disposition). Full build/review provenance in the POSTURE line below.
> **WI-1393 triage evidence (file:line):** dead-end = only `link/new` refs are route-reg `_layout.tsx:72/101` + tests, no `router.push`/`href` anywhere. Anchor points to wire: **A1** `SupportHubMentorTab.tsx:203-208` (supporter-hub cold-start empty state, text-only today); **A2** same file header 187-200 (persistent +Add); **A3** `SupportHubSubjectsTab.tsx:32-37` (parallel empty state). **BLOCKING FORK:** `/link/new` HARD-REQUIRES `supporteePersonId` (`new.tsx:68-84` missing-param error, `:43` throws) → it's create-contract-to-an-existing-person, NOT from-scratch; a naked empty-hub button lands on the error screen. Options posted: (1)[REC] MVP = picker of existing managed persons w/o a contract → `/link/new?supporteePersonId=..&relation=..`; (2) larger = cross-account new-person invite (own WI, defer). Person source exists: add-child/managed persons (`AccountAdminSheet.tsx`, `ParentHomeScreen.tsx`). Dossier gap table: `docs/plans/v2-dossier/07-trigger-flow-logic-map.md` (Trigger Matrix line 172, gap line 193).
> **POSTURE (2026-07-04 10:26Z):** ORION RULED option (1) MVP (muxnav-inbox-040). WI-1393 driven **Captured→Backlog→READY** — page `3928bce9-1f7c-814e-9117-d21b6b21c1b7`, Enhancement/P1/Assisted/effort M/tags navigation; DoR passed (mechanicalOk), 3 checklist items confirmed w/ evidence. ACs authored (option 1): A1/A2/A3 affordances route through a managed-person picker → `/link/new?supporteePersonId=..`; 0-eligible → guide to add-child (no dead-end); HARD nav-preserve guardrail (V0-off/V0-on/V1); red-green forward-trigger coverage. **IN FLIGHT (2026-07-04 ~15:58Z):** (1) DONE — option-2 cross-account invite captured = **WI-1580** (Feature/P2, WS-33, related to WI-1393). (2) WI-1393 **builder DISPATCHED** (background agent `a78fd137c48dc2127`) to `.worktrees/WI-1393` branch `WI-1393`; brief = option-1 MVP, A1/A2/A3 → managed-person picker → `/link/new?supporteePersonId=..`, 0-eligible degrade, nav-preserve, red-green + haste-fallback, tsc/lint/i18n, /commit own-scope, `gh pr create`, then STOP at Gate-1 (builder self-triages advisory; NO merge). Builder CLAIMED (verified Cosmo WI-1393 Stage=**Executing**). **PR #1907 OPEN** (branch WI-1393, https://github.com/cognoco/eduagent-build/pull/1907), mergeStateStatus=BLOCKED (expected). CI in progress, NO reds (changes + Merge-completeness pass; run-smoke/API-Quality-Gate/claude-review/main/Flag-ON/CodeRabbit pending). Impl (per builder, re-verify at Gate-1): 3 anchors → shared `SupportPersonPickerSheet` → `/link/new?supporteePersonId`; 0-eligible → add-child degrade; eligibility = `useLinkedChildren` w/o a person scope (NO new API) via new `use-eligible-supportees` hook; commit `8510af4`. **GATE-1 ITERATION (2026-07-04 ~post-CI):** CI **all-green 9/9** on head `00b7d197`; **claude-review = APPROVED** (0 must / 0 should, 2 CONSIDER). CodeRabbit = 3 actionable: **2 Major = NEW internal `jest.mock` escapes** (`subjects.test.tsx` mocks `use-eligible-supportees`; `use-eligible-supportees.test.ts` full-mocks `scope-context`) — violate repo NON-NEGOTIABLE (GC1/GC6; gc1-allow invalid since `mentor.test.tsx` proves the real code runs) + 1 Minor dup a11y label. **FIXES LANDED + VERIFIED (head now `be88d661c`, 2 commits):** MUST#1 (subjects.test.tsx use-eligible-supportees mock removed) ✓, MUST#2 (use-eligible-supportees.test.ts ZERO jest.mock/gc1-allow) ✓ — independently confirmed via `gh api contents?ref=WI-1393`; the 3 remaining subjects.test.tsx gc1-allow mocks are PRE-EXISTING (origin/main same set, mirror mentor.test.tsx). Builder reports SHOULD#3+OPTIONAL#4 fixed, real-impl tests 41/41, tsc --build clean, GC1 CI exit 0, i18n clean. **MERGE-READY (Gate-1 CLEAN, 2026-07-04):** CI **9/9 green** on `be88d661c`. claude-review=APPROVED (0/0, 2 CONSIDER). CodeRabbit=0 MUST/0 SHOULD; its re-flagged Minor dup-a11y-label is a **STALE FALSE-POSITIVE** — verified in source: header btn `!activePersonScope && personScopes.length>0` (L233) vs empty-state btn `personScopes.length===0` (L249) = mutually exclusive. Remaining = Trivial/CONSIDER style nits only. NO legacy shell files touched. **Merge-ready packet posted to ORION = outbox `mobile-ux-nav-50`.** GATED ON: (a) ORION Gate-1 go + (b) operator merge-HOLD (~14:40Z) lift. **ON GO:** `gh pr merge 1907 --squash --delete-branch`, then resume builder `a78fd137c48dc2127` → `/cosmo:execute complete --fixed-in <squash-sha>` (→ Reviewing). inbox current at **muxnav-inbox-051**; next id **-51**. Do NOT merge without BOTH gates cleared. (3) after Close: WI-1580 stays Captured for later triage/refine. **TOOLING TRAP:** cosmo `--dedup` judge unrunnable here — claude judge blocked by `ANTHROPIC_API_KEY` precedence, codex judge fails on `--ask-for-approval` arg drift; fall back to MANUAL dedup + note it. Watchers unreliable across teardowns — **manual-tail the inbox each turn** (`tail -n 8 _state/inbox.jsonl`); re-arm both as backstop (inbox `bejcyl6yh`, Cosmo-Stage `bwieng50f`). **Verify all stages vs Cosmo REST on resume** (local tracker/clacks were reverted once by a concurrent shared-tree committer, outbox mobile-ux-nav-15; Cosmo authoritative; Stage dual-path `.status.name // .select.name`). WS-33 data source `36fd1119-9955-4684-8bfe-deb145e6a21f`, Workstream page `3918bce9-1f7c-81ae-97c1-d15ad8951beb`.
> _(Lines below this box are HISTORICAL — WI-1283/WI-1208/WI-1317 all Closed now; keep for provenance, but trust the box above + Cosmo.)_
- **Repo:** cognoco/eduagent-build. Shepherd does NOT write code (delegates to builders in `.worktrees/WI-NN`), does NOT switch branches. Merges: `gh pr merge <n> --squash --delete-branch` ONLY on strict-green (all required checks SUCCESS **and** the claude-review VERDICT read = APPROVED/no valid must-fix/should-fix — read the comment via `gh api repos/cognoco/eduagent-build/issues/<n>/comments`, not the check colour). After merge, resume the WI's builder to `/cosmo:execute complete --fixed-in <squash-sha>` (detach HEAD to squash first) → Reviewing.
- **LIVE background agents:** WI-1283 builder = `ae344166ad5418b32` (DONE — **PR #1837**, head `5ded34cc234feb59a2a7143920eddfbd04fb1713`, CI running; shepherd owns CI wait → strict-green merge → resume builder to `complete --fixed-in <squash>`). WI-1283 worktree `.worktrees/WI-1283`. Fix mirrors WI-1209 goBack: `handleBack` fallback = `FEATURE_FLAGS.MODE_NAV_V2_ENABLED ? '/(app)/subjects' : '/(app)/library'` at `shelf/[subjectId]/index.tsx:55-63`; +2 tests (V2-on/off), red-green verified. Coverage-gap capture DONE → **WI-1317** (WS-33, Hygiene, P3, Captured — child-subject route unwired e2e coverage; page 3928bce9-1f7c-81d2-99e6-cf9bcf99d004). Leave Captured (ORION #5: don't fix this push); deferred follow-up, not gating this push (graduation-gating is ORION's call, same as WI-1184-parked).
- **Watchers (persistent; DIE on session end/crash/compaction — reconcile, don't blind-readd):** inbox `b4bhcpl18` (`tail -n0 -F _state/inbox.jsonl`), Cosmo-Stage `btbxcnads` (`bash _quartet/clacks/orch-stage-monitor.sh 3918bce9-1f7c-81ae-97c1-d15ad8951beb 180`). Manifest: `_state/monitor-manifest.json` (has live task-ids). Re-arm both as background Bash on every resume.
- **WI-1184 = CLOSED** by ORION directly (muxnav-inbox-018, operator ruled option A): Stage=Closed, Resolution=Cancelled (closest enum), Resolved=2026-07-03, full provenance comment (true disposition=not-reproducible; cites WI-1318 for missing Not-Reproducible value + no-close-from-Refining tool gap). DONE — shepherd does not touch it.
- **In flight:** WI-1208 — 3rd bounce (05:51Z) was NEW real evidence (eb9423945/PR#1827 = genuine V2 pick-book back fix + green landing commit), NOT the park scenario → re-completing HONESTLY via executor `afc5080f22e2a964d` (Fixed In=eb9423945). See execution log for full rationale. If THIS attempt bounces on genuinely NEW grounds, re-verify from source before deciding; only park if a bounce reveals no honest completion path remains. WI-1209/1210/1142 ✅ CLOSED 2026-07-03 05:50.
- **WS-33 status (2026-07-03 ~10:48Z):** 8 CLOSED — WI-1204, WI-1208, WI-1209, WI-1210, WI-1212, WI-1142, WI-1283 (nav-core) + WI-1184 (Cancelled/not-reproducible, ORION-closed). WI-1248 re-homed → WS-34 (blocked-by WI-1298). **LANE WORK COMPLETE — 9 CLOSED** (WI-1142, WI-1184, WI-1204, WI-1208, WI-1209, WI-1210, WI-1212, WI-1283, WI-1317); WI-1248 re-homed → WS-34. **ONLY OPEN: WI-904** (Backlog, select-type, OPERATOR-HANDLED directly per muxnav-inbox-017/018 — shepherd HOLDS, no action, no routing; NOT the lane's to drive). WI-1317 = ✅ CLOSED 12:04Z (PR #1850 squash `742732d3349a1373e2948bd5d6c30cb9cb77ba76`, claude-review APPROVED 0/0/0; non-gating opt-in Playwright project `child-subject-detail`, w06 spec, 5 green stability runs, red→green proved; Fixed In=742732d3 landed squash after a clean bounce whose ONLY cause was the reviewer evaluating pre-merge). **Full workstream graduation gated SOLELY on WI-904 (operator's). Shepherd has NO remaining actionable WS-33 work.** Do NOT declare graduation until a full Cosmo query shows every item Closed/deferred-out. Do NOT declare graduation until a full Cosmo query shows every item Closed/deferred-out. ⚠️ tracker git-tracked in shared main tree, was reverted/stashed by a concurrent session earlier (outbox mobile-ux-nav-15) — **re-verify stages vs Cosmo REST before acting.**
- **No uncommitted lane code anywhere.** ~49 pre-existing `apps/api/eval-llm/snapshots/**` diffs in main tree are NOT lane work — do not touch.
- **CI-watch pattern:** background bash loop `for i in $(seq 1 70); do s=$(gh pr checks <n> --json name,state,bucket); … exit when no bucket==pending … done`.

## RESUMED 2026-07-03 ~20:15 (operator "Continue" — hold released)
- **Reconcile:** no new inbox directive after muxnav-inbox-011. Live WS-33 read: WI-1204/1212 Closed; WI-1142/1208/1209/1210 all still Reviewing (NO verdicts in the ~45min gap — watching for a reviewer stall). Cosmo-Stage watcher bvjzn5ovy alive (fired 20:15); inbox watcher — rely on direct-reads at decision points as fallback.
- **WI-904 ANOMALY:** an out-of-slice WI ("Dictation playback pacing", Enhancement P2) surfaced in WS-33 at Stage=Executing but unclaimed/unstarted (inconsistent). NOT mine, NOT touched — flagged to ORION (outbox mobile-ux-nav-11, needs-orchestrator). Not in the graduation set.
- **WI-1184** — verdict **NOT-REPRODUCIBLE** (qa repro attempt done): static no-hang-vector on HEAD 9e42b9ea3 (reworked ~6 commits since symptom); live staging repro not obtainable (`parent-ux-pass.spec.ts:113-119` hits the route but is unwired → "No tests found"). **BLOCKED ON TOOLING GAP, ESCALATED** (outbox mobile-ux-nav-12, needs-orchestrator). No sanctioned path to close a **Refining**-stage item as not-reproducible: close.ts requires Stage=Reviewing; refine.ts only --to-ready; triage.ts dispositions scoped to Captured/Triaging; Resolution enum = Done/Wontfix/Cancelled/Duplicate only (NO Not-Reproducible). ORION's close-at-triage lesson assumed pre-Triaging stage — WI-1184 at Refining falls in the gap. Executor did NOT force (stayed at Refining, untouched). Recommended: (b) add Not-Reproducible Resolution + close.ts-accepts-Refining [honest fix, Cosmo-improvement WI] + (c) park short-term. **RULED (muxnav-inbox-013): option (c) now + (b) long-term.** PARK WI-1184 at Refining (do NOT force any tool, do NOT hand-edit Stage; REJECT triage-cancel — `Cancelled` = false provenance, same principle as WI-1208). **WS-33 MAY GRADUATE with WI-1184 documented-parked** (blocks nothing). Not-reproducible evidence (HEAD 9e42b9ea3, 3 clean useQuery hooks, unwired `parent-ux-pass.spec.ts:113-119`) stays here so a resumed shepherd/operator closes it once the tool path exists. Honest fix (add Not-Reproducible Resolution + close.ts-accept-Refining) = **ORION capturing** as a Cosmo-improvement (sibling WI-1293/1296), NOT my build. → WI-1184 is DONE from my side (parked-documented). Coverage-gap capture (ORION #5) in flight: general executor `ad4415f4ace7d86cc` capturing "child-subject route has no wired e2e coverage" to WS-33 P3 Captured.
- **WI-1283** — **Ready** (refined, red-green AC clause present, non-dup of 1209 confirmed). Builder dispatched (`.worktrees/WI-1283`, claimant wi1283-builder, agentId ae344166ad5418b32). Localized flag-aware shelf Back fix; GATE-0 determines exact target vs navigation-contract.ts. Workstream Order unset (cosmo:bundle-owned; not needed).
- **WI-904** — OFF-PLATE (ORION ruling muxnav-inbox-012: zombie-Executing mis-assignment, do not touch; ORION owns correction). Not in graduation set.

## Current position — CHECKPOINT 2026-07-02 ~19:30 (session budget ending; ORION muxnav-inbox-011)
Autonomous execution well advanced. **6 PRs merged on strict-green** (#1824/1825/1830/1831/1834 for WI-1212/1204/1142/1210/1209; WI-1208 no-PR). **2 WIs Closed** (WI-1204, WI-1212). No uncommitted builder code anywhere (all worktrees clean; all PRs merged).

### RESUME POINTER (a fresh shepherd resumes losslessly from here)
- **Re-arm/reconcile watchers** per `_state/monitor-manifest.json` (inbox + Cosmo-Stage on WS-33) — see monitor-hygiene; both were live at checkpoint.
- **In review, awaiting reviewer close (no action unless bounced):** WI-1210, WI-1142, WI-1209 (all clean — completed, mechanical DoD passed), WI-1208 (3rd attempt — PARK if it bounces, per ORION; do not fabricate a commit).
- **On any reviewer rework bounce (Reviewing→Executing):** read the reason via Notion REST comments (`/v1/comments?block_id=<pageid>` — the cached `notion-fetch` view lags ~30 min); re-dispatch/re-complete; Bug AC must carry a red-green-revert clause (all 4 Bug WIs already amended).
- **Not started (pick up next):** (1) **WI-1184** — repro-blocked; dispatch a qa/browse executor (doppler `-c stg`) to run the staging Chrome/CDP walkthrough (Family Home→child profile→curriculum→subject row→`[subjectId]`); if NOT reproducible → close as not-reproducible at TRIAGE (ORION process lesson), do NOT ride execute→review into the WI-1208 DoD dead-end. (2) **WI-1283** — my capture, small `shelf/[subjectId]` Back-nav bug (WS-33 nav-core); triage→refine→execute, OR defer if de-scoped.
- **Deferred out of WS-33:** WI-1248 → WS-34 (blocked-by WI-1298). WI-1298 (Button API) new in WS-34. Neither gates WS-33.
- **WS-33 graduation** = WI-1204✅, 1212✅, 1210, 1142, 1209, 1208 (disposition-pending), 1184, 1283 all Closed.
- **Open ruling loops:** none pending (008/010 resolved; 011 = this checkpoint). Do NOT start NEW builder dispatches per muxnav-inbox-011 until released.
- **Shared-tree:** ~49 pre-existing uncommitted `apps/api/eval-llm/snapshots/**` diffs in main tree — NOT lane work, do not touch.

## Launch gate
**Released** (was: gated on WS-33 On hold). Refine directive `muxnav-inbox-002` + autonomous-execute
directive `muxnav-inbox-003` issued 2026-07-02. No further gate — the shepherd refines, sequences,
then executes without waiting. Residual conditional block: WI-1184 needs a staging repro; if
unobtainable, surface `blocked`.

## Change log
- **2026-07-02** — Lane provisioned by orchestrator (ORION). WS-33 resolved: "Mobile UX & Navigation",
  On hold, 8 WIs (6 Captured / 1 Refining / 1 Ready). Tracker + clacks channel created; shepherd
  (prime-and-hold) + reviewer kickoffs authored; Cosmo-Stage + outbox monitors armed. Awaiting
  operator release directive.
- **2026-07-02** — Released. Operator granted ORION full autonomous execution authority (shepherd
  works for orchestrator; no execute go/no-go gate). Reviewer spawned + live on WS-33. Issued
  `muxnav-inbox-002` (refine 6 Captured → DoR + sequence) then `muxnav-inbox-003` (proceed straight
  into execution, escalate only design forks / hard blocks). Shepherd running.
- **2026-07-02** — **Slice DoR + sequenced.** Dispatched a read-only researcher (whole-slice triage
  → `_state/refinement-proposal.md`, file:line evidence) + a general executor (applied triage/refine
  to Cosmo, re-read-verified). Adjudication: refuted the WP hypothesis (disjoint files → 3 separate
  Items); WI-1208 verify-first (likely already-fixed); WI-1209 localize (don't touch shared helper);
  WI-1210 correctness-anchored copy default (non-blocking); WI-1184 held Refining (repro-blocked,
  parked 800). 6 Captured → Ready, ×100 order assigned, Effort set. No blocking escalation. Entering
  execution wave (builders per order). Checkpoint decision emitted to outbox.

## Execution log (durable — compaction-safe)
- **WI-1212** — ✅ **CLOSED** (reviewer, 2026-07-02 18:44 — first graduation). PR **#1824** merged squash **349a7356905e30a4eab2db0d5ed699142073a62c**; all CI green incl. claude-review APPROVED 0/0/0.
- **WI-1208** — ⚠️ **DOUBLE-BOUNCED, ESCALATED** (parked in Executing). Already-fixed (no new work, pre-existing passing test `pick-book/[subjectId].test.tsx:417-429`). Reviewer /cosmo:qa DoD rejects twice on a genuine already-fixed-vs-DoD gap: (1) completion-summary cited short file paths not resolvable from repo root [fixable: need `apps/mobile/src/app/(app)/…` prefix]; (2) Fixed In needs a commit whose required checks are GREEN, but the historical fix commit 75ace69609 shows a FAILED CI run and there is no new landing PR. Escalated to ORION (outbox mobile-ux-nav-6, needs-orchestrator). **RULED (muxnav-inbox-008, option b):** close as VERIFIED ALREADY-FIXED / not-reproducible; Fixed In stays honest = 75ace69609 (predates WI); green-landing-commit DoD **waived** for this resolution class (reviewer applies), LIVE evidence = passing test on green main. Rejected false-provenance (a) + make-work (c). Re-engaged WI-1208 builder — re-completed a 3rd time (2026-07-02 ~19:00) with honest Fixed In=75ace69609, fully-qualified paths, resolution-class declared IN PROSE (no Resolution flag exists in `/cosmo:execute complete` → WI-1293). Now at **Reviewing (3rd attempt)** — awaiting verdict. **RESUME:** if it bounces a 3rd time on the green-landing-commit DoD or the append-not-replace stale-path scan (WI-1296), PARK it (Executing, blocks nothing) per ORION ruling — do NOT fabricate a commit; ORION owns the tooling fix. Also FYI: complete's draft-PR step cross-referenced unrelated pre-existing PR #1832 (four-strands-language-planner) — untouched.
- **WI-1208 — 3RD BOUNCE (2026-07-03 05:51Z Reviewing→Executing) = NEW EVIDENCE, NOT the park scenario → HONEST RE-COMPLETE (autonomous, muxnav-inbox-003).** The 3rd rejection was CORRECT and code-verified by shepherd: the prior summary was factually WRONG — it claimed pick-book `handleBack` has NO MODE_NAV_V2 conditional and cited only 75ace69609 (V0 shelf branch). **Ground truth:** the MODE_NAV_V2-aware branch that satisfies this WI landed in **eb94239453e1bab5e5de20029d520026b53d3f71 (PR #1827, "stabilize V2 subject hub back paths", merged 2026-07-02, in origin/main → green at merge)** — `git show eb9423945` shows the added flag branch (subjectId→subject-hub under V2 / shelf under V0-V1; no-subjectId→subjects vs library) + 167 lines of V2-on/off tests in `pick-book/[subjectId].test.tsx`. WI captured 2026-06-30 < eb9423945 2026-07-02 → genuine recent WI-satisfying fix. **This DISSOLVES the park premise** (park was pre-authorized on "no green landing commit can exist" — now falsified). Re-dispatched a scoped Sonnet executor (`afc5080f22e2a964d`) to re-claim + re-complete with **Fixed In=eb9423945** + corrected 4-section summary describing actual V2-aware source + explicit V2-on/off verification (clears all 3 reviewer points). No fabrication, no worktree/PR/code, no missing tooling. Visibility note to ORION = outbox **mobile-ux-nav-14** (needs-orchestrator; "flag me if you'd rather I park regardless"). WI-1293/1296 remain valid tooling gaps but did NOT block this item — it was genuinely completable.
- **Process lesson (ORION):** a verified-already-fixed bug should be closed as not-reproducible at **TRIAGE (verify-first)**, before execute/review, to avoid this DoD dead-end. Apply to future WS-33 verify-first items (e.g. any repro-negative outcome on WI-1184).
- **WI-1283** — NEW, captured (Bug P3, WS-33, Captured, linked 1208/1209) from the WI-1208 verify: `shelf/[subjectId]/index.tsx:55-57` handleBack hardcodes `/(app)/library`, no MODE_NAV_V2 branch (sibling of 1209). Parked (unrefined follow-up; order ~900).
- **WI-1204** — ✅ **CLOSED** (reviewer, 2026-07-02 ~18:50 — AC red-green amendment + re-complete cleared the DoD). PR **#1825** merged squash **267b743f6b616801155af254a0aacfa992781f11**; safe-area inset fix in `camera.tsx`, red-green guard 4 variants; all CI green, claude-review APPROVED w/ 1 non-blocking CONSIDER (logged/deferred).
- **WI-1210** — ✅ **CLOSED** (reviewer, 2026-07-03 05:50). PR **#1831** merged squash **a8e31d79fb4d3a57d0aa6c2b96ef8cf7ccc5f9f0**; precedence fix in `apps/api/src/services/subject.ts` (in-flight-books query; NO new discriminator), red-green 3 variants; all CI green, claude-review APPROVED 0/0/0. Fixed In = squash.
- **WI-1142** — ✅ **CLOSED** (reviewer, 2026-07-03 05:50). PR **#1830** merged squash **c049cc29761fd835790b409297b490926c3284eb**; RQ-backed Jest regression test (strategy a), red-green verified; needed 1 rework round for a claude-review SHOULD_FIX (unguarded `globalThis.fetch` → try/finally), then APPROVED. Fixed In = squash.
- **WI-1209** — ✅ **CLOSED** (reviewer, 2026-07-03 05:50). PR **#1834** merged squash **5d0677a533328030b9defa76b429998454095c96**; Fixed In = squash. Prod fix pre-existed (commit eb9423945 / PR #1827); this WI added the missing regression coverage (empty-state Back→Subjects with `canGoBack()=true` under MODE_NAV_V2=**false**; PR #1827 only had =true) in `apps/mobile/src/app/(app)/subject-hub/[subjectId]/index.test.tsx`. Test-only, red-green-revert verified both flag states, claude-review APPROVED 0/0/0.
- **WI-1248** — ✅ **RE-HOMED to WS-34 + DEFERRED** (ORION ruling muxnav-inbox-010, option B). Now: Workstream=WS-34 (`3918bce9-1f7c-8142-9b75-dfcafbc94d65`), Stage=Backlog, claim released, **blocked-by WI-1298**, Workstream Order unset. NO code was written. Not decomposed (premature — depends on the Button API change). No longer gates WS-33.
- **WI-1298** — ✅ **NEW, captured in WS-34** (Button-API prerequisite): "Button.tsx: add style/className override + danger variant (unblocks CTA sweep WI-1248)". Enhancement, P3, design-system, Stage=Captured, page id `3918bce9-1f7c-8164-9435-efc457fe8e7f`. Body records the 5 judgment classes from WI-1248 recon. WI-1248 is blocked-by this.
- **Tooling gaps captured by ORION:** WI-1293 (execute.ts complete has no Resolution/not-reproducible flag — confirmed in source: complete accepts only artifacts-dir + ci-status positional [success/green/escalated] + `--fixed-in`) + **WI-1296** (append-not-replace deadlock: stale bounced completion-summary re-flagged by qa) — related WI-1266. Park-on-3rd-bounce STANDS for WI-1208.
- **WI-1283** — PR **#1837 MERGED strict-green** 2026-07-03 05:58 (claude-review APPROVED 0/0/0, all required checks pass, CLEAN). Squash SHA **09edc2dc1359194353cdfbde368436a25b770b0f**. Flag-aware `handleBack` in `shelf/[subjectId]/index.tsx:55-63` (`MODE_NAV_V2_ENABLED ? '/(app)/subjects' : '/(app)/library'`, mirrors WI-1209 goBack) + 2 red-green V2-on/off tests. Builder `ae344166ad5418b32` resumed to `/cosmo:execute complete --fixed-in 09edc2dc1` → will land at Reviewing. Awaiting builder confirm + reviewer close.
- **WI-1208 re-complete AFFIRMED by ORION (muxnav-inbox-014, ref mobile-ux-nav-14):** honest re-complete with Fixed In=eb9423945 SUPERSEDES park-on-3rd-bounce; ORION confirmed the park premise (no green landing commit) was falsified by the reviewer's evidence. Proceeding as dispatched (executor `afc5080f22e2a964d`).

## Review-loop notes (rework + systemic fix)
- **WI-1204 rework bounce (2026-07-02 18:23):** reviewer rejected on a MECHANICAL DoD gate — a `Type=Bug` requires the Acceptance-Criteria TEXT to declare a **red-green-revert regression guard** clause naming what the guard asserts. NOT a code defect (guard was built + verified; PR #1825 merged). **Systemic:** refine wrote the same under-specified AC for every Bug (1204/1208/1209/1210). ORION confirmed this maps to tracked **WI-1266** (DoR regression-language check looser than the /cosmo:review Type=Bug guard). **Fix DONE:** general executor appended a red-green-revert clause to the AC of all four Bug WIs (original AC intact, re-read verified); re-completed **WI-1204** (`--fixed-in 267b743f6`) and **WI-1208** (`--fixed-in 75ace69609`, already-fixed) → both back to Reviewing. WI-1209/1210 AC-only (they complete later against conformant AC). No real guard gap anywhere — WI-1208 has a pre-existing passing test. Cross-lane learning emitted (outbox mobile-ux-nav-4/5); ORION directive muxnav-inbox-007 acked.
- **Reviewer IS live + acting** on WS-33 (bounced 1204 + 1208 on the AC gate; both re-completed).
- **Shared-tree note:** main working tree carries ~28 pre-existing uncommitted `apps/api/eval-llm/snapshots/**` diffs (NOT this lane's; do not touch — builders work from origin/main in isolated worktrees, unaffected). Flag only.
