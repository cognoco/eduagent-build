═══════════════════════════════════════════════════════════════════════
# 🔻 FRESH-SESSION RESUME — READ THIS FIRST (handoff 2026-07-03 ~11:35Z; prior session halted gracefully on machine reboot per orch-043)
═══════════════════════════════════════════════════════════════════════
You are the NEW shepherd for **WS-28 "V2 finalization"** (Initiative INI-33 App v2). Assume ZERO memory of the prior session. ROLE: orchestrate typed executors to drive WS-28 WIs to Cosmo close; **NEVER do execution-class work in-seat** (dispatch builders/executors; spot-verification is OK). **Gate-1** = you self-merge strict-green PRs. **Gate-2** = a SEPARATE Codex reviewer closes items at Reviewing (you NEVER run /cosmo:review). **Charter: M4 → M5 → M6** (M4 gates both). Standing rules in the STANDING DIRECTIVES + MISREAD-TRAPS blocks below — read them.

## STATE PER WI
- **WI-1307 (M4) — Executing, claim `claude:wi1307-m4:WI-1307` (may be lapsed → re-claim before completing).** Machinery LANDED: PR #1836 + #1844 both MERGED on main. proof-(a) web-e2e green. **proof-(b) PART 1 DONE** = fallback OTA PUBLISHED (update group **802cab19-1907-4197-9cd4-1bd2d9358b78**, branch=fallback, rtV=1.0.1, android+ios). **proof-(b) PART 2 = rider-R1 device artifact-E2E IN PROGRESS** (human tester **Zuzka, S10e**; background gate, hours-scale). **NEXT ACTION when R1 verdict lands → run the "R1-PASS RUNBOOK" section further down this file → /cosmo:execute complete WI-1307. Do NOT complete before R1 green.**
- **WI-1308 (M5 retire-V0) — Captured, REFINED (not claimed).** AC authored (page 3918bce9-1f7c-812d-b130-da8af013f717): V0-surface removal list + ratchet lockstep + preconditions + preservation gate + 3 PRODUCT scope-decisions **D1-D3** flagged for nav-owner. **M5 EXECUTION = S6-IRREVERSIBLE + HUMAN-CONFIRM + gated on M4-proven — never autonomous; produce preservation gate (annotated pushed tag + docs/archive/retired-code.md entry) then STOP for human go.**
- **WI-1334 (nav flag-combo drift) — not mine to close.** Evidence verified + appended to AC (page 3928bce9-1f7c-8187-a4e5-c01bc63cf638): ci.yml preview-OTA triple real; fallback profile REAL drift. For nav-owner to rule fix-or-sanction.
- **WI-1305 (M1 seam, WS-37's) — MERGED (#1846).** PR-gate check WIRED into my checklist: required job **`main`** (change-class `identity-v2-seam`, glob `apps/api/src/services/identity-v2/**`) — NOT the non-blocking "Flag-ON integration (IDENTITY_V2_ENABLED)" diagnostic.

## GATES HELD (+ owner) — all awaiting others:
1. R1 device-E2E → Zuzka/operator (background). 2. Fallback **V0=true** ruling → nav-owner/orch (my open escalation v2-finalization-68: fallback profile is V0=true/V1=true/V2=false, NOT clean Config F V0=false; rule accept-vs-republish; R1 asserts likely pass regardless). 3. WI-1308 D1-D3 → nav-owner/product. 4. M5 execution → human-confirm. 5. M1 seam signal → WS-37.

## IN-FLIGHT PRs: NONE open (#1836/#1844 WI-1307 MERGED; #1846 WI-1305 MERGED).
## CLAIMS HELD: WI-1307 only (may be lapsed).
## COSMO REST: DB `f170be9e-04ae-45d4-9618-28f2438666bd`, `$NOTION_TOKEN`, `Notion-Version: 2022-06-28`, filter property "ID" unique_id.number. Page-ids: 1307=3918bce9-1f7c-81b1-9416-ccbe2eb94eed, 1308=3918bce9-1f7c-812d-b130-da8af013f717, 1334=3928bce9-1f7c-8187-a4e5-c01bc63cf638.
## CLACKS: outbox=`_state/outbox.jsonl` (I write; last line = shutdown post), inbox=`_state/inbox.jsonl` (read-only; last = orch-043). Levels: needs-operator/needs-orchestrator/blocked ESCALATE (orch monitors fire only on these); info=FYI. Every hold/dispatch line carries `sol_by` (UTC).
## FIRST MOVES for the fresh session: (1) `git fetch origin main` (local checkout is STALE — read origin/main via `git show`). (2) re-arm inbox monitor on `_state/inbox.jsonl` + (optional) heartbeat timer. (3) read inbox tail for any ruling past orch-043. (4) if R1 verdict present → R1-PASS RUNBOOK; else hold armed. Prior monitors (bo00voy3l inbox, boxkrekok heartbeat) DIED with the prior session — re-arm fresh.
═══════════════════════════════════════════════════════════════════════

# STATUS 2026-07-02 ~20:45Z — REACTIVATED / NEW CHARTER M4→M5→M6 (most current — read FIRST)

> **🗂️ FILL-QUEUE (orch-041, R1 background gate — Zuzka/S10e, hours-scale; work continues, R1 interrupts on ruling):** [4] R1-pass runbook — ✅ DONE (in-state, above). [1] WI-1308 M5 refine — ✅ DONE (AC authored, page 3918bce9, Stage left Captured, inventory pinned, 3 product scope-decisions D1-D3 flagged). [2] WI-1334 evidence verify — ✅ DONE (AC appended, page 3928bce9): Part-A ci.yml preview-OTA triple V0/V1/V2='true' VERIFIED (flags at :616-618, not the cited :604-606 header); Part-B fallback profile (on,on,off) = REAL DRIFT vs sanctioned-only-V0=false combos, grandfathered 1-of-4. **Escalated needs-orchestrator (v2-finalization-68):** the drift means proof-(b) bundle 802cab19 is V0+V1 not clean Config F → nav-owner rules if V0=false re-publish needed (R1 asserts likely still pass). [3] WI-1305 seam-check WIRED — ✅ DONE. **M6/M4 GATE CHECK = required job `main`** (job id `main`, no display-name override; the "API co-located integration tests (apps/api/src)" step, `pnpm exec nx run api:integration-api`), triggered by change-class **`identity-v2-seam`** on glob **`apps/api/src/services/identity-v2/**`** (check-change-class.sh → integration=true → ci.yml:401-403). ⚠️ NOT the `integration-flag-on` / "Flag-ON integration (IDENTITY_V2_ENABLED)" job — that's `continue-on-error:true` NON-BLOCKING diagnostic, do not name it as the seam gate. PR #1846 (WI-1305/R6) merged 09:47Z. **FILL-QUEUE COMPLETE (4/4).** Further non-blocked work = judgment; M5 EXECUTION stays deferred/human-confirm (no M5 build).
> **⚙️ STANDING DIRECTIVES (orch-037, 2026-07-03 — codified in orchestrator-protocol.md on Nexus main):** (1) LIVENESS — every outbox line that pauses/holds/dispatches long work MUST carry an `sol_by` (sign-of-life-by, UTC) deadline; orch probes at deadline+grace + wakes; ~2h active-lane silence = suspect; post a heartbeat (level info) if long work is legitimately still running. (2) MCP OUTAGE ≠ WORK STOPPAGE — cosmo bun CLIs (NOTION_TOKEN over REST) + raw REST are MCP-independent; NEVER halt a lane because Notion MCP is down. — Also (orch-032): lane-STOPPING holds post at needs-operator/needs-orchestrator, never info. — Also (repo commit-skill): NO `--force`/`--force-with-lease`/`--amend` on pushed commits (builders correctly refuse); fix a behind-main branch by merging origin/main FORWARD into it (merge commit + normal push), NOT rebase; OR bring it current SERVER-SIDE via `gh pr update-branch <PR>` (no local hooks — sidesteps worktree Node/harness defects entirely; used for #1844). — Also (orch-040 P1 / WI-1345 INTERIM until fix lands): the full jest suite in a `.worktrees/*` checkout (incl. via pre-push) can commit an empty "Test User" **init** commit that CLOBBERS the branch (root: `check-merge-invariant.test.ts` unisolated git ops — this is the WI-1207 corruption mechanism). BRIEF EVERY BUILDER: (1) no full jest in worktree; (2) verify tsc + targeted `jest --findRelatedTests` only; (3) push `SKIP_PRE_PUSH=1` (sanctioned narrow bypass, CI is the gate); (4) "init" commit appears → `git reset --mixed origin/<branch>`, no panic-force-push. Also worktree Node=v26 vs repo 22.x breaks `jest.spyOn(globalThis,'fetch')` (v2-finalization-56 flag).


> **orch-025: STAND-DOWN LIFTED (reverses orch-024). New core sequence M4 → M5 → M6; M4 gates BOTH M5 and M6.** Authority: resume-note 11-ws18-28-resume-note.md + convergence spine 08 (operator-ratified). ACKed v2-finalization-41. Monitors: live inbox **b89r8voe0** (retired the post-crash duplicate b6elvkzna). Session survived IDE crash.
> **M4 — WI-1307 (NEW, reversible):** build a V2=off/V1=on FALLBACK CHANNEL + a real E2E pass. Config F is UNPROVEN (no env runs it); rollback = OTA channel-promotion of a PREBUILT, TESTED bundle, so the artifact MUST exist first. GATES M5+M6. → dispatch a builder ONCE WI-1307 reaches Ready (currently WS=none/Captured; orch homing to WS-28 + triaging).
> **M5 — WI-1308 (NEW, ⚠️ IRREVERSIBLE, HUMAN-CONFIRM):** retire V0 + flags-off legacy shell, before ship, gated on M4. **THIS REMOVES THE FLAG-FLIP/OTA ROLLBACK PATH back to V1/V0 (rollback becomes git-revert only) — the S6-class hard boundary. DO NOT execute autonomously.** Produce the preservation gate (annotated+pushed tag + docs/archive/retired-code.md register entry; NO backup branch), then STOP for explicit human confirmation. Only after M4's rollback is proven.
> **M6 (reversible — unship=OTA):** ship V2. Gate = M1+M4+M5 done, seam smoke PR-gated, 7 publish prompts green (already are). Supporter S0–S5 surfaces ARE in MVP scope (operator ruling) → coordinate WS-32 build items (WI-1121/1127/1134/1135/1136/1137) with WS-32's orchestrator.
> **WI-904:** LEFT WS-28 → WS-33 (UX). Do NOT re-take.
> **IN PROGRESS (orch-026):** WI-1307 Ready/Assisted → dispatched builder **wi1307-m4** in 2 PHASES (scope-risk: spans V0×V1×V2 nav-flag matrix = PR-376 half-migration surface). PHASE 1 = enumerate full flag surface + plan, NO edits, STOP+report; I review/confirm; PHASE 2 = claim+worktree+build Config F channel+bundle+E2E. Holding for the phase-1 map. **Self-review authority (orch-027): I confirm the gap map + release Phase 2 myself; escalate to orch ONLY if the map surfaces an UNOWNED matrix cell or a PRESERVATION CONFLICT (shipped flag state would regress pre-M5).**
> **M1 COORDINATION (orch-026):** M1 moved to WS-37 (Seam Hardening, WI-1301..1305), run by the WS-18/Identity-Cutover shepherd. My M4 rollback-proof readiness gates on the **WI-1305** (cross-boundary profile-v2 seam test) signal that shepherd sends me. M6 ship gates on M1+M4+M5 + PR-gated seam smoke + 7 publish prompts (green). Sync WS-32 supporter items (WI-1121/1127/1134/1135/1136/1137) with WS-32 orch.
> Acked orch-026 = v2-finalization-42. Prior 16/17 closed remain terminal.
> **M4 PHASE-2a DONE (wi1307-m4):** PR **#1836** (4 additive files: eas.json fallback profile [prod-modeled], mobile-fallback-ota.yml [triple-gated, never auto-fires], playwright config-f-smoke + spec). Proof (a) REAL 4/4 (V1 shell renders, V2 surfaces absent, family+study). CI running → watch **bv0psiq6y** → Gate-1 merge on green. NO complete (M4 not done until (b) proven). Surfaced orch v2-finalization-44.
> **(b) proof — BLOCKED on TWO things:** (i) operator OTA-authorization (billed eas build + eas update, standing 'no OTA unless asked' rule) — DECISION open to operator; (ii) MISSING secret EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY_PRODUCTION (only _PREVIEW wired) — ops action (Doppler prd → eas env:create) before (b) can run. Also time-align with M1/WI-1305 signal.
> **3 infra flags RESOLVED (orch-028):** (1) missing prod CLERK secret → **WI-1310** (P1, WS-35 Dev-Infra); WI-1307 now Blocked-by WI-1310. (2) env:sync V2-strip → **WI-1311** (P2 Bug, WS-35). (3) empty-State claim gap FIXED (State=Active stamped on 1306/1307/1308).
> **Proof (b) = DOUBLE-BLOCKED, HELD:** (i) operator OTA/eas-build authorization + (ii) WI-1310 (prod secret) landing — both operator-side; orch relays the ruling + secret status. Acked orch-028 = v2-finalization-45. Proceeding: merge #1836 on green; time-align (b) with M1/WI-1305.
> **▶ RELAX RULED → BUILDER DISPATCHED (orch-035, 2026-07-03 08:31Z; v2-finalization-54):** operator SUPERSEDED orch-034's wire-not-relax — RC public SDK keys NEVER existed (Test Store app only; prod binary itself ships EMPTY RC keys, purchases-off-by-design via `apps/mobile/src/lib/revenuecat.ts:38-48`), so the preflight is stricter than the binary it protects. RULED: **relax** — Clerk+Sentry stay hard-required; RC iOS/_ANDROID → WARN-if-empty (loud `::warning::` + TEMPORARY-until-store-apps comment, re-harden tracked by **WI-1328** — RevenueCat production monetization setup (MVP), P1, WS-35; its phase 5 re-hardens this preflight [orch-036, relayed to builder]). Dispatched builder **wi1307-relax-rc** (sonnet, worktree `.worktrees/WI-1307-relax-rc-preflight` off origin/main, YAML-only, NO eas/workflow runs, commit-skill + `gh pr create`, does NOT merge). NEXT: on builder report → spot-verify diff (only the preflight step + comment; RC keys stay in `env:` block; publish step untouched) → **Gate-1 merge on strict-green (mine)** → orch re-dispatches `gh workflow run mobile-fallback-ota.yml --ref main -f confirm=publish` (operator prod-Env approval click = publish-go) → R1 artifact-E2E → proof-(b) verdict → `complete`. Inbox ear bo00voy3l.
> **▶ PR #1844 OPEN (relax RC preflight) — REBASING to clear merge-invariant (2026-07-03 ~08:40Z):** builder wi1307-relax-rc opened **PR #1844** (branch `wi-1307-relax-rc-preflight`), workflow-file-ONLY. Diff spot-verified GOOD: `required_env=(CLERK,SENTRY)` hard-exit-1; `warn_env=(RC_IOS,RC_ANDROID)` warn-only `::warning::`; env: block still feeds all 4; publish step untouched; WI-1328 referenced in comment+warning (orch-036); git identity correct (vetinari@zaf.fleet). BLOCKER: `Merge completeness check` (WI-680 invariant) FAILED direction-a — branch 3-behind origin/main (busy shared checkout landed session/stream-fallback commits), merge would drop 5 main-side files. RESOLVED without bypass: rebase/force-push was WRONG (commit-skill forbids --force/--amend/--rebase on pushed commits; builder correctly refused TWICE). Fix path: builder forward-merged main (clean) but its local push BLOCKED by a broken local harness — worktree Node **v26.3.0** vs repo engines 22.x → pre-push jest `jest.spyOn(globalThis,'fetch')` fails 37/38 in gemini.test.ts (environmental, untouched file). Routed around it SERVER-SIDE: **`gh pr update-branch 1844`** (GitHub base→branch merge, no local hooks). New head **59439ad6**, branch now current with main. Builder stood down. **Watcher byozcdnan** armed on PR #1844 checks-settle. NEXT: on strict-green → triage claude-review/CodeRabbit → **Gate-1 SQUASH-merge** (3 commits + server-merge → 1 clean commit) → orch re-dispatches `gh workflow run mobile-fallback-ota.yml --ref main -f confirm=publish` → R1 E2E. INFRA FLAG raised v2-finalization-56 (Node-26 worktree defect, fleet-wide, WI candidate). (eas.json env:sync _V2-strip drift left uncommitted — correct; WI-1311 landmine, not this PR.)
> **✅ PR #1844 MERGED (Gate-1 squash, 2026-07-03 08:52Z) → HANDED BACK FOR PUBLISH RE-DISPATCH (v2-finalization-57 needs-orchestrator, sol_by 09:25Z):** relaxed preflight (Clerk+Sentry hard / RC pair warn-only) + WI-1328 refs now on main; `mobile-fallback-ota.yml` preflight will PASS. claude-review APPROVED no-issues; strict-green. Requested orch re-dispatch `gh workflow run mobile-fallback-ota.yml --ref main -f confirm=publish` (I don't dispatch OTA on a relay — orch holds the human's direct go, dispatched last time per orch-031). On re-dispatch → run queues, WAITS on operator `production` Environment approval click (real publish-go) → on publish success I arm run watcher → **R1 artifact-E2E** (dev-client → fallback branch, vs PUBLISHED bundle) → proof-(b) verdict → `/cosmo:execute complete` WI-1307. Watchers: inbox bo00voy3l. sol_by 09:25Z.
> **✅ PUBLISH SUCCEEDED (run 28649734659, 2026-07-03 09:02Z) → ⛔ R1 BLOCKED ON ENV (v2-finalization-59 needs-orchestrator, sol_by 09:45Z):** proof-(b) part 1 DONE — fallback OTA published: branch=**fallback**, rtV=**1.0.1** (matches prod binary → OTA applies), android+ios, **update group 802cab19-1907-4197-9cd4-1bd2d9358b78**, dashboard expo.dev/accounts/zuzanka14/projects/mentomate/updates/802cab19…. R1 (rider-1 artifact-E2E vs PUBLISHED bundle via dev-client on fallback branch) CANNOT run in this headless orchestrator env — probed: no adb / no emulator / no Maestro / no EAS auth; web-e2e can't test a mobile hbc bundle. R1 = device-QA-class (cf. WI-904). Surfaced DECISION: (a) operator runs on physical S10e [recommend] — load update 802cab19 on a dev-client, confirm V1 guardian shell (recaps tab; family+study V1 tabs) + V2 surfaces ABSENT (same asserts as proof-(a) nav-shell.spec.ts); (b) device-capable fleet runner/CI; (c) rule if a baked-flag inspection substitutes (rider-1 reads as needing real runtime). **WI-1307 STAYS EXECUTING — no complete until R1 green.** Watcher b6bbzv5zo ended (run terminal). Inbox bo00voy3l live.
> **⛔ PROOF-(b) PUBLISH HELD — RevenueCat secret gap (orch-033/034, 2026-07-03 08:20Z; my report v2-finalization-52 needs-orchestrator crossed identical):** run **28647845287 FAILED at preflight** `Verify required production publish secrets` (exit 1, NO publish, nothing touched). Clerk ✓ (wired) + Sentry ✓; **EMPTY: `EXPO_PUBLIC_REVENUECAT_API_KEY_IOS` + `_ANDROID`** — known pre-launch gap, absent from Doppler prd too (only REVENUECAT_WEBHOOK_SECRET), dashboard-only/operator. **RULED: WIRE (don't relax preflight)** — fallback bundle must carry same client keys as prod binary for faithful rollback + meaningful R1 E2E. UNBLOCK PATH (orch+operator own it, NOT me): operator fetches both from RevenueCat dashboard → Doppler prd → orch pipes to GH secrets → orch re-dispatches `gh workflow run mobile-fallback-ota.yml --ref main -f confirm=publish`. **MY POSTURE: idle-armed on inbox bo00voy3l; on orch's re-dispatch notice → re-arm run watcher → R1 artifact-E2E on publish success → proof-(b) verdict → `/cosmo:execute complete`.** No independent lane work meanwhile (M5 prep gated on M4-proven; M1=WS-37; supporters=WS-32-built). WI-1307 Executing. Discipline note (orch-032): lane-stopping holds post at needs-operator/needs-orchestrator, NEVER info.
> **▶ PUBLISH DISPATCHED BY ORCH ON DIRECT HUMAN GO (orch-031, 2026-07-03 08:17Z) — LANE RESUMED (v2-finalization-50):** integrity hold RESOLVED at source — human operator instructed orch directly (verbatim: "go with your recommendation" / "A on D1, workflow on D2; secret pipe approved — run it" / "get the lane moving again"); **orch dispatched the workflow itself, I never ran the OTA** (no peer-relay laundering — the boundary held and was honored). Run **28647845287** (`mobile-fallback-ota.yml`, `--branch fallback`) status=**waiting** on `production` GH Environment approval (human click). Watcher armed = monitor **bmnnjb2c8** (fires on terminal state, success OR preflight/publish fail). NEXT: on publish success → dispatch device-E2E executor for **R1** (dev-client → fallback branch, run Config-F nav-shell flow vs PUBLISHED bundle) → spot-verify → report proof-(b) verdict → `/cosmo:execute complete` (M4 done ONLY after artifact-E2E green). On failure → diagnose + needs-orchestrator. Inbox ear = bo00voy3l.
> **✅ MECHANISM RULED (orch-030, 2026-07-03 07:13Z) → HELD ON HUMAN PUBLISH-GO (v2-finalization-49):** operator ruled **(A)+WORKFLOW** — proof-(b) = run the fallback-branch OTA AS-BUILT via `mobile-fallback-ota.yml` (workflow_dispatch, `confirm=publish`, then `production` GH Environment approval). Fallback-branch model ACCEPTED CANONICAL (publish tested bundle→fallback branch; real rollback = repoint production channel→fallback). GH secret `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY_PRODUCTION` now WIRED (Doppler=SoT, re-pipe on rotation — WI-1310). **3 RIDERS in WI-1307 AC + my gate checklist:** R1 — M4 NOT done at publish; proof-(b) = publish **+ artifact-E2E against the PUBLISHED fallback bundle** (dev-client → fallback branch), BOTH required. R2 — rotation FYI. R3 — **M5/M6 GATE:** if V2 ship changes runtimeVersion, RE-publish + RE-prove fallback against the new binary BEFORE M5 executes. **DISPATCH COMMAND (one-action-ready):** `gh workflow run mobile-fallback-ota.yml --ref main -f confirm=publish`. **HOLD REASON:** standing user rule = no `eas update`/OTA (incl. dispatching the workflow that runs it) unless the HUMAN asks directly; orch's "gate doesn't apply, it's fallback" is a peer narrowing a standing user instruction → I require the human's direct in-session go (asked now). On confirm: dispatch → watch run → artifact-E2E per R1 → report → THEN `complete` (M4 done only after artifact-E2E green). Production channel stays operator-gated.
> **⛔ PROOF-(b) HELD — MECHANISM CONTRADICTION (v2-finalization-48, needs-orchestrator, 2026-07-03 ~07:00Z):** pre-flight (spot-verified vs origin/main) found orch-029's "OTA to the PRODUCTION channel" does NOT match the merged machinery, which publishes ONLY to a new `fallback` branch (`mobile-fallback-ota.yml:110`); NO production-channel OTA path exists in the repo. (1) No `channel:fallback` binary was ever built (proof-(b)="not a fresh build") → `eas update --branch fallback` reaches ZERO devices — fine IF rollback = publish-to-fallback-then-repoint-production-channel (channel promotion), NOT fine as literal "OTA to production." (2) Fallback workflow preflight hard-fails (`:66`/`:84`, exit 1) without GH Actions secret `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY_PRODUCTION` — a GITHUB secret ≠ the Doppler value orch-029 cited; workflow-dispatch path blocked today (manual-CLI publish w/ Doppler env sidesteps). FLAGS CORRECT: inline MODE_NAV=true/_V1=true/_V2=false; WI-1311 strip N/A (fallback not in EAS_PROFILE_MAP). runtimeVersion=1.0.1/appVersion → OTA pure-JS, MATCH-conditional (EAS-auth-unverifiable). **DECISION for orch/operator:** (A) proof-(b)=fallback-branch OTA as-built [recommend] vs (B) literal prod-channel publish [needs new path/manual CLI + channel↔binary mapping]; AND execution path = workflow_dispatch (wire GH secret) vs manual operator CLI. Executor `wi1307-preflight` released. Local tree 9-ahead/20-behind origin/main (others' unpushed commits — untouched). HOLDING; no eas update.
> **▶ PROOF-(b) UNBLOCKED (orch-029, 2026-07-03 06:45Z):** operator ruling — WI-1310 DISSOLVED (EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY already `pk_live_` in Doppler prd + baked in the running prod binary; re-scoped verify-only/Awaiting-Info, does NOT block). Mechanism RULED: proof-(b) = **OTA to the `production` channel** (NOT a fresh build — that's M6; zero prod users make OTA-reversibility the right tool). GATED SEQUENCE: run pre-checks (i)+(ii) NOW → report at needs-operator → HOLD for operator's explicit publish confirm (relayed by orch). DO NOT run `eas update`. Acked = v2-finalization-47. **Dispatched READ-ONLY executor `wi1307-preflight` (sonnet):** (i) runtimeVersion match [current prod-binary runtime vs what an OTA targets now + native-change scan; policy=appVersion → OTA=pure JS], (ii) exact publish env [MODE_NAV/_V1/_V2 + API URL verbatim; VERIFY WI-1311 _V2-strip landmine — env:sync silently drops _V2]. On its report: spot-verify the _V2 value + runtime verdict, then surface both verbatim to operator at needs-operator. NO complete until publish proves rollback.
> **✅ GATE-1 DONE (v2-finalization-46, ~20:55Z):** #1836 merged `--merge`, **8ceee3628 on main**. M4 MACHINERY LANDED. claude-review APPROVED/0-blocking; CodeRabbit advisory-only (action-pin quick-win ota.yml:65 + minor missing test assertion spec:64); CONSIDER = known WI-1310 gap. **WI-1307 stays Executing — NO `complete` until proof-(b) authorized+unblocked.** Shepherd now IDLE-ARMED on (b): awaiting operator OTA/eas-build decision (open DECISION) + WI-1310 landing + M1/WI-1305 seam signal. Monitor b89r8voe0 live.

## 📓 R1-PASS RUNBOOK (task 4, orch-041) — one-action close when operator rules "R1 pass"
> Trigger: operator confirms R1 PASS = on the physical device, a dev-client loaded published update **802cab19** (fallback branch, rtV 1.0.1) and rendered the **V1 guardian shell** (recaps tab; family+study V1 tabs) with **V2 mentor-shell surfaces ABSENT** (same asserts as proof-(a) `nav-shell.spec.ts`).
> 0. **Capture R1 evidence** (who/device/when/what-rendered) → append to WI-1307 AC via REST + a rationale comment. **Verify WI-1307 claim still live** (`Claimed By` + `Claim Expires > now`); if expired, `/cosmo:execute claim` WI-1307 first.
> 1. **`/cosmo:execute complete` WI-1307** — it authors `Fixed In` from git HEAD (machinery landed: #1836 `8ceee3628` + relax squash from #1844, both on main) + Stage→Reviewing + Resolved. Completion-summary content ready:
>    - **What was done:** Built + published the Config-F (V1-on/V2-off) fallback rollback channel and proved it end-to-end (reversible OTA path; gates M5+M6).
>    - **What changed:** `eas.json` `build.fallback` profile (prod-modeled); `.github/workflows/mobile-fallback-ota.yml` (triple-gated workflow_dispatch; RC preflight relaxed to warn-only, WI-1328-tracked); `playwright.config.ts` `config-f-smoke` project + `e2e-web/flows/config-f/nav-shell.spec.ts`.
>    - **Verification:** proof-(a) web-e2e 4/4 (V1 renders, V2 absent, family+study); proof-(b) publish (update **802cab19**, branch fallback, rtV **1.0.1**, android+ios) + **R1 device artifact-E2E PASS** (operator, S10e, published bundle).
>    - **Caveats:** RC preflight warn-only (RC public SDK keys never existed = parity w/ prod binary; re-harden = WI-1328 phase 5). Fallback branch has NO dedicated binaries by design; real rollback = repoint production channel→fallback (channel promotion). Node-26 worktree + init-commit + env:sync-_V2-strip defects (WI-1345/1311/56) are infra, not this WI.
>    - **Follow-ups:** WI-1328 (RC re-harden), WI-1310 (verify-only), WI-1311, WI-1345; **M5/WI-1308 now unblocked (M4 rollback proven)**; M6.
> 2. **Landedness:** confirm `Fixed In` commit is on `origin/main` BEFORE the reviewer checks (both PRs merged → fine; if bounce, re-finalize claim→complete).
> 3. **Gate-2:** separate Codex reviewer fires on Stage-ENTRY (Executing→Reviewing via `complete`). Watch inbox/stage for verdict (close or bounce). I do NOT run `/cosmo:review`.
> 4. **M4-DONE spine entry** (`/nexus:checkpoint` or cosmo spine): record M4 complete (proof-a+b green) → **unblocks M5 (WI-1308) + M6**; report orch. **RIDER R3 carried:** if M6's V2 ship changes runtimeVersion, RE-publish + RE-prove fallback BEFORE M5 executes (M5/M6 precondition, in WI-1308 AC + my checklist).
> 5. On a **FAIL**: R1 fail = a real proof-(b) finding → diagnose (bundle env? flag bake? render regression?), report needs-orchestrator; WI-1307 stays Executing.

# STATUS 2026-07-02 ~18:35Z — WOUND DOWN / STAND-DOWN (superseded by orch-025 resume)

> **orch-024 WIND-DOWN: lane complete, full stand-down.** 16/17 CLOSED; only WI-904 parked (Refining, on-device-QA-blocked). TAKE NO NEW WORK. WS-Safety-Eval (WI-1154/1155/781) reassigned to Orion Quartet (WS-31) — NOT mine. Nothing in Execute. Session PRESERVED, surface only needs-operator.
> Wind-down housekeeping DONE: removed my closed-item worktrees (WI-1120/1124/1173/1174/1175/1207 + review-wi-1120/1171 + wi-1170-review); branches left intact (per user "worktrees only"); other lanes' worktrees untouched. Ack v2-finalization-39.
> Monitors: inbox **b89r8voe0** only (light; re-armed after an IDE-crash resume 2026-07-02, prior ids bg4vdu2ot/b6elvkzna dead). Sign-of-life sent v2-finalization-40. Shepherd idle-but-armed for needs-operator; no further action expected on this lane.

# STATUS 2026-07-02 ~15:40Z — WI-1124 CLOSED (superseded)

> **WI-1124 CLOSED by Gate-2 (Resolution=Done, Fixed In pinned e5f6d8a82).** LANE = **16/17 CLOSED**. Only WI-904 remains (Refining) — operator-parked deferral (dictation-pace, perceptual on-device QA blocked, orch-002); NOT shepherd-actionable, do NOT nudge/merge PR #1749.
> Reported both orch-023 milestones (Reviewing v2-finalization-37, Closed v2-finalization-38). Back to STAND-DOWN/idle-hold per orch-022: PREP-ONLY, not self-assigning triage backlog, doing NOTHING on candidate WS-SAFETY-EVAL (WI-1154/1155/781) until explicit go.
> Monitors: inbox **bg4vdu2ot** live (catches re-task); all stage watchers retired. Session preserved, shepherd idle-but-armed.

# STATUS 2026-07-02 ~15:30Z — WI-1124 FINALIZE (orch-023) (superseded)

> **orch-023 (operator-authorized): pick up WI-1124 and close it — SUPERSEDES the orch-021/022 park.** Work already LANDED (e5f6d8a82, PR #1726, on main; Fixed In+Resolved set) but stranded at Executing (unclaimed). Author crowka/zuzana.kopecna = configured git identity, agent-developed, nothing to coordinate.
> Dispatched `wi1124-final`: claim → complete from a DETACHED worktree pinned at e5f6d8a82 (orch caution: PIN Fixed In to e5f6d8a82, do NOT auto-derive from stale cwd HEAD — WI-1247 hazard) → Executing→Reviewing → Gate-2 close. If complete refuses on a missing artifact, produce it (don't force).
> Light single-item watch **b4s5u7k4i** armed (polls WI-1124 to Closed). Report to orch at Reviewing + at Gate-2 close (orch-023 asked for both). Otherwise still idle-hold on triage backlog. After this: 16 closed + WI-904 parked = lane's new end-state.
> Monitors: inbox **bg4vdu2ot** live; full stage poller still STOPPED (using the light single-item watch instead).

# STATUS 2026-07-02 ~12:00Z — STOOD DOWN / IDLE-HOLD (superseded)

> **orch-022: STAND DOWN, PREP-ONLY mode.** WS-28 active work COMPLETE (15 closed + 904/1124 parked). Directive: idle-hold, do NOT self-assign triage/backlog, preserve session. I am the first freed lane; a re-task likely follows.
> **Monitors trimmed to LIGHT:** stage poller bx6new64d STOPPED (lane terminal); ONLY inbox watcher **bg4vdu2ot** live (catches the re-task directive). If resuming and bg4vdu2ot is dead, re-arm just the inbox tail; re-arm the stage poller only if a lane with active items is (re)assigned.
> **Candidate next charter (NOT a go — do nothing until explicit operator/orch approval):** WS-SAFETY-EVAL = WI-1154 (P1 minor-routed drug-synthesis eval leak) + WI-1155 (envelope signal discipline) + WI-781 (CONCEPT_CAPTURE decision). Cutover-independent, LLM/eval-competency fit.
> Heartbeat sent v2-finalization-36; holding silent per directive.

# STATUS 2026-07-02 ~11:55Z — LANE END-STATE REACHED (superseded)

> **WS-28 ACTIVE WORK COMPLETE. 15/17 CLOSED; the other 2 are operator/orch-parked deferrals.**
> CLOSED (15): 1118,1120,1122,1130,1131,1133,1168,1169,1170,1171,1172,1173,1174,1175,1207 (own-eyes swept 11:55Z).
> PARKED (not shepherd-actionable): **WI-904** (Refining — dictation-pace, perceptual QA blocked, orch-002; do NOT nudge/merge PR #1749) + **WI-1124** (Executing/held per orch-021 — P3 do-not-pick-up, no cleanup).
> No open lane work, no live claims, no pending PRs. Monitors stay armed (inbox bg4vdu2ot, stage bx6new64d) to catch any late Gate-2 bounce. Reported to orch v2-finalization-35. Shepherd IDLE-BUT-ARMED — lane fully closes when operator/orch disposes of WI-904 + WI-1124.
> Session hazards escalated: shared .git/config Test-User identity (orch FIXED; WI-867 re-harden → WS-25) + unidentified worktree git-init corruptor (v2-finalization-30). 4 AC-drift items (1102/1118/1120/1175) closed via legit gate/canon reframes — Fable-audit signal logged.

# STATUS 2026-07-02 ~11:35Z (superseded)

> **15/17 CLOSED.** WI-1120 + WI-1175 CLOSED by Gate-2 (landedness re-finalize + AC gate→deliverable reframe both accepted). Monitors: inbox bg4vdu2ot, stage bx6new64d. Outbox last=v2-finalization-34; inbox last=orch-021 (acked -34).
> **orch-021 rulings applied:** shared .git/config identity FIXED by orchestrator (repo→vetinari@zaf.fleet); WI-1175 reframe NOT reversed (proceeded, closed); **WI-1124 = HOLD, do NOT pick up (parked, no cleanup)**; WI-867 re-hardening routed to bug-lane/WS-25 (not my lane).
> **WI-1207: #1818 MERGED (8060b4ae on main, Gate-1 clean — claude-review APPROVED, Merge-completeness SUCCESS, author Lord Vetinari after amend).** Both blockers resolved (KEEP_PATTERNS @ JournalTabView.tsx:883; merge-completeness was a stale GH merge-ref, cleared on re-push). ONLY STEP LEFT: wi1207-fix runs `/cosmo:execute complete` on 8060b4ae (already on main → no landedness bounce) → Reviewing → Gate-2 close. Nudged; if it idles, dispatch a fresh finalizer. NOTE: my "amend f3ae3b242f/idled" nudges were stale-fetch reads — executor had already amended to 8060b4ae correctly; reconciled.
> **Lane end-state:** reaches 16 CLOSED + WI-904 & WI-1124 as parked deferrals (both operator/orch-owned) once WI-1207 lands. WI-904: do NOT nudge/merge PR #1749.

# STATUS 2026-07-02 ~11:15Z (superseded)

> **13 CLOSED (WI-1118 confirmed closed). Live monitors: inbox bg4vdu2ot, stage bx6new64d.** Retired 2 duplicate monitors that survived compaction + double-fired (b9hvkttcm inbox, b828yuw72 stage). A ~3h silence gap (08→11Z) was a suspended background stall-timer + zero inbound events — monitors themselves fine (delivered orch-019/020). Do NOT arm bare `sleep N` background timers for stall detection — they get suspended; rely on monitors + executor reports.
> **3 executors running (post-bounce fixes):**
> - `wi1120-refinal`: WI-1120 bounce was PURE landedness-timing (reviewer checked before #1816 merged). #1816 now merged (a4798547e), ed3806ef6 on main. Re-finalized claim→complete → back at Reviewing → expect CLOSE.
> - `wi1175-refinal`: WI-1175 bounce = genuine AC gate-vs-deliverable mismatch. Re-scoping AC (gate→conditional-review-deliverable, per orch-012 + reviewer's own offered path-b) via REST PATCH + rationale comment, then re-finalize. Veto-window noted to orch (v2-finalization-32). 4th AC-drift item.
> - `wi1207-fix`: PR #1818 has 2 REAL blockers — 'Merge completeness check' FAILURE + claude SHOULD-FIX (missing scripts/i18n-keep.ts KEEP_PATTERNS for new dynamic journal.practice.type.* keys). Reviewer bounced WI-1207→Executing. Builder adding KEEP_PATTERNS + verifying merge-completeness cause → SKIP_PRE_PUSH re-push → re-green → re-finalize. THEN I Gate-1 merge #1818.
> **WI-1124** (S0-R T10 cross-writer lifecycle test + GC6 cleanup): unexpectedly Executing/Active, owner empty, Fixed In e5f6d8a — prior-session progress exists; P3, NOT dispatched by me; asked orch whether to pick up now or hold (v2-finalization-32).
> **Merge-timing lesson (recurring):** builders complete-before-merge → Fixed In not-yet-on-main → reviewer bounces for landedness (hit WI-1120, WI-1207). Mitigation: merge the PR FAST after complete; re-finalize if bounced in the gap. Outbox last=v2-finalization-32; inbox last=orch-020 (acked via -31/-32).

# STATUS 2026-07-02 ~08:05Z (superseded)

> **12 CLOSED unchanged. 2 at REVIEWING awaiting Gate-2: WI-1175 (since ~07:41) + WI-1120 (since ~07:55, Fixed In ed3806ef6 now LANDED on main via merged PR #1816).** Both PRs #1807 (WI-1175 artifact) + #1816 (WI-1120 test) MERGED with full Gate-1. WI-1120 claude-review APPROVED (1 CONSIDER nit on toEqual([1,1]) brittleness — advisory, intentional contract-pin, not fixed).
> **WI-1207:** worktree was CORRUPTED mid-push by an external `Test User "init"` git-fixture race (a1cfdd810 wholesale-delete). I did WI-1246 surgery: reset --hard to validated fix 0c053c06f (JournalTabView + regression test + i18n, all present), garbage discarded, main untouched. Handed back to wi1207-build to retry with `SKIP_PRE_PUSH=1 git push --force-with-lease` (validation already passed; pre-push window = the collision window; CI authoritative). Hazard escalated v2-finalization-30 (fleet-wide, WI-1246-class; root cause = git-fixture repo-path/cwd collision, candidates scripts/check-change-class.test.ts:95-103 et al).
> **CONFIRMED-ACTIVE RISK:** Codex hit usage limits on BOTH #1807 + #1816 inline bots. Gate-2 Cosmo reviewer is ALSO Codex → WI-1175 + WI-1120 closes MAY STALL at Reviewing. Flagged v2-finalization-29. If either dwells at Reviewing past ~08:20Z, escalate needs-operator (Codex top-up or alt close path). Stage monitor bx6new64d will fire on close.
> **NEXT:** (1) watch WI-1175/WI-1120 Reviewing→Closed (stage monitor); escalate if stalled. (2) wi1207-build report → Gate-1 merge #<new PR> → Reviewing. (3) WI-1124 (P3) dispatch builder after 1207 lands. (4) WI-904 parked — do NOT nudge. Outbox last=v2-finalization-30; inbox last=orch-018 (all acked).

# AUTHORITATIVE page-id → WI map (verified 2026-07-02 ~07:55Z — DO NOT trust memory/position)

| WI | page id |
|----|---------|
| WI-904 | 3858bce91f7c8139ab26dd039ada375d |
| WI-1118 | 38d8bce91f7c81578369cc1f31f42c4d |
| WI-1120 | 38d8bce91f7c81b4b8b0efa35f2693d9 |
| WI-1122 | 38d8bce91f7c81ddb18bd34bb3077a7d |
| WI-1124 | 38d8bce91f7c81c5a103f27b79a36f80 |
| WI-1130 | 38d8bce91f7c81b9a570fe58b10e5549 |
| WI-1131 | 38d8bce91f7c811ab811e35bcf93b8e3 |
| WI-1133 | 38d8bce91f7c81c69495d22061c6f2fa |
| WI-1168 | 38f8bce91f7c81079932c70dd9fc2367 |
| WI-1169 | 38f8bce91f7c817bb275f7057eb89214 |
| WI-1170 | 38f8bce91f7c81c6b958c0d78d027b6b |
| WI-1171 | 38f8bce91f7c817eac9be214728a691e |
| WI-1172 | 38f8bce91f7c81d0b40cdd49831e0d52 |
| WI-1173 | 38f8bce91f7c81c49ba4d5ee3daf5bae |
| WI-1174 | 38f8bce91f7c811ca8a9ceb82fe014a1 |
| WI-1175 | 38f8bce91f7c81b8b139e93377fc8990 |
| WI-1207 | 38f8bce91f7c817280acfdaea89a3893 |

> **BUG CAUGHT 07:50Z:** I briefed wi1120-build with WI-1173's page id (38f8bce9…c49ba4…) as "WI-1120" — WRONG. No damage: `/cosmo:execute complete` resolves the page from the branch/[WI-NNNN] commit tag, NOT the hand-supplied page id, so WI-1120 moved correctly (Reviewing, Fixed In ed3806ef6) and WI-1173 was untouched (still Closed/ef8b7160). LESSON: hand-supplied page ids in executor briefs are advisory only for REST reads; the writer is branch-driven. Always verify a page's `.properties.ID.unique_id.number` before hand-citing an id. Poll script keys by payload unique_id so its events are correct.

# WS-28 Shepherd — Live State (POST-COMPACT RESUME 2026-07-02 ~07:15Z)

> **RESUMED 2026-07-02 ~07:15Z.** Monitors RE-ARMED: inbox **bg4vdu2ot**, stage **bx6new64d** (baseline reset after full direct-read of 17). 12 CLOSED unchanged. **PR #1807 MERGED** (WI-1175 artifact, 2e9942dcf on main, Gate-1 clean).
> **Three executors RE-DISPATCHED (prior ones died in compaction; worktrees persisted on disk):**
> - `wi1175-final` — discard eas.json noise → `/cosmo:execute complete` from `.worktrees/WI-1175` (branch @2e9942dcf, merged) → close-as-met-with-caveats (caveats=1120/1207/1124/904). Finalize-only, no code.
> - `wi1120-build` — `.worktrees/WI-1120` had UNCOMMITTED test edits (MentorCelebration.test.tsx). Finish assertions (reduced-motion rest scale/opacity=1 + exiting undefined) → run jest green → commit test-only (NOT eas.json) → push HEAD:WI-1120 → PR → complete. Impl+AC untouched (orch-017).
> - `wi1207-build` — `.worktrees/WI-1207` had the fix COMMITTED (ac7d3ac49) but on STALE main, unpushed, no PR. Rebase onto main → ensure regression test → validate → push → PR → complete.
> **eas.json shows modified in EVERY worktree = `pnpm env:sync` noise; never staged.** All three briefs carry the WI-1246 push-guard (no /commit auto-push; assert branch!=main; explicit push HEAD:<branch>; on any main-landing STOP+escalate).
> **RISK flagged to orch (v2-finalization-29):** the PR-inline Codex bot hit usage limits on #1807 ("reached your Codex usage limits for code reviews"). The Gate-2 Cosmo reviewer is ALSO Codex → may be throttled, could stall 1175/1120/1207 closes at Reviewing. Watching; escalate if a Reviewing item sits > ~20min unreviewed.
> **On each executor green report:** Gate-1 verify (checks SUCCESS, inline `pulls/<n>/comments`, mergeStateStatus CLEAN) → `gh pr merge <n> --merge` → executor's `complete` moves it Reviewing → Gate-2 closes. WI-1124 (P3) dispatch a builder once 1120/1207 land. WI-904 parked — do NOT nudge.

# WS-28 Shepherd — Live State (compaction snapshot 2026-07-02 ~06:50Z)

> **COMPACTION 2026-07-02 ~06:50Z.** 17-item lane, **12 CLOSED**: 1118,1122,1130,1131,1133,1168,1169,1170,1171,1172,1173,1174. Monitors LIVE (persistent, should survive compaction — RECONCILE + direct-read all 17 after resume): inbox **b9hvkttcm**, stage **b828yuw72**. Clacks: outbox last=**v2-finalization-28**, inbox last=**orch-018** (all acked). Notion MCP down → REST only.
>
> **TOP RESUME ACTIONS (priority order):**
> 1. **WI-1175 — merge PR #1807** (docs-only artifact, OPEN/CLEAN). Gate-1 verify (all checks SUCCESS, claude-review green/legit-skip, no valid inline must/should-fix, mergeStateStatus=CLEAN) → `gh pr merge 1807 --merge` → then `wi1175-research` runs `/cosmo:execute complete` (close-as-met-with-named-caveats per orch-012; caveats = open items 1120/1207/1124/904) → Gate-2 closes. wi1175-research was watching #1807 CI; confirm its green report.
> 2. **WI-1120** — `wi1120-diag` (builder) adding reduced-motion test assertions (rest scale/opacity + exiting undefined), test-only, Executing, no PR yet. On its green report: Gate-1 merge its PR (`--merge`) → it re-finalizes (claim→complete, fresh Stage-entry) → Gate-2 closes.
> 3. **WI-1207** — `wi1207-builder` landing Practice-access-in-Journal fix + regression test, Executing, no PR yet. On green report: Gate-1 merge → complete → Gate-2.
> 4. **WI-1124** — P3 GC6 test-hygiene, Executing, NOT dispatched (orch: slot after P1/P2). Dispatch a builder once 1120/1207 land.
> 5. **WI-904** — P2 Refining deferral (QA failed 30 Jun, approach changed, Owner empty), non-blocking; do NOT nudge/merge PR #1749.
>
> **CRITICAL MECHANISM (learned this session, in memory [[project_gate2_reviewer_stage_entry_trigger]]):** Gate-2 Codex reviewer fires ONLY on Stage-ENTRY to Reviewing. A within-stage flip (Awaiting-Info→Active) or Resolved-bump SILENTLY STALLS it. To re-trigger review after rework/AC-refinement: route via claim→complete (fresh Stage-entry), NEVER a within-stage flip. For an already-merged item, re-finalize via detached worktree @ merged commit so Fixed In re-derives unchanged.
> **AC-drift note:** orch flagged systemic AC-quality drift (WI-1102 stale-AC, WI-1118 AC-vs-canon supersession, WI-1120 self-contradictory AC) as a Fable-audit candidate. AC edits use direct REST PATCH of Acceptance Criteria + rationale comment (refine.ts blocks Executing/Reviewing; AC is content not a protected field).

# WS-28 Shepherd — Live State (superseded header below, 2026-07-01 ~16:18Z)

> **SCOPE EXPANDED 17 items (orch-012, 2026-07-02).** WS-28 DoD = ALL 17 related items resolved, not the original 12. Monitors: inbox **b9hvkttcm**, stage **b828yuw72** (17 ids, restarted). 7/7 publish-critical prompts pass = readiness, NOT lane-completion. V2 product ship/hold = OPERATOR call (orch surfacing; does not gate lane).
>
> **CLOSED (11):** 1122,1130,1131,**1133**,1168,1169,1170,1171,1172,1173,1174.
> **WI-1175** Executing — CONDITIONAL-SHIP review done; orch ruled close-as-met-with-named-caveats. `wi1175-research` pushing WI-1170-retraction correction to PR #1807 (docs-only); then I Gate-1 merge → it runs `complete` → Gate-2 closes.
> **OPEN lane work (dispatched 2026-07-02 ~06:00Z):**
> - **WI-1207** P2 Practice-access bug (Journal) → builder `wi1207-builder` (claim→worktree→land fix+regression test→PR→complete). Stale FixedIn 85cae5c3f from a review-bounced prior attempt.
> - **WI-1118** P1 — DONE except re-review trigger STUCK. AC #3 refined topic-scoped, PR #1721 merged+green, rationale posted, State=Active/Stage=Reviewing. Reviewer did NOT self-trigger (within-stage flip); Resolved-bump (orch-016 mechanism) ALSO failed (~12min, no action). Inference: reviewer keys on STAGE-ENTRY (WI-1120 picked up in ~6min via real Executing→Reviewing) not Resolved-bump. orch-018 REVERSED the no-round-trip caution. `wi1118-diag` round-trip re-finalized (claim→Executing→detached-worktree @ fff37f64→complete→fresh Reviewing entry, Resolved 06:45, Fixed In unchanged, claim cleared) — genuine Stage-entry the reviewer keys on. Reviewer-watch b3wz4ep6h armed (~12min; escalate if it stalls even now = reviewer idle). `wi1118-diag` stood down. Root-cause saved to memory [[project_gate2_reviewer_stage_entry_trigger]].
> - **WI-1120** P1 animation — RULED orch-015 option A. AC6 corrected to match built behavior (direct AC-PATCH + rationale, a11y basis) + re-finalized Executing→Reviewing via /cosmo:execute complete (detached worktree @ merged 8b9d650d; Fixed In unchanged, on main; PR #1720 already MERGED — ZERO code). Resolved bumped fresh 06:16 → reviewer re-reviewed. **BOUNCED AGAIN 06:22 (Reviewing→Executing):** corrected AC6 requires the TEST to assert rest scale/opacity + no exiting, but landed test (:48-70) only asserts surface/copy/onMarkSeen. Impl IS correct (verified: scale/opacity=1, exiting=undefined under reduceMotion), so assertions would pass if added. RULED orch-017 = option A (test-only). `wi1120-diag` re-tasked as BUILDER: claim→worktree WI-1120→add assertions (rest scale/opacity + exiting undefined under reduceMotion, must run+pass)→PR→complete→I Gate-1 merge. Impl+AC untouched, no rebuild. B rejected (soften-to-pass = drift). WI-1246 push-guard now enforced in main.
> - **WI-1102 citation:** orch's AC-refinement precedent cite is WRONG (1102 = unrelated billing item; 587 also unrelated). No real precedent; used WI-1102's close-note *mechanism* (REST AC edit + rationale comment). Flagged v2-finalization-21.
> - **WI-1124** P3 GC6 test-hygiene → QUEUED (orch: low-pri, slot after P1/P2).
> - **WI-904** P2 Refining deferral (QA ran+failed 30 Jun; approach changed; Owner empty) — non-blocking, external re-triage.
> No live claims on any active item (verified 06:00Z). Sign-of-life v2-finalization-15; acks orch-011=16, orch-012 handled; escalation v2-finalization-17.

Repo: /Users/vetinari/nexus/_dev/eduagent-build. Shepherd operates on `main`; builders in `.worktrees/WI-NN`.
**Notion MCP tools disconnected — use REST (`curl` + `$NOTION_TOKEN`, `Notion-Version: 2022-06-28`) for all Cosmo reads/writes.**

## WS-28 = 17 WIs (orch-012). CLOSED (12): 1122,1130,1131,1133,1168,1169,1170,1171,1172,1173,1174,**1118** (Closed 06:4xZ after round-trip re-finalize triggered the reviewer). OPEN: 1120 (building test), 1207 (building), 1175 (finalizing), 1124 (P3 queued), 904 (deferral).

## NOTE: stage-poll IDS[] positions are NOT WI-ordered — the differ keys by unique_id from the API payload, so it's correct, but do NOT hardcode a page-id→WI mapping by position. WI-1172 page = 38f8bce91f7c81d0b40cdd49831e0d52; WI-1175 page = 38f8bce91f7c81b8b139e93377fc8990.

## In flight / remaining (3):
- **WI-1171** MERGED→Reviewing, awaiting Gate-2 close. PR **#1788** merged 606d80fba, Fixed In **eeccd6c3b** on main. 3 inline findings (Codex P1 appeal-state-reset + 2 CodeRabbit minors) all fixed+verified. Codex reviewer to close → then WI-1175 unblocks (last blocker).
- **WI-1172** (progress preservation, docs + 1 test). PR **#1778** OPEN (head **6556d8895**, was 3e96b98b1, branch WI-1172). CI CLEAN+green. **BOUNCED 16:2x** to `wi1172-pr` for 1 CodeRabbit minor (canonical status label, `06-screen-function-access-map.md:145`). Fix → re-green → `complete` → **I merge `--merge`.** Still Stage=Executing.
- **WI-1175** (publish review, researcher, runs LAST). Ready; `blockedBy` = WI-1171 + WI-1172 (1173 now closed). On unblock: dispatch a **researcher**; it must carry **WI-904 as a logged deferral** in its publish recommendation. ExecPath=Manual → the ship/hold recommendation may be a **needs-operator** call.
- **WI-904** (dictation pace). **PARKED indefinitely** (State=Awaiting Info) — perceptual on-device QA needed, QA person unavailable (operator ruling orch-002). Standalone (no blockedBy edges — verified). Rework PR #1749 is code-green but held UNMERGED. **Do NOT nudge; do NOT merge #1749 without flagging operator.** WI-1175's DoD accepts it as a documented deferral, so the lane can reach "publish recommendation" with 904 in limbo.

## Monitors (RECONCILE after any restart — monitor-hygiene; they are session-scoped and die silently):
- Inbox watcher: task **bz35kr6q3** — `tail -F .../v2-finalization/_state/inbox.jsonl`
- Cosmo Stage poll: task **b7chwm1x0** — `bash <scratchpad>/ws28-stage-poll.sh` (12 WIs, awk differ, 90s). Poll script path: `/private/tmp/claude-506/-Users-vetinari-nexus--dev-eduagent-build/2ff2c2c0-.../scratchpad/ws28-stage-poll.sh` (regenerate from manifest if scratchpad gone; IDS[] = the 12 WS-28 page ids).
- Manifest: `_state/monitor-manifest.json`. On restart: re-arm both, direct-read all 12 stages to catch verdicts missed while down.

## Merge policy (lane): builders run `complete` BEFORE I merge (Fixed In = branch head), so **merge with `gh pr merge <n> --merge` (merge-commit, NOT squash)** to keep the Fixed-In commit on main and avoid the WI-1174 landedness bounce. Repo allows merge/squash/rebase. After merge, verify `git merge-base --is-ancestor <FixedIn> origin/main`.

## Gates: Gate-1 = I self-merge each strict-green PR (all required checks SUCCESS; claude-review green OR legit paths-ignore skip for docs-only; no valid Codex/CodeRabbit/claude must/should-fix — **check INLINE comments `pulls/<n>/comments`, not just review body**; mergeStateStatus=CLEAN). Gate-2 = separate Codex reviewer session closes at Stage=Reviewing (I don't run it; my Stage monitor catches verdicts).

## Active ruling — WI-1246 (tooling hazard, WS-23, P1): `/commit` auto-push fork can push PR-lessly to shared `main` (fired twice). INTERIM until fixed: builders must NOT use `/commit` auto-push — commit in worktree, assert `branch != main`, explicit `git -C <worktree> push origin HEAD:<branch>`; on ANY main-landing the builder STOPS + escalates, and **I (shepherd) own the surgical shared-tree cleanup** (reset stray commit; restore only the builder's own files; never touch other sessions' dirty files). Both active builders already guarded.

## Clacks: outbox (I write) `_state/outbox.jsonl` — last = **v2-finalization-11** (ack orch-009). inbox (read-only) `_state/inbox.jsonl` — last = **v2fin-orch-009**. All orchestrator directives acked.

## Next actions:
1. Reconcile monitors; direct-read 12 stages.
2. On #1788 / #1778 strict-green → merge-commit each; verify Fixed In on main.
3. On WI-1171 & WI-1172 → Closed → WI-1175 unblocks → dispatch researcher (carry WI-904 deferral; ship/hold may be needs-operator).
4. Lane closes when all 12 Closed (WI-904 remains a documented deferral, not a blocker).

## M4 DONE — 2026-07-10 (recorded by claude:orion, operator-supervised)
WI-1307 R1 device proof PASSED against the PUBLISHED fallback bundle (channel-repoint rehearsal on the release preview APK, runtimeVersion 1.0.1; solo/study + family + guardian-study shells all asserted by testID; forbidden tabs absent). Evidence appended to WI-1307 AC + rationale comment on the page. WI-1307 finalized via /cosmo:execute complete → Stage=Reviewing, Fixed In = https://github.com/cognoco/eduagent-build/commit/20364364cc8e168f9d801287490dd65c499304f4 (ancestor of origin/main, the bundle-source commit). Preview channel reverted to preview branch and verified. Zero new builds/publishes consumed. Escalation v2-finalization-68 (V0=true drift) resolved: V0 reads are V1-short-circuited, bundle accepted as-is. M5 (WI-1308, human-confirm, S6-irreversible) and M6 now unblocked; rider R3 carries (runtimeVersion change at M6 forces re-publish + re-prove before M5). Gate-2 review of WI-1307 belongs to the separate reviewer session, not the executor.
