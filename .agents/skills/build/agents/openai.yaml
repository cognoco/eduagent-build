interface:
  display_name: "Build"
  short_description: "Safe EAS mobile build trigger"
  default_prompt: "Use $build to trigger or inspect a safe EAS mobile build."
policy:
  allow_implicit_invocation: true
