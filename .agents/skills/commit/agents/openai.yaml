interface:
  display_name: "Commit"
  short_description: "Safe EduAgent git commit workflow"
  default_prompt: "Use $commit to commit the current changes safely."
policy:
  allow_implicit_invocation: true
