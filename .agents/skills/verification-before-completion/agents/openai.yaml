interface:
  display_name: "Verification Before Completion"
  short_description: "Run the command before claiming work is done; evidence over assertions"
  default_prompt: "Use $verification-before-completion before claiming any work is complete, fixed, or passing. Run the verification command in this turn before making the claim."
policy:
  allow_implicit_invocation: true
