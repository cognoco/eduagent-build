interface:
  display_name: "Project Memory"
  short_description: "Read and maintain EduAgent memory"
  default_prompt: "Use $project-memory to load and maintain EduAgent project memory."
policy:
  allow_implicit_invocation: true
