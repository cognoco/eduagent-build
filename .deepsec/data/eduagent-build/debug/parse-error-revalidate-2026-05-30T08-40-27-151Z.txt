# deepsec parse-failure debug dump
# phase: revalidate
# agentType: claude-agent-sdk
# timestamp: 2026-05-30T08:40:27.151Z
# error: Error: Agent produced revalidation output that wasn't parseable JSON: Unterminated string in JSON at position 9475 (line 37 column 254). First 400 chars: I have completed a thorough trace of all six findings. Let me compile my verdicts. **Verification summary:** - **Mobile archive (acl-check):** PATCH `/subjects/:id` calls `assertNotProxyMode(c)` at `subjects.ts:171` (commit `385d5734c`, 2026-05-24, WI-76 batch 2), which fails closed (no `profileMeta`→403; `isOwner===false`→403; `X-Proxy-Mode` header→403). Client also disables the button via `canW
# batch (5 files):
  - apps/mobile/src/app/(app)/progress/[subjectId]/index.tsx
  - apps/api/src/services/llm/router.ts
  - apps/api/src/services/llm/project-response.ts
  - apps/api/src/services/llm/stream-envelope.ts
  - apps/api/src/services/llm/extract-json.ts
# --- raw agent output begins on next line ---
I have completed a thorough trace of all six findings. Let me compile my verdicts.

**Verification summary:**
- **Mobile archive (acl-check):** PATCH `/subjects/:id` calls `assertNotProxyMode(c)` at `subjects.ts:171` (commit `385d5734c`, 2026-05-24, WI-76 batch 2), which fails closed (no `profileMeta`→403; `isOwner===false`→403; `X-Proxy-Mode` header→403). Client also disables the button via `canWrite` (WI-371). Both recommendation items already implemented.
- **Safety-filter bypass:** Commit `dff1e27bb` (2026-05-29) added `isSafetyPolicyError`, made `withRetry` throw immediately on non-transient, and gated fallback on `transient`. Every recommendation item is present.
- **Circuit-breaker liveness:** `canAttempt` sets `probeInFlight=true` synchronously (721/728); `routeAndStream`(1299)/`attemptStreamProvider`(1411) return lazy streams; all `probeInFlight` resets are on iteration paths → un-iterated stream leaks the probe. Real, narrow, self-healing.
- **Envelope leak (project-response):** empty/non-string/missing reply → `return rawContent` (line 98) with `private_sources`/`signals` intact; tests cases 7/8 encode it; consumers include GDPR export (`export.ts:456`) + downstream prompts; schema documents `private_sources` as "never rendered to the learner."
- **Stream mismatch:** `streamEnvelopeReply` first-match `/"reply"\s*:/` (no depth); top-level schema non-strict (no `.strict()`, strips unknown keys); persisted `cleanResponse = envelope.reply` (top-level) via `classifyExchangeOutcome`→`parseEnvelope`. No streamed-vs-parsed reconciliation → divergence real.
- **Code-fence:** Commit `189238295` (2026-05-25) anchored the regex to `^…$`; current line 20 only strips whole-response fences.

```json
[
  {
    "filePath": "apps/mobile/src/app/(app)/progress/[subjectId]/index.tsx",
    "title": "Parent proxy mode can archive a child subject",
    "verdict": "fixed",
    "reasoning": "The finding's core security claim is that the API path PATCH /subjects/:id 'does not call assertNotProxyMode, while other write routes do.' This is false against current code: apps/api/src/routes/subjects.ts:171 calls assertNotProxyMode(c) as the first statement of the PATCH handler (comment [WI-177 / DS-088]; introduced/expanded by commit 385d5734c 'close proxy-mode write-guard gaps' 2026-05-24). assertNotProxyMode (middleware/proxy-guard.ts) fails closed on a server-derived signal: throws 403 when profileMeta is absent, when profileMeta.isOwner===false (a parent acting on a child profile, exactly the proxy case), and when the X-Proxy-Mode header is 'true'. So a parent in read-only proxy mode resolving the child via X-Profile-Id is rejected at the server before updateSubject runs. The client side is also hardened: canWrite = !navigationContract.isParentProxy (line 55), the hide button is disabled={updateSubject.isPending || !canWrite} (line 629), and confirmHideSubject early-returns when !canWrite (line 145) — the WI-371 mobile read-only guards. Both halves of the finding's own recommendation are already implemented, so the described archive-by-proxy exploit is blocked at the trust boundary. Verdict: fixed."
  },
  {
    "filePath": "apps/api/src/services/llm/router.ts",
    "title": "Provider safety blocks are retried and failed over as transient outages",
    "verdict": "fixed",
    "reasoning": "The finding claims 'the retry helper retries every thrown error without first checking whether it is transient' and that SafetyFilterError (no HTTP status) is therefore retried, circuit-tripped, and failed over to another provider. Current code refutes every step. withRetry (line 807) does `if (!isTransientError(err)) throw err;` before any retry. isTransientError (line 611) short-circuits `if (isSafetyPolicyError(err)) return false;` at the very top, and isSafetyPolicyError (654-679) matches `instanceof SafetyFilterError`, recurses into err.cause, and matches name/errorCode/code/type against safety markers (SafetyFilterError, ContentFilterError, content_policy_violation, etc.). routeAndCall computes `transient` and does `if (!transient) throw err;` (line 931) BEFORE selecting any fallback; attemptProvider re-throws non-transient (1054); and wrapStreamWithCircuitBreaker gates pre-first-byte fallback on `if (transient && chunksYielded === 0 && fallbackConfig)` (1179). isTransientError also classifies non-429 4xx as terminal (617) and validation-marker errors as terminal (621). git show dff1e27bb ('fix(api): stop retrying LLM safety blocks', WI-224, 2026-05-29, 13h ago) is the diff that introduced isSafetyPolicyError and all the non-transient throw/branch guards — the finding describes the exact pre-commit behavior, and its line numbers (397/408/461/473/555/566) point at getModelConfig/circuit-field code in the reorganized file, not the retry logic. Every recommendation item is implemented. Verdict: fixed."
  },
  {
    "filePath": "apps/api/src/services/llm/router.ts",
    "title": "HALF_OPEN probeInFlight can leak on the lazy streaming path and wedge a provider circuit",
    "verdict": "true-positive",
    "adjustedSeverity": "BUG",
    "reasoning": "Verified end-to-end. canAttempt() mutates state synchronously: on OPEN→HALF_OPEN it sets cb.probeInFlight=true (721) and returns true; in HALF_OPEN it returns false if probeInFlight (727) else sets probeInFlight=true and returns true (728). Every paired reset of probeInFlight lives on an iteration path: recordSuccess (1156, after the for-await drains), recordFailure (1115 zero-chunk fallback, 1160 catch), and the explicit `getCircuit(circuitKey).probeInFlight = false` in catch blocks — all inside wrapStreamWithCircuitBreaker's async generator body. routeAndStream (1299) and attemptStreamProvider (1411) call canAttempt() and then build a LAZY stream (chatStream→wrapStreamWithCircuitBreaker→teeEnvelopeStream→streamEnvelopeReply are all lazy generators) and return it without awaiting iteration; the finally block (1233) only resolves onStopReason, it never resets probeInFlight. I confirmed the consumer chain (streamMessage at session-exchange.ts:3083 creates the stream during the call, returns {stream,onComplete}, and the SSE route iterates later) leaves a real window: if the returned stream is never iterated (setup exception, client abort before first read, or abandonment before recordSuccess/Failure runs), probeInFlight stays true and every subsequent HALF_OPEN canAttempt returns false → CircuitOpenError until the isolate recycles. The non-streaming routeAndCall path is immune because it awaits inline. Reachability is narrow (request must land in the 60s HALF_OPEN window AND the stream go un-iterated) and the state is per-isolate/ephemeral, so BUG (not HIGH) is the correct severity, exactly as the finding scoped it. Real bug, correctly characterized."
  },
  {
    "filePath": "apps/api/src/services/llm/project-response.ts",
    "title": "Read projector leaks raw LLM envelope (private_sources / signals) when reply is empty or non-string",
    "verdict": "true-positive",
    "reasoning": "Confirmed against code, tests, schema, and consumers. projectAiResponseContent's Step-4 fallback (78-98) only returns the projected reply when typeof parsed.reply==='string' AND reply.length>0; otherwise it falls through to `return rawContent` (98), and also `return rawContent` when extractFirstJsonObject yields null (80). rawContent is the ORIGINAL, un-stripped envelope JSON. So an envelope object with an empty (''), missing, or non-string reply that also carries private_sources/signals is returned verbatim. The unit tests encode this as intended behavior: case 7 ('{\"reply\": \"\"}' → returns input, 196-208) and case 8 ('{\"reply\": 42}'/object → returns input, 212-233). The envelope schema (packages/schemas/src/llm-envelope.ts:474-480) documents private_sources as 'never rendered to the learner' (provenance for complaint review / hallucination audits), and reply is z.string().min(1) so any empty/non-string reply is by definition a schema-invalid row — exactly the pre-[BUG-934] malformed rows this projector exists to sanitize on read. I confirmed the consumers surface the result to users and downstream models: export.ts:456 pipes it into the GDPR data export, and bookmarks.ts:80, embeddings.ts:208, learner-profile.ts:1700, session/session-context-builders.ts:526 feed it into UI/prompt context. The projector therefore fails OPEN on its target rows, leaking documented-private side-channel state. The 'never drop characters the user saw render' rationale does not apply to an empty-reply envelope (nothing was rendered), so returning the raw side-channel is strictly worse than returning empty — the finding's critique is correct. Impact is bounded (legacy malformed rows; internal metadata not credentials/PII; disclosure largely to the data owner), consistent with MEDIUM. Real information-disclosure / defense-in-depth gap."
  },
  {
    "filePath": "apps/api/src/services/llm/stream-envelope.ts",
    "title": "Streaming extractor can show a different reply than the one parsed and persisted",
    "verdict": "true-positive",
    "reasoning": "Verified the divergence is real. streamEnvelopeReply scans for the FIRST regex hit of REPLY_KEY_RE=/\"reply\"\\s*:/ (22, 343) with no JSON-depth/string-context awareness, then emits that value and transitions to after_reply, discarding the remainder (433-436). The persisted text takes an independent path: streamMessage.onComplete (session-exchange.ts:3142) calls classifyExchangeOutcome(rawResponse), which calls parseEnvelope (exchanges.ts:1816) → JSON.parse + llmResponseEnvelopeSchema, and envelopeToParsedExchange sets cleanResponse = cleanLearnerVisibleReply(envelope.reply) (1669/1690) — the TOP-LEVEL reply. The top-level schema is z.object({...}) with NO .strict() (llm-envelope.ts:428-488), so unknown keys are stripped, not rejected. Therefore a crafted/drifted envelope like {\"meta\":{\"reply\":\"A\"},\"reply\":\"B\"} streams 'A' (first regex match inside meta) but parses/persists 'B' (top-level after meta is stripped). I confirmed there is no reconciliation: the only replace-frame mechanism (sourceReplacement, 3212-3215) fires solely when the source-audit safety fallback rewrites text, not on stream-vs-parse mismatch. The result is exactly the finding's claim — the learner can see live text that never matches the persisted transcript, GDPR export, or parent-facing assistant message. A concrete attack is describable (prompt-injection coaxing a leading object with a nested reply key); reliability is model-dependent, which is why MEDIUM/medium-confidence is appropriate, but the code defect and audit-integrity impact are real. The recommendation (depth-aware extraction, strict schema, or compare streamed vs parsed before completion) is sound."
  },
  {
    "filePath": "apps/api/src/services/llm/extract-json.ts",
    "title": "Code-fence stripping matches fences inside valid JSON replies",
    "verdict": "fixed",
    "reasoning": "The finding states the code-fence regex is 'unanchored and runs before brace walking,' so a fence inside a valid envelope's reply text gets selected as the body. Current code refutes this: line 20 uses the anchored full-string regex /^```(?:json)?\\s*([\\s\\S]*?)```$/ against trimmed text, so it only unwraps when the ENTIRE response is a single fence — precisely the fix the finding recommends. git show 189238295 ('fix: Code-fence stripping matches fences inside valid JSON replies', 2026-05-25) is the diff that changed the regex from the old unanchored /```(?:json)?\\s*([\\s\\S]*?)```/ to the anchored form; the finding's title is verbatim that commit's subject. Furthermore, extractFirstBalancedObject walks brace depth while respecting string literals and escapes (47-69), and extractFirstJsonObject returns the first balanced object immediately if it is valid JSON (26-28) — so for a valid envelope whose reply string contains a ```json fence, the walker captures the whole outer envelope (the fence is inside a string) and isJsonObject confirms it, returning the envelope rather than the inner fenced body. extractFencedJsonObjectAfter (unanchored, global) is only consulted when the first balanced object is NOT valid JSON (30-34), which does not apply to a valid envelope. The reported failure mode cannot occur in the current code. Verdict: fixed."
  }
]
```