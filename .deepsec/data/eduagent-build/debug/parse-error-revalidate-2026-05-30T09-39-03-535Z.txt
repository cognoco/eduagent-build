# deepsec parse-failure debug dump
# phase: revalidate
# agentType: claude-agent-sdk
# timestamp: 2026-05-30T09:39:03.535Z
# error: Error: Agent produced revalidation output that wasn't parseable JSON: Unterminated string in JSON at position 7128 (line 31 column 212). First 400 chars: I now have everything needed. All investigations are complete and confirmed against the current working tree (HEAD `f4a6294f3` on `main`, which includes all four relevant fix commits). Here are my verdicts. ## Summary of investigation | # | File | Finding | Verdict | |---|------|---------|---------| | 1 | router.ts | Safety blocks retried/failed-over | **fixed** (dff1e27bb + 7df790bf9) | | 2 | r
# batch (5 files):
  - apps/api/src/services/llm/router.ts
  - apps/api/src/services/llm/project-response.ts
  - apps/api/src/services/llm/stream-envelope.ts
  - apps/api/src/services/llm/extract-json.ts
  - apps/api/src/services/llm/providers/gemini.ts
# --- raw agent output begins on next line ---
I now have everything needed. All investigations are complete and confirmed against the current working tree (HEAD `f4a6294f3` on `main`, which includes all four relevant fix commits). Here are my verdicts.

## Summary of investigation

| # | File | Finding | Verdict |
|---|------|---------|---------|
| 1 | router.ts | Safety blocks retried/failed-over | **fixed** (dff1e27bb + 7df790bf9) |
| 2 | router.ts | HALF_OPEN probeInFlight leak | **true-positive** (BUG) |
| 3 | project-response.ts | Raw envelope leak on empty reply | **true-positive** |
| 4 | stream-envelope.ts | Streamed ≠ persisted reply | **true-positive** |
| 5 | extract-json.ts | Code-fence stripping | **fixed** (189238295) |
| 6 | gemini.ts | API key in URL | **fixed** (da98bd477) |
| 7 | gemini.ts | Only 'SAFETY' block reason honored | **true-positive** |

```json
[
  {
    "filePath": "apps/api/src/services/llm/router.ts",
    "title": "Provider safety blocks are retried and failed over as transient outages",
    "verdict": "fixed",
    "reasoning": "The current code implements exactly the finding's recommendation. `isTransientError` (line 610) now begins with `if (isSafetyPolicyError(err)) return false;`, classifies non-408/429 4xx as non-transient (`if (status >= 400 && status < 500) return false;`), and returns false for `isValidationPolicyError`. `withRetry` (line 807) throws immediately on non-transient errors instead of retrying, and `routeAndCall`'s catch (line 931) does `if (!transient) throw err;` before reaching `getFallbackConfig`, so a SafetyFilterError is never failed over to another provider. `isSafetyPolicyError` (line 654) matches `SafetyFilterError` instances, recurses into `.cause`, and matches name/code/type markers including `content_filter`/`content_policy_violation`. Git confirms commit dff1e27bb ('stop retrying LLM safety blocks', WI-224) and 7df790bf9 ('classify provider safety and 4xx terminally') are both in HEAD ancestry, with router.test.ts coverage added. The finding's cited line numbers (397, 408, 461, 473…) no longer correspond to the retry/classification logic — they reference a pre-fix file layout. The vulnerable behavior described is gone."
  },
  {
    "filePath": "apps/api/src/services/llm/router.ts",
    "title": "HALF_OPEN probeInFlight can leak on the lazy streaming path and wedge a provider circuit",
    "verdict": "true-positive",
    "adjustedSeverity": "BUG",
    "reasoning": "The mechanism is real and confirmed by static analysis. `canAttempt()` has a synchronous side effect: line 721 sets `probeInFlight=true` on the OPEN→HALF_OPEN transition, and line 728 sets it before returning true in HALF_OPEN. The only resets — `recordSuccess` (596) and `recordFailure` (601) — execute inside `wrapStreamWithCircuitBreaker`, whose body (an async generator) runs ONLY upon iteration. `routeAndStream` (line 1299) and `attemptStreamProvider` (line 1411) both call `canAttempt()` and then return a lazy `StreamResult` without iterating it. If the returned stream is never iterated (SSE setup throws after routeAndStream resolves, client abort during setup, or an exception before the for-await loop in the consumer — e.g. teeEnvelopeStream is lazy and session-exchange does post-resolve work at session-exchange.ts:3083+), `probeInFlight` stays true. While stuck in HALF_OPEN with probeInFlight=true, `canAttempt` returns false at line 727 for every subsequent request, forcing fallback (or `CircuitOpenError` when no fallback exists, e.g. the gemini_only Family policy). No timeout or finalizer resets it; only isolate recycle clears it. The non-streaming `routeAndCall` path is unaffected because it awaits inline and always reaches recordSuccess/recordFailure. Impact is genuinely minor and self-healing (per-isolate ephemeral state, narrow HALF_OPEN window), so BUG severity is correct — but the defect itself is real, not a false positive."
  },
  {
    "filePath": "apps/api/src/services/llm/project-response.ts",
    "title": "Read projector leaks raw LLM envelope (private_sources / signals) when reply is empty or non-string",
    "verdict": "true-positive",
    "reasoning": "Confirmed by code and its own tests. In `projectAiResponseContent`, when content is structurally-valid envelope JSON whose `reply` is empty/non-string, Step 3's strict `parseEnvelope` fails (the schema requires `reply` min(1) — llm-envelope.ts:441), then Step 4 extracts and JSON.parses but the guard `reply.length > 0` (line 92) is false, so execution reaches line 98 `return rawContent` — the full raw envelope JSON. project-response.test.ts Cases 7/8 (lines 196-233) assert exactly this (`{\"reply\":\"\"}` and `{\"reply\":42}` return rawContent), and the schema documents `private_sources` as 'never rendered to the learner' (llm-envelope.ts:474-480). The consumer chain is real: export.ts:448-462 pipes `ai_response` content through this projector into the user-facing GDPR data export, and bookmarks.ts, session-context-builders.ts, learner-profile.ts, embeddings.ts, session-recap.ts, homework-summary.ts feed it into UI/downstream-LLM context. So for a legacy malformed row (empty/non-string reply with a populated `private_sources`/`signals` block) the internal side-channel — relied_on source IDs, factual_confidence, state-machine signals — is disclosed verbatim in a regulatory export and re-injected into prompts. The projector's stated purpose is precisely to prevent this, so it is a genuine defense-in-depth bypass. Severity is bounded as the finding states (internal metadata, not credentials/cross-user PII, and it requires a pre-[BUG-934] malformed row to exist), placing it at the low edge of MEDIUM, but the behavior and disclosure path are real."
  },
  {
    "filePath": "apps/api/src/services/llm/stream-envelope.ts",
    "title": "Streaming extractor can show a different reply than the one parsed and persisted",
    "verdict": "true-positive",
    "reasoning": "Mechanism confirmed end-to-end. `streamEnvelopeReply` locates the reply via `REPLY_KEY_RE = /\"reply\"\\s*:/` (line 22) and emits the FIRST match anywhere in the raw stream — for input like `{\"foo\":{\"reply\":\"x\"},\"reply\":\"y\"}` it emits the nested `x`, then transitions to `after_reply` and discards the rest. The completion path is independent: session-exchange.ts:3097 awaits `rawResponsePromise` (full raw text), classifyExchangeOutcome→parseExchangeEnvelope→parseEnvelope→`envelopeToParsedExchange` sets `cleanResponse = cleanLearnerVisibleReply(envelope.reply)` from the TOP-LEVEL Zod-validated reply (exchanges.ts:1669,1690). Crucially, `llmResponseEnvelopeSchema` is `z.object({...})` with NO `.strict()` (llm-envelope.ts:428), so Zod silently STRIPS the unknown `foo` key and validates successfully with `reply=\"y\"` — meaning the persisted/transcript/parent-dashboard/export message is `y` while the learner was streamed `x`. There is no comparison between the streamed reply and the parsed reply anywhere in the onComplete flow. This is an audit/integrity desync: a child could induce (via prompt injection, which the threat model treats as in-scope) a live display that does not match the persisted record the parent reviews. Exploitability is conditional — it requires the model to emit a specific adversarial JSON shape with a nested reply key positioned before the top-level reply (no organic envelope field is named `reply`), so it is not trivially triggered, but the mechanism is real and the MEDIUM/medium-confidence rating is appropriate. The recommendation (depth-aware extraction, strict schema, or stream-vs-parsed comparison) is sound."
  },
  {
    "filePath": "apps/api/src/services/llm/extract-json.ts",
    "title": "Code-fence stripping matches fences inside valid JSON replies",
    "verdict": "fixed",
    "reasoning": "The finding describes an unanchored fence regex run before brace-walking. The current `extractFirstJsonObject` (line 20) uses an anchored full-string match: `const fenceMatch = trimmed.match(/^```(?:json)?\\s*([\\s\\S]*?)```$/);` — it only unwraps a fence when the ENTIRE trimmed response is a single fence, exactly the finding's recommended fix. Git confirms commit 189238295 ('fix: Code-fence stripping matches fences inside valid JSON replies', WI-222 — title identical to this finding) is in HEAD ancestry and changed the regex from the unanchored `/```(?:json)?\\s*([\\s\\S]*?)```/` to the anchored `^…$` form, adding extract-json.test.ts coverage. A code fence appearing inside a valid envelope's reply text no longer hijacks extraction. The downstream fence helper `extractFencedJsonObjectAfter` (line 78) is a deliberate recovery path that runs only when the first balanced object is NOT valid JSON, so it does not reintroduce the bug for valid replies."
  },
  {
    "filePath": "apps/api/src/services/llm/providers/gemini.ts",
    "title": "Gemini API key leaked in URL query parameter captured by Sentry breadcrumbs",
    "verdict": "fixed",
    "reasoning": "The current code passes the key via the `x-goog-api-key` request header, not the URL. The non-streaming `chat` (lines 217-225) sets `headers: { 'Content-Type': 'application/json', 'x-goog-api-key': apiKey }` against `url = ${GEMINI_BASE_URL}/${config.model}:generateContent` (no `?key=`), and `chatStream` (lines 257-265) does the same against `:streamGenerateContent?alt=sse`. This is precisely the finding's recommended remediation. `git log -S '?key=${apiKey}'` shows commit da98bd477 ('fix(ci): harden WI-83 supply-chain gates', in HEAD ancestry) removed `?key=${apiKey}` from both URLs and added the header. With the key no longer in the request URL, it cannot appear in Sentry fetch breadcrumbs, CF analytics, or wrangler tail subrequest logs. The finding's cited lines 202/237 now point at the provider-factory comment block and `extractResponseText(data)` respectively — stale references to the pre-fix layout."
  },
  {
    "filePath": "apps/api/src/services/llm/providers/gemini.ts",
    "title": "Only 'SAFETY' block reason is treated as a safety filter; other Gemini block reasons trigger cross-provider failover",
    "verdict": "true-positive",
    "reasoning": "Confirmed unfixed in current code. `extractResponseText` raises `SafetyFilterError` only for `promptFeedback.blockReason === 'SAFETY'` (line 175) and `candidates[0].finishReason === 'SAFETY'` (line 182); the streaming parser likewise (lines 296, 301). A grep confirms there is NO handling of PROHIBITED_CONTENT, BLOCKLIST, SPII, or RECITATION. For those terminal block reasons Gemini returns HTTP 200 with no text and no `data.error`, so the non-streaming path reaches `throw new Error('Gemini returned empty response')` (line 197) — a bare Error with no status, no safety marker, no validation marker. In the router, `isTransientError` therefore returns true (no SafetyFilterError, no HTTP status, not a validation error), so `withRetry` retries up to 4x and `routeAndCall` then invokes `getFallbackConfig` to send the identical prompt to OpenAI/Anthropic. The streaming path yields zero chunks for these blocks, and `wrapStreamWithCircuitBreaker` (router.ts:1108) treats `chunksYielded === 0 && fallbackConfig` as a pre-first-byte failure and fails over. Importantly, commit 7df790bf9 (which the safety-block fix work touched) modified gemini.ts ONLY in the HTTP-error path (createProviderHttpError for non-OK responses) — it did not touch the block-reason taxonomy — and the OpenAI content_filter fix in that commit was in openai.ts, so finding #1's fix does NOT cover this gap (gemini never throws SafetyFilterError for non-'SAFETY' blocks). The retry/paid-fallback waste prong is high-confidence and certain; the safety-bypass prong is real but partially mitigated (OpenAI/Anthropic moderation, plus the router's SAFETY_RULES preamble applies to all providers), and Gemini's minor-specific thresholds (SAFETY_SETTINGS_FOR_MINORS) are not replicated on fallback providers — a genuine if partial weakening for a minors-only app. MEDIUM with low confidence on the safety angle is a fair rating; the recommendation to map all terminal block reasons to SafetyFilterError is correct."
  }
]
```