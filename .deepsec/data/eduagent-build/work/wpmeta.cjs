// Work Package family metadata
module.exports = {
'WP-ACL': {name:'Access-control checks missing or bypassable on protected routes — DeepSec 2026-05-16',
  rationale:'These findings share one failure pattern: protected operations that do not verify the caller is authorised for the target resource (missing requireProfileId/withProfile/requireAccount, absent ownership checks, or bypassable guards).',
  strategy:'Audit each flagged route/handler for a server-verified profile/account check; standardise on the canonical guards (requireProfileId(), withProfile(), requireAccount(), assertNotProxyMode()); add ACL coverage at the route-group level where per-route checks are inconsistent.'},
'WP-COST': {name:'Unbounded or abusable API-cost and rate-limit gaps — DeepSec 2026-05-16',
  rationale:'Endpoints and LLM call paths that can be driven to excessive cost or volume — missing or bypassable rate limits, unbounded LLM invocations, and quota-accounting gaps.',
  strategy:'Add or tighten rate limiting / quota enforcement on the flagged paths; ensure LLM metering is decremented atomically before the call; cap loops and batch sizes server-side.'},
'WP-RACE': {name:'Race conditions and non-atomic state updates — DeepSec 2026-05-16',
  rationale:'Concurrent or interleaved operations that read-modify-write shared state without atomicity, producing lost updates, corrupted state, or duplicate effects.',
  strategy:'Make the flagged updates atomic — DB transactions, conditional/optimistic writes, or idempotency keys; eliminate read-then-write windows; serialise where ordering matters.'},
'WP-LLM': {name:'LLM prompt-injection and output-integrity gaps — DeepSec 2026-05-16',
  rationale:'User-controlled text reaches LLM prompts as instructions, or LLM output drives state without structured validation — prompt injection, system-prompt leakage, and fragile free-text parsing.',
  strategy:'Treat all user/external text as data inside clearly delimited blocks; route state-driving LLM responses through llmResponseEnvelopeSchema + parseEnvelope() with server hard caps; never parse [MARKER] tokens from free text.'},
'WP-XTEN': {name:'Cross-tenant / cross-profile data-isolation gaps — DeepSec 2026-05-16',
  rationale:'Operations where a caller can read or write another profile/family’s data by supplying or racing an identifier (X-Profile-Id swaps, unscoped queries).',
  strategy:'Enforce profileId scoping on every flagged read/write via createScopedRepository(profileId) or the sanctioned parent-chain join; verify ownership before mutating child records.'},
'WP-LOGIC': {name:'Logic bugs producing incorrect behaviour — DeepSec 2026-05-16',
  rationale:'Defects in business/control logic that yield wrong results, false success, silent fallbacks, or incorrect cleanup — not a single shared mechanism but a shared category.',
  strategy:'Triage each finding individually; fix the specific logic defect and add a regression test that fails without the fix. Likely splits into smaller bundles during refinement.'},
'WP-CONSENT': {name:'Consent, privacy and age-gate enforcement gaps — DeepSec 2026-05-16',
  rationale:'Regulatory-critical paths (consent capture/withdrawal, privacy preferences, COPPA age gates) that can be bypassed, raced, or left in an inconsistent state.',
  strategy:'Make consent/privacy state transitions atomic and observable; enforce age/consent gates server-side before the protected action; ensure withdrawal and deletion are durable and retryable.'},
'WP-CICD': {name:'CI/CD supply-chain and secret-exposure risks — DeepSec 2026-05-16',
  rationale:'GitHub workflows and actions that run mutable third-party tags, expose secrets to PR-controlled code, or log secrets.',
  strategy:'Pin all external actions to immutable commit SHAs; never pass secrets to local actions loaded from PR-controlled checkouts; apply least-privilege job permissions; scrub secrets from logs.'},
'WP-DATA': {name:'Data-loss and durability gaps in async/background work — DeepSec 2026-05-16',
  rationale:'Background work and deploy/migration paths where events can be dropped, jobs lost, or data destroyed without recovery.',
  strategy:'Route durable side effects through Inngest; ensure non-core dispatches use safeSend(); fix DB pool lifecycle and migration ordering; add observability for silent loss.'},
'WP-WEBHOOK': {name:'Webhook auth and billing-integrity gaps — DeepSec 2026-05-16',
  rationale:'Webhook handlers and billing flows vulnerable to misconfigured auth, replayed/duplicated events, or bypassed billing checks.',
  strategy:'Verify webhook signatures and idempotency on every handler; reject sandbox/test-mode events in production; make billing state transitions idempotent and observable.'},
'WP-STALE': {name:'Stale-state and cache-coherence bugs — DeepSec 2026-05-16',
  rationale:'Code that acts on stale cached data, async results, or navigation state after the underlying state has changed.',
  strategy:'Invalidate or revalidate cached/async state at the flagged sites; guard against acting on results from a superseded context.'},
'WP-INPUT': {name:'Input-validation and injection gaps — DeepSec 2026-05-16',
  rationale:'Endpoints that accept insufficiently validated input — path traversal, CSV injection, parameter pollution, non-finite numeric corruption, weak crypto.',
  strategy:'Validate and normalise input at the boundary with the shared Zod contracts; escape/encode on output; replace insecure crypto primitives.'},
'WP-DISCLOSE': {name:'Information-disclosure gaps — DeepSec 2026-05-16',
  rationale:'Paths that leak internal state or sensitive information to clients or logs.',
  strategy:'Strip internal/sensitive fields from responses and logs at the flagged sites; return only client-appropriate data.'},
'WP-SCORE': {name:'Score and progress-integrity gaps — DeepSec 2026-05-16',
  rationale:'Learning score/progress values that can be inflated or tampered with by the client.',
  strategy:'Compute and persist scores server-side from server-verified inputs; reject client-supplied score/progress values.'},
};
