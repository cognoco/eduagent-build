**What was done:** Checked WI-651 (CF identifier secrets were never propagated after BUG-721 placeholdering) after Echo could not start it. The issue is still real for Doppler canonical storage: GitHub Actions has the seven Cloudflare render secret names, but Doppler dev/stg/prd did not expose valid 32-hex values for those same seven names in the coordinator presence/shape check.

**What changed:** No code changes. Current committed deploy/render guards already exist and pass locally, but the external secret-source requirement cannot be completed safely by guessing or printing values. The item needs a secure backfill/rotation decision.

**Verification:** GitHub Actions secret-name check found `CF_ACCOUNT_ID`, `CF_KV_SUBSCRIPTION_ID_DEV`, `CF_KV_SUBSCRIPTION_ID_STG`, `CF_KV_SUBSCRIPTION_ID_PRD`, `CF_KV_COACHING_ID_DEV`, `CF_KV_COACHING_ID_STG`, and `CF_KV_COACHING_ID_PRD`. Doppler presence/shape checks for the same seven names across dev/stg/prd returned false for valid 32-hex values. Local committed guards passed: `node apps/api/scripts/render-wrangler-kv.mjs --check apps/api/wrangler.toml`; `node --test apps/api/scripts/verify-wrangler-kv-binding.test.mjs` passed 9/9; `pnpm exec jest --config apps/api/jest.config.cjs --runTestsByPath apps/api/src/wrangler-config.test.ts --runInBand --no-coverage` passed 1 suite and 11 tests.

**Caveats / Follow-ups:** Escalated for human/secure-secret handling. Decide whether to backfill Doppler from the existing GitHub secret source, recover from known history, or rotate the Cloudflare identifiers first; then add/verify a pre-merge or runbook guard if the existing render/verify checks are not sufficient for this policy.
