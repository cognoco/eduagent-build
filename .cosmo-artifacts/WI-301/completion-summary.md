**What was done:** Implemented WI-301 owner-context elevation protection for profile switching.

**What changed:** Added `OWNER_ELEVATION_REQUIRED` to shared schema error codes. Parsed Clerk `fva` from JWT claims into `AuthUser.factorVerificationAge`. Made `POST /v1/profiles/switch` require fresh primary-factor `fva` before a non-owner active profile can switch into an owner profile, while preserving owner-to-owner and non-owner-to-non-owner switch behavior plus `OWNER_ELEVATION_GATE_ENABLED='false'` rollback behavior. Updated mobile profile switching to drive Clerk `useReverification` for non-owner-to-owner taps while keeping the server check authoritative, preserved switch error codes in the mobile profile provider, and updated `docs/architecture.md` with the owner-elevation contract and ADR link.

**Verification:** Red repro before implementation showed API stale/absent `fva` non-owner-to-owner switch returned `200` instead of expected `403`, and mobile child-to-owner row tap did not invoke the re-verification wrapper. Worker focused and broad verification passed, including schemas/api/mobile typechecks, lint, `check:decision-adr-link`, and `git diff --check`. Coordinator reran `pnpm exec jest --config apps/api/jest.config.cjs --runInBand --runTestsByPath apps/api/src/routes/profiles.test.ts apps/api/src/services/profile.test.ts apps/api/src/middleware/auth.test.ts --no-coverage`, which passed 3 suites and 129 tests. Coordinator reran `pnpm exec jest --config apps/mobile/jest.config.cjs --runInBand --runTestsByPath apps/mobile/src/app/profiles.test.tsx --no-coverage`, which passed 1 suite and 26 tests. Remote `origin/WI-301` matches commit `c8df72fa52703a47181fbd442aa83d4d9eba40dd`.

**Caveats / Follow-ups:** Clerk JWT templates must emit `fva`; absent `fva` fails closed for non-owner-to-owner switches while the gate is enabled. The default schemas Jest config currently discovers no tests, so the schema error test was run by the worker with explicit `--testMatch`. Mobile Jest emits existing Expo native-module warnings in this environment. No PR was created.
