**What was done:** Fixed WI-952 so the SubjectHubNotesSection empty-state Add note action is no longer an enabled no-op when the draft is empty or whitespace-only.

**What changed:** `apps/mobile/src/components/subject-hub/SubjectHubNotesSection.tsx` now derives whether the draft contains trimmed text and disables the empty-state Add note button when it does not. `apps/mobile/src/components/subject-hub/SubjectHubNotesSection.test.tsx` now covers empty draft, whitespace-only draft, valid non-empty empty-state submission, and verifies valid note submission clears the draft.

**Verification:** RED command: `pnpm test:mobile:unit -- src/components/subject-hub/SubjectHubNotesSection.test.tsx --no-coverage`; output summary: `SubjectHubNotesSection` failed on `disables the empty-state add action until the draft has text` because `accessibilityState.disabled` was undefined, with the rest of the file passing. GREEN command after the fix: `pnpm test:mobile:unit -- src/components/subject-hub/SubjectHubNotesSection.test.tsx --no-coverage`; output summary: `PASS @eduagent/mobile apps/mobile/src/components/subject-hub/SubjectHubNotesSection.test.tsx`, with the focused suite passing. Post-format command: `pnpm test:mobile:unit -- src/components/subject-hub/SubjectHubNotesSection.test.tsx --no-coverage`; output summary: focused suite passed again. Lint command: `pnpm exec eslint apps/mobile/src/components/subject-hub/SubjectHubNotesSection.tsx apps/mobile/src/components/subject-hub/SubjectHubNotesSection.test.tsx`; output summary: exit 0 with no findings. Type command: `pnpm exec tsc -p apps/mobile/tsconfig.json --noEmit`; output summary: exit 0. Commit hook output summary: lint-staged completed, `sync-skills` reported nothing to do, i18n orphan check reported no findings, and KEEP pattern cites resolved. Push command: `git push origin HEAD:WI-952`; output summary: rerun reported `Everything up-to-date`, and `git ls-remote origin refs/heads/WI-952` read back `d4779202196c7f4dc20a5ca7c0f05a555cf5748d`.

**Caveats / Follow-ups:** Stopped before Cosmo complete for coordinator review; no pull request was created. The first push attempt exceeded the tool timeout, but remote readback and the repeat explicit push confirmed the branch is saved.
