**What was done:** WI-902 (dictation full-text persistence + history) code and migration 0126 landed on origin/main via PR #1618 (squash dd58d9bdb). The reviewer's bounce was a closure-verification artifact, not a code defect: the integration suite failed only because the Doppler dev validation DB lacked the `sentences` column — migration 0126 was committed but unapplied there (the repo's known baseline-seeding drift: only 22 hashes tracked in `drizzle.__drizzle_migrations` though 0030–0125's tables already exist from prior `db:push`, so `drizzle-kit migrate` cannot run cleanly).

**What changed:** No new code. The already-committed migration 0126 (`ALTER TABLE "dictation_results" ADD COLUMN "sentences" jsonb;`) was applied directly to the dev validation DB (additive, forward-only, no reset) and its hash recorded in `drizzle.__drizzle_migrations`.

**Verification:** All 17 cases in `apps/api/src/services/dictation/result.integration.test.ts` pass — including the WI-902 persistence cases (`persists source sentences on record and returns them in history`, `returns sentences=null for old-client path`, history ordering/bounding, profile-scoping) and the IDOR scoping tests. Migration 0126 confirmed on origin/main at dd58d9bdb adding the column.

**Caveats / Follow-ups:** The dev validation DB was manually migrated for 0126 — the sanctioned surgical fix for the repo's documented baseline-seeding drift (project_schema_drift_pattern.md), not a schema change. CI's ephemeral integration DB applies migrations cleanly from scratch, so this artifact is validation-DB-specific. None outstanding.
