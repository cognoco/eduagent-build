**What was done:** Added a non-empty-catch guard to `apps/api/src/services/safe-non-core.ts` so `safeSend()` calls that throw capture the error in Sentry rather than swallowing it silently; scoped the `onFailure` handler in `apps/api/src/inngest/functions/auto-file-session.ts` to write only to the session's owning profile database via `getStepDatabase(profileId)` rather than an unscoped connection. Resolved a merge conflict on `auto-file-session.ts` from the concurrent `@inngest-admin` scope-annotation sweep (WI-1075) by keeping the annotation from main and the expanded imports from this branch. The `@inngest-admin` annotation on `apps/api/src/inngest/functions/billing-subscription-store-teardown.ts` (no-db) was inherited from main via hotfix PR #1654 rather than added directly in this branch, after an incorrect `event-profile` annotation was identified and corrected.

**What changed:** `apps/api/src/services/safe-non-core.ts` — non-empty catch block replacing the empty swallow; `apps/api/src/inngest/functions/auto-file-session.ts` — `onFailure` handler now calls `getStepDatabase(profileId)` and carries the `@inngest-admin: event-profile` annotation from the WI-1075 sweep; `apps/api/src/inngest/functions/billing-subscription-store-teardown.ts` — `@inngest-admin: no-db` annotation inherited from main.

**Verification:** All required CI checks passed (SUCCESS): API quality gate, flag-ON integration, merge-completeness check, Playwright web smoke, changes, claude-review (APPROVED, 0 must-fix / 0 should-fix findings), main. `mergeStateStatus` was CLEAN before merge. PR #1603 squash-merged to main as commit cb2fb93f.

**Caveats / Follow-ups:** Broader `onFailure` hardening across other Inngest functions is out of scope for this item and documented in the scope note on the WI page.
