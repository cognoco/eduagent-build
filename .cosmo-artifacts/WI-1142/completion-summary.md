**What was done:** Added the missing regression floor for the Study→Family switch-CTA transition on child entry surfaces (BRIDGE-04 path). Implemented strategy (a) from the Acceptance Criteria — a React-Query-backed Jest test that exercises the `switchingToFamily` guard-hold in `apps/mobile/src/components/guards/RequireFamilyContext.tsx` through a real query-cache path, closing the gap where the existing Playwright probe seeded Family mode directly and the existing Jest harnesses used a static ProfileContext literal that could not reproduce the optimistic profiles-cache write the real switch performs.

**What changed:** One test-only file, `apps/mobile/src/components/guards/RequireFamilyContext.test.tsx` — a new describe block adds a wrapper whose `ProfileContext.Provider` value is derived from a real `useQuery` subscription against the same `QueryClient` that `apps/mobile/src/lib/app-context.tsx`'s mode-switch mutation writes to, on the child entry surface route. The product guard `apps/mobile/src/components/guards/RequireFamilyContext.tsx` is unchanged (the underlying bug was fixed earlier under WI-878; this only locks it).

**Verification:** Red→green demonstrated — reverting the guard render-gate condition to the pre-fix form drove the new test RED (child surface renders one write early); restoring it returned the suite to green. The guard file test run passed all cases green with the guard intact. The claude-review SHOULD_FIX was addressed by wrapping the test body's `globalThis.fetch` override in a `try/finally` so teardown always runs even on a mid-test assertion throw; red→green was re-confirmed with that guard in place. Mobile `tsc --noEmit` and scoped ESLint were clean; the pre-push validation gate passed on both commits. PR number one-eight-three-zero merged with all required CI checks green and claude-review APPROVED.

**Caveats / Follow-ups:** The GC6 boy-scout consider finding was dispositioned in-thread as not-applicable — the file contains only external-boundary mocks (`@clerk/clerk-expo`, `expo-router`), no internal module mocks to sweep. Boy-scout note flagged for a separate look, not this WI: `apps/mobile/test-utils/screen-render.tsx` (repo-root-relative, no `src/`) appears to be a stale near-duplicate of `apps/mobile/src/test-utils/screen-render.tsx`.
