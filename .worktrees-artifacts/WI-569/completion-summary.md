Completion Summary — WI-569 (WP-W0-baseline: migration-chain baseline reset, MMT-ADR-0012)

What was done:

One-time documented baseline reset of the Drizzle migration chain per MMT-ADR-0012, on branch WI-569 (PR #845, green 9/9 checks at a16642538). Removed reference-only migrations 0106/0107 from the effective journal; authored 0108_identity_foundation_baseline.sql creating the 17-table identity foundation from empty (8 core: person, login, organization, membership, subscription, guardianship, supportership, consent_grant; 3 person-retain: consent_receipt, deletion_audit, financial_record; 6 pre-baseline amendment: regimes, policy_cells, policy_rules, knowledge_assertions, allowed_models, subscription_payers; 2 ENUMs: policy_kind, model_tier) plus a coherent 0108_snapshot.json. Adversarial-review hardening folded in: membership closed-role-set CHECK, payer_person_id NOT NULL + RESTRICT FK, policy_cells age-band bounds CHECK. Dev + staging DB resets executed under shepherd conditional-GO with a pre-reset audit, plus an extension approval dropping T2-era residuals on staging.

What changed:

apps/api/drizzle/meta/_journal.json — idx 106/107 removed, idx 108 appended (entries 0–105 + 108). apps/api/drizzle/0108_identity_foundation_baseline.sql — new baseline migration (60 statements). apps/api/drizzle/meta/0108_snapshot.json — new latest snapshot (prevId = 0105's id); stale 0106/0107 snapshots deleted. apps/api/drizzle/README.md — baseline doc + reset-execution record, including the factual erratum: 0106 WAS applied to staging (journal id=107) contrary to the "never applied to any environment" planning premise, and staging journal id=108 was an orphaned early-0107 (sturdy_monster_badoon, nudge migration later renumbered out); both stale rows left in place (inert under Drizzle's when-cutoff). packages/database schema — T1 fossils removed (profiles.clerkUserId, subscriptions.organizationId, organizations/memberships table definitions, membership_role enum); dead T1 integration tests deleted; shape tests adjusted. Dev DB: db:push --force (fossils dropped) + direct 0108 SQL apply (60/60 statements OK). Staging DB: DROP TABLE organizations, memberships CASCADE (49/28 rows — verified backfill/spillover-test data, no user data) + 4 approved hygiene drops (profiles.clerk_user_id, subscriptions.organization_id, nudges.direction, nudge_direction enum) + extension-approved T2 residual drops (organization_invitations, 0 rows verified; membership_role enum, sole dependent confirmed pre-drop) + drizzle-kit migrate (0108 applied, journal row id=109 exactly once).

Verification:

CI green on PR #845: 9/9 checks at a16642538 (main incl. apply-migrations + API integration tests, API Quality Gate, claude-review, CodeRabbit, Playwright web smoke, rollback-section, decision-adr-link, maestro-validator, changes). Staging drizzle-kit migrate re-run reports no-op (no second journal row; 109 rows total). Both DBs verified ALL OK by scripted information_schema/pg_type checks: all 17 baseline tables present; plural T1 tables, T1 ALTER columns, nudge artifacts, and T2 residuals gone; baseline organization/membership intact after drops. Adversarial review (CodeRabbit + Codex) triaged: all High/Medium fixed in 3fd5b85c8; deferrals documented as PR comment replies with spec citations.

Caveats / Follow-ups:

Caveats — dev is push-managed: until WI-570 lands the Drizzle TS schema for the 17 baseline tables, a db:push:dev will offer to drop them (answer NO or re-apply 0108's SQL afterwards; documented in the drizzle README). PR #845 deliberately NOT merged — the shepherd holds conditional merge authority and merges after their own green re-verification. Stale staging journal rows (id=107, id=108) intentionally left; inert. Follow-ups — WI-570: Drizzle TS schema for the 17 baseline tables (closes the dev push-drop caveat; rewrites the deleted T1 integration tests against the new tables). Post-baseline hardening candidates from review deferrals: person↔login reciprocal 1:1 constraint (blocked v1 by the invite-flow's login-before-attach ordering) and a single-primary-payer partial unique index on subscription_payers.
