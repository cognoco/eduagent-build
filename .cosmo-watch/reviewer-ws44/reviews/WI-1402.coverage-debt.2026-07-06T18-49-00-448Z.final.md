Review complete. WI-1402 bounced to rework — the work itself is verified good; it just hasn't landed on main.

## Review report — WI-1402 (cover /now overflow and candidate feeds), WS-44 Coverage Debt

**Disposition: REWORK** (Reviewing → Executing, tag `rework`, comment `3958bce9-1f7c-81eb-8bb6-001da2802b1a`)

### Why rework (single failing DoD item)

**DoD 4 — landed on base branch — fails.** Fixed In commit `f9be078ad` exists only on branch `WI-1402`; it is **not** an ancestor of `origin/main` (content-verified: the new `needs_deepening` integration cases are absent from main's `now.integration.test.ts`). PR **#1953** is OPEN, `mergeStateStatus=BLOCKED`, required `main` CI job still pending, claude-review/CodeRabbit pending. This lane's established pattern (WI-1414, WI-1411/#1943) is merge-then-review with Fixed In = the squash SHA on main; this item reached Reviewing pre-merge. No code changes needed — merge PR #1953 once required checks are green, re-run `/cosmo:execute complete` with the **squash** SHA, resubmit.

### Everything else verified — QA-style evidence (all gathered fresh, not trusted from the summary)

- **Mechanical DoD** (`review.ts --check`): ✓ — summary sectioned, Fixed In, dates, AC present.
- **QA verify** (`qa.ts verify`): commit exists ✓, both cited test files exist ✓, 1 AC needing judgment — judged below.
- **AC1 (overflow route coverage):** 3 new route tests assert the /now query contract (400 on `scope=person` without `personId`), delegation to `buildNowOverflow` with the authenticated `PROFILE_ID` + forwarded scope, and the parsed response shape. **Re-ran now: 6/6 green.**
- **AC2 (integration coverage beyond retention_due/ledger_moment):** new DB-seeded cases cover `unfinished_session`, `needs_deepening`, `challenge_ready` (self scope), `parked_item`, and supporter person + hub scopes with exact `personId`/`edgeId` assertions. **Re-ran now against real Neon: 8/8 green.**
- **AC3 (aging-window promotion via DB path):** verified against `now-feed.ts` `RANKING` — base `PARKED_ITEM=4` sorts *below* `CHALLENGE_READY=3`, so the test's parked-first assertion (8-day-old item vs 7-day window) can only pass through the `PROMOTED_AGING=1.5` path. Genuine promotion proof, not default ordering.
- **Lane invariant (real behavior):** ✓ — no new internal mocks (the route-test `jest.mock('../services/now-feed')` is pre-existing on main with an explicit `gc1-allow` annotation; new tests extend that sanctioned pattern while real behavior is exercised in the DB integration lane); assertions are exact-order/exact-id, nothing weakened; no faked evidence.

### Commands run
`review.ts --check` · `qa.ts verify` · `git fetch/merge-base/branch --contains/show/diff` · `gh pr list/view/checks` (PR #1953) · focused jest unit + integration suites re-run in the executor's worktree `.worktrees/WI-1402` (at exactly `f9be078ad`, clean) · final pre-disposition PR re-check.

### Policy overrides applied
None. WP-child waiver (lane policy) was not needed — `Altitude=Item`. No `allowed_red`/flake-lane/DoD overrides used.

### Cosmo mutations
1. `Stage: Reviewing → Executing` (rework), via `review.ts --disposition rework --actor claude-code:reviewer-ws44 --workstream WS-44`
2. Rework comment posted on the WI page with the full evidence + required action.

Local evidence kept under `.cosmo-watch/reviewer-ws44/` (`wi-1402-check.json`, `wi-1402-qa.json`, `wi-1402-diff-*.txt`, `wi-1402-rework.json`).

---
**[ BOTTOM LINE ]** WI-1402 bounced to rework on landing only — tests are real and re-verified green (6/6 unit, 8/8 DB integration), but PR #1953 is unmerged/BLOCKED and the Fixed In commit is not on main.

**[ FYI ]**
- PR #1953 flipped from draft→ready mid-review; once required checks go green it's merge-ready — the rework cycle should be a merge + `complete --fixed-in <squash-sha>` + resubmit, no code changes.
- Executor completed pre-merge, deviating from the WS-44 pattern (prior closes cited merged squash SHAs) — worth a shepherd nudge if it recurs.
