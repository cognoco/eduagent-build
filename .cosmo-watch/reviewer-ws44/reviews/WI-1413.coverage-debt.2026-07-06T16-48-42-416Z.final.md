REWORK disposition fully applied and confirmed live in Cosmo. Here is the review report.

---

## WI-1413 review — disposition: **REWORK** (bounce)

**WI-1413** — *S3 avatar-admin parity coverage; AccountAdminSheet row/route tests* · Type=Task · P2 · producer `codex:shepherd:coverage-debt` (≠ reviewer, gate satisfied).

### What passed
- **Landed:** PR #1947 **MERGED** to `main`; merge commit `98951bf` = `Fixed In`, confirmed ancestor of `origin/main`.
- **Required CI green:** all 4 branch-protection contexts pass — `main`, `API Quality Gate`, `Playwright web smoke`, `Merge completeness check`. The lone red, `Flag-ON integration (IDENTITY_V2_ENABLED)`, is **not required** and is the known unrelated **WI-1654 — family-v2 `listFamilyMembersV2` row-order flake** (a mobile-test-only PR cannot affect an API-integration lane) → correctly non-blocking.
- **AC1 / AC2 genuinely met:** `AccountAdminSheet.test.tsx` renders the **real** `<AccountAdminSheet/>` and asserts real testIDs + real `push` routes across the full owner row set, denied-gate shared rows, and reach paths. No theatre.
- **AC4:** D4 parity rows (devices/withdrawal/breakdown) correctly deferred to **WI-1416 — D4 account-admin product fork**.

### What failed (the bounce reason)
`apps/mobile/src/app/(app)/account/index.test.tsx` introduces a **new internal mock** of `../../../lib/navigation` `goBackOrReplace` behind a `// gc1-allow` escape. But `goBackOrReplace` (`navigation.ts:87`) is a pure function — `router.canGoBack() ? router.back() : router.replace(fallback)` — needing **no native context**. The escape's stated condition ("code genuinely cannot be exercised / no real implementation available") is false, so per AGENTS.md this is the forbidden "don't feel like wiring the real thing" case, which the **lane invariant makes rework**. Fix is ~2 lines: drop the mock, set `canGoBack: () => false` on `mockRouter`, assert `mockRouter.replace('/(app)/home')`. The sibling `AccountAdminSheet` child-stub mock was **accepted** (legitimate mount-boundary stub; real render would reproduce the child's whole provider harness and duplicate its dedicated coverage).

**Consistency:** sibling **WI-1404** (same lane, closed done by this reviewer) was required to convert its internal mocks to `jest.requireActual()` rather than annotate — WI-1413 must meet that same bar.

### Commands run
`review.ts --check`; Notion REST (page props, AC, completion summary); `git merge-base --is-ancestor 98951bf origin/main`; `gh pr view/checks 1947`; `gh api …/protection/…/contexts`; `git show 98951bf` (both test files); read `account/index.tsx`, `navigation.ts`, test mock headers; `WI-1404` precedent commit `f53d7fca4`.

### Cosmo mutations made
`Stage: Reviewing → Executing`; tag `rework` added; rework note comment posted (`3958bce9-…-001defdfdc83`). Evidence saved to `.cosmo-watch/reviewer-ws44/WI-1413-review-2026-07-06.md` + `WI-1413-review-result.json`.

### Policy overrides applied (this lane)
Landing branch `main` (verified). WP-child formality waived (direct Item, no WP — not bounced for it). Required-checks-only greenness applied to the non-required Flag-ON red. **Enforced** the lane invariant "tests must exercise real behavior." No code edited; no other watcher modified or stopped.

---
**[ BOTTOM LINE ]** WI-1413 bounced to **rework** — merge + CI + core coverage are sound, but a new `gc1-allow` mock of the exercisable pure helper `goBackOrReplace` violates the lane's real-behavior invariant; fix is ~2 lines.

**[ FYI ]**
- Item is now `Stage=Executing`, tag `rework`; the shepherd's Stage monitor will pick it up (reviewer does not notify it).
- The merged code stays on `main` — rework means a small follow-up PR, not a revert.
- WI-1654 flake remains the standing non-blocking red on this lane's mobile PRs.
