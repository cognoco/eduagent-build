Rework applied and confirmed. Review complete.

## WS-44 Reviewer Report — WI-1407 (consent/minor gate coverage)

**Disposition: REWORK** (Reviewing → Executing).

### QA / DoD verification (verified now, not trusted from summary)
| DoD criterion | Result | Evidence |
|---|---|---|
| Mechanical gate (summary sectioned, Fixed In, dates, AC) | ✓ | `review.ts --check` → `mechanicalOk: true` |
| AC1 — ProfileBasicsStep under-18 child-save gate | ✓ real behavior | renders real component; asserts `save-basics-adult-required`, Continue disabled, `mockFetch`/`onComplete` **not** called |
| AC2/AC3 — MentorMemoryScreen self privacy writes | ✓ real behavior | grant/decline assert `learner-profile/consent` body `{consent:'granted'\|'declined'}`; injection toggle + clear-all assert correct endpoints; url **not** `/test-profile-id/` (no childProfileId) |
| AC4 — Maestro minor-owner rejection flow | ✓ honest | yaml in commit (94 lines), marked **verify-at-e2e-run**, no faked device evidence |
| AC-test red-green-revert | ✓ | documented: removing `adultGatePasses` → test fails; restored → green |
| Lane invariant (real behavior, no weakened/mocked internals) | ✓ | **no new internal `jest.mock`** (GC1 clean); GC6 deferral counts recorded (3+7 pre-existing) |
| CI green | ✓ | `gh pr checks 1939` → 13 passed / 0 failed; Claude Review **APPROVED** |
| reviewer ≠ executor | ✓ | Executed By `codex:builder:WI-1407` vs reviewer `claude-code:reviewer-ws44` |
| **dod.4 — Actually landed** | ✗ **BLOCKER** | PR #1939 **OPEN**, `637d09dc` **not an ancestor of origin/main** (branch WI-1407, 1 ahead of `d619dce16`) |

The one failing gate is landing. Closing would mark the Cosmo item Closed while the code never reaches `main` — so it cannot be `done`. All other work is genuinely good; re-review will be fast once merged.

### Commands run
`git branch --show-current/-a --contains/merge-base --is-ancestor` · `gh pr list/checks 1939` · `gh api …/issues/1939/comments` · `git show 637d09dc` (metadata + both test diffs + yaml stat) · `review.ts --check` · `qa.ts verify --skip-tests` · Notion REST page-props pull · `review.ts --disposition rework` (dry-run + real).

### Policy override applied
WP-child formality waived (WS-44 direct-Item slice) — not needed here (no WP). No DoD criterion relaxed. Landing branch `main`. Note: `qa.ts` flagged the yaml as "failed" — a **false positive** (checkout artifact: `main` is checked out, branch `WI-1407` is not); the file is present in the commit.

### Cosmo mutations made
- **Stage: Reviewing → Executing** (tag `rework`)
- Comment posted: `3948bce9-1f7c-81d6-8fa6-001d513dc1ec` (precise remedy + full good-findings list + pilot-workflow note)

Evidence isolated under `.cosmo-watch/reviewer-ws44/` (`WI-1407-evidence.md`, `WI-1407-review-result.json`). Did **not** edit code, did **not** revert worktree changes, did **not** modify/stop any other watcher. I do not notify the shepherd — its Stage monitor will catch the Executing transition.

---
**[ BOTTOM LINE ]** WI-1407's test work is real, CI-green, and DoD-clean on every axis except one — the PR isn't merged — so I bounced it to **rework** with a merge-and-re-point-Fixed-In remedy; re-review will be trivial.

**[ FYI ]**
- The blocker is purely landing: PR #1939 is CLEAN + 13/13 green + Claude-APPROVED, just unmerged. Whoever lands WS-44 items should merge it and re-point `Fixed In` to the merged commit.
- Pilot-relevant ambiguity surfaced on the WI comment: WS-44 hasn't established whether the executor lands **before** Reviewing or a reviewer/shepherd lands **at** close. The reviewer DoD requires landed code and `/cosmo:review` does not merge — worth an orchestrator ruling for the lane.

**[ DECISIONS ]**
1. Confirm the WS-44 landing convention (recommended: **executor merges before completing to Reviewing**, so `Fixed In` always references a merged/main commit). This unblocks WI-1407 and sets the pattern for the rest of the wave.
