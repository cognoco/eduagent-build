Transition confirmed: `In Review→Closed`, disposition `done`, applied, close comment posted.

## Review complete — WI-1403 (`WI-1403` — S4 /now supporter artifact-exclusion negative test; test-coverage gap in WS-44 Coverage Debt lane)

**Disposition: DONE** (item closed, Resolution=Done).

### Evidence gathered
- **Landed:** merge commit `e2c59bd9…` is an ancestor of `origin/main` (local main was stale — fetched to confirm); PR #1945 state `MERGED`, base `main`, mergeCommit matches `Fixed In`.
- **CI green:** 9 required checks passed / 0 failed on the merged commit (incl. API Quality Gate + Flag-ON integration).
- **AC met (real HTTP/DB path, per lane invariant):** the added test (`now.integration.test.ts`, +161) seeds real rows via `database.insert` — supportership edge, learning session, parked item, mentor-activity-ledger, retention card — then hits `/v1/now?scope=person` and `?scope=supporter-hub` through the real Hono route. Asserts kinds `=== ['retention_due']` (allowed structural card retained), and that `parked_item` / `ledger_moment` kinds **and** the secret markers are absent. No-edge path returns 403 with `ERROR_CODES.FORBIDDEN` + exact message.
- **Targets real behavior:** confirmed in source — `now-feed.ts` returns `[]` for parked/ledger collectors on non-self (supporter) visibility; `resolveNowTarget` (line 364) throws `ForbiddenError('You do not have access to this person.')`. Anti-fall-through is proven behaviorally (seeded retention card for the inaccessible child → 403, not 200-with-card).
- **Lane invariant clean:** zero internal `jest.mock('./…'|'@eduagent/…')` — real DB, real error types. No weakened assertions, no faked device evidence (pure API integration, no device dependency).
- **Producer-is-not-closer:** `Executed By = codex:builder:WI-1403`; closer actor `claude-code:reviewer-ws44` — distinct. Type=Task → no red-green-revert gate required.
- **Cross-cutting sweep:** N/A — single-endpoint (`/now`) coverage add; WI dedup note records the sibling (WI-1201) covers a different endpoint.

### Commands run
- `review.ts --check WI-1403` (mechanical gate ✓); `qa.ts verify WI-1403` (2✓ verified, 1 needs-judgment AC → judged with evidence above)
- `git merge-base --is-ancestor … origin/main`; `gh pr view/checks 1945`; `git show e2c59bd9…`; source greps of `now-feed.ts` / `now.ts`
- `review.ts --pickup WI-1403` (Reviewing→In Review); `review.ts --disposition done --actor claude-code:reviewer-ws44`

### Policy overrides applied
- **WP-child formality waived** (lane policy, direct-Item slice) — no other DoD criterion relaxed. No allowed-red / flake / dod-override invoked (none needed; all required checks green).

### Cosmo mutations
1. Reviewer claim `reviewer:claude-code:reviewer-ws44` (Stage=In Review) → then cleared.
2. Stage `Reviewing → In Review → Closed`, Resolution=Done, close comment `3958bce9…` posted.

Evidence isolated under `.cosmo-watch/reviewer-ws44/`. No other watcher modified or stopped; no code edited; no worktree changes reverted.

---
**[ BOTTOM LINE ]** WI-1403 passes full DoD — real HTTP/DB negative-path coverage, CI green, landed on main — closed **Done** by an independent actor.

**[ FYI ]**
- Shepherd's own Cosmo-Stage monitor will pick up the Closed verdict; no notification sent (per protocol).
- Per lane tracker, WI-1402 was intentionally held pending WI-1403 settling (both touch `/now`); it can now proceed at shepherd/orchestrator discretion.
