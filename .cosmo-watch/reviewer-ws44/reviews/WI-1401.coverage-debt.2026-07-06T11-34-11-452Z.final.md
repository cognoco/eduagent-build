# WI-1401 Review — Disposition: REWORK

**WI-1401 (Retire or fix stale + placeholder e2e yamls reporting false coverage — WS-44 Coverage Debt item, was Stage=Reviewing)** is bounced to `Executing` for one precise gap: **AC6's required emulator execution of the repaired Maestro flows never happened — anywhere.**

## What passed (verified against reality, not the summary)

- **Landed:** PR #1941 merged to `main`; `Fixed In` = squash-merge commit `25cb0887`, confirmed an ancestor of `origin/main` after fetch (the initial QA "commit not found" was just my stale local clone). All 14 PR checks green; `maestro-validator` green on the merge commit. Mechanical DoD gate: 0 gaps.
- **AC1–AC5 (static):** all five scoped YAMLs addressed. I independently re-ran the removed-token grep at the Fixed-In commit — `Tonight`, `Conversation starters`, `titleEvening`, `grace-period-banner`, `switch-to-teen`, `cancel-deletion-button` survive only in comments, zero live assertions. Every replacement testID exists in source at that commit (`ParentHomeScreen.tsx:526,600`; `child/[profileId]/index.tsx:733,976,992,1033`), assertions were not weakened to generic screen-visible checks, the post-auth flow is properly parked (`blocked` tag + rationale + inventory rows updated), and retention/library's relabel matches its actual pre-existing tags. No GC1/GC6 or assertion-weakening violations.

## Why rework, not done

AC6 explicitly requires "emulator execution for repaired Maestro flows." The completion summary honestly admits no local device run — and my CI investigation proved CI supplied nothing either:

1. The push-triggered **Mobile Maestro E2E Tests** job on the merge commit (run 28787489102) selected only the **2 root-level** flows; the repaired parent/consent flows (nightly-tagged, in subdirectories) never ran.
2. Those 2 flows **both failed** ("2/2 Flows Failed") yet the job reported green: android-emulator-runner runs each script line in a separate shell, so `MAESTRO_EXIT` is lost and `exit ${MAESTRO_EXIT:-0}` always exits 0 (`e2e-ci.yml:472-479`). The green E2E check is structurally vacuous — it cannot stand in for device evidence.
3. The risk is concrete, not ceremonial: `parent-dashboard.yaml` hard-asserts `parent-home-child-starter-${CHILD_PROFILE_ID}`, which renders only after the dashboard row loads (`ParentHomeScreen.tsx:394-401,525`) — a load-timing race only a device run proves. Closing without it would recreate exactly the never-executed-YAML false coverage this WI exists to eliminate.

The kickoff's only policy override (WP-child formality waived for WS-44) was applied but not needed — direct Item, no WP. No other DoD criterion was relaxed.

## Cosmo mutations made

1. WI-1401: pickup `Reviewing → In Review` as `reviewer:claude-code:reviewer-ws44` (comment `3958bce9…fa74`), then disposition **rework** `In Review → Executing` with the full failure note posted (comment `3958bce9…b087`); reviewer claim released. Envelope + evidence saved under `.cosmo-watch/reviewer-ws44/`.
2. Filed two Captured bugs for the pre-existing CI infra defects (inheriting WI-1401's Project/Workstream): **WI-1651** — e2e-ci exit-code masking makes the Maestro job always-green; **WI-1652** — CI maestro selection runs only 2 root-level flows, all subdirectory smoke/pr-blocking flows are false CI coverage.

Commands run: `review.ts --check / --pickup / --disposition rework`, `qa.ts verify` (×2), `git fetch / cat-file / branch --contains / show / grep` at the Fixed-In commit, `gh pr view/checks`, `gh api …/check-runs`, `gh run view 28787489102 --log`, `capture.ts` (batch of 2). No code edited; no unrelated changes touched.

---
**[ BOTTOM LINE ]** WI-1401 reworked to `Executing`: the YAML repairs are statically sound, but AC6's emulator execution is missing and CI structurally cannot substitute for it — the executor must run the three repaired flows on an emulator and cite per-flow pass evidence.

**[ FYI ]**
- The **Mobile Maestro E2E Tests** CI check has been silently vacuous: flow failures are masked to green (`MAESTRO_EXIT` lost across line-split shells), and only 2 of ~29 tagged flows are ever selected. Both smoke flows currently fail on main (app never reaches sign-in). Filed as **WI-1651 (exit-code masking)** and **WI-1652 (flow selection)** — both at Captured with a regression-test DoR gap flagged for triage.
- The capture dedup LLM judge errored ("judge did not return JSON") on both rows; items were still created cleanly with no duplicate candidates from the structured pass.
