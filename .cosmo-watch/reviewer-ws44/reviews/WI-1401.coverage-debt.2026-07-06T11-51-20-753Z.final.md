**Disposition: REWORK** — WI-1401 (Retire or fix stale + placeholder e2e yamls reporting false coverage) bounced Reviewing → Executing, second bounce, sole failing gate AC6 (emulator execution of repaired Maestro flows).

## What the review established

**QA verification (facts re-checked against reality):**
- Fixed In commit `25cb0887` exists, is PR #1941 squash-merged, and is an ancestor of `origin/main` (the lane's landing branch). Mechanical DoD gate passed (`review.ts --check` exit 0); completion summary present and sectioned.
- **AC1–AC5 all hold**, verified against the commit's tree (not the working tree, which is one commit behind): all five scoped YAMLs repaired/retired; removed literals (`Tonight`, `Conversation starters`, `titleEvening`, `grace-period-banner`, `switch-to-teen`) survive only in comments, never in live assertions; every new testID exists in source (`ParentHomeScreen.tsx:526,600`, `child/[profileId]/index.tsx:976,992,1033`); assertions were re-targeted to specific testIDs, not weakened to generic screen-visible checks (lane invariant satisfied on the static side); `blocked` is a registered CONVENTIONS.md:122 tag with parked-not-executed semantics; all three flow-inventory docs updated consistently.
- **AC6 fails.** It requires emulator execution for repaired flows. The completion summary honestly admits none was run, and I proved CI can't substitute: run 28787489102 on the merge commit selected only 2 root-level flows (`--include-tags=smoke,pr-blocking`), both **failed** ("2/2 Flows Failed"), yet the job reported green because `MAESTRO_EXIT` is lost across line-split `sh -c` shells — a vacuous green. The three repaired parent flows never ran on any device. The risk is concrete, not ceremonial: `parent-dashboard.yaml` hard-asserts `parent-home-child-starter-<id>` with no wait, but that node renders only after the dashboard row loads and a starter resolves (`ParentHomeScreen.tsx:394-401,525-532`).
- **Process finding:** a prior reviewer session (same actor) rejected this exact submission at 11:46:17Z for AC6; it was flipped back to Reviewing at 11:50:00Z with zero delta — no new commit, no new comment, unchanged summary. My rework note flags that a third no-delta resubmission escalates to the operator.

**Commands run:** `qa.ts verify WI-1401` (verified, 1✓/0✗/1 judgment) · `review.ts --check` · `git merge-base --is-ancestor` · `git show`/`git grep` at `25cb0887` · source greps for all new/removed testIDs · `gh run view 28787489102` (+ log grep) · `review.ts --wi-id WI-1401 --disposition rework --actor claude-code:reviewer-ws44 --workstream WS-44`.

**Cosmo mutations:** Stage Reviewing → Executing with tag `rework`; rework comment posted (`3958bce9-1f7c-812b-8028-001d1c6d0b8d`). No other mutations; no code edited.

**Policy overrides applied:** none needed — the WP-child waiver is moot (Altitude=Item, no WP); no `allowed-red`/flake-lane override used.

**Evidence retained** under `.cosmo-watch/reviewer-ws44/`: `WI-1401-qa-verify.json`, `WI-1401-review-check.json`, `WI-1401-commit.diff`, `WI-1401-envelope.json`.

---
**[ BOTTOM LINE ]** WI-1401 bounced to rework a second time: the change itself is good and landed, but AC6's required emulator execution never happened anywhere, and the item was resubmitted four minutes after the prior rejection with no new work.

**[ FYI ]**
- The green "E2E Tests" CI check is structurally vacuous (exit-code masking in `e2e-ci.yml` + top-level-only flow selection) — pre-existing infra defects, kept out of this WI's scope per the prior adjudication; worth confirming they're actually filed as WIs.
- If the executor keeps flipping the stage without work, the next reviewer pass should go `--disposition human` rather than a third rework.
