Review complete — **WI-1405 (Billing v2 live-path + child-facing + top-up test coverage gaps) is closed as done.**

## Disposition: DONE (Reviewing → Closed)

## QA verification (evidence gathered, verified NOW — not trusted from the summary)

**Landed:** PR #1940 merged to `main` at 2026-07-06 09:54Z; `Fixed In` commit `093dffc` is the PR's squash-merge commit and is the tip of `origin/main` (`git merge-base --is-ancestor` confirmed). PR checks: 14 passed / 0 failed; the close-time SHA-aware CI gate independently re-pulled required checks on `093dffc` and reported green.

**Re-ran the delivered tests locally at the merged commit, all green:**
- The 3 new API integration suites against the real Neon DB — 13 tests passed (quota-provision-v2, metering middleware, family-v2)
- API test-seed unit suite — 145 passed (includes the new `child-quota-exceeded` seed scenario)
- The 3 edited mobile suites — 127 passed; mobile `tsc --noEmit` exit 0
- Maestro static validator — 7/7 (the new flow's testIDs, seed scenario, and tags all resolve against the app)

**AC-by-AC:** all six ACs map to real assertions — details in the evidence log. Highlights: the metering test adversarially sets `profileMeta.isOwner: true` for a child profile and still expects `role: 'child'`, pinning DB-membership role resolution (AC1/AC2's red-green property); the child 402 test seeds a live owner top-up of 500 credits and asserts the child sees `topUpCreditsRemaining: 0` with the credits unconsumed; the top-up poll test's first post-purchase poll returns the *unchanged* baseline and must not confirm, pinning strict `>` over `>=` (AC5's red-green property).

**Lane invariant (real behavior):** no new internal mocks in the diff. API suites hit real DB + real services/middleware with only `jest.spyOn(inngest, 'send')` (durable-dispatch boundary). Mobile suites reuse pre-existing annotated external/native/transport boundary mocks. No weakened assertions. Device evidence is honest: the Maestro flow and completion summary explicitly defer device execution (AC4/AC6) rather than claiming it ran.

**Advisory review triage:** the newest Claude Code Review verdict was CHANGES_REQUESTED (0 blocking, 1 SHOULD_FIX) claiming GC6-escape misuse on the feature-flags mock in `session/index.test.tsx`. I verified the finding's premise is already satisfied: that mock has been the canonical `jest.requireActual` + targeted `FEATURE_FLAGS` override pattern since before this PR — the PR only added an annotation comment to the existing line. `FEATURE_FLAGS` is a build-time constant that genuinely can't be varied at runtime, so the single-export override is the sanctioned GC6 conversion, not a coverage weakening. Triaged as not blocking. The earlier CHANGES_REQUESTED rounds (metering fixture ambiguity, missing annotation) were each addressed by follow-up commits on the PR.

## Commands run
`review.ts --check WI-1405` (mechanical gate: 0 gaps) · `gh pr view/checks/diff 1940` + check-runs API on PR head and merge commit · `gh api issues/1940/comments` (review verdicts) · `git merge-base --is-ancestor` · `git pull --ff-only origin main` (local checkout was exactly one commit behind; needed the merged files to re-run tests) · jest (API integration config, API unit config, mobile config) · `tsc --noEmit` (mobile) · Maestro validator · `review.ts --wi-id WI-1405 --disposition done --actor claude-code:reviewer-ws44 --workstream WS-44`

## Policy override applied
Only the WS-44 kickoff waiver: WP-child formality waived (direct Item slice, no Work Package required). No other DoD criterion relaxed; landing branch `main` matched the default. No flake-lanes or structured DoD overrides were needed — CI was genuinely green.

## Cosmo mutations
- Stage: Reviewing → Closed (Resolution=Done, claim cleared, producer-is-not-closer gate passed: producer `codex:builder:WI-1405` ≠ closer `claude-code:reviewer-ws44`)
- Close comment posted on the WI page (`3958bce9-1f7c-8177-b3de-001db9674a01`)
- Result envelope + full evidence log saved under `.cosmo-watch/reviewer-ws44/` (`WI-1405-review-result.json`, `WI-1405-review-evidence.md`)

---
**[ BOTTOM LINE ]** WI-1405 passed the full DoD — merged, required CI green, all six ACs backed by real-DB/real-component tests I re-ran green this session — and is now Closed as Done.

**[ FYI ]**
- The WI body's audit notes list surfaces the ACs deliberately did not contract: ChildPaywall 24h notify-cooldown untested, no billing e2e for cancellation/downgrade/purchase-failure/past-due, and the `onSlowPoll` half of purchase-confirmation polling. These remain open WS-44 surface for future items, not gaps in this one.
- I fast-forwarded the shared checkout's `main` by one commit (the WI-1405 merge itself) to re-run the tests; no other working-tree state was touched.
