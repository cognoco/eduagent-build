# WI-1413 review — reviewer claude-code:reviewer-ws44 — 2026-07-06

Item: WI-1413 "S3 avatar-admin parity coverage" | Type=Task P2 | Stage=Reviewing
Executed By: codex:shepherd:coverage-debt (producer != reviewer OK)
Fixed In: 98951bfe5408c7d91c4238510cc14efd1e69e1a3 | PR #1947 (MERGED to main)

## DoD evidence
- LANDED: 98951bf is ancestor of origin/main; PR #1947 state=MERGED, mergeCommit=98951bf, base=main. PASS
- REQUIRED checks (branch protection: main, "API Quality Gate", "Playwright web smoke", "Merge completeness check"): all PASS.
- Non-required red: "Flag-ON integration (IDENTITY_V2_ENABLED)" = known unrelated WI-1654 family-v2 row-order flake. PR touches only 2 mobile test files -> cannot affect API integration lane. Non-blocking per DoD (required-only greenness).
- AC1 (owner row set + denied-gate shared rows): AccountAdminSheet.test.tsx renders REAL <AccountAdminSheet/>, asserts learning-preferences/mentor-memory/mentor-language/profile/security/subscription/notifications/add-child/family-settings/privacy/help/sign-out; denied gate hides owner-only, preserves shared. REAL BEHAVIOR. PASS
- AC2 (reach paths): real fireEvent.press + mockPush route assertions (accommodation, mentor-memory?returnTo=account, more/account, profiles, subscription, notifications, create-profile, more, privacy, help, security->more/account). PASS (mockPush = external expo-router mock, legit)
- AC3 (wrapper mounts sheet + back uses goBackOrReplace fallback): account/index.test.tsx renders REAL AccountScreen, presses real back button. Meets AC but via 2 internal collaborator mocks (see finding).
- AC4 (D4 boundary deferred to WI-1416): no devices/withdrawal/breakdown assertions; deferral stated. PASS
- AC5 (verification cmds): cited in commit + summary; CI green. PASS

## FINDING (lane-invariant violation -> rework)
account/index.test.tsx adds NEW internal mock of ../../../lib/navigation goBackOrReplace with
`// gc1-allow: route wrapper test captures goBackOrReplace fallback without native navigation context`.
goBackOrReplace (apps/mobile/src/lib/navigation.ts:87) = pure fn: `canGoBack() ? back() : replace(fallback)`.
No native context needed -> real impl IS available in test env -> gc1-allow condition ("code genuinely cannot be
exercised / no real implementation available") is FALSE. This is the "don't feel like wiring the real thing"
case the escape forbids. Lane invariant (kickoff + tracker + AGENTS.md "Tests Must Reflect Reality"): a green
test that mocks exercisable internal code (GC1/GC6) is rework.

Fix (small, ~2 lines): drop the ../../../lib/navigation mock; add `canGoBack: () => false` to mockRouter,
press back, assert mockRouter.replace('/(app)/home'); optionally add canGoBack:()=>true asserting mockRouter.back().

ACCEPTED as-is: the sibling child-stub mock of ../../../components/account/AccountAdminSheet (Mock A) is a
defensible mount-boundary stub — real render would require reproducing the child's entire provider-mock harness
and duplicate its dedicated coverage. Leave it. No product-code change required.

## Commands run
- bun review.ts --check WI-1413 -> mechanicalOk:true, 6 manual checks
- Notion REST: page props + AC + Description + completion-summary blocks
- git merge-base --is-ancestor 98951bf origin/main -> YES
- gh pr view 1947 / gh pr checks 1947 -> MERGED, required green, Flag-ON red (non-required)
- gh api branches/main/protection required contexts
- git show 98951bf (both test files); read AccountScreen index.tsx, navigation.ts goBackOrReplace, test mock headers

## Disposition: REWORK

## Lane precedent (consistency)
WI-1404 (f53d7fca4, same lane, closed DONE by this reviewer) resolved its GC1 concern by CONVERTING internal mocks to jest.requireActual() ("canonical requireActual as-cast form" / "Pattern A") — the real-impl fix AGENTS.md prescribes — NOT a gc1-allow annotation. WI-1413 took the annotation shortcut on exercisable code. Bouncing WI-1413 is consistent with the bar already applied to WI-1404.
