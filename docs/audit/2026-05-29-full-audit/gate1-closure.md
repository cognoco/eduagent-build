# Gate-1 closure — identity-foundation scope ruling

> Closed 2026-06-09. **Decision record.** Generated by `identity-foundation-gate1-finalize.mjs` from `gate1-disposition.json`.

## What was decided

1. **Governing policy: layered** — `in_scope=true` means the rewrite must satisfy the invariant by construction (an acceptance criterion on a rewrite work package); each live defect is additionally routed to an `interim_owner` to patch now; `execution_blocking_if_deferred` flags those that cannot wait (→ Phase N.0 pull-forward).
2. **All 4 cluster sub-decisions accepted at layered default** — A (minor-PII→third-party, compliance register C-1/C-3/C-4) IN; B (billing/quota integrity, ADR-0002) IN; C (envelope/router correctness, ADR-0016/0014) IN; D (module structure) pre-split (5 IN / 9 OUT).
3. **F-130 ruled** (min-age consent gate, birthYear-only) IN / `security-pii-api` / **blocking** — sibling of F-145; was the one pre-gate in-IF orphan with no interim owner.
4. **11 atoms applied at coordinator leans** (see `gate1-worklist.md`). 6 architecture unknown-priority rows (F-103/107/108/109/111/112) were cluster-defaulted OUT without individual reads.

## Result

- **49 in-IF model obligations** (was 5 pre-gate): 3 kept-in + 46 flipped OUT→IN; 2 flipped IN→OUT (F-106, F-176).
- **125 in-other-workstream** + **9 deferred** — routed with named interim/target owners.
- **0 contested** remaining (85 scope-ruled + 40 provenance-cleared + already-confirmed).

## Patch-now list — 11 execution-blocking (Phase N.0 pull-forward candidates)

Live/urgent defects to fix independent of the rewrite:

| ID | Pri | Interim owner | Finding |
|---|---|---|---|
| F-019 | P2 | security-pii-inngest | freeform-filing retry transmits minor's transcript to external LLM without re-checking GD… |
| F-020 | P1 | security-pii-inngest | recordChildCapNotificationForSubscription does not re-verify child belongs to subscriptio… |
| F-092 | P2 | security-pii-inngest | monthlyReportGenerate trusts (parentId, childId) event pair without re-verifying family l… |
| F-117 | P1 | security-pii-api | Proxy-mode session write protection relies on a client-side redirect for non-metered writ… |
| F-118 | P1 | security-pii-api | Consent request can target arbitrary same-account profiles |
| F-121 | P0 | billing-subscriptions | Trial-expiry cron can downgrade a just-converted paying subscriber (missing status='trial… |
| F-122 | P0 | security-pii-api | Deletion cancellation/restoration checks are not atomic with final deletes |
| F-130 | P2 | security-pii-api | Minimum-age enforcement uses birth year instead of full birth date |
| F-133 | P2 | security-pii-api | Only 'SAFETY' block reason treated as safety filter; other Gemini block reasons trigger c… |
| F-144 | P1 | security-pii-api | Parent proxy sessions can mutate child progress state |
| F-145 | P1 | security-pii-api | Pronouns age gate fails open when profile birthYear is missing |

## Handoff to Phase M (four-bucket triage)

`L-gap-delta.md` is finalized (renderer-owned) with `Disposition` (M-seed) / `Interim owner` / `Blk`. M sorts every row into: (1) already-handled, (2) clear-in (the 49 model obligations), (3) clear-out (named workstream, the 125 routed), (4) defer (the 9). The execution-blocking tag feeds N.0.
