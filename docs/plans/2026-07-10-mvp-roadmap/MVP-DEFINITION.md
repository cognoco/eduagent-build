> **Roadmap-of-record (ratified 2026-07-10).** Graduated copy per PLAN.md Phase 5; working files, inventory, coverage/gaps provenance live in `_wip/mvp-roadmap/`. Sibling-file references (COVERAGE.md, GAPS.md, inventory.jsonl) resolve there.

# MentoMate MVP Definition — capability tree

**Status: RATIFIED 2026-07-10.** Canon + compliance evidence passes folded in (agents canon-surface / compliance-floor, cited per node). Remaining `⧖` markers = Phase-3 code-verification items, not open ratification questions — all 10 ratification questions + Q7 are ruled (see §"Open questions FOR RATIFICATION"). Ratified by: Zuzka (product) + Jørn (operator) — Phase-2 exit of `PLAN.md`.

**How to read a node:** `IN` = MVP fails without it · `DEGRADED` = ships reduced, named reduction · `OUT` = explicitly not MVP (fast-follow or killed) · `FILL` = approved for the MVP window as opportunistic capacity-fill — launch does **not** gate on it (distinct from IN: never argue "it's in MVP" from a FILL item). Every non-obvious state cites its ruling. `⧖` = state pending evidence pass.

**Launch definition (two gates, already ruled):** *store go-live* (technical launch) vs *public launch* (gated by closed beta WI-1506 with 5–10 families). "MVP" below = store go-live quality + the beta running.

---

## 1. Audiences & identity

*(Evidence: canon pass 2026-07-09 — `docs/canon/identity/ontology.md`, `prd.md`, shell spec, compliance register.)*

- **Launch account tier = credentialized only (13+, own login).** The managed tier (under-13, guardian-created charge) is **built but activation-deferred** pending a separate 10–12 audience ruling (`shell-redesign.md:250`; register:89 sets the 13+ floor). ⚠ Note: the counsel packet frames "children are a core audience (guardian-owned accounts with linked child profiles)" — under the launch posture those children are 13+ self-consenting teens. → ratification item 10 (confirm the launch family model consciously).
- Personas at launch: solo adult (first-class, highest revenue segment) · independent consent-capable minor (13+) · family operator (admin + guardian×N + Payer ± learner) · supporter (edge capacity, any age). Charge (managed minor) and managed-adult (UC-1) — built-but-dormant.
- Roles are `{admin, learner}` only; guardian/supporter are edge **capacities** (inv 5/6). Guardianship (consent authority) vs Supportership (granted visibility) structurally distinct; neither implies the other (inv 14).
- **Supporter ceiling = recap/grades layer only** — never notes, mentor memory, or transcripts, at every tier (ruled 2026-06-09, `prd.md:404-424`). Linking ceremony: symmetric visibility contract, never one-tap; credentialized supportee can revoke anytime (revocation flow flagged net-new by the spec). WI-1393 anchors closed. Ceremony mobile screens `link/*` (WI-1137) — **IN (ruled 2026-07-10)**: backend + contract components exist; screens make the ceremony reachable. Execution check: initiate person-picker must cover the join-my-family teen path, not only managed children.
- "Join my family" v1: parent invites existing 13+ teen → org join + opt-in Supportership, **no auto-Guardianship** (`prd.md:449-465`). Build enabler = **WI-1753** (cross-account existing-teen join, split from WI-1580, launch-IN) — nothing built today covers this path.
- **Guardian write-authority over a credentialized charge = ALLOWED, intentional (ruled 2026-07-10, WI-787).** The guardianship edge is the authority; an own login does not dissolve it. Scope: canon entry + intentional-gap comment in `verifyPersonOwnershipV2` / legacy twin + pinning test (guardian write → 200) so any future change is deliberate. **Scope boundary (sharpened per review 2026-07-10):** "write-authority" here = the profile-scoped learning-data writes governed by the ownership gates (`verifyPersonOwnershipV2` / legacy `verifyProfileOwnership`) — it does NOT extend guardian reach into surfaces canon separately suppresses for a credentialized charge (notes wall, mentor-memory management, transcripts — supporter-ceiling rules stand), nor into the teen's own account admin, security, or billing. Each of those is its own product/legal surface; this ruling covers the ownership gate only. Revisit trigger: teen-autonomy surfaces / 16+ boundary work.
- Cross-account linking — **SPLIT (ruled 2026-07-10, adversarial-review follow-up).** The existing-teen family join is cross-account *by definition* (prd.md v1 = existing-account teen; the only built entry point `pushLinkNewForManagedPerson` is managed-child-only, `navigation.ts:204-207`), so it cannot ride under a parked WI: extracted to **WI-1753 (cross-account existing-teen family join — launch-IN**, Captured, join-my-family v1 enabler). WI-1580 retains only the unrelated **external supporter invite** into `/link/new` — **OUT/fast-follow**: no value pre-launch for strangers-as-supporters; high-risk trust boundary (anti-enumeration, minor-consent routing); stays Parked, resurface = launch + first real cohort.
- Consent-denial state: **pending counsel Q2** — ruled direction = first-class "denied" dormant state distinct from withdrawal (Item 4-D2) unless counsel mandates erasure. **Counsel-independent behavior audit created 2026-07-10: WI-1761** (gap G2); build WI follows the Q2 answer.

## 2. Onboarding & consent

- Guardian consent flow, child profile creation, solo/skip path (never force add-child) — **IN**.
- Trial preview lesson — **OUT** (Item 1 ruling 2026-07-05: revisit with funnel data). Honest substitute funnel (sample topic → marketing → signup) is the launch path.
- Email consent-withdrawal (P0) — **IN**, shipped (WI-1340 chain; prod config finalizing).
- Tutor-language picker at onboarding/settings — **IN** (ratified-13; WI-1496 + reachability).

## 3. Learner loop (the core)

- Tutoring sessions (Socratic, envelope-governed, server caps) — **IN** (live).
- Reviews / SM-2 retention loop — **IN** (live). Ratified-13 bug set attaches here: cooldown-on-completion (WI-1466), dual push-cron (WI-1461), weak-topic resurfacing (WI-1446).
- Challenge Round — **IN as built** (server-owned mastery policy); production flip + grader bake-off: bake-off (WI-1438) **IN** as a gate; the flip itself = **WI-1754 (ruled 2026-07-10, closes gap G1)** — see verified-learning ruling below. Two-axis mastery doctrine RULED (Item 5, 2026-07-05): SM-2 and Challenge are separate axes; blocked→scaffolded-relearn recovery ladder; fix coarse eligibility. Reconcile with proposed MMT-ADR-0031/0032 (WI-1657 spec) — one source, ADRs ratify the ruling.
- Notes / Journal — **IN** (journal redesign shipped). Note-correctness umbrella (WI-1491) **OUT** (killed as umbrella; narrow slices only, later).
- Verified-learning loop (WI-1657 spec, slices WI-1445/1454/1464/1465/1469/1502/1658/1665/1666/1703/1704/1705/1754) — **RULED 2026-07-10 as a thin sellable launch loop, not the full architecture.** Product premise: the kid must want to use it, but parents pay, so MVP needs one end-to-end receipt loop that feels rewarding to the learner and credible to the parent: **Challenge proof → scheduled review → learner promise → parent receipt**.
  - **Launch-gating IN (engine spine):** WI-1469 — mastery-axis rule first (define "verified", "due again", "stale", "mastered" across SM-2 + Challenge); WI-1666 — loop e2e/eval pack scaffold early, before visible rollout; WI-1446 — `needs_deepening_topics` promotion/expiry to prevent stuck or hidden recovery states; WI-1464 — Challenge calibration / all-or-nothing mastery bar / staging-beta readiness; WI-1754 — controlled Challenge Round beta/prod flip for the launch cohort (new G1 execution item; flag flip only after the engine gates pass, with rollback + cohort limits).
  - **Launch vertical-slice IN (kid proof + parent receipt):** WI-1445 — **narrow next-review write only** after WI-1469; writes `retention_cards.nextReviewAt` on Challenge verification so the app follows through. This does **not** reopen the broader answer-correctness chain. WI-1502 — narrow learner-facing review-promise card ("we'll check this again..."). WI-1121 — missing Now-feed moments as read-time projections (detail below), not ledger writers. WI-1705 — **ruled now, not queued:** parent proof surface = **V2 family/parent home card, child detail on tap**; do not target nav-V1 Recaps as the launch surface. WI-1658 — parent proof receipt narrowed to one current-prod/launch-shell card + tap-through detail, no rich report system. WI-1703 — minimal verified-artifact provenance contract / source + verification taxonomy so launch copy cannot falsely treat ordinary notes as verified proof.
  - **FILL (approved MVP-window work; launch does not gate on it):** WI-1704 — evidence-links substrate / durable evidence references; default FILL, promote only if the WI-1658 build proves the narrow proof card needs it. WI-1452 — felt-knowing evidence-citation substrate overlaps WI-1704; fold/duplicate into WI-1704 in Cosmo, do not execute separately. WI-1465 — low-stakes per-concept re-prove path; useful recovery depth, second wave. WI-1454 — concept-targeted review for weak concepts, second wave. WI-1665 — rich recap consumption of verified artifacts; **IN only if the launch shell's family surface renders recaps**, otherwise FILL/fast-follow behind the parent-home receipt.
  - **OUT / fast-follow:** WI-1491 — note-correctness umbrella stays OUT as an umbrella (bring back only narrow slices later); WI-1667 — delete superseded partial leftovers / cleanup; full evidence browser, transcript-adjacent citation UI, and rich parent report redesign.
  - **Reshape:** WI-1657 is the slice-map/spec umbrella, not an execution item. Once this ruling is ratified and the child WIs carry the scope, close/retire WI-1657 rather than dispatching it.
  - **Date-pressure degrade line:** if launch pressure hits, proof presentation degrades before the engine spine: WI-1445/WI-1502/WI-1658/WI-1703 may drop to **DEGRADED** for days-later delivery, but WI-1469→WI-1464→WI-1754 plus WI-1666 stays intact. Do not cut the verification engine while marketing verified learning.
- Answer-correctness chain (WI-1443→1445) — **OUT/fast-follow** (Item 2, co-signed); the only launch-IN carve-out is the narrow WI-1445 Challenge→SM-2 `nextReviewAt` write above, after WI-1469.
- "Coming up next" recaps — **KILLED** (Item 3).
- Freeform Ask-Anything + library filing at 5 exchanges — **IN** (MMT-ADR-0021, live).
- Activity-ledger Now-feed moments (WI-1121) — **IN (ruled 2026-07-10; mechanism corrected per review 2026-07-10).** Make the 5 missing moments (`snapshot_ready`, `topic_mastered`, `retention_due`, `needs_deepening_added`, `recap_ready`) appear in the V2 Now feed as **read-time projections in `now-feed.ts`**, per MMT-ADR-0022 (activity feed = derive-on-read + thin seen-state). Those five kinds were deliberately pruned from `ledgerKindSchema` (`packages/schemas/src/activity-ledger.ts:3-8`) — do **not** re-widen the ledger or add writers unless ADR-0022 is amended. `reward_receipt` descoped (dangling, no producer/spec). Visibility-field gap splits to a follow-on WI.

## 4. Guardian / family loop

- Family-mode home, recaps, progress view, coaching cards — **IN** (live surfaces). Parent proof-receipts (WI-1658) — **IN, narrow (ruled 2026-07-10)**: one V2 parent-home card + tap-through detail, per the verified-learning ruling in §3 and the WI-1705 surface decision.
- Parking-lot return — ruled resumable-object flow (Item 4-D3); **fast-follow build WI created 2026-07-10: WI-1766** (Parked).
- Parent-on-behalf provenance (Item 4-D1) — ruled durable ownership-vs-authorship model; **schema work, fast-follow build WI created 2026-07-10: WI-1765** (Parked).
- Child-to-parent nudges, child-allocated top-ups, parent cap-banner actions — **OUT** (killed/deferred per classification + decision pack).
- Supporter cold-start (Support hub empty/kickstart experience): API route `GET /scopes/coldstart` (WI-1127) — **IN (ruled 2026-07-10)**, sequenced first in the chain; mobile surfaces SupporterColdStart + SupporterSelfLearningDoorway (WI-1135) — **IN, full scope (ruled 2026-07-10)**. Build-state precision: `resolveSupporterColdStart()` service + schema exist; the **route does not exist yet** and neither do the mobile surfaces — route + surfaces ARE the build, nothing here is route-ready. Rationale: the beta family-operator's first session is the cold-start path; a blank Support hub makes the beta measure confusion, not the product.
- Supporter co-learning ("learn this too" doorway + payoff chip, WI-1136) — **OUT/fast-follow (ruled 2026-07-10).** Delight feature, no journey breaks without it; spec is execution-ready and privacy design done (structural reads only). Candidate for the first post-launch delight wave; stays Parked.

## 5. Library & curriculum

- Books/topic-maps generation, shelf navigation — **IN** (live; book-gen quality gate exists).
- Prerequisites advisory-only, never lock — doctrine (WI-587). Mastery-gated progression (WI-1662) — **RULED 2026-07-10: the gate is on CREDIT, not on MOVEMENT.** Navigation stays free (doctrine untouched: any topic, any time, never locked, override everywhere). Mastery gates what *counts*: only Challenge-verified mastery renders as proven progress in learner receipts and parent views; unverified progress shows as "explored/in progress". Soft "suggested next" ordering allowed. Copy rule: replace "progress gated on demonstrated mastery" with verified/proven-progress language — never wording that implies blocking. No build; WI-1662 closes via review gate with this ruling as its artifact.

## 6. Languages & voice

- UI locales: 7 (en/de/es/ja/nb/pl/pt) — **IN**. Tutor-prose: 10 (superset incl cs/fr/it) — **IN**.
- Wrong-language TTS fix (WI-1447) — **IN** (ratified-13).
- Four-strands language teaching — **IN as a minimum credible language-course spine, not merely "as built" chat practice (ruled 2026-07-10).** Product promise check: we sell "learn anything"; parents pay only if language learning looks like a real path their kid wants to return to. MVP therefore needs a logical build-up of language work: **understand something → say/write something → get corrected → repeat it better → drill familiar language faster → save retention/progress → know what comes next.**
  - **Item rulings (execution scope):**
    1. **Keep `four_strands` language mode live** — **IN as built, with no rollback.** Existing language pedagogy mode, vocabulary memory, CEFR progress, fluency drill persistence, and graded-input card remain part of launch.
    2. **WI-1447 — wrong-language STT/TTS locale fallback** — **Launch-gating IN.** If the app teaches French but speaks/listens in the wrong locale, the language promise is visibly broken.
    3. **WI-1755 — language-mode safety/eval guard** — **Launch-gating IN.** Scope: harden freeform language-intent detection — as a **hardening of the existing LLM-based detector in `language-detect.ts`, not a new system** (amendment 2026-07-10); add regression examples for French history / Spanish politics / Celsius; prove Socratic `EVALUATE` / `TEACH_BACK` blocks cannot leak into `four_strands`; include eval snapshot coverage because this touches LLM prompt routing. Closes gap G10. (WI-1757 = parallel-creation duplicate, Parked → Duplicate close via review gate.)
    4. **WI-1552 — adaptive next-activity selector for Four Strands sessions** — **Launch-gating IN, narrow — AC reshaped (amendment 2026-07-10).** A within-session selector ALREADY EXISTS (`chooseNextLanguageStrand()` + failure-escalation to `language_focus` + per-strand artifacts, `language-session-engine.ts:556-597`). The real gap is **cross-session**: persist the next-practice pointer and give every language subject a visible continue/next-practice path. Extend the existing selector; do not rebuild it.
    5. **WI-1547 — upgrade graded input generation beyond seed passages** — **Launch vertical-slice IN, narrow.** Must ship one child-credible graded reading/listening input path with known/target vocabulary, CEFR-ish difficulty, and a comprehension check. Full content quality/auteur system remains FILL.
    6. **WI-1756 — structured meaning-output card/loop** — **Launch vertical-slice IN, narrow.** Server already emits `meaningOutput`, but mobile must render one structured output task and support direct correction + retry. Without this, the course spine is input-heavy and not a Four Strands loop.
    7. **WI-1548 — repeat-after-me and shadowing activities end-to-end** — **Launch vertical-slice IN, narrow.** One repeat-after-me/shadowing activity using target audio, learner voice, transcript comparison, and retry. This is a beginner-language credibility floor, not pronunciation science. **Absorbs WI-1492** (existing `SpeakingPracticeCard` is the presumed surface). **Blocked-by WI-1447** — target audio needs the correct-language TTS fix first (dependency set in Cosmo, amendment 2026-07-10).
    8. **WI-1549 — persist language speaking attempts with transcript comparison scores** — **Launch vertical-slice IN, narrow and paired with WI-1548.** Persist attempt, target text, transcript, lexical match, attempts count, and activity link. No phoneme scoring.
    9. **WI-1553 — language session-end learning summary** — **Launch vertical-slice IN, narrow.** Show a simple receipt: words/chunks practiced, correction/retry, fluency/speaking attempt, and next practice. This is the "I actually learned something" close. **Derive-from-events (amendment 2026-07-10):** build the receipt from existing session events (strand counts, graded-input verdicts, speaking attempts) — WI-1550/1551 contribute nothing to it.
    10. **WI-1492 — wire SpeakingPracticeCard into live four-strands session flow** — **MERGED into WI-1548 (amendment 2026-07-10; supersedes "FILL unless fastest path").** The existing tested-but-unwired card is the presumed repeat-after-me surface, so the escape clause fires with near-certainty — no standalone item. Parked; formal Duplicate close via review gate.
    11. **WI-1394 — re-home CEFR vocabulary browser into the V2 shell** — **FILL.** Useful for progress credibility, but launch does not need a full vocabulary browser if the session receipt and continue path are present.
    12. **WI-1550 — language-native competency profile model** — **OUT/post-launch, unconditionally (amendment 2026-07-10 struck the "FILL only for tiny receipt fields" conditional).** The WI-1553 receipt is ruled **derive-from-events** — session events already record strand counts and comprehension verdicts — so no competency-model fields are needed for launch.
    13. **WI-1551 — evaluate language sessions into competency updates** — **OUT/post-launch, unconditionally (amendment 2026-07-10 struck the "FILL only if WI-1553 needs a summary" conditional).** Same derive-from-events ruling; no evaluator on the launch path.
    14. **WI-1554 — Four Strands balance and skill profile in language progress UX** — **FILL.** Product-review telemetry is useful; learner-facing skill-profile UX waits until the competency model exists.
    15. **WI-1493 — convert the 4-strands doc into a real plan / enumerate gaps** — **Reshape/close after this ruling is reflected in Cosmo.** It is not an execution item once the itemized IN/FILL/OUT list exists.
    16. **Full language-product ambition** — **OUT/post-launch.** Full Four Strands mastery architecture, CEFR-by-skill model, phoneme-level pronunciation scoring, rich adaptive curriculum, full course authoring system, and Duolingo-like standalone positioning. Do not market those at launch.
  - **Degrade line (amendment 2026-07-10):** under date pressure the language slice degrades in this order — **first** the speaking pair (WI-1548 + WI-1549, together), **then** the meaning-output card (WI-1756), **then** the graded-input upgrade (WI-1547); the session receipt (WI-1553) degrades **last**. The floor that never cuts: mode live (item 1) + safety guard (WI-1755) + locale fix (WI-1447) + visible continue path (WI-1552).
  - **Cross-vertical yield (amendment 2026-07-10):** if the verified-learning slice (§3) and the language slice cannot both land, **the language slice yields** — the engine spine and receipt serve every subject; language is one vertical.
- Voice-first (Epic 17) — **OUT wholesale** (obsolete spec; re-scope umbrella WI-1459 post-MVP). BUT the shell spec independently rules **"voice input everywhere"** (mic on every input incl. cold-start, `shell-redesign.md:117`) and homework dictation is first-class — so the MVP voice floor = transcription-input everywhere + current TTS, NOT zero-voice. Epic-17's conversational voice-first UX stays out. **Q6 RULED 2026-07-10: baseline CONFIRMED as the MVP floor.** Rationale: "voice is critical" is strongest for pre-typing children — deferred with the dormant under-13 tier, so confirming the baseline is coherent with the age-floor ruling, not a retreat; the floor is voice-in everywhere + TTS out + the WI-1548 speaking slice as the flagship voice moment. Deferral creates no lock-in (voice-first is additive UX; the hard realtime-transport work costs the same later, and realtime APIs are getting cheaper); WI-1459 re-scopes post-MVP, ideally alongside waking the under-13 tier. **WI-1486 (defer mic permission to intentional action) pulled quarantine → FILL** — cheap protection of the voice floor's first impression.

## 7. Notifications & reachability

- Push permission wiring (WI-1441) + review/daily nudges — **IN** (ratified-13).
- Reachability T4–T6 (reciprocal nudges etc.) — **OUT** (killed/deferred).

## 8. Billing & monetization

- Free 10/day + 100/mo · Plus 700/mo dual-cap — **IN** (live). **Numbers operator-confirmed 2026-07-10** as the launch pricing — citable in canon and store copy.
- RevenueCat store purchases (WI-1328), store listings (WI-1335), APNs/FCM prod creds (WI-1337) — **IN**, launch-ops (OPQ-6 wave B).
- Payment-failed notify (WI-1474→G1/WI-1555) + past-due banner (WI-1475) — **IN before paid launch** (classification 2026-07-03). Billing silent-fail escalation (WI-1399) — **IN** (ratified-13).
- Annual-plan push at trial end (WI-1661), cancellation-save flow (WI-1660) — **OUT/fast-follow (ruled 2026-07-10).** Zero users at launch means no trial cohort ending and no cancellations to save; both are optimizations on a billing loop that must first exist. First post-launch monetization wave, WI-1660 (retention) ahead of WI-1661 (upsell).

## 9. Safety

*(Evidence: compliance-floor pass 2026-07-09, file:line cites therein.)*

- Deterministic gates — **IN, live unconditionally**: dangerous-procedure gate (full block for minors, `dangerous-procedure-gate.ts`); minor-PII echo redaction + structured Sentry alarm (`exchanges.ts:183-251`); under-18 Gemini/Vertex ban CI-guarded (`router.ts:550-559`); voice = transcription-only, no emotion inference (AI Act Art 5(1)(f) posture).
- Suitability judge — **IN**: flags ruled ON in prd (Phase-0 #2, 2026-07-07); currently default-false in `config.ts:189,200`; enablement WI-1686 captured.
- **Crisis-disclosure: ALREADY IMPLEMENTED** (WI-1358, closed) — detection → learner-facing resources, operator-alarmed telemetry, **deliberately NO guardian notification** (ruling se-032: guardian-may-be-abuser failure mode). **Contradiction RULED 2026-07-10 (Q8, operator): se-032 STANDS.** WI-1690's "+ guardian notification" wording (from the 2026-07-07 option-B ruling, event 33) is **struck — superseded**; WI-1690 rescoped to detection + in-app resources + operator telemetry, no guardian notification on any code path. Conditional stays: counsel Q3 (OPQ-22) may require a mandatory-reporting legal-duty channel — designed *with* counsel if so, not pre-built.
- Daily blocked-safety-event digest (WI-1691) — **IN**, net-new confirmed (only per-event Sentry alarms exist today). Full guardian-notification UX + human-review queue — **OUT/fast-follow** (WI-1692).
- Flag-a-reply v1 (telemetry-only, WI-1499) + shake-to-comment — **IN** (Item 6 ruling).

## 10. Compliance & legal

*(Evidence: compliance-floor pass 2026-07-09; register = `docs/compliance/identity-compliance-register.md`; early-pass = `docs/compliance/2026-07-04-launch-compliance-closure-check-early-pass.md`.)*

**THE launch gate (C-5, register:77-81): DPIA signed + DPO appointed before the first consent-gated child onboards.** DPIA is draft v0.1 unsigned; no DPO. Everything else sequences under it:

- Controller entity contradiction (policy says Cognoco s.r.o.; DPIA/ROPA say Norway-TBD) — counsel Q1 / WI-1559; blocks privacy-policy publish.
- Privacy-policy publish TODOs (`privacy-policy.html:67-73`): DPO name, controller address, Art-27 EU rep, UK rep, final age floor.
- Age floor — **RULED 2026-07-10 (operator): 13+ is the confirmed LAUNCH floor**, not a forever cap. Privacy policy + store worksheet state 13+ now; the store rating is obtained to *conform* to 13+ (the rating no longer votes on the floor); the worksheet's age-11 stale claim rides the WI-1577 final-gate refresh. The 2026-06-08 build-for-10+ strategy stands unchanged: future non-US 10–12 opening under GDPR parental authorization (US under-13 stays blocked → COPPA stays dormant) remains roadmap — keep the scaffolding warm + under-13 bounce instrumentation, and word the policy so a later floor change is an update, not a rewrite.
- DPIA name-minimization (first name sent verbatim to LLM, `exchange-prompts.ts:734`) — counsel Q4 / WI-1558: tokenize vs disclose.
- Consent-denial build direction — counsel Q2; current build behavior on denial NOT yet audited (pre-wire both outcomes).
- Consent audit trail before hard-delete — WI-1442. Retention-period values — counsel-owned placeholders (register:92).
- **False claims to fix pre-publish:** privacy policy §3 claims "aggregated de-identified analytics" — no such system exists; store data-safety worksheet says age 11 vs code 13 + cites legacy tables (refresh = the FINAL-GATE re-run, WI-1577 territory).
- Provider DPA signatures — process item, unverifiable from repo.
- **EU AI Act — RULED 2026-07-10 (Q5, operator): pre-wired conditional.** Defensive posture is BUILT (C-2: no emotion inference, transcription-only voice, prohibition-floor primitives in MMT-ADR-0013). **WI-1659 (compliance plan / classification self-assessment) = IN unconditionally** — needed under either classification outcome. **WI-1195 (in-chat Art-50 AI disclosure) = IN** — cheap transparency floor, applies regardless of classification. **WI-1663 (technical file + QMS skeleton) + WI-1664 (school/institutional-deployment tripwire) = staged on the counsel classification answer**: OUT if not-high-risk; auto-promote to launch-gating IN if Annex-III high-risk. **The classification question (register:100-102) is escalated as the TIME-CRITICAL item of the counsel packet (OPQ-22)** — Annex-III high-risk obligations become enforceable 2026-08-02.
- Launch compliance closure check (WI-1507, parked) — re-runs against actual data flows at the end.

## 11. Trust package (Item 6 ruling — IN as a coherent slice)

First-week mentor plan · memory checkpoint · in-app support path (incl. shake-to-comment) · visible review promise · flag-a-reply v1. **Design-pass mechanics RULED 2026-07-10 (G8, operator): ONE batch design pass (WI-1767, Zuzka) over all five slices precedes the builds** — Blocking relations set on WI-1497/1498/1499/1501/1502; capacity-shaped.

## 12. Platform & quality floor

- V2 LLM-routing cutover — **IN, sequenced** (Phase-0 #1: bake-off → staging + live gates → prd flip; WI-1685; rollback = flag flip). Legacy Gemini deletion (WI-1436) — **OUT** (post-soak).
- Prompt caching program — **IN post-cutover** (Phase-0 #4; WI-1687→1688). LLM spend guardrails + kill switch (WI-1505) — **IN** (addendum ruling).
- Zero native e2e → Maestro smoke baseline (WI-1400) + auth resilience branches (WI-1406 subset) — **IN** (ratified-13).
- profileId CI guard (WI-1449, closed) — done. RLS activation — **OUT** (app-layer scoping ruled sufficient for launch).
- Observability: Sentry re-enable (WI-1336) + launch-health alerts on 6 signals (WI-1500) — **IN**. Dashboard — fast-follow.
- Activation events wiring (WI-1689; WI-1570 merged in as duplicate, F2) + first-party analytics sink (**WI-1762**, gap G3) — **IN** (Phase-0 #5 + addendum; PostHog fast-follow). Voice-floor coverage audit (**WI-1763**, gap G4) rides the Q6 ruling.
- Dogfood prod build (WI-1503) + closed beta (WI-1506) — **IN** (beta gates public launch).
- S3 Journal structural hygiene (WI-1134) — **FILL (ruled 2026-07-10).** PATH B: bless `milestoneCard.*` namespace (no 7-locale key-rename churn); split the 1106-line `JournalTabView.tsx`; relocate `AccountAvatar` to `components/chrome/`. Pre-cleans the surface WI-1620's Journal-parity work touches.
- Exact-birth-date age-gating mirrors, 3 mobile sites (WI-1259) — **FILL (ruled 2026-07-10).** Server gate already exact-date and authoritative; aligns client pre-checks so client/server never disagree on an age computation at birthday boundaries.
- V2 shell publish readiness (WS-28 M-chain, WI-1307 fallback proof) — **IN** (the live program). S6 V0/V1 retirement — **OUT** (deferred, irreversible). **F4 resolved 2026-07-10:** WI-1308's "retire before ship" wording struck; item rewritten to post-launch S6-milestone timing.
- Learner/supporter shared-surface architecture (WI-1620, Design) — **IN as MVP-window design work (ruled 2026-07-10; Blocked cleared).** Runs parallel to the supporter builds (WI-1127/1135/1137 are supporter-only, no learner twin, no conflict); output (shared core + audience adapters + guardrail tests + migration slices) governs all subsequent dual-audience surface work. Execution slices ruled individually when the design lands; Journal empty-state parity expected first.
- **Shell disambiguation (statement, not a question):** two "V2"s exist — the *nav-flag* V1/V2 mode shells (`navigation-contract.ts`, shipped behind flags) and the *mentor-is-the-app* 3-tab redesign (spec S0–S3 landed, S4/S5 partial, S6 TODO; production still V0). The MVP ships **the shell state the WS-28 publish-readiness chain proves** — V2-shell-minus-S6 with V0/V1 kept as the rollback channel (WI-1307). Any doc citing "the nav model" must say which. Billing quota numbers (Free 10/day+100/mo, Plus 700/mo) are project-memory-sourced, not identity canon — treat pricing as operator-confirmable at ratification.

## 13. Explicitly OUT (consolidated kill list)

Trial preview lesson · "coming up next" recaps · note-correctness umbrella · resumable-practice engine (copy fix shipped) · child top-ups + cap-banner actions · child-to-parent nudges · rotating greeting pool · concept-star as specced (re-homed per ratified-13) · RLS activation · S6 retirement · voice-first wholesale · answer-correctness chain (fast-follow) · Item 4-D1/D3 builds (fast-follow) · external supporter invite WI-1580-residual (fast-follow; the existing-teen family-join slice was split OUT of it into launch-IN WI-1753) · supporter co-learning WI-1136 (fast-follow).

---

## Open questions FOR RATIFICATION (the actual decision surface)

1. **Verified-learning-loop slices** (WI-1657 map) — **RULED 2026-07-10**, see §3: engine spine + one receipt loop IN, FILL for the substrate/second-wave items, umbrella retires.
2. **4-strands rework** — **RULED 2026-07-10**, see §6: language learning is launch-IN as a minimum credible course spine, with a visible continue path and one end-user learning loop; the full language-mastery architecture remains FILL/post-launch.
3. **Monetization captures** WI-1660/1661 — **RULED 2026-07-10**, see §8: both OUT/fast-follow (first post-launch monetization wave).
4. **WI-1662** mastery-gated progression — **RULED 2026-07-10**, see §5: gate on credit, not movement; never-lock doctrine stands untouched.
5. **AI Act cluster sizing** — **RULED 2026-07-10 (operator): pre-wired conditional**, see §10. Plan (WI-1659) + Art-50 disclosure (WI-1195) IN; technical file + QMS skeleton (WI-1663) and institutional-deployment tripwire (WI-1664) staged on the counsel classification answer (OUT if not-high-risk, launch-gating if high-risk); classification question escalated time-critical (2026-08-02 enforcement).
6. **Voice tension** — **RULED 2026-07-10**, see §6: baseline confirmed (voice-in everywhere + TTS + WI-1548 speaking slice); Epic-17 stays post-MVP with WI-1459; WI-1486 pulled to FILL.
7. Counsel-dependent forks (denial state, DPIA fix, crisis legal floor) — decided by counsel answers, pre-wired here.
8. **Crisis guardian-notification contradiction** — **RULED 2026-07-10 (operator): se-032 never-notify posture STANDS**, see §9. WI-1690's guardian-notification wording struck (supersedes the 2026-07-07 option-B wording); WI-1690 rescoped to detection + resources + operator telemetry. Counsel Q3 may add a legal-duty reporting channel later — designed with counsel, not pre-built.
9. **Age floor cementing** — **RULED 2026-07-10 (operator): 13+ confirmed as the LAUNCH floor**, see §10. Not a forever cap — the non-US 10–12 outside-COPPA expansion hope stays on the roadmap (2026-06-08 strategy unchanged). Policy + worksheet state 13+ now; store rating conforms to the floor; worksheet age-11 fix rides WI-1577.
10. **Launch family model** — **RULED 2026-07-10 (operator-confirmed): 13+ only at launch.** Credentialized 13+ accounts only; managed under-13 tier stays dormant; "family" at launch = parent + self-consenting 13+ teens via join-my-family (WI-1753) + opt-in Supportership. The counsel packet's "children are a core audience" framing means *13+ children* for MVP. **Operational consequences:** (a) closed-beta family recruitment must screen for families with 13+ teens — a family whose kids are all under 13 cannot exercise the family model; (b) WI-1185 (parents create/manage child-scoped subjects from child context) is a managed-tier feature → **OUT, defers with the dormant under-13 tier**.

### Post-launch open ends already tracked in canon (no ruling needed now, listed so they're not re-found)

VPC vendor + under-13 method · R13 guardian-attachment initiation · co-guardian one-of/all-of rule (counsel) · Family-Sharing Payer recording · below-floor teen join-my-family variant (all cited in `prd.md`/`ontology.md`/register as ROADMAP/counsel-owned).
