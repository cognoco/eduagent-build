# Rejected Candidates (verifier false-positive log)

> The 33 finder candidates the adversarial verifier rejected on independent re-read. Included for transparency — this is the find→verify pattern preventing plausible-but-wrong findings from reaching the inventory.

| File | Line | Literal | Why rejected |
|---|---:|---|---|
| `app/(app)/child/[profileId]/session/[sessionId].tsx` | 359 | your child | childDisplayName is passed to AddToMyLearningButton and forwarded to useCloneFromChild(). Inspection of use-clone-from-child.ts confirms childDisplayName is never referenced in an… |
| `app/(app)/child/[profileId]/topic/[topicId].tsx` | 265 | your child | Same pattern as session/[sessionId].tsx line 359 — childDisplayName is passed to AddToMyLearningButton → useCloneFromChild(), but inspection of use-clone-from-child.ts confirms th… |
| `app/(app)/dictation/complete.tsx` | 422 | Pick from gallery (E2E) | The entire Pressable containing this label is gated behind `isE2E` (process.env.EXPO_PUBLIC_E2E === 'true'). This is test-only infrastructure and will never render to real users i… |
| `app/(app)/dictation/complete.tsx` | 431 | Pick from gallery (E2E) | Same as line 422 — inside the isE2E-gated block. Test-only UI that never appears to real users. |
| `app/(app)/quiz/results.tsx` | 175 | config.title (Perfect round! / Great round! / Nice effort!) | Not an actual literal at this line — it is a variable reference (config.title). The real hardcoded literals are at lines 72, 77, 82. This candidate is a duplicate misidentificatio… |
| `app/(app)/quiz/results.tsx` | 181 | config.title (Perfect round! / Great round! / Nice effort!) | Not a literal at this line — {config.title} is a variable expression. The actual string literals are at lines 72, 77, 82. Finder mis-labelled a variable as a literal. |
| `app/(app)/shelf/[subjectId]/book/[bookId].tsx` | 1340 | Loading notes... | Hardcoded static prop in the loading skeleton (loading state only), same string as line 579 which is already flagged as a violation. This duplicate in the loading stub is rendered… |
| `app/(app)/topic/[topicId].tsx` | 119 | session / sessions | These are partial words ('session' and 'sessions') used in a pluralization ternary inside a helper function, not standalone user-visible strings. The full formatted string at line… |
| `app/create-profile.tsx` | 549 | Cancel | "Cancel" exists in en.json as common.cancel. While this file does not call t('common.cancel') explicitly, the key exists and the string matches — marking as no novel violation as … |
| `app/create-profile.tsx` | 656 | Done | "Done" already exists in en.json as common.done. This is a common word that maps to an existing key; not a novel gap even though the file doesn't call t() here. |
| `app/create-profile.tsx` | 692 | Continue | "Continue" already exists in en.json as common.continue. Not a novel gap even though the file doesn't call t() here. |
| `app/create-subject.tsx` | 861 | Ancient Egypt | This is a starter-chip topic suggestion string (STARTER_CHIPS constant), rendered as a tappable content label suggesting a learning subject. Topic/content names are not UI chrome … |
| `app/dev-only/seed-pending-redirect.tsx` | 100 | Seeding pending redirect (path= | This file is under dev-only/ path — it is a developer/test-seeding screen not shown in production. The CLAUDE.md explicitly lists dev-only/ as excluded from i18n requirements. |
| `app/dev-only/seed-preview-state.tsx` | 115 | Loading... | This file is under dev-only/ path — it is a developer/E2E-seeding screen gated behind IS_E2E_BUILD, not shown in production. Exempt per CLAUDE.md dev-only exclusion. |
| `app/dev-only/seed-preview-state.tsx` | 128 | Seeding preview state... | This file is under dev-only/ path — it is a developer/E2E-seeding screen gated behind IS_E2E_BUILD, not shown in production. Exempt per CLAUDE.md dev-only exclusion. |
| `app/preview/both.tsx` | 125 | {opt.label} | This is a JSX expression interpolation {opt.label}, not a string literal. The finder incorrectly flagged the JSX expression as a literal. The actual string values are in the OPTIO… |
| `app/preview/both.tsx` | 128 | {opt.description} | This is a JSX expression interpolation {opt.description}, not a string literal. The finder incorrectly flagged the JSX expression as a literal. The actual string values are in the… |
| `app/session-transcript/[sessionId].tsx` | 272 | MentoMate | MentoMate is the product brand name. Brand names are conventionally not translated. Marking as non-violation consistent with the brand-fixed text exception. |
| `components/MentomateLogo.tsx` | 65 | Mentomate | MentomateLogo.tsx is explicitly listed in CLAUDE.md as a brand-fixed component where hardcoded strings are permitted. The accessibilityLabel names the brand logo — this is brand-f… |
| `components/MentomateLogo.tsx` | 71 | ment | MentomateLogo.tsx is explicitly listed in CLAUDE.md as a brand-fixed component where hardcoded strings are permitted. 'ment' is the first half of the brand wordmark — it must not … |
| `components/MentomateLogo.tsx` | 97 | mate | MentomateLogo.tsx is explicitly listed in CLAUDE.md as a brand-fixed component where hardcoded strings are permitted. 'mate' is the second half of the brand wordmark — it must not… |
| `components/common/BookPageFlipAnimation.tsx` | 471 | Loading content | BookPageFlipAnimation.tsx matches the *Animation.tsx pattern listed in CLAUDE.md as a brand-fixed animation component where hardcoded strings are permitted. |
| `components/common/CheckmarkPopAnimation.tsx` | 119 | Success | CheckmarkPopAnimation.tsx matches the *Animation.tsx pattern listed in CLAUDE.md as a brand-fixed animation component where hardcoded strings are permitted. |
| `components/common/DeskLampAnimation.tsx` | 204 | Thinking | DeskLampAnimation.tsx matches the *Animation.tsx pattern listed in CLAUDE.md as a brand-fixed animation component where hardcoded strings are permitted. |
| `components/common/LightBulbAnimation.tsx` | 304 | Thinking | LightBulbAnimation.tsx is an *Animation component — CLAUDE.md explicitly permits brand-fixed text in *Animation components. The label is on a decorative accessibilityRole='image' … |
| `components/common/MagicPenAnimation.tsx` | 584 | Writing animation | MagicPenAnimation.tsx is an *Animation component — CLAUDE.md permits brand-fixed text there. The label is on a decorative accessibilityRole='image' View and is never rendered as v… |
| `components/common/MagicPenAnimation.tsx` | 725 | Writing animation | Same as line 584 — second render path in MagicPenAnimation.tsx (*Animation component, decorative accessibilityRole='image'). |
| `components/common/RewardBurst.tsx` | 262 | Solid work / Correct / Nice / Solved / Strong / Got it | This is not a single literal — the finder conflated the six variant labels into a slash-separated description. The actual render expression is `{message ?? config.label}`, which d… |
| `components/common/celebrations/CelestialCelebration.tsx` | 75 | Celebration animation | The CLAUDE.md exemption covers *Celebration.tsx components. While the exemption text specifically mentions brand-fixed hex values, this component is a decoration-only overlay (poi… |
| `components/library/BookCard.tsx` | 79 | STATUS_LABELS[status] | This is a JSX expression {STATUS_LABELS[status]}, not a string literal. The actual hardcoded strings are the values in the STATUS_LABELS object at lines 21-24, which are already f… |
| `components/library/BookCard.tsx` | 90 | progressLabel | This is a JSX expression {progressLabel}, not a string literal. The actual hardcoded strings are the 'Ready to open' / 'Build this book' literals at lines 35-36, which are already… |
| `components/progress/SubjectProgressRow.tsx` | 175 | Continue | This is not a literal 'Continue' in JSX — the rendered value is {ACTION_LABEL[action]}, which is a dynamic expression. The finder incorrectly attributed the literal from the ACTIO… |
| `components/tell-mentor-input.tsx` | 57 | Examples really help me understand fractions. | Same literal as line 47 — already flagged there. This is a duplicate placeholder string used for a different audience variant but is otherwise identical in content and treatment. … |

