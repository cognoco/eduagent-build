# Memory Evidence Package — Action 5 (memory-relevant elements): Article 9 position and suppression/exclusion evidence

**Controller:** ZWIZZLY AS, Oslo, org.nr 811 696 072
**Product:** MentoMate
**Drafted:** 2026-08-07 (agent draft for operator and DPO review)
**Revised:** 2026-08-08 — Gaps 1 and 2 only, following WI-3142 (Art 9 suppression-corpus extension) and the operator ruling of the same date that persistent memory is English-only at unlock. Statements elsewhere in the document that the corpus covered health/disability alone were corrected in the same pass, so the narrative and the gaps cannot disagree.
**Repository state examined:** working tree of the MentoMate monorepo as of 2026-08-07, re-verified 2026-08-08 for the sections revised. All file:line citations refer to files opened and read during drafting. The 2026-08-07 draft reported the state of the codebase without modifying it; the 2026-08-08 revision accompanies a change-set that does modify it (WI-3142), and every claim added in that revision cites code landed in the same change-set.

---

## Element scope

Defined by the DPO (Stephan Hartmann), 2026-08-07, verbatim:

> "the memory-relevant elements of Action 5, particularly the Article 9 position and evidence that sensitive or safeguarding-related content is suppressed or excluded from persistent memory where required"

Context: the action register (`docs/compliance/DPO exchanges/2026-07-26-action-register-tracker.md`, Action 5 row and the interim-conditions section) records the interim condition that persistent memory and profiling remain disabled until the memory evidence package is approved, and the 2026-08-07 confirmation that the unlock set includes "the memory-relevant elements of A5 — Art 9 position + evidence that sensitive/safeguarding content is suppressed or excluded from persistent memory where required". The crisis/escalation procedure as a standalone document is the remainder of Action 5 and is outside this slice; it is touched on below only where the safeguarding path intersects the memory write path.

## Current position

MentoMate's persistent memory consists of (i) a per-learner learning profile (`learning_profiles` table: interests, strengths, struggles, communication notes, learning style), (ii) derived `memory_facts` rows with vector embeddings, (iii) per-session summaries, and (iv) per-session transcript embeddings with a stored text copy. The profile fields are populated by an LLM transcript-analysis step that emits free-text signals — including verbatim `topic` strings derived from the child's conversation — which are stored as-is after gating (`apps/api/src/services/learner-profile.ts:401-408`, `topic: signal.topic.trim()`). The Article 9 exposure is therefore real and foreseeable: free text derived from a child's free conversation enters persistent storage.

A server-side suppression mechanism **does exist today** for the highest-risk category. Every write to the profile fields and to `memory_facts` passes through the persisted-learning-text safety gate (WI-2628, MMT-ADR-0036: `apps/api/src/services/learning-text-safety/scan.ts`, wired into the write transaction at `apps/api/src/services/learner-profile.ts:1692-1809`). The gate deterministically blocks person-attributed clinical/diagnostic text (block reasons `person_attribution`, `diagnostic_inference`, fail-closed `unclear`; `scan.ts:82-85`), is enforced in the database transaction with a coverage check (not merely instructed to the LLM), fails closed on ambiguity for LLM-authored text, and blocked items feed an operator digest (`apps/api/src/services/blocked-safety-digest.ts`). A retroactive remediation path for already-persisted rows also exists (WI-2753: `apps/api/src/services/learning-text-safety/persisted-remediation-memory.ts`). Separately, a deterministic special-category redaction filter runs at every learner-data egress boundary — all LLM provider calls (`apps/api/src/services/llm/router.ts:1840, 2378`) and the Voyage embedding call (`apps/api/src/services/embeddings.ts:170`) — replacing first-person special-category disclosures with `[sensitive personal information removed]` (`apps/api/src/services/learner-egress-filter.ts:4-13, 54-81`).

The honest limits of that position are threefold. First — **substantially narrowed since the 2026-08-07 draft, and this is the one change of substance in this revision** — the write-gate's protected-lexeme corpus originally covered health/disability/diagnostic terms only. WI-3142 extended it to the remaining enumerated Article 9 domains: racial or ethnic origin, political opinions, religious and philosophical beliefs, trade-union membership, sex life and sexual orientation, and (narrowly) genetic data. Biometric data is deliberately not represented, on the reasoning that Article 9 defines it by the identification *purpose* rather than by any vocabulary a learner would type (`apps/api/src/services/learning-text-safety/corpus.ts:24-69`, scope-policy block). The residual limit is one of **language, not category**: only the English set is authored and expert-reviewed; the other nine corpora are LLM-prepared, marked `model-generated`, and are not relied on as a compliance control. Per the operator ruling of 2026-08-08, **persistent memory is English-only at unlock**, so the reviewed-language limit and the enabled-language set now coincide (Gaps 1 and 2). Second — **also closed since the draft, by WI-3140** — a safeguarding disclosure that trips the deterministic safety tripwire now sets a session-level marker that skips both memory-producing steps; what remains is that the disclosure is still *captured* as an ordinary transcript event on the 30-day purge clock, which the firewall deliberately does not change (details in §Suppression and exclusion analysis and Gap 3). Third, the stored `session_embeddings.content` column retains the raw, unfiltered conversation text (the egress filter applies only to the payload sent to Voyage), bounded by the 30-day transcript purge. These are recorded as findings, not papered over.

Operationally, persistent memory collection is disabled by default and consent-gated in code: `memory_consent_status` defaults to `'pending'` and `memory_collection_enabled` defaults to `false` (`packages/database/src/schema/learning-profiles.ts:36-46`), and both the analysis-transmission step and the profile write refuse unless consent is `'granted'` and collection is enabled (`apps/api/src/inngest/functions/session-completed.ts:1722-1748`; `apps/api/src/services/learner-profile.ts:1649-1662, 1742-1747`). This is consistent with the interim condition that memory and profiling stay disabled pre-approval. [GAP: this section cites the code defaults; a production-database verification that no learner row has `memory_consent_status = 'granted'` should be captured as a dated evidence artifact before submission — proposed remedy: one-off read-only query against production with screenshot/record. Note the estate memory records zero users pre-launch, which if verified makes this trivially true.]

## Article 9 position (memory-specific)

**Categories that can incidentally arise.** In a free-text mentoring conversation with a child, all Article 9 categories are foreseeable in incidental disclosure or inference: health and disability (including learning-disability and mental-health inferences — the category the product is structurally most likely to *generate*, since the analysis step characterises how a learner learns), religious or philosophical beliefs, sexual orientation and sex life, racial or ethnic origin, political opinions. Data concerning a child's exposure to abuse (a safeguarding disclosure) is not itself an enumerated Art 9 category but is treated in this package as safeguarding-sensitive content of at least equivalent gravity; it can also carry Art 9 content (e.g. health consequences).

**What the current position document says.** `docs/compliance/art9-special-category-position.md` (status: "Proposed controller position for DPO/privacy-counsel advice", as of 2026-07-24) states that MentoMate does not ask for or intend to use special-category data; acknowledges that incidental disclosure, inference, transmission and temporary storage "are … foreseeable and must not be described categorically as impossible"; and proposes safeguards including to "exclude sensitive characterisations from persistent learning memory, profiling, assessment, and guardian-facing summaries" and to "handle genuine crisis content through a separate safeguarding procedure". It draws the distinction between *discussing* a sensitive topic and creating a *sensitive characterisation of the learner* ("this learner is likely dyslexic" being outside product purpose). The historical "no Article 9 processing" decision is superseded and retained only as provenance (`docs/compliance/art9-special-category-position.md`, final paragraph).

**The exclude-from-memory safeguard is implemented for every enumerated category in English.** The proposed exclusion is server-side enforcement at the storage boundary (the persisted-learning-text gate, evidence below), and since WI-3142 its protected-lexeme corpus spans health/disability, racial or ethnic origin, political opinions, religious and philosophical beliefs, trade-union membership, sex life and sexual orientation, and genetic data. The position document's exclusion claim should therefore be presented to the DPO as *implemented at the storage boundary for all enumerated categories, in English; prepared but unreviewed in the other nine conversation languages, which are out of scope for the unlock because memory is English-only at unlock* — see Gaps 1 and 2. Biometric data is excluded by a documented judgment rather than an oversight (Gap 1).

**Art 9(2) condition — open.** The position document expressly asks the DPO to determine (1) whether the intended-use vs incidental-disclosure distinction is sound, (2) whether foreseeable incidental handling constitutes Article 9 processing at all, and (3) "which Article 9 condition, if any, is required" (`art9-special-category-position.md`, "Advice required"). No Art 9(2) condition has been adopted by the controller. No condition is asserted in this package. If the DPO concludes a condition is required for the incidental/transient handling, the realistic candidates on the current design are Art 9(2)(a) (explicit consent — problematic for children and for data the user did not set out to provide) or a minimisation-based position that the processing is limited, unintended, suppressed at the persistence boundary, and time-bounded (30-day raw-content purge, §below); the choice is precisely the advice requested. [GAP: Art 9(2) condition determination — this is the DPO's deliverable under Action 5; this package supplies the factual substrate.]

## Evidence inventory

| # | Claim | Evidence | Status |
|---|-------|----------|--------|
| 1 | Art 9 position document exists; supersedes earlier "no Art 9 processing" decision; requests DPO determination of the Art 9(2) condition | `docs/compliance/art9-special-category-position.md` (dated 2026-07-24; status "Proposed") | Exists (thin — position proposed, condition open) |
| 2 | Persistent memory collection is off by default and consent-gated in code | `packages/database/src/schema/learning-profiles.ts:36-46` (defaults `'pending'` / `false`); write gate `apps/api/src/services/learner-profile.ts:1742-1747`; transmission gate `apps/api/src/inngest/functions/session-completed.ts:1722-1748` | Exists |
| 3 | Verbatim LLM-derived free text (topic strings, interests, communication notes) enters persistent profile storage | `apps/api/src/services/learner-profile.ts:346-351, 401-408, 422-428` (`signal.topic.trim()` etc.); prompt emitting the signals at `learner-profile.ts:77-124` | Exists (this is the exposure the suppression evidence must answer) |
| 4 | Server-side, transaction-enforced gate blocks person-attributed special-category text from profile fields AND memory_facts before write | `apps/api/src/services/learning-text-safety/scan.ts` (deterministic scan; block reasons `scan.ts:82-85`; fail-closed matrix per header comment); wiring: `apps/api/src/services/learner-profile.ts:1692-1809` (COLLECT/ENFORCE passes, in-transaction coverage check, sanitised projection written at 1795-1809) | Exists (all enumerated Art 9 domains except biometric — see row 5 for the language limit) |
| 5 | Gate corpus: English set authored and expert-reviewed across all six domains; 9 other languages LLM-prepared, marked `model-generated`, flagged as requiring native-speaker review | `apps/api/src/services/learning-text-safety/corpus.ts:24-69` (WI-3142 scope policy: why the new domains default to attribution-scoped detection, which terms were deliberately excluded, and why biometric data is not lexeme-expressible), `corpus.ts:108-111` (`LexemeScope`), `corpus.ts:147-442` (`const EN`, `confidence: 'reviewed'`). Generation provenance: `docs/compliance/memory-unlock-package/evidence/2026-08-08-art9-corpus-generation-<lang>.json`, one per language, carrying the model id, the exact prompts, the raw responses and a per-term homograph analysis | Partial by language, not by category (EN reviewed; cs/de/es/fr/it/ja/nb/pl/pt unreviewed — and per the 2026-08-08 ruling, memory is English-only at unlock) |
| 5a | Detection is **attribution-scoped**, so mentioning a special category in schoolwork is not a block — only characterising a person is | `corpus.ts:93-107` (`unnamed-attribution-only`); `apps/api/src/services/learning-text-safety/scan.ts:237` (policy assignment), `scan.ts:427-436` (the bare-capitalized-name heuristic is suppressed for this scope, so `Martin Luther was Catholic.` in a history note is `clear` while `The learner is Catholic.` blocks). Both directions are test-pinned per domain and per language in `scan.test.ts` | Exists |
| 6 | Gate blocks are observable (counts, no content) via operator digest | `apps/api/src/services/blocked-safety-digest.ts` (daily buckets + digest email) | Exists |
| 7 | Retroactive remediation of already-persisted clinical text across memory_facts and topic_notes | `apps/api/src/services/learning-text-safety/persisted-remediation-memory.ts` (WI-2753) | Exists |
| 8 | Special-category redaction at every learner-data egress boundary (all LLM providers + Voyage embeddings) | `apps/api/src/services/learner-egress-filter.ts:4-13` (cue + term regexes covering health, religion, sexual orientation, ethnicity, political, genetic/biometric), `:54-81`; applied at `apps/api/src/services/llm/router.ts:1840, 2378` and `apps/api/src/services/embeddings.ts:170` | Exists (egress minimisation, not storage exclusion; English-only regexes; educational-context exemption) |
| 9 | Deterministic safety tripwire detects catastrophic categories (self-harm method-seeking, sexual content re minors, first-person abuse disclosure) pre-LLM and short-circuits the exchange | `apps/api/src/services/safety-tripwire.ts:48-172` (categories + patterns); `apps/api/src/services/exchanges.ts:1897-1934` (input + image screening, short-circuit) | Exists |
| 10 | Tripwire/crisis events are recorded as **metadata only** — no disclosure content in the safety-event log, operator alarm, or telemetry | `apps/api/src/services/exchanges.ts:100-145` (`emitCrisisRedirectEvent`: correlation id, flow, session/profile ids; comments "Metadata only", "no disclosure content") | Exists |
| 11 | Tripwire-hit conversations are excluded from persistent memory | **Implemented, WI-3140.** Session-level marker `learning_sessions.safety_flagged_at` written on any tripwire hit or `crisis_redirect` turn (`apps/api/src/services/safety-flag.ts`; call sites `apps/api/src/services/session/session-exchange.ts:4405-4411, 4826-4834, 4949-4957`); a non-null marker skips the whole transcript-analysis step (`apps/api/src/inngest/functions/session-completed.ts:1750-1774`, before the event read at `:1776`) and the whole embedding step (`:2107-2128`). Regression-proved end-to-end in `apps/api/src/inngest/functions/session-completed-safety-firewall.integration.test.ts`. | Exists — the learner's disclosure message is still persisted as an ordinary `session_events` row (`session-exchange.ts:4005-4021`) on the 30-day purge clock (row 14); the firewall governs *memory*, not capture |
| 12 | LLM analysis prompt instructs suppression of sensitive content | **Not present.** `SESSION_ANALYSIS_PROMPT` (`apps/api/src/services/learner-profile.ts:77-124`) instructs exclusion only of `{suppressedTopics}` — topics a parent/learner has explicitly hidden — and conservative signal extraction; it contains **no instruction** to omit health/religion/sexuality/safeguarding content. (Noted for completeness; a prompt instruction would in any case be prompt-level, not enforcement — the enforcement is row 4.) | **GAP** (defence-in-depth; see Gap 4) |
| 13 | Parent/learner-driven per-topic suppression list, honoured server-side on every merge and fed to the analysis prompt | `learning_profiles.suppressed_inferences` (`packages/database/src/schema/learning-profiles.ts:30`); enforced in `mergeInterests`/`mergeStrengths`/`mergeStruggles`/`mergeCommunicationNotes` (`apps/api/src/services/learner-profile.ts:247-255, 301-306, 371-375, 419-425`); suppress-on-delete `:775-781`; fed to prompt `apps/api/src/inngest/functions/session-completed.ts:1790-1823` | Exists (user-choice mechanism, complements — does not substitute for — content-based exclusion) |
| 14 | Raw conversation content is time-bounded: 30-day transcript purge live in production; session embeddings rebuilt from summary text at purge | `docs/compliance/evidence/2026-07-26-retention-schedule-draft.md` §3.2 and rows 1/6 (code-verified against `apps/api/src/inngest/functions/transcript-purge-cron.ts` and `apps/api/src/services/transcript-purge.ts`) | Exists (per the retention draft; full retention treatment is the Action 6 section) |
| 15 | Raw transcript text is embedded and a raw text copy is stored alongside the vector | `apps/api/src/services/embeddings.ts:242-301` (`extractSessionContent` → `storeSessionEmbedding(… content …)`); `packages/database/src/schema/embeddings.ts:31` (`content: text('content').notNull()`). The egress filter (row 8) applies to the Voyage payload (`embeddings.ts:170`) but the **stored** `content` is the pre-filter text. | Exists — **finding** (see Gap 5) |
| 16 | Embedding storage is gated on memory consent | **Not gated on memory consent.** The embedding step checks GDPR/LLM-disclosure consent only (`apps/api/src/inngest/functions/session-completed.ts:2058-2092`), not `memory_consent_status`/`memory_collection_enabled` — unlike the profile-analysis step (`:1722-1748`) | **GAP** (see Gap 6) |
| 17 | Guardian-facing struggle notifications pass the same content gate | `apps/api/src/services/learner-profile.ts:1791-1794` (`filterSafeStruggleNotifications` over the gate verdicts) | Exists |

## Suppression and exclusion analysis

This section gives the write-path account requested by the DPO: what enters persistent memory, from where, and what filtering exists at each stage. Throughout, **prompt-level instruction** (asking an LLM to behave) is distinguished from **server-side enforcement** (deterministic code that decides what persists regardless of LLM behaviour). Only the latter is claimed as a control.

**Stage 0 — capture.** Every learner message and mentor reply in a session is persisted as a `session_events` row (`apps/api/src/services/session/session-exchange.ts:4005-4021`). There is no content-based filtering at capture; the transcript is the raw record of the conversation. Raw transcripts are purged on a 30-day clock (evidence row 14; owned by the Action 6 section).

**Stage 0a — the safety tripwire (safeguarding path).** Before any LLM call, `detectCatastrophicSafetyTrigger` (`apps/api/src/services/safety-tripwire.ts:180-190`) screens the input (and OCR-extracted image text, `exchanges.ts:1912-1934`) for three catastrophic categories: self-harm method-seeking/intent, sexual content involving a minor, and first-person abuse disclosure. On a hit the exchange short-circuits to a deterministic crisis-redirect reply (helpline, trusted adult; `safety-tripwire.ts:221-253`) and a safety event is emitted. The safety event pipeline is content-free by design: log line, Sentry operator alarm, and telemetry event carry correlation id, flow, session/profile ids only — "Metadata + profileId-scoped pointers ONLY — no disclosure content" (`exchanges.ts:100-145`). The safety event itself therefore contributes nothing sensitive to persistent storage. Additionally, the conversational prompt binds a `crisis_redirect` envelope signal for model-detected safeguarding concerns (`apps/api/src/services/exchange-prompts.ts:378, 800-808`) — that signal is observational telemetry, likewise content-free at emission.

**The tripwire now creates a memory exclusion (WI-3140).** Detection stamps the session-level marker `learning_sessions.safety_flagged_at`, and the session-completion pipeline skips both memory-producing steps when it is set: transcript analysis returns before the event read (`session-completed.ts:1750-1774`, ahead of `:1776`) and the embedding step returns before anything reaches Voyage or `session_embeddings` (`:2107-2128`). Exclusion of safeguarding content from persistent memory therefore no longer rests only on the downstream gates (Stages 2-3, which target diagnostic characterisations): there is now a deterministic firewall at the safeguarding-detection point. What the firewall does NOT do is un-capture the conversation — the learner's disclosing message remains a `session_events` row until the 30-day purge. See Gap 3 for the full write/enforcement map and the regression guard.

**Stage 1 — transcript analysis (LLM, prompt level).** At session completion, if and only if memory consent is `'granted'`, collection is enabled, and GDPR/LLM-disclosure consent is intact (`session-completed.ts:1722-1748` — the gate deliberately sits before transmission, not just before the write), the transcript is sent for analysis. Egress passes the special-category redaction filter at the router (`router.ts:1840, 2378`; filter at `learner-egress-filter.ts:54-81`): first-person disclosure cues combined with special-category terms (health, religion, sexuality, ethnicity, political, genetic/biometric) are replaced with `[sensitive personal information removed]`, with an educational-context exemption so schoolwork about these topics is not destroyed. This is server-side, deterministic — but it is an **egress/transmission minimisation** (English-pattern-based), not a storage control. The analysis prompt (`learner-profile.ts:77-124`) then instructs conservative extraction of interests/strengths/struggles/communication notes and exclusion of `{suppressedTopics}`. The prompt contains no instruction regarding sensitive categories (Gap 4); and even if it did, a prompt instruction is not enforcement and is claimed as nothing more than defence-in-depth.

**Stage 2 — the profile/memory write (server-side enforcement — the operative control).** `applyAnalysis` (`learner-profile.ts:1614-1830`) is the sole write path from analysis output to persistent memory. It (a) re-checks GDPR consent outside and inside the transaction (`:1659-1662, 1731-1740`) and the memory-consent/collection gate (`:1742-1747`); (b) merges the LLM signals honouring the parent/learner `suppressed_inferences` list (evidence row 13); and (c) passes **every candidate string** — profile fields, struggle notifications, and the derived `memory_facts` row texts — through the persisted-learning-text gate before anything is written (`:1692-1722` evaluation; `:1769-1789` in-transaction coverage checks; `:1773-1809` only the sanitised projection and gated fact rows are persisted). The gate itself (`scan.ts`) deterministically blocks person-attributed clinical text (`person_attribution`, `diagnostic_inference`), routes user-authored ambiguity to an independent judge whose pool excludes the producing vendor, and **fails closed** (`block`, reason `unclear`) for LLM-authored text with missing producer identity and for migration ambiguity. Blocks are counted into an operator digest without content (`blocked-safety-digest.ts`). This is the concrete implementation of the position document's "exclude sensitive characterisations from persistent learning memory" — since WI-3142 spanning **every enumerated Article 9 domain except biometric** (the corpus, evidence rows 5 and 5a), with authored, expert-reviewed coverage in English only (Gaps 1-2).

**Stage 3 — embeddings.** Two distinct stores. (i) `memory_facts.embedding`: vectors are generated only for fact rows that survived the Stage 2 gate (`session-completed.ts:151-178` embeds existing `memory_facts` rows), and the Voyage payload passes the egress filter (`embeddings.ts:159-183`). (ii) `session_embeddings`: the raw session transcript (up to 8,000 chars of user + mentor turns) is embedded per session; the Voyage payload is egress-filtered (`embeddings.ts:170`), but the row **stores the pre-filter raw text** in its `content` column (`embeddings.ts:285-301`; `packages/database/src/schema/embeddings.ts:31`) and the step is gated on GDPR consent but **not** on memory consent (`session-completed.ts:2058-2092`) — Gaps 5-6. At the 30-day transcript purge the raw-content embedding row is replaced by a summary-derived one (retention draft §3.2), which time-bounds but does not eliminate the exposure.

**Stage 4 — read-back into prompts.** Memory is injected into future mentor prompts only when consent is `'granted'` and injection is enabled (`buildMemoryBlock`, `learner-profile.ts:848-853`), so a suppression at Stage 2 also suppresses all downstream surfacing to the child and (via the gated struggle notifications, evidence row 17) to guardians.

**Summary of the account.** Server-side enforcement exists at the persistence boundary for special-category characterisations across every enumerated Article 9 domain except biometric (transaction-enforced, fail-closed, observable, with retroactive remediation), plus deterministic special-category redaction at every external-transmission boundary, plus consent gating that keeps the entire collection path off by default. What does not yet exist: native-speaker-reviewed corpora in the nine non-English conversation languages (bounded for the unlock by the English-only ruling); and memory-consent gating plus filtered storage on the session-embedding path. The safeguarding-to-memory firewall, open at the time of the draft, was closed by WI-3140 (Gap 3).

## Gaps and proposed remediation

Numbered findings; "proposed" throughout — none of these remedies is implemented unless cited above. Tracking IDs are given where a repo artifact names one; otherwise marked for capture.

1. **Write-gate corpus covered health/disability only — CLOSED for English by WI-3142 (Art 9 suppression-corpus extension).** The corpus now carries person-attributable status terms for racial or ethnic origin, political opinions, religious and philosophical beliefs, trade-union membership, sex life and sexual orientation, and genetic data, alongside the original health/disability set. Implementation: `apps/api/src/services/learning-text-safety/corpus.ts:24-69` (scope policy), `corpus.ts:147-442` (the authored English set), `apps/api/src/services/learning-text-safety/scan.ts:237, 427-436` (detection policy).

    Three judgments in that extension are the DPO's to accept or challenge, so they are stated rather than buried:

    - **Detection is attribution-scoped, not mention-scoped.** The new terms are matched only inside a construction that attributes the status to a person — `the learner is X`, `her X`, and the Japanese topic/possessive particles — never on a bare mention. Treating them like the health terms (which fire on mention alone, then go to the judge) would route essentially all history, civics and religious-education homework through an LLM judge. This implements the position document's own discussion-vs-characterisation distinction at the detector rather than at the judge.
    - **Attribution to a bare capitalized name does not fire for these domains.** `The learner is Catholic.` blocks; `Martin Luther was Catholic.` does not. The health domains keep the name heuristic (`Petr has dyslexia.` still blocks). The residual exposure is a third party named in learner text whose status is asserted with a proper name and no pronoun; that was judged the better trade against hard-blocking, with no judge appeal, the commonest sentence shape in history homework.
    - **Biometric data is not represented, deliberately.** Article 9 covers biometric data processed *for the purpose of uniquely identifying* a person. That is a property of the processing, not of any vocabulary a learner types; "fingerprint" and "facial recognition" are computing-curriculum topics whose attributed forms are not disclosures. Genetic data is carried only through multi-word, inherently disclosure-shaped phrases ("DNA test result", "genetic screening"). MentoMate performs no biometric identification, so there is no processing for the storage gate to suppress.

    Terms whose attributed form is ordinary prose more often than a disclosure were excluded and the exclusions are pinned by test: bare colour and continent adjectives, "conservative"/"liberal", "orthodox", "straight", "asexual" (core biology), bare "union", "coming out". Note the corpus necessarily contains pejorative ethnic identifiers: it is a detector, and a slur used to characterise a learner is exactly what must not be persisted.

2. **Nine of ten language corpora are unreviewed — BOUNDED, not closed.** Operator ruling, 2026-08-08: **persistent memory is English-only at unlock.** The nine non-English corpora were nonetheless prepared best-effort by LLM generation so that native-speaker review has a concrete artifact to review; they remain marked `confidence: 'model-generated'`, are explicitly **not load-bearing as a compliance control**, and **native-speaker review is a precondition for enabling persistent memory in any non-English conversation language**. The reviewed-language limit and the enabled-language set therefore coincide at unlock, which is what makes this bounded rather than open.

    Provenance is on file so the DPO can weigh an unreviewed artifact for what it is: one dated JSON per language under `docs/compliance/memory-unlock-package/evidence/2026-08-08-art9-corpus-generation-<lang>.json`, each carrying the model (`google/gemini-2.5-pro` via OpenRouter), the exact system and per-domain prompts, the raw model responses, a per-term homograph/false-positive analysis, and the accept/reject decision for every candidate. The generator is committed and re-runnable at `scripts/generate-art9-corpus.mjs`. No model recommendation can widen detection on its own: every generated term is applied at the attribution-scoped default, and promotion to mention-scope detection is a human decision recorded in code.

    Generation used Gemini on operator instruction. This is offline corpus authoring — no learner data, profile or transcript is sent — and is therefore outside the MMT-ADR-0014 exclusion of Gemini from serving learners under 18, which governs the product's learner-facing inference path.

    Two residual limits are recorded rather than smoothed over. First, the generated sets have had no native-speaker review, so both misses and false positives should be assumed present. Second, Japanese needed post-generation curation before it was fit to ship at all: CJK terms are matched without word boundaries and the Japanese attribution window spans 24 characters, so generic category nouns became substring traps — 民族 ("ethnic/folk") blocked "私は民族音楽が好きです" ("I like folk music"), and バイ ("bi") matched inside バイオリン ("violin"). Thirteen such terms were removed, each with its reason, in the generator's curation list; the removals are pinned by test. That class of defect is the clearest available evidence of what native-speaker review is still expected to find in the other eight languages. [Tracking: capture native-speaker review as a Cosmo WI per language before any non-English memory unlock.]
3. **Tripwire-to-memory firewall (was the headline finding) — CLOSED, WI-3140.** *Was:* a first-person abuse disclosure or self-harm statement that fired the tripwire was still included in the analysis input sent to the LLM (redacted only insofar as the English egress regexes catch it) and still stored verbatim in `session_embeddings.content`; exclusion from *profile* memory depended on the analysis LLM not emitting it as a signal and the Stage 2 gate catching clinical attributions — neither a safeguarding-specific control. *Now:* a session-level, server-side, deterministic firewall. **Marker:** `learning_sessions.safety_flagged_at` (`packages/database/src/schema/sessions.ts:159-172`; migration `apps/api/drizzle/0169_crazy_wildside.sql`) — a nullable timestamp and nothing else, content-free by design, so recording *that* a disclosure happened never re-persists *what* was said (same metadata-only constraint as `emitCrisisRedirectEvent`, ruling se-032). **Write sites** (`apps/api/src/services/safety-flag.ts` → `flagSessionSafety`, `profileId`-scoped per PRIN-04), called from `apps/api/src/services/session/session-exchange.ts:4405-4411` (non-streaming) and `:4826-4834, 4949-4957` (streaming — the deterministic leg is stamped before the stream is handed to the caller, so a mid-stream disconnect cannot lose it; the model-layer leg once the envelope is parsed), covering both detector families: the deterministic tripwire (text, image-OCR screen, and the unscreenable-image fail-safe, surfaced as `ExchangeResult.safetyFlagged` / `ExchangeStreamResult.safetyFlagged` in `apps/api/src/services/exchanges.ts`) and the model-layer `signals.crisis_redirect` envelope signal. **Enforcement sites** — a fresh in-step read of the marker skips the entire step: `analyze-learner-profile` before the transcript is read or sent to the LLM (`apps/api/src/inngest/functions/session-completed.ts:1750-1774`) and `generate-embeddings` before anything reaches Voyage or the index (`:2107-2128`); each emits a structured `safety.*_skipped` log line. **Regression guard:** `apps/api/src/inngest/functions/session-completed-safety-firewall.integration.test.ts` — a flagged session run through the real pipeline against a real database yields zero profile-memory writes, zero `memory_facts` rows and zero `session_embeddings` rows, while an unflagged control still produces both; a companion negative-path case proves the marker cannot be written across profiles. Detector coverage is pinned in `apps/api/src/services/exchanges.safety-flag.test.ts`. This evidences the position document's "handle genuine crisis content through a separate safeguarding procedure" as applied to memory. *Still open (deliberately out of WI-3140 scope):* the flagged session's raw transcript remains in `session_events` on the ordinary 30-day purge clock — shortening the retention clock for flagged sessions was the optional layer (b) and is not implemented.
4. **Analysis prompt gives no sensitive-content instruction.** Defence-in-depth only (enforcement is Stage 2), but cheap: add an explicit instruction to `SESSION_ANALYSIS_PROMPT` to never emit signals characterising health, disability, religion, sexuality, ethnicity, politics, or safeguarding status, mirroring the position document's discussion/characterisation distinction. Prompt changes trigger the eval-harness obligation (PRIN-10). [GAP: no tracking ID — capture.]
5. **Raw pre-filter transcript text stored in `session_embeddings.content`.** The egress filter protects the Voyage transmission but the stored copy is raw (`embeddings.ts:285-301`), persisting any special-category disclosure for up to ~30 days in a second location beyond `session_events`. *Proposed remedy:* store the filtered text (the string actually embedded) rather than the raw extract — a one-line change at the `storeSessionEmbedding` call spine — and remediate existing rows. [GAP: no tracking ID — capture.]
6. **Embedding step not gated on memory consent.** `generate-embeddings` checks GDPR consent only (`session-completed.ts:2058-2092`); the analysis step checks memory consent as well (`:1722-1748`). If session embeddings are classified as part of "persistent memory" for the unlock (they are learner-content-derived and persist), the memory-consent gate should be symmetric. *Proposed remedy:* add the `memory_consent_status`/`memory_collection_enabled` check to the embedding step, or record a deliberate classification (with DPO concurrence) that session embeddings serve session continuity rather than profiling and sit outside the memory feature. [GAP: no tracking ID — capture; decision item for the DPO.]
7. **Art 9(2) condition undetermined.** Open by design — the position document requests exactly this advice. *Remedy:* DPO determination on the substrate above; the final DPIA and RoPA then reflect it (per `art9-special-category-position.md`).
8. **Production-state verification artifact missing.** Code defaults prove the design; a dated production query proving no profile has memory consent granted (and no `memory_facts` rows exist) would close the "disabled today" claim evidentially. *Remedy:* capture read-only production evidence at package submission (consistent with the pre-launch zero-users record).
