# =============================================================================
# Staging Deployment Workflow
# =============================================================================
# Placeholder — original Railway API + Web deployment removed.
# Will be replaced with Cloudflare Workers deployment in Epic 0.
# =============================================================================

name: Deploy to Staging

on:
  pull_request:
    branches: [main]
    types: [opened, reopened]

  workflow_dispatch:

concurrency:
  group: staging-deployment-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  deployments: write
  pull-requests: write

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Staging deployment not yet configured."
          echo "Cloudflare Workers deployment will be added during Epic 0."
