# Rollback — 0078 drop learning_modes.mode + consecutive_summary_skips + learning_mode enum

## Rollback possible?

Yes, technically — the schema can be reconstructed. Re-create the `learning_mode` enum, re-add both columns with their previous defaults. No real user data exists at the time of this migration (pre-launch; see `feedback_pre_launch_no_users.md`), so any rollback is over disposable dev/seed rows.

## Data loss

All values of `learning_modes.mode` and `learning_modes.consecutive_summary_skips` are permanently dropped. Pre-launch, no production rows of consequence exist — only dev seed data, which is regenerable via `pnpm run db:push:dev`.

## Recovery procedure

```sql
CREATE TYPE "public"."learning_mode" AS ENUM ('serious', 'casual');
ALTER TABLE "learning_modes" ADD COLUMN "mode" "learning_mode" NOT NULL DEFAULT 'serious';
ALTER TABLE "learning_modes" ADD COLUMN "consecutive_summary_skips" integer NOT NULL DEFAULT 0;
```

Schema-side recovery: revert `packages/database/src/schema/progress.ts` to the prior commit and re-run `pnpm run db:push:dev` against dev. Seeded values are regenerated by the seed fixtures.
