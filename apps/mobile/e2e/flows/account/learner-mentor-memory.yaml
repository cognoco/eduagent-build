# Learner mentor memory - empty state and controls
# Tags: nightly, account
# Seed: language-subject-active
# Covers: More -> learner mentor memory empty state
# Usage: ./scripts/seed-and-run.sh language-subject-active flows/account/learner-mentor-memory.yaml
appId: com.mentomate.app
tags:
  - nightly
  - account
---
- runFlow:
    file: ../_setup/seed-and-sign-in.yaml

- extendedWaitUntil:
    visible:
      id: "learner-screen"
    timeout: 15000

- tapOn: "More Tab"

# Wait for the More screen to render — the Learning Mode section header is
# always above the fold; sign-out-button lives at the bottom of the scroll,
# so use the section header as the screen-loaded signal instead.
- extendedWaitUntil:
    visible:
      id: "learning-mode-section-header"
    timeout: 10000

- scrollUntilVisible:
    element:
      id: "mentor-memory-link"
    direction: DOWN
    timeout: 10000

- tapOn:
    id: "mentor-memory-link"

- extendedWaitUntil:
    visible:
      id: "mentor-memory-all-empty"
    timeout: 10000

- assertVisible:
    id: "memory-status-text"

- takeScreenshot: 01-learner-memory-empty

- pressKey: back

# Back returns to More — confirm the screen by its header, then verify
# sign-out-button is reachable by scrolling to it.
- extendedWaitUntil:
    visible:
      id: "learning-mode-section-header"
    timeout: 10000

- scrollUntilVisible:
    element:
      id: "sign-out-button"
    direction: DOWN
    timeout: 10000

- takeScreenshot: 02-back-to-more
