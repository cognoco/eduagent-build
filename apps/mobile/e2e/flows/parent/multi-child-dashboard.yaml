# Flow: Parent Multi-Child Dashboard — View and Navigate Multiple Children
# Validates: parent with 3 children sees all children listed, can tap into each,
# and verify per-child data (subjects, sessions). Tests scroll behavior when
# children list extends beyond the fold.
# Tags: nightly, parent
#
# Journey: UX Journey 4 (Parent Oversight)
# Epics: Epic 5 (FR85-FR90)
# Prerequisite: Seeded parent user with 3 linked children
#               (scenario: parent-multi-child — creates Emma, Lucas, Sofia)
appId: com.mentomate.app
tags:
  - nightly
  - parent
---
# 1. Seed & sign in as parent
- runFlow:
    file: ../_setup/seed-and-sign-in.yaml
    env:
      SEED_SCENARIO: "parent-multi-child"
      API_URL: ${API_URL}

# 2. Parent owners land on LearnerScreen; enter the Family dashboard.
- runFlow:
    file: ../_setup/open-family-dashboard.yaml

# ── Visual integrity: dashboard heading ────────────────────────────────
- assertVisible:
    text: "Family"
- assertVisible:
    text: "Everyone you're learning alongside."

- takeScreenshot: multi-child-01-dashboard

# ── Verify all 3 children are listed ──────────────────────────────────
# Child 1: Emma (teen, Mathematics, 10 exchanges completed)
- extendedWaitUntil:
    visible:
      text: "Emma"
    timeout: 10000

# Child 2: Lucas (learner, Science, 5 exchanges completed)
- scrollUntilVisible:
    element:
      text: "Lucas"
    direction: DOWN
    timeout: 10000

# Child 3: Sofia (teen, History, no sessions yet)
- scrollUntilVisible:
    element:
      text: "Sofia"
    direction: DOWN
    timeout: 10000

- takeScreenshot: multi-child-02-all-children-visible

# ── Drill into Child 1 (Emma) ─────────────────────────────────────────
- scrollUntilVisible:
    element:
      text: "Emma"
    direction: UP
    timeout: 10000

- tapOn:
    text: "Emma"

# Wait for child detail screen
- extendedWaitUntil:
    visible:
      text: "Emma"
    timeout: 10000

# Verify subject info — Mathematics is seeded for Emma
- assertVisible:
    text: "Mathematics"

- takeScreenshot: multi-child-03-emma-detail

# Navigate back to dashboard
# Use pressKey:back only — tapOn back-button + pressKey = double-back exits dashboard
- pressKey: back

# Wait for dashboard to reload
- extendedWaitUntil:
    visible:
      id: "dashboard-scroll"
    timeout: 10000

# ── Drill into Child 2 (Lucas) ────────────────────────────────────────
- scrollUntilVisible:
    element:
      text: "Lucas"
    direction: DOWN
    timeout: 10000

- tapOn:
    text: "Lucas"

- extendedWaitUntil:
    visible:
      text: "Lucas"
    timeout: 10000

# Verify subject info — Science is seeded for Lucas
- assertVisible:
    text: "Science"

- takeScreenshot: multi-child-04-lucas-detail

# Navigate back
# Use pressKey:back only — tapOn back-button + pressKey = double-back exits dashboard
- pressKey: back

- extendedWaitUntil:
    visible:
      id: "dashboard-scroll"
    timeout: 10000

# ── Drill into Child 3 (Sofia — no sessions) ─────────────────────────
- scrollUntilVisible:
    element:
      text: "Sofia"
    direction: DOWN
    timeout: 10000

- tapOn:
    text: "Sofia"

- extendedWaitUntil:
    visible:
      text: "Sofia"
    timeout: 10000

# Verify subject info — History is seeded for Sofia
- assertVisible:
    text: "History"

- takeScreenshot: multi-child-05-sofia-detail

# Navigate back
# Use pressKey:back only — tapOn back-button + pressKey = double-back exits dashboard
- pressKey: back

- extendedWaitUntil:
    visible:
      id: "dashboard-scroll"
    timeout: 10000

- takeScreenshot: multi-child-06-complete
