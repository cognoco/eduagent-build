# Runtime-assembled, system, or DRAFT-placeholder testIDs that C3 can't
# statically resolve. Each entry must carry an inline comment explaining
# why static extraction misses it. Empty lines and lines starting with `#`
# are ignored. See docs/audit/e2e/validator-spec.md → C3.

# ── Android system resource IDs ──────────────────────────────────────
# Maestro can target Android-native UI by resource id (e.g. the system
# date-picker). These are intentionally not part of app source.
android:id/date_picker_header_year
# API 34 Photo Picker media-tile resource; AOSP and Google API package variants.
com(?:\.google)?\.android\.providers\.media\.module:id/icon_thumbnail

# ── DRAFT flow placeholders ──────────────────────────────────────────
# These flows are blocked on a real testID being added to the app
# (see header DRAFT notice in each flow). The placeholder is intentional
# and exists to keep the flow shape correct while the underlying feature
# is unimplemented. Remove from this list (and from the flow) once the
# real testIDs land.
#   - apps/mobile/e2e/flows/session/sse-reconnect-banner.yaml
#   - apps/mobile/e2e/flows/learning/first-curriculum-polling-timeout.yaml
<PLACEHOLDER_TESTID>
