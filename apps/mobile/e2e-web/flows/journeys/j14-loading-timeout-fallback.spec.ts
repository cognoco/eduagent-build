import { test } from '@playwright/test';

test.fixme(
  'J-14 loading timeout → fallback actions work',
  'HomeScreen timeout is currently unreachable on web because (app)/_layout.tsx blocks on profile-loading before Home mounts.'
);
