What was done:
Completed the WI-1145 identity-v2 integration-seed rework by adopting Codex PR #1638 (branch codex/api-export-unblock) as the fix vehicle, per operator ruling. #1635 (52e455e2b) landed the unconditional v2 identity/membership/consent seeds in the cross-package integration harness and greened the flag-ON lane, but left the pre-collapse flag-OFF lane red; #1638 completes that rework.

What changed:
Test-harness only (6 files, no product code). route-fixtures.ts: added ensureV2ProfileAnchorForTest (idempotent organization + person parent inserts) before the unconditional v2-edge seeds, so flag-off seeds no longer FK-fail on absent v2 parent nodes. consent-web.integration.test.ts: readConsentState made store-agnostic — prefers a resolved legacy consentStates row over a stale pending v2 consent_request, so the flag-off product's legacy CONSENTED state is read instead of the never-updated v2 row. parent-dashboard.integration.test.ts: repairs the v2 anchor + parent membership before inserting v2 relationship edges. legacy-identity-anchors.ts: ensure-parent anchor support. The two self-sub-seeding suites (alias-merge-v2, inngest-quota-reset) opt out of the v2 baseline-subscription anchor (seedBaselineSubscription:false) so createSubscriptionV2's legacy-twin write resolves the quota_pools FK on the journaled CI chain.

Verification:
#1638 required main check (run 28390479048, head 1813a9322) ran the cross-package api:test:integration step UNMASKED: 57/57 suites, 430/430 tests, with all 6 previously-red flag-off consent/family suites (parent-dashboard, onboarding-dimensions, consent-web, learner-profile-child-consent, pronouns-age-gate, consent-onboarding-gate) explicitly PASS. Flag-ON integration lane PASS. claude-review APPROVED; mergeStateStatus CLEAN; Merge-completeness green. Independently corroborated by a local clean-DB flag-off red-green (fresh tests DB migrated 0000-0125, env -u IDENTITY_V2_ENABLED): the consent person-FK reds reproduce exactly without the fix, and ensure-parent alone greens only 4/6 — the consent-web/parent-dashboard read-divergence fixes are required for the remaining 2.

Caveats / Follow-ups:
The local doppler -c stg config sets IDENTITY_V2_ENABLED=true, so un-overridden local integration runs are flag-ON (false green) — reproducing the flag-OFF CI lane requires env -u IDENTITY_V2_ENABLED. #1647 (the earlier 2-file seedBaselineSubscription opt-out) is superseded by #1638 and closed unmerged. Gate-2 close is performed by a separate reviewer (reviewer != the Codex session that authored #1638).
