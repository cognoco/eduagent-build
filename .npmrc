# Flatten node_modules for Expo web mode compatibility.
# pnpm's strict layout prevents Babel/Metro from resolving transitive
# plugins when bundling for web. This is the standard fix for Expo + pnpm.
shamefully-hoist=true
