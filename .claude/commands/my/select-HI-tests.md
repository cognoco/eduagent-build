launch 10 subagents and this is the task:  Pick the top 20 most-edited test files  
  (via git log --name-only) that still have internal mocks and convert                                              
    their internal mocks to jest.requireActual — Update documentation. 