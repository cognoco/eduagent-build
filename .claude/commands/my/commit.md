Invoke the `commit` skill to commit the current changes.

This is a stub that delegates to `.claude/skills/commit/SKILL.md` — the skill runs in a fresh subagent with `context: fork` for token efficiency. Do not execute the commit inline.
