We need to run the full e2e suite. The goal is to 1. run full e2e suite 2. note all failures 3. investigate each one of them 4. either fix the test or fix the real bug.5. repeat 1-4 until all e2e tests pass. Use as many subagents as you want. Each test that you touch and has a mock needs to be rewritten to mirror the real  implementation. If the test fails because there is a real bug, fixt the code, not the test.
