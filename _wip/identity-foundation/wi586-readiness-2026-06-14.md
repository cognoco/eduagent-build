# WI-586 — dev-rehearsal readiness verdict (2026-06-14)

**Source:** read-only verification investigator (Sonnet), all facts cited against `main`.
**Verdict: READY to start the dev rehearsal — 12/12 checks PASS.** One thing to clear first
(the failing `Deploy` workflow, see below).

> Reframe (important): WI-586 is **not** a bare drop — it is the full **convergence runbook**
> (`2026-06-11-cutover-plan.md` §4): freeze → Neon PITR marker → ownerless disposal → final
> reseed → verify exit 0 → FK re-point (`M-REPOINT`) → **flip `IDENTITY_V2_ENABLED=true`** + soak
> → drop 5 legacy tables (`M-DROP`) → grep-clean. The flag is still `false` (system on legacy).
> Operator-driven: Jorn owns the Doppler flags/flip, Neon marker, Inngest drain, deploys, and the
> go decisions at 3 STOP gates. Recovery: trivial→step5, clean-reverse→step6 (frozen), **PITR-only
> post-flip**, **impossible post-drop** (Neon PITR rewind to the marker).

## Preconditions on `main` — PASS
- **A1 schema + flags PASS** — `packages/database/src/schema/identity.ts` has all 17 new-model
  tables; `apps/api/src/config.ts:171` `IDENTITY_V2_ENABLED` (default false); `config.ts:185-186`
  `MAINTENANCE_READONLY` + `MAINTENANCE_BLOCK_INNGEST` (typed envSchema).
- **A2 RLS PASS** — `mentor_activity_ledger` has `profile_id` + `.enableRLS()` (schema/activity-ledger.ts:28,50);
  migration `0112` enables RLS; `rls-coverage.test.ts` is a dynamic scanner (no static KNOWN_PROFILE_TABLES list) and covers it.
- **A3 CI PASS (caveat)** — last completed `CI` green at `bf94cdf19`; HEAD is +3 docs-only commits
  (CI in progress, no code changes). **`Deploy` workflow FAILED on the 2 most recent runs
  (27500159926 / 27500039930)** — separate from CI; investigate before the rehearsal in case it's a
  staging-DB/Worker issue that would foul the reseed env.
- **A4 migrations PASS** — present: `0106, 0108, 0109, 0112, 0113, 0114 (M-HOMES), 0115 (M-RESEED2)`;
  highest is `0116`. **`M-REPOINT` + `M-DROP` confirmed ABSENT** (expected — they are WI-586's own
  deliverables, to author inside the freeze window). This is the front-load.
- **A5 reseed PASS** — `packages/database/scripts/verify-identity-reseed.mjs` exists (read-only, all
  8 new tables + subscription_payers, `--inventory` mode); `0109` reseed block committed.

## Orphan re-homes (drop loses nothing) — ALL PASS
- **B1** person: `conversationLanguage`/`pronouns`/`avatarUrl`/`defaultAppContext` (identity.ts:109-112; DDL 0114).
- **B2** subscription store-correlation ids: stripe_customer/subscription/last_event(+ts), revenuecat_original_app_user/last_event(+ts), trial_ends_at, cancelled_at (identity.ts:295-306; DDL 0114; idempotency partial indexes present).
- **B3** `consent_request` table (identity.ts:791-877): guardianEmail, token, tokenExpiresAt, resendCount, recipientChangeCount; status vocab pending/requested/approved/denied/expired.
- **B4** guardianship: revokedAt (367), qualification (363, 9-value CHECK).
- **B5** is_owner replacement: `membership.roles` text[] (231, {admin,learner} CHECK) + `subscription.payerPersonId` (285); `subscription_payers` join table (749).
- **B6** `archived_at` → person (identity.ts:115, annotated "NOT consent state"); archival/visibility at person layer.
- **B7** `has_premium_llm` → derived: `services/identity-v2/profile-v2.ts:65-76 deriveHasPremiumLlm()`; router derives llmTier from plan_tier. No orphaned column.

## Next steps (front-loads — read-only / non-destructive, safe to do while PRG-14 runs)
1. **Author `M-REPOINT` (57-FK re-point) + `M-DROP` (drop 5 tables + orphaned enums)** migrations with
   the verbatim §4.2 `## Rollback` section — reviewable PRs, applied only during the convergence.
   Drop SQL (from plan §4 step 8): `DROP TABLE consent_states, family_links, profiles, subscriptions, accounts;`
   + `DROP TYPE consent_status, consent_type, location_type, subscription_status, subscription_tier;`
2. **Investigate the failing `Deploy` workflow** (the only readiness flag).
3. Then: operator-driven dev rehearsal (full runbook) → staging (+ 24h soak) → prod (post-BUG-12, non-gating to close).
