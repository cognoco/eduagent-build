**What was done:** Implemented WP-W1-spine (WI-571 — structural foundation for the identity-platform rebuild). Two workstreams merged:
(1) Behavior-preserving carve of `session-exchange.ts` — routing constants and LLM routing functions extracted to `session-exchange-router.ts`; `resolveReadyToFinish` extracted to `session-exchange-spine.ts`; original file re-exports both via `import` + `export { X } from` so external consumers and internal call sites continue to resolve unchanged.
(2) TDD greenfield scaffold for the policy-engine module — `engine.ts` (evaluatePolicyCell, two-primitive model per MMT-ADR-0013, fail-closed for all inputs until W2/W3 wires DB reads), `router.ts` (resolveExchangeRouter, throws NoEligibleModelError on empty eligibility set, JSDoc W3 wiring obligation to map to CircuitOpenError), `judge.ts` (resolveJudgeConfig, vendorIndependent + reasoningMode:off per MMT-ADR-0016), barrel `index.ts`. Branch rebased onto main after WI-572 (PR #859) merged with no conflicts.

**What changed (11 files):** `apps/api/src/services/session/session-exchange.ts` (carve + re-exports), `session-exchange-router.ts` (created), `session-exchange-spine.ts` (created), `apps/api/src/services/policy-engine/engine.ts` (created), `engine.test.ts` (created), `router.ts` (created), `router.test.ts` (created), `judge.ts` (created), `judge.test.ts` (created), `index.ts` (created), `index.test.ts` (created). `_plan-WI-571.md` at repo root (plan artifact, committed). CodeRabbit P2-minor test assertions tightened in `judge.test.ts` + `router.test.ts`.

**Verification:** 94 tests pass (72 existing `session-exchange` tests preserved + 22 new policy-engine tests). Pre-push hook: tsc incremental clean + surgical tests pass. Eval harness (`pnpm eval:llm`): 0 snapshot diff — no prompt drift from structural carve. PR #860 CI all green: `main` (7m32s), `API Quality Gate` (1m43s), `claude-review`, `CodeRabbit`, `changes`, `Playwright web smoke`. P2 findings addressed: engine.ts fail-closed for known/known (test + code), router.ts W3 wiring note via JSDoc, judge.test.ts exact assertion, router.test.ts all-three-fields assertion.

**Caveats / Follow-ups:** (1) `NoEligibleModelError` must be mapped to `CircuitOpenError` at `index.ts:394` + `routes/sessions.ts:1126` when W3 (WP-W3-envelope-router) wires this into the LLM call path — JSDoc on the class documents the obligation. (2) `evaluatePolicyCell` returns `consentRequired: true` for ALL inputs (including known/known) — intentionally fail-closed scaffold; W2/W3 must wire real DB reads from `policy_cells` / `policy_rules` to replace this stub. (3) The `_plan-WI-571.md` artifact at repo root is committed for traceability; may be removed in a future cleanup pass.
