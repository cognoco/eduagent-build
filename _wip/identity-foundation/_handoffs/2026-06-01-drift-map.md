# Handoff — Identity Foundation, next up: the Drift Map (Phase A)

**From:** session "drift-map", 2026-06-01 · **Repo:** `/Users/vetinari/nexus/_dev/eduagent-build`
**Next session focus:** produce the **drift map** (Phase A of the roadmap). Methodology is already
decided (below) — execute it, don't re-litigate.

---

## Orient in 4 reads (do NOT re-derive — these capture all prior reasoning)

1. `_wip/identity-foundation/README.md` — what this work is, the clean-cut decision, the anti-drift
   **guardrails**, decision log.
2. `_wip/identity-foundation/ROADMAP.md` — the live tracker: phases A–F, cross-cutting threads, the
   **sibling-plan re-triage** section, F-gate checklist. **This is the spine.**
3. `_wip/identity-foundation/identity-reconstructed-prd.md` — product intent reverse-engineered from
   the rejected plans, tagged `[STATED]/[IMPLIED]/[GAP]/[CONFLICT]`. **Its §11 is the product-intent
   agenda** (Phase B), and its findings seed the drift map.
4. `_wip/identity-foundation/archive/` — the 3 superseded 2026-05-31 identity plans (org-membership /
   t1-data-model / t2-auth). **Discussion input only**, bannered SUPERSEDED.

Supporting: `docs/audit/2026-05-29-full-audit/issue-summary.md` (why this is a foundation re-platform,
not a refactor). The DeepSec-R2 audit triage lives in `docs/audit/2026-05-29-full-audit/claude/` — its
re-triage folds into the drift map.

---

## The task: drift map (Phase A) — agreed methodology

A **three-way reconciliation**, organized **by domain area**, **citation-backed** (every claim cites
`file:line` or `doc:§` — this session twice caught assert-from-memory errors; trust nothing uncited):

- **Intent** (where we're going): the reconstructed PRD + the archived plans + the brain-dump in this
  conversation's history. *Source the intent; don't reconstruct it.*
- **Canonical docs** (presumed stale): `docs/PRD.md` (1760 ln), `docs/architecture.md`,
  `docs/audience-matrix.md`, `docs/project_context.md`, `CONTEXT.md`, `docs/ux-design-specification.md`.
- **Code/schema** (ground truth, as-built): `packages/database/src/schema/`, the auth/consent/billing
  services + middleware.

**Scoping discipline:** identity/account/org/role/consent/billing cluster → **deep**; other drifted
areas → **light** (staleness banner + refresh priority). Do **not** line-by-line diff the monsters
(`docs/specs/epics.md` ≈ 381K; the 134K nav plan) — mine them as targeted evidence. Archives are tiny
(7 files) → include lightly for the "how/when did it drift" angle.

**Per area, classify:** drift *type* · *direction* (is code already drifting toward the new intent, or
stuck on the old model?) · blast radius · **PRD-refresh priority** (so the map doubles as the refresh
backlog) · open questions for the vision-lock.

**Fold into the same doc** (it's the same analysis): the **audit re-triage** (which DeepSec-R2 MUSTs
are identity-coupled → fold into the rewrite vs. independent → do now) and the **sibling-plan
provisional-tagging** (below).

**Output:** `_wip/identity-foundation/drift-map.md`.

---

## A concrete sub-task to do as part of A (safe, low-risk)

**Provisionally tag the 7 sibling plans** still in `docs/plans/` (the 2026-05-31 fan-out). Add to each
a one-line note: *"classification pending re-triage against identity-foundation clean-cut target — see
`_wip/identity-foundation/ROADMAP.md`."* Do **not** archive/supersede them and do **not** move them
into `_wip/` — that's premature categorization (the real split happens after the domain model, Phase D).
Preliminary classification table + the plan list are in ROADMAP.md §"Sibling-plan re-triage".

---

## Hard constraints / traps (from this session)

- **Product intent FIRST; model derived from it.** The drift map *informs* intent (Phase B); it does
  not design the model. Don't let org/membership table shapes from the archived plans pre-shape anything.
- **`_wip/` doc structure is a PILOT** — reversible until Phase C decides chunk-vs-monolith repo-wide.
- **Don't delete migration `0106`** — it's committed (PR #668, merged to `main`) AND applied to shared
  DBs. T1 is inert but slated for forward-only revert at Phase F, not now.
- **No Cosmo work items** until the F gate. Tracking stays repo-only in `ROADMAP.md`.
- **Consent/COPPA under own-logins** is the load-bearing unknown — flag it hard wherever it surfaces.

---

## State of the working tree

- `_wip/identity-foundation/ROADMAP.md` (new) and the `README.md` edit (added the ROADMAP row) were
  created **this session and are uncommitted** — verify with `git status`. The README, reconstructed
  PRD, and `archive/` were set up in a prior session (likely committed).
- No code changed this session. `main` HEAD: `fd2232ab7` (PR #668 merge — the T1 identity schema).

## Suggested skills for the next session

- **`Explore` subagents (read-only, sonnet)** — fan out the drift-map reading by area (one agent per
  cluster: identity / consent / billing / curriculum-sessions / …), each returning a cited per-area
  drift record; synthesize the map yourself. The reading is broad; the synthesis is yours.
- **`modern-cli-tooling`** (`rg`/`fd`/`jq`) for the code-side comparison.
- **Do NOT** use `/grill-with-docs` or `/office-hours` for the drift map — those are for Phase B
  (product intent) and Phase D (domain model), at a different altitude. The drift map is analysis, not
  interview.
- Optionally `/commit` to persist the uncommitted `_wip/` files (repo uses the **`commit`** skill, never
  raw git / `/zdx:commit`).
