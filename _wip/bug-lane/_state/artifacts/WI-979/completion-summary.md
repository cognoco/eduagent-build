What was done: Tightened three response-schema fields that had widened their constrained enums to bare `z.string()`, so the API now rejects invalid enum values it previously accepted. This work had already landed on `main`; this finalization records it against the Work Item after a just-in-time primary-source verification.

What changed: In `packages/schemas/src`, the `teachingPreferenceResponseDataSchema.method` field now uses `teachingMethodSchema` and `analogyDomain` uses `analogyDomainSchema` (kept as nullable-and-optional per this item's acceptance criteria) in `assessments.ts`, and `subjectSessionSchema.sessionType` now uses `sessionTypeSchema` in `subjects.ts`. Regression tests asserting that invalid enum values are rejected were added in `packages/schemas/src/assessments.test.ts` and `packages/schemas/src/subjects.test.ts`.

Verification: confirmed by primary source on `main` that all three fields use the canonical enum schemas and that the regression tests are present and reference this item; the landing pull request is `cognoco/eduagent-build` number 1403 (commit `74f1a811f`), verified as an ancestor of `origin/main`. The schema test target is the verification command for this change.

Caveats / Follow-ups: a separate canon tension is explicitly out of this item's scope and must NOT be resolved here — several response schemas use the banned nullable-and-optional combination, and this item's own acceptance criteria mandated that combination on `analogyDomain`, so the contradiction needs a design ruling rather than a blind sweep; it is captured as a new low-priority Hygiene Work Item, not folded into this one.
