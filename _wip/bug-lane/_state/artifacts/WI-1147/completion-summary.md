What was done: Repaired stale `session.completed` integration test fixtures that emitted `mode: null` (plus the sibling optional-not-nullable fields), which the schema tightening from WI-972 began rejecting and which reddened the required main integration check; brought every fixture back into conformance with the optional-not-nullable contract so the gate is green again.

What changed: Fixtures in `apps/api/src/inngest/functions/session-completed.integration.test.ts` were updated to omit the optional fields rather than pass null (the `mode`, `qualityRating`, and `exchangeCount` fields on the `session.completed` event are optional-not-nullable per `packages/schemas/src/inngest-events.ts`); incidental same-suite fixture touch-ups landed in `apps/api/src/inngest/functions/weekly-progress-push.integration.test.ts`, `apps/api/src/routes/snapshot-progress.integration.test.ts`, `apps/api/src/services/billing/alias-merge.integration.test.ts`, and `apps/api/src/services/retention-data.integration.test.ts`.

Verification: the required main integration check and all other required CI gates passed on the merged pull request; the only red was the advisory non-gating Flag-ON identity-v2 job, which exercises an unrelated account-export test outside this change. The fix is a test-fixture conformance correction, not a production code path, so no runtime behavior changed.

Caveats / Follow-ups: this same pull request also carried an interim quarantine for WI-1153 (five integration tests default-skipped via a G7-sanctioned conditional-callee guard, runtime-overridable via the `UNQUARANTINE_WI_1153` environment variable) — that quarantine is NOT in WI-1147 scope and does not represent WI-1147 incompleteness; un-quarantining those tests and fixing the underlying shared-database test-isolation flake is tracked under WI-1153.
