> **LIVE STATUS (2026-06-19 ~07:05Z — post orch-020 re-engagement; supersedes any earlier "stood down" banner):**
> Lane RE-OPENED after the land. new-llm→main **LANDED** (merge `c86bcc110`). WI-842/843/845/846 **Closed**;
> WI-844 bounced (wrong Fixed In `8d5577a36`→`ddd9beef2` + reviewer's invalid "Flag-ON = required" premise,
> rebutted from branch-protection) → re-finalized → **Closed PASS**. NOW: **WI-848** (decay-path
> `xp_ledger.status` desync, Bug/P2) — **LANDED**: PR #1236 (base=main) merged via SQUASH `10edb5f8` (orch-021,
> strict-green verified in-seat: 5 required pass, Flag-ON ic-116 baseline only, claude-review 0must/0should).
> WI-848 finalized -> **Reviewing** (Fixed In = squash `10edb5f8`, on main — no reconciliation/ancestor-bounce);
> WS-13 reviewer closes Gate-2. Optional CONSIDER nit (calibration verified-path assertion) DECLINED (merged code;
> fold-in-if-touched). Worktrees cleaned; PR #1236 watcher stopped (merged). TWO PERSISTENT monitors remain: inbox
> (high-water `orch-021`) + workstream Gate-2 Stage watcher (catches WI-848 close/bounce). Outbox high-water `prg17r2-023`.
>
> **LANE SCORECARD:** WI-842/843/844/845/846/848 **ALL CLOSED** (WI-848 Gate-2 PASS 2026-06-19). Round-2 new-llm
> audit→remediation→land lane **COMPLETE — zero open work**. Monitors kept live (inbox @ `orch-021` + workstream Gate-2)
> per operator whole-workstream instruction; stop on operator go or session end. Outbox high-water `prg17r2-024`.

# PRG-17 round 2 — new-llm AUDIT catalog (DRAFT, pre-WI)

> **Mission (orch-004, 2026-06-18 21:03Z):** AUDITOR not lander. Fix NOTHING inline, push NOTHING to
> new-llm. PR #1232 stays OPEN as the base=main CI oracle — **DO NOT MERGE / DO NOT CLOSE**. The
> new-llm→main landing is **DEFERRED** to a future round, blocked on this catalog clearing. Report
> this drafted catalog to the orchestrator FIRST; **do not create Cosmo WIs** until they route
> WI-home + ownership and greenlight. One actionable entry per finding — do NOT root-cause to the
> bottom.

## Catalog entries

### C1 — GC1 base=main ratchet fires on inherited new-llm internal mocks
- **Repro:** PR #1232 `main` check → step "GC1 — no new internal jest.mock" FAILs. `git diff origin/main...origin/new-llm --unified=0 -- '**/*.test.tsx'` shows **5** added internal `jest.mock('./…'|'../…')` lines, all in `apps/mobile/src/hooks/use-navigation-contract.test.tsx` (full-replace mocks of `useActiveProfileRole`, `useParentProxy`, `useSubscriptionStatus`, `useAppContext`, `useProfile`). They appear net-new because the integration PR's base is `main` (per-WI review saw base=new-llm and never tripped GC1).
- **Suggested fix (trivial):** add `// gc1-allow: <reason>` sticker on each `jest.mock(` line (round-1 WI-695 precedent), or convert to Pattern-A `jest.requireActual` spread. **A drafted fix already exists** parked in `stash@{0}` of worktree `.worktrees/new-llm-sync` (branch `new-llm-r2-gc1`) — 5 gc1-allow stickers, GC1 staged-mode green, nav-contract test PASS. Parked (not staged) per orch-005 baseline guardrail so it can't mask its own finding during audit; recover with `git stash apply stash@{0}`. NOT pushed. A later executor can lift it directly.
- **Severity:** blocks landing (required `main` check). Trivial to clear.
- **Owner:** ours (new-llm test hygiene).

### C2 — Flag-ON integration lane broadly red (POST /v1/profiles 500s + ~30 cascading FAILs)
- **Repro:** PR #1232 "Flag-ON integration (IDENTITY_V2_ENABLED)" (continue-on-error) → cross-pkg step `api:test:integration` shows ~30 FAIL incl. `profiles-routes.integration.test.ts` (repeated `POST /v1/profiles → 500`). Underlying Postgres errors in the run log: `quota_pools_subscription_id_subscriptions_id_fk` FK violation, `subjects_profile_name_lower_active_uq` duplicate key, `profiles_conversation_language_check` CHECK violation. (`role "root" does not exist` lines are container-teardown noise, ignore.)
- **NOT introduced by #1232:** ic-116 diff vs main baseline run **27772532944** (headSha `c0ec04a3a` = #1232 base) = **48 FAIL == 48 FAIL, `comm -13` empty**. Ambient on `main` itself — the lane is `continue-on-error` for exactly this.
- **Root (do NOT bottom out):** CI's flag-on lane runs the committed-migration / **pre-repoint** DB while code is **post-cutover** (legacy `profiles`/`accounts`/`subscriptions` chains diverge). See `.claude/memory/feedback_allowed_red_lane_masks_integration_test_reds.md`.
- **Suggested fix (1-line):** either refresh the CI flag-on integration DB to post-repoint, or apply the WI-808 dual-write seed pattern (seed legacy twins) across the failing fixtures. (Likely a program/identity-foundation item, not this lane.)
- **Severity:** ambient/pre-existing; allowed-red today. Real debt, not a #1232 blocker.
- **Owner:** ours (CI integration-DB / test-seed infra).

### C3 — Other base=main-only reds on #1232: NONE beyond GC1
- **Evidence:** CI terminal on #1232 — every other base=main forward-only ratchet PASSED: `decision-adr-link` ✅, `maestro-validator` ✅, `reference-only-gate` ✅, `rollback-section` ✅, `changes` ✅, `claude-review` ✅ (green = ran), Merge completeness ✅, API Quality Gate ✅, Playwright web smoke ✅, run-smoke ✅. Local re-run in worktree confirmed: `no-clinical-copy` 0-new, `i18n-jsx-literals` 0-new, `decision-adr-link` 0-new. GC1 is the sole base=main-only failure.
- **No entry needed** — recorded for completeness.

### C4 — Canon-fit / functional smoke of V2-shell + LLM deltas: NO BLOCKER
- **Codex audit-engine result (agentId `a5584a9668d5a9163`, fresh session, complete):** CLEAN — zero new findings. Task A: the 5 parked gc1-allow stickers are correct + truthful (all 5 mocked hooks are provider/context-backed; no Pattern-A `requireActual` needed; `--cached --check` clean, 0 TS parse diagnostics) → corroborates C1. Task B: GC1 is the ONLY `GITHUB_BASE_REF`-keyed ratchet (`scripts/check-gc1-pattern-a.ts:218`); every other guard is a current-tree baseline/allowlist scan, structurally immune to the base=main class; independently re-ran each (i18n-jsx-literals/no-clinical-copy/decision-adr-link/persona-fossil/test-only-exports/i18n-staleness/untranslated-key all 0-new) → corroborates C3 with structural proof. No DB migration diff vs main (`0106`/`0107` journal skip is documented reference-only baseline). origin/main is full ancestor of HEAD (0/44).
- **Canon-fit (dimension ii, my pass):** the entire V2-shell delta (nav/mentor/subject-hub/journal screens) is gated behind `MODE_NAV_V2_ENABLED` (commit `ac2d53ae5`); prod build is V2-off, so landing to main is flag-dark in prod — no shipped-flag-state regression (AGENTS.md "Profile Shapes" hard constraint) and aligned with the mentor-is-the-app spec's explicitly deferred/irreversible S6 cutover (commit `787099e79`). LLM delta is additive: new eval flows `app-help-v2` + `now-park-return` with snapshots committed, plus the S0-R retention update gate (`5fed808e9`).
- **Functional smoke (dimension iii):** local validation gate (pre-compaction, in worktree) green — `tsc --build` exit 0, nav-contract suite PASS 1511/0 (exercises the flag-gated V2 shell logic), `eval:llm` Tier 1 clean (exercises the new prompt builders). recaps.ts WI-821 path verified threading `identityV2Enabled` (L286-289).
- **No actionable entry** — dimensions (ii)+(iii) clear; recorded for completeness alongside C3.

### C5 — `screen-navigation.test.ts`: 4 V2 shell screens fail the exit-navigation audit (MASKED behind GC1 on #1232)
- **Repro:** `cd apps/mobile && pnpm exec jest src/app/screen-navigation.test.ts --no-coverage` → **4 failed / 79 passed / 83 total**. Failing: `(app)/account.tsx`, `(app)/journal.tsx`, `(app)/mentor.tsx`, `(app)/subjects.tsx` — `EXIT_PATTERNS.some(p => p.test(source))` returns false (no back/close/dismiss/`router.back` affordance in source). These are V2-shell screens introduced on new-llm without the exit-nav affordance the audit requires.
- **Why missed in round-1 audit / why it matters now:** on PR #1232 the `main` job runs `bash -e` and **fails fast at the GC1 step**, so the "Lint, test, typecheck, build (affected)" step NEVER RAN — this failure was masked. Clearing GC1 (WI-842) lets the job reach the test step → this surfaces. Confirmed pre-existing on origin/new-llm (reproduces on the WI-842 branch whose only delta is the GC1 test-file edit) and on CI #1233 (job 82238629087).
- **Suggested fix (1-line intent):** add the audited exit-navigation affordance to the 4 V2 screens, OR if they are intentionally exit-less tab roots, add them to the audit's allowlist with rationale.
- **Severity:** blocks #1232 `main` (REQUIRED check) once GC1 is cleared. **Owner:** ours (V2-shell).

### C6 — `use-api-query.guard.test.ts`: inline-queryFn ratchet rose 93→97 (MASKED behind GC1 on #1232)
- **Repro:** `cd apps/mobile && pnpm exec jest src/hooks/use-api-query.guard.test.ts --no-coverage` → "**Inline queryFn+combinedSignal sites rose to 97 (baseline 93)**". V2 data hooks (e.g. `use-account.ts`) added 4 inline `queryFn` sites above the committed `BASELINE=93` in the test. Forward-only count ratchet vs baseline constant (not GITHUB_BASE_REF-keyed → invisible to the ratchet-class hunt, and masked by the same GC1 short-circuit on #1232).
- **Suggested fix (1-line intent):** refactor the 4 new inline queryFn sites to the sanctioned `useApiQuery` helper, OR bump `BASELINE` 93→97 with justification per the ratchet's accept path.
- **Severity:** blocks #1232 `main` (REQUIRED) once GC1 is cleared. **Owner:** ours (V2-shell data hooks).

> **AUDIT-SCOPE CORRECTION:** round-1 catalog C3 ("GC1 is the sole base=main-only red") was scoped to the GITHUB_BASE_REF *ratchet* class and did not run the full mobile affected-test suite vs main. C5/C6 are base=main-EXPOSED *test* failures hidden behind the GC1 `bash -e` short-circuit on #1232. They are real and block the land; WI-842 (GC1) is necessary but NOT sufficient for a green #1232 `main`.

---

## Catalog summary (for orchestrator routing)

**Actionable entries: 2.** Both pre-existing/structural, neither introduced by the round-2 work itself:
- **C1 — GC1 base=main ratchet** (5 inherited internal mocks surface as net-new vs base=main). Trivial fix parked in `stash@{0}`. Owner: ours (test hygiene). Blocks a future landing; clears in minutes.
- **C2 — Flag-ON integration lane 500s** (POST /v1/profiles, ambient on main, allowed-red today). Owner: ours (CI integration-DB / pre-repoint seed). Real debt, not a #1232 blocker.

**Cleared dimensions (no entry): C3** (no other base=main-only red — Codex structurally proven), **C4** (canon-fit: V2 flag-dark + spec-aligned; LLM additive; functional smoke green).

**Landing readiness (REVISED after WI-842 execution surfaced C5/C6):** clearing GC1 (C1) is necessary but NOT sufficient. The required `main` check on #1232 fails fast at GC1 today; once GC1 clears, the affected-test step runs and fails on **C5** (screen-navigation exit-nav, 4 V2 screens) + **C6** (inline-queryFn ratchet 97>93). Both must also clear for a green #1232 `main`. C2 stays allowed-red. So the land needs C1 **+ C5 + C6**.

**Actionable entries: 4, all now WI-homed** — C1=**WI-842** (GC1, fix ready, stays open as 1 of 3), C2=**WI-817** (flag-on, allowed-red), C5=**WI-843** (screen-nav exit-nav on 4 V2 screens, ours/V2-shell), C6=**WI-844** (inline-queryFn ratchet 93→97, ours/V2-shell). All in WS-13/PRG-17 (per orch-008: new-llm-correctness blockers).

**"new-llm green" = WI-842 (GC1) ∧ WI-843 (C5) ∧ WI-844 (C6).** Remediation EXECUTION of WI-843/844 + the new-llm→main land are HARD-HELD for operator go (orch-008). WI-842 stays claimed/open (no `complete`; AC unreachable until the combined green-up merges). gc1-allow kept as-is; Pattern-A skipped (optional polish).

---

## COMPACTION STATE (read this first on resume)

> **✅ LANE COMPLETE (post orch-019, 2026-06-19): LANDED to main + all 5 WIs finalized → Reviewing; STOOD DOWN.** new-llm→main merged --no-ff (merge commit `c86bcc110`; `8d5577a36` ancestor of main). Orchestrator verified clean (required all green; claude-review re-run APPROVED 0/0/0 — the earlier red was a verdict-artifact-upload glitch; Flag-ON ic-116 allowed-red diffed). FINALIZED via cosmo:execute complete (Fixed In=8d5577a36, lifecycle summaries authored): WI-842 (GC1), WI-843 (C5 screen-nav), WI-844 (C6 use-api-query), WI-845 (C7 recaps→withProfile + recaps GC6 + XP-sync doc), WI-846 (MF2 subject-hub V2 nav + _layout phantom-route + SF4). Reviewer closes Gate-2. WI-848 (decay-path xp_ledger residual, Bug/P2) STAYS OPEN as tracked follow-up. Monitors stopped; inbox watcher stopped; STOOD DOWN per orch-019. Reported prg17r2-018. **5 additive explicit-refspec pushes, zero force/collision** (one accepted bare-push incident early; upstream hazard neutralized). Inbox last=`orch-019`, outbox last=`prg17r2-018`.
>
> **(superseded) CURRENT STATUS: strict-green verified; orchestrator landing --no-ff; holding for land confirmation.** orch-018: orchestrator independently verified #1232 head 8d5577a36 — ALL required checks GREEN; the claude-review CHECK red is a VERDICT-ARTIFACT-UPLOAD glitch (run log computed APPROVED, green:true, 0must/0should/0consider, then failed only on 'claude-review-verdict.json not found'; 3 token fallbacks → NOT exhaustion/findings; AGENTS.md 'workflow broken not findings'). Flag-ON=ic-116 allowed-red (non-required; UNSTABLE not BLOCKED → mergeable). Orchestrator RE-RAN claude-review (27802815993) for a clean green, then does the --no-ff LAND. MY LANE + WS-18/847 FULLY EXHAUSTED — NO FURTHER ACTION; HOLD. Stopped strict-green monitor bgzr745yw (orchestrator verified directly, bypassing my stale-comment monitor). ON LAND CONFIRMATION (main carries new-llm): finalize WI-842/843/844/845/846 → Reviewing via cosmo:execute complete (Fixed In=landed/squash sha); WI-848 stays open (tracked follow-up). Inbox watcher `b0r6bizgb` live for the land confirmation. Pushes: 5 additive, all explicit-refspec FF, zero force/collision. Inbox last=`orch-018`, outbox last=`prg17r2-017`.
>
> **(superseded) CURRENT STATUS: my items exhausted + pushed 8d5577a36; WS-18 pushed cb53a46f2; awaiting settled re-review.** orch-017: accept verdict + WI-848 (non-blocking) + DO option (a) — added documenting comments at BOTH XP-sync removal sites (retention-data.ts processRecallTest + review-calibration-grade.ts handleReviewCalibrationGrade) citing 5fed808e9 + verified-at-insert invariant + WI-848 (comment-only; prettier clean, api tsc exit 0); committed 8d5577a + additive-pushed (FF cb53a46f2→8d5577a36). After this my items are DONE — every claude-review finding in my charter addressed (ratchets C5/C6/C7, MF2 subject-hub, _layout phantom-route, SF3 recaps-auth, recaps GC6, SF4 self-cleared, XP-sync documented+WI-848). WS-18/847 pushed cb53a46f2 ('clear #1232 land-unblock review items' — apply-retention/identity-graph/duck-type). #1232 head=8d5577a36, fresh CI+review RUNNING (checks pending; latest posted review still the d1ed91348 0must/3should). HOLD: strict-green monitor `bgzr745yw` (review rows→0/0) + `b0r6bizgb` (inbox). On settled 0must/0-valid-should + required all SUCCESS + Flag-ON allowed-red DIFFED → REPORT strict-green → orchestrator lands --no-ff → finalize WI-842/843/844/845/846. Pushes: 5 additive, all explicit-refspec FF, no force/collision. Inbox last=`orch-017`, outbox last=`prg17r2-017`.
>
> **(superseded) CURRENT STATUS: my lane clean; XP-sync→WI-848; holding for (a)-vs-accept + WS-18 push.** XP-SYNC VERDICT (sub-agent): INTENTIONAL/superseded — commit 5fed808e9 removed the retention-path xp_ledger sync deliberately; post-sunset insertSessionXpEntry writes xp_ledger.status='verified' AT INSERT, so the verified path is safe and the reviewer's 'users lose XP credit' claim is REFUTED. Residual REAL-but-LOW gap: decay path (xpChange==='decayed') updates retention_cards.xpStatus but NOT xp_ledger.status (progress.ts displays the latter → stale 'verified' after decay); pre-launch/0-users/display-only. Routed → **WI-848** (Bug/P2, retention/XP lane, page 3848bce9-1f7c-8152-ac07-df55e607fc7f); NOT fixed inline. Reported prg17r2-017 — asked: add doc-comments at the 2 removal sites to clear the should-fix (option a) OR accept WI-848 as tracked resolution (option b, done). origin/new-llm=d1ed91348 (MINE); WS-18/847 still not pushed (Flag-ON red, apply-retention unchanged); latest review 0 must-rows / 3 should-rows (XP-sync + WS-18's identity-graph + apply-retention.ts). HOLD. Monitors: bgzr745yw (strict-green rows→0/0) + b0r6bizgb (inbox). Inbox last=`orch-016`, outbox last=`prg17r2-017`.
>
> **(superseded) CURRENT STATUS: my lane clean; NOT strict-green; WS-18 not pushed + NEW XP-sync should-fix needs owner.** origin/new-llm=`d1ed91348` (MINE). Latest review (02:49Z) CHANGES_REQUESTED, must-fix=0 (NONDETERMINISTIC — apply-retention-update.test.ts unchanged + Flag-ON still RED; WS-18 never pushed it), 3 should-fix rows ALL non-mine: [NEW] retention-data.ts + review-calibration-grade.ts syncXpLedgerStatus REMOVED (potential XP-credit regression, retention/XP lane, needs owner); [WS-18/847] identity-graph.ts cache + apply-retention-update.ts duck-type. SF4 GONE (my /profiles comment worked) → MY LANE FULLY CLEAN, zero of my items remain. Required checks all PASS (Flag-ON non-required, allowed-red, still fail). HOLD: won't touch WS-18/identity/retention-XP. Reported prg17r2-016 (route XP-sync finding; confirm WS-18/847 inbound). Strict-green monitor `bgzr745yw` (counts actual review rows → 0must/0should) + `b0r6bizgb` (inbox). On 0-valid-should + Flag-ON resolved/diffed → REPORT strict-green → orchestrator lands → finalize WI-842/843/844/845/846. Pushes: 4 additive, all explicit-refspec FF. Inbox last=`orch-015`, outbox last=`prg17r2-016`.
>
> **(superseded) CURRENT STATUS: my lane fully complete + pushed; strict-green waits on WS-18/WI-847.** orch-015 rulings executed: (1) recaps.test.ts GC6 → CONVERTED to canonical jest.requireActual + targeted-override (dropped redundant gc1-allow; GC1 base=main exit 0, recaps 13/13, prettier clean), pushed additive d1ed91348 (WI-845); (2) SF4 AccountAdminSheet /profiles RULED INVALID by orchestrator — my fix stands, excepted-invalid, no action. EVERY real claude-review item in my scope is DONE + on new-llm: ratchets C5/C6/C7 (WI-843/844/842), MF2 subject-hub goBack + _layout phantom-route (WI-846), SF3 recaps auth-doc + recaps GC6 (WI-845); SF4 excepted. REMAINING for strict-green (0must/0-VALID-should): ONLY WS-18/WI-847 (MF1 apply-retention-update.test.ts + SF apply-retention-update.ts + SF identity-graph.ts). HOLD: don't touch WS-18/identity; tip moves under me from WI-847 (additive, expected). Monitors: `b0z7ndanz` (#1232 verdict→must-fix=0) + `b0r6bizgb` (inbox). On 0must/0-valid-should → REPORT strict-green → orchestrator lands --no-ff → finalize WI-842/843/844/845/846 → Reviewing. Pushes: 4 additive rounds, all explicit-refspec FF, no force/collision. Inbox last=`orch-015`, outbox last=`prg17r2-015`.
>
> **(superseded) CURRENT STATUS: my V2-nav/recaps lane clean + 2 rounds pushed; holding for WS-18 + recaps-owner + reviewer-error.** Round-1 push (6273f98ef) re-review dropped 2MF/4SF→1MF/3SF (cleared MF2 subject-hub + SF3 recaps-auth). Round-2 PUSHED (additive FF→`3f4ce8e4a`): _layout.tsx +'subject-hub' to HIDDEN_TAB_ROUTES (bug-763 phantom-route guard) + AccountAdminSheet /profiles clarifying comment. Validated (tsc clean, related 74/74). RESIDUAL 0must/0should OWNERSHIP: [WS-18 identity] MF1 apply-retention-update.test.ts + SF apply-retention-update.ts + SF identity-graph.ts; [recaps area, owner TBD] recaps.test.ts GC6 (new-llm's test edit; I lean to take it — adjacent to WI-845/C7 — on orchestrator nod); [CLAUDE-REVIEW ERROR, operator-only] SF4 AccountAdminSheet /profiles — reviewer insists on dead route '/(app)/profiles'; my /profiles is CORRECT (top-level app/profiles.tsx); will NOT change to a dead route, flagged for operator exception. MY real lane work DONE (ratchets C5/C6/C7 + MF2 + subject-hub phantom + recaps auth-doc + AccountAdminSheet typed/commented). Monitors: `b0z7ndanz` (#1232 verdict→must-fix=0) + `b0r6bizgb` (inbox). HOLD: don't touch WS-18/identity; tip may move under me (WS-18, expected). Report strict-green at 0must/0should → orchestrator lands. WI-842/843/844/845/846 claimed. Inbox last=`orch-014`, outbox last=`prg17r2-014`.
>
> **(superseded) CURRENT STATUS: my fixes pushed+banked; holding for WS-18 + land-ready re-review.** orch-014 routed MF1/SF1/SF2 to the WS-18 shepherd (identity area: apply-retention-update + identity-graph) — NOT mine — and AUTHORIZED option (iii): additive-pushed my 3 held fixes. DONE: pushed `6273f98ef` (EXPLICIT refspec, clean additive FF acc824811→6273f98ef); origin/new-llm=6273f98ef; #1232 head=6273f98ef, fresh CI run 27801348776 in progress. This banks MF2/SF3/SF4 (reviewed-clean) → PR drops to 1MF (MF1) + 2SF (SF1/SF2), all WS-18's. NOW HOLDING: do NOT touch MF1/SF1/SF2; WS-18's executor pushes its fixes on top (additive — tip WILL move under me, EXPECTED, don't fight). Land-ready monitor `b0z7ndanz` watches #1232 claude-review verdict → must-fix=0; on 0must/0should + required SUCCESS + allowed-red DIFFED → REPORT strict-green → orchestrator lands --no-ff. WI-842/843/844/845/846 stay claimed until then. Inbox last=`orch-014`, outbox last=`prg17r2-013`. Watcher `b0r6bizgb` live.
>
> **(superseded) CURRENT STATUS: push-gate (b) unmet — WI-825 settled without MF1; holding for coordination.** orch-013 granted autonomous push authority (gate: tip stable ≥30min AND WI-825 must-fixes cleared/converging). At settle: gate (a) MET (acc824811 stable 30min, WI-825 stopped pushing) but gate (b) FAILED — WI-825's commits never touched apply-retention-update.test.ts/.ts; MF1 STILL LIVE (zero ensureLegacyProfileAnchorForTest) and STILL the must-fix on the current acc824811 claude-review (00:33Z BLOCKED 2MF/4SF: MF1 apply-retention [WI-825], MF2 subject-hub [MINE, held-fixed], SF1/SF2 [WI-825 untouched], SF3/SF4 [MINE, held-fixed]); Flag-ON still red. PR can't reach strict-green: MF1 is WI-825/817's lane (orch-013: keep untouched) and they went quiet with it open. MY fixes ready+held at `6273f98ef` (pushing now only drops 2MF→1MF, can't reach strict-green). NOT pushing (gate b unmet); NOT touching MF1/SF1/SF2/C2. Reported `prg17r2-013` — recommend (i) WI-825 resume MF1, else (ii) reassign MF1 to me, else (iii) authorize my interim additive push. Awaiting orchestrator coordination. Inbox last=`orch-013`, outbox last=`prg17r2-013`. Watcher `b0r6bizgb` live; convergence monitor ended (settle fired).
>
> **(superseded) CURRENT STATUS: my fixes committed+held; monitoring WI-825 convergence; awaiting push-window.** All my lane's claude-review items done + committed `6273f98ef` (on acc824811, NOT pushed): MF2→WI-846 (subject-hub goBack branches on MODE_NAV_V2_ENABLED + a V2-on regression TEST added — both branches verified), SF4 folded into WI-846 (AccountAdminSheet /profiles as Href; reviewer's /(app)/profiles was a dead route), SF3→WI-845 (recaps auth-doc). FRESH adversarial review (MF2 logic fix)=ZERO must/should (1 consider=V2-test gap, CLOSED). Validated: tsc clean, subject-hub 4/4 (incl new V2 test), screen-nav 80/80, route-context 4/4, recaps 903. **orch-013 GRANTED autonomous push-window authority** (no manual signal needed). PUSH GATE (both required): (a) new-llm tip STABLE ≥30min (no new WI-825 commits) AND (b) WI-825 must-fixes (MF1/SF1/SF2 + C2/Flag-ON) cleared/converging on claude-review (not pushing into churn). ADDITIVE-only: on any non-FF reject → ABORT, re-fetch, re-eval, NEVER force. STOP-CONDITION: if new-llm won't settle by ~2-3h or re-reviews churn → STOP + report for operator AM cross-lane sequencing. Monitors: `beksf5o80` (WI-825 settle: new-llm tip stable ≥30min OR Flag-ON→pass) + `b0r6bizgb` (inbox). ON WINDOW: rebase 6273f98ef onto settled tip → push EXPLICIT refspec HEAD:new-llm → re-trigger claude-review → 0must/0should → report strict-green → orchestrator lands --no-ff. NOT touching MF1/SF1/SF2/C2 (WI-825/817/805). WI-842/843/844/845/846 claimed/Executing (finalize→Reviewing post-convergence + clean re-review). Inbox last=`orch-012`, outbox last=`prg17r2-012`.
>
> **(superseded) CURRENT STATUS: my claude-review items prepared + under review; holding push for window.** orch-012: HOLD land (review BLOCKED); do NOT race WI-825; take ONLY my items, PREPARE but DON'T push until signaled. DONE (worktree FF'd to acc824811): MF2→**WI-846** (Bug/P2, page `3848bce9-1f7c-8128-9e86-c4596391d55f`, claimed) — subject-hub/[subjectId]/index.tsx goBack fallback now branches on FEATURE_FLAGS.MODE_NAV_V2_ENABLED (V2→/(app)/subjects else /(app)/library); SF4 folded into WI-846 — AccountAdminSheet.tsx router.push('/profiles' as Href) (route is /profiles TOP-LEVEL; reviewer's /(app)/profiles was WRONG); SF3→WI-845 — recaps.ts /recaps/self auth-boundary doc comment (posture correct, self-scoped). VALIDATED: mobile+api tsc clean, route-context guard 4/4 (count unchanged), recaps 903, subject-hub+AccountAdminSheet related 7/7, screen-nav 80/80. Full adversarial review (MF2 logic fix) RUNNING. NOT touching MF1/SF1/SF2/C2 (WI-825/817/805 lanes). NEXT: review-clean → COMMIT (prepared, held) → MONITOR #1232/new-llm for WI-825 convergence to settle → on orchestrator window: rebase onto settled tip + push EXPLICIT refspec → claude-review to 0must/0should → report strict-green → orchestrator lands. WI-842/843/844/845/846 stay Executing (finalize→Reviewing only after convergence + clean re-review). Inbox last=`orch-012`, outbox last=`prg17r2-011`. Watcher `b0r6bizgb` live.
>
> **(superseded) CURRENT STATUS: ratchet mission done + main green; claude-review BLOCKED; holding for coordination.** Ratchet chain enumerated → C5/C6/C7 all fixed + on new-llm; on CURRENT head **acc824811** (CI 27797393312=SUCCESS) ALL REQUIRED checks PASS (main ✓, claude-review ran ✓, run-smoke ✓ [flake cleared], API Quality Gate/Merge completeness/Playwright web smoke ✓); mergeState=UNSTABLE (required green, non-required C2 red). HEAD MOVED 589a6773b→acc824811 via concurrent **WI-825** commits (another session converging the C2/Flag-ON identity-v2 integration lane). claude-review COMMENT verdict=**BLOCKED, 2MF+4SF** on the WHOLE PR (~100 files, all sessions): MF1=apply-retention-update.test.ts missing ensureLegacyProfileAnchorForTest (C2/WI-825 lane, not mine); MF2=subject-hub/[subjectId]/index.tsx V2 nav regression (legacy /library fallback vs V2 /subjects — real logic bug → route to WI per STOP-AND-REPORT, not my file); SF3=recaps.ts /recaps/self owner-guard DOC (MINE, my C7 lines — auth posture correct, fix=1-line comment, take on go); SF1/SF2/SF4 other sessions. Reported `prg17r2-011`. Did NOT push past 589a6773b (won't push into moving branch until orchestrator sequences convergence). WI-842/843/844/845 stay claimed/Executing (NOT finalizing→Reviewing while PR review-BLOCKED). Inbox last=`orch-011`, outbox last=`prg17r2-011`. Inbox watcher `b0r6bizgb` live.
>
> **(superseded) CURRENT STATUS: C7 fixed + reviewed-clean + pushed; watching #1232 (head 589a6773b).** Ratchet chain enumerated (one-pass local) → C7 sole residual; C7 (recaps.ts /recaps/self → withProfile) committed `589a6773b`, FRESH adversarial review = ZERO findings (behavior-preserving, no IDOR, scope-clean), pushed via EXPLICIT refspec `HEAD:new-llm` (clean FF ddd9beef2→589a6773b; bare-push hazard neutralized — branch upstream repointed to new-llm-r2-gc1). origin/new-llm=`589a6773b`. #1232 head=589a6773b, fresh CI run 27797333818 in progress (all checks pending); `main` should now pass (C5/C6/C7 all fixed+verified locally: route-context guard 108, 903 recaps tests, api tsc clean). Monitor `be3ft597j` watching main + claude-review. NEXT: strict-green → report prg17r2-011 → finalize WI-842/843/844/845 → Reviewing → orchestrator lands. Non-required reds remain non-blocking (C2 Flag-ON ambient; run-smoke J-03 flake). Inbox last=`orch-011`, outbox last=`prg17r2-010`.
>
> **(superseded) CURRENT STATUS: ratchet chain enumerated + C7 fixed; fresh review running.** orch-011 = drive new-llm strict-green AUTONOMOUSLY (no per-red routing); enumerate the WHOLE base=main ratchet chain in one local pass; fix each ratchet-intended or justified-bump; STOP+route only if a red is non-trivial; ONE consolidated WI; fresh adversarial review; explicit-refspec push only. DONE: enumerated ALL base=main ratchets locally (GC1, screen-nav, use-api-query, route-context, no-clinical-copy, i18n-jsx-literals, decision-adr, persona-fossil, test-only-exports, i18n staleness/untranslated + full 14 api guards + 23 scripts tests) → **C7 is the SOLE residual trip** (no siblings). WI-845 (page `3848bce9-1f7c-8155-b2a0-cd3a640b00be`) created WS-13/PRG-17/MentoMate/Task/P2/Assisted/Item, State=Active, CLAIMED (Stage=Executing, claimant claude:vetinari:WI-845). C7 FIXED: migrated recaps.ts `/recaps/self` inline-unwrap → `withProfile(c)` (behavior-preserving; the 2 pre-existing sites untouched); route-context guard GREEN (108≤108), 903 recaps tests pass, api tsc clean. Fresh adversarial review on C7 running. NEXT: review-clean → commit → **push EXPLICIT refspec `HEAD:new-llm`** (never bare) → drive #1232 to STRICT green → finalize WI-842/843/844/845 → Reviewing → orchestrator lands. Inbox last=`orch-011`, outbox last=`prg17r2-010`.
>
> **(superseded) CURRENT STATUS (post prg17r2-010): #1232 main RED on C7; awaiting fix-inline-vs-route ruling.** orch-010 ACCEPT executed: #1233 CLOSED; #1232 mergeable (no conflict). Checks settled: claude-review PASS; required API Quality Gate / Merge completeness / Playwright web smoke PASS. But REQUIRED `main` FAILED — NOT my green-up: the `main` bash -e chain cleared GC1 (mobile C5/C6 tests now PASS in CI), then died at **C7 = route-context withProfile ratchet 109>108** (`apps/api/src/routes/recaps.ts` +1 inline `requireProfileId(c.get('profileId'))`, HEAD=3 vs main=2 — new-llm's recaps work, pre-existing, masked behind GC1; mask proven by run-duration 4min→7.5min). CHAIN RISK: bash -e stops at first failure → more masked reds may follow C7. Non-required reds (non-blocking): Flag-ON integration (C2 ambient) + run-smoke (J-03 ModeSwitcher flake, 10/11). Reported `prg17r2-010` recommending (i) fix C7 inline (recaps.ts→withProfile) + WI for traceability, iterate to strict-green; alt (ii) route C7 to a WI first. NOT self-expanding scope w/o ruling. WI-842/843/844 stay claimed/Executing (AC unreachable until chain clears). Inbox last=`orch-010`, outbox last=`prg17r2-010`. Inbox watcher `b0r6bizgb` live.
>
> **(superseded) CURRENT STATUS (post orch-010): INCIDENT RESOLVED = ACCEPT (A); WATCHING #1232 to strict-green.** orch-010 ruled ACCEPT — no revert/force-push; new-llm=`ddd9beef2` (FF, intended green-up), main UNCHANGED, #1232 head=ddd9beef2 gates the content. DONE: closed PR #1233 (superseded); confirmed #1232 `mergeable=MERGEABLE` (main+1 from WI-805 squash does NOT conflict — no main-merge needed). REQUIRED checks (main branch protection): API Quality Gate ✓, Merge completeness ✓, Playwright web smoke ✓, **`main` PENDING** (the previously GC1/C5/C6-blocked check; should now pass). Non-required reds (non-blocking): Flag-ON integration = C2 ambient allowed-red (ic-116, diff vs baseline); `run-smoke` = 1 flaky test J-03 ModeSwitcher visibility-timeout (10/11 pass, unrelated to my mobile-hook/account changes, non-required). WATCHING `main` + `claude-review` (monitor `bpmx14sn9`) → on strict-green report `prg17r2-010` + finalize WI-842/843/844 → Reviewing (cosmo:execute complete; Fixed In=ddd9beef2) → orchestrator does new-llm→main `--no-ff` land. RULE going forward: push only with explicit refspec `HEAD:<wi-branch>`, never bare. Inbox last=`orch-010`, outbox last=`prg17r2-009`.
>
> **(resolved) ⚠ INCIDENT (2026-06-19, reported prg17r2-009): BARE-PUSH misfire landed the green-up directly on PROTECTED new-llm.** The forked /commit ran a bare `git push`; this worktree branch's upstream is `origin/new-llm` (worktree-setup tracking), so it FAST-FORWARDED origin/new-llm 722aa51a→`ddd9beef2` — landing GC1+C5+C6 onto new-llm directly, bypassing PR #1233, claude-review, and the orchestrator's merge authority. Fast-forward only (no history rewritten); substance = exactly the intended green-up, fully validated pre-push + adversarial-review-clean. PR #1233 head new-llm-r2-gc1 still at 6d548c1 (behind, superseded). I did NOT force-push and did NOT touch main/WI-805/prod/drop. ROOT CAUSE: branch upstream=origin/new-llm + bare push (should have used explicit refspec HEAD:new-llm-r2-gc1, as for WI-842). AWAITING orchestrator ruling: (A) accept fast-forwarded state → watch new-llm CI strict-green → orchestrator lands new-llm→main; or (B) revert (force-push new-llm→722aa51a is orchestrator-only) → I re-push to #1233 for the gate. Inbox last=`orch-009`, outbox last=`prg17r2-009`.
>
> **(prior) CURRENT STATUS (post orch-009 REMEDIATION-GO, 2026-06-19): EXECUTING the combined green-up as runner-as-executor.** orch-009 authorized overnight remediation + autonomous execution under full executor-protocol rigor. I am EXECUTOR for new-llm green-up = WI-842 (GC1) ∧ WI-843 (C5) ∧ WI-844 (C6), all claimed (Stage=Executing). PROGRESS: rebased `new-llm-r2-gc1` onto current origin/new-llm (722aa51a docs-only, clean); C5 FIXED (3 V2 tab roots → EXEMPT in screen-navigation.test.ts; account.tsx given a real `goBackOrReplace(router,'/(app)/home')` back affordance) — jest 80/80 green; C6 FIXED (migrated useJournalRecaps + useNowOverflow to useApiQuery; left 3 non-migratable new sites inline — useNowFeed custom-opts+cache, use-subject-hub ×2 useQueries; BASELINE 93→95 empirical + contract-comment rewritten to document the exception) — guard green. tsc clean, related tests 17/17, prettier clean. C5/C6 edits UNCOMMITTED in worktree; GC1 already committed (0f27ac8 + 40973ff). NEXT: adversarial review (fresh session, running) → full local validation (check-change-class.sh --run) → commit+push onto #1233 → drive STRICT green → REPORT. MERGE BOUNDARY: I NEVER merge; orchestrator merges the green-up PR into new-llm + does the `--no-ff` new-llm→main land on my strict-green report. #1232 stays base=main oracle until the land; no main/WI-805/prod/drop by me. Inbox last=`orch-009`, outbox last=`prg17r2-008`. Lines below this banner are earlier-phase history — trust the banner + EXECUTION STATE block on conflict.

- **Role/mission:** integration-RUNNER → AUDITOR (orch-004) → now also GC1 executor (orch-006, WI-842 only). Audit catalog complete + routed.
- **Worktree:** `.worktrees/new-llm-sync`, branch `new-llm-r2-gc1` (tracks `origin/new-llm`). **GC1 fix is COMMITTED** (`6d548c1`, multiline gc1-allow, prettier-stable) and PUSHED to PR #1233 — NOT the parked-stash state anymore (`stash@{0}` still exists as the original single-line artifact, superseded by the commit). Worktree HEAD = `6d548c1`. Shared root is on `main`; never checkout/switch/stash there.
- **PR #1232** (new-llm→main): keep OPEN, do-not-merge/do-not-close. `origin/main` `c0ec04a3` is a full ancestor of `origin/new-llm` (44 ahead / 0 behind) → clean no-op merge; recaps.ts judgment call already resolved in-tree (new path threads `identityV2Enabled`, L286-289). The `--no-ff` landing is operator-go only.
- **CI terminal on #1232:** `main` FAIL (GC1 only) · Flag-ON integration FAIL (ambient baseline) · everything else incl. claude-review PASS.
- **Local gate (worktree, done green):** tsc --build exit0 · nav-contract suite PASS 1511/0 · eval:llm Tier1 clean · base=main ratchets all 0-new.
- **Codex review:** COMPLETE (agentId `a5584a9668d5a9163`) — CLEAN, zero new findings, corroborates C1 + C3. Folded into C4. Resume via SendMessage to that id if needed.
- **Channel:** `_wip/new-llm-integration/_state/{inbox,outbox}.jsonl`. Inbox last = `orch-008` (route C5/C6→WIs done; WI-842 stays open as 1-of-3; HARD HOLD remediation+land for operator). Outbox last = `prg17r2-008`. Inbox watcher Monitor `bwjpty5p8` (persistent) armed — **RE-ARM after any session restart** (dies on session end): re-issue a Monitor tailing new lines in inbox.jsonl. CI monitors `b5era915k` + `by42xw8se` ended.
- **Next inbound:** operator remediation-go for WI-843/844 + the land, and the execution-mechanism ruling. Until then: HOLD.

## EXECUTION STATE — C1 = WI-842 (orch-006 greenlight, orch-007 land-auth)

- **C2 → WI-817** (page `3838bce9-1f7c-8189-8575-dd82b9b6a40b`): commented linking C2 audit + catalog. DONE, no new WI.
- **C1 → WI-842** (page `3838bce9-1f7c-81dc-a2dc-d7270fe8f03c`): created in WS-13 / MentoMate / Task / P2 / Assisted / Item; State=Active set; CLAIMED via cosmo:execute (Stage=Executing, claimant `claude:vetinari:WI-842`, artifacts `/tmp/wi-842-artifacts`).
- **Fix:** worktree `.worktrees/new-llm-sync` branch `new-llm-r2-gc1`. Commit `97cb6f1` (initial stickers) had a BUG — pre-commit prettier moved each `// gc1-allow` off the `jest.mock(` line, failing GC1 single-line check. **FIXED in commit `6d548c1`**: converted all 5 to MULTILINE `jest.mock(` form (comment between open-paren and specifier — script-supported + prettier-stable; `prettier --check` clean). Verified: GC1 staged exit 0, **GC1 CI-mode base=main exit 0 (no offending lines)**, nav-contract jest 2/2, pre-push passed. Pushed `97cb6f1..6d548c1` to `new-llm-r2-gc1`.
- **PR #1233** (head `new-llm-r2-gc1`, base `new-llm`, OPEN): driving to green. Monitor `by42xw8se` armed on its checks. claude-review is the gate (Codex already cleared the stickers in audit → no separate adversarial pass).
- **orch-007 land-auth:** new-llm→main land now authorized at ORCHESTRATOR level on STRICT green (no longer operator-AM-deferred). MY mechanics unchanged: #1233 green → I merge #1233 to new-llm (executors MAY merge to new-llm; base=new-llm) → confirm #1232 strict-green → REPORT (b). I NEVER self-merge new-llm→main — orchestrator does the --no-ff land after verifying my strict-green report. Hard-stop stands: no main/WI-805/prod/drop direct touches.
- **BLOCKED (reported prg17r2-007):** WI-842 GC1 fix is correct + locally green (GC1 staged exit0, GC1 CI-mode base=main exit0, nav-contract 2/2, prettier-stable). BUT #1233's required `main` check is RED on the affected-test step — **C5 + C6 unmasked** by clearing GC1. #1233 cannot go green, #1232 `main` cannot go green, until C5/C6 also clear. WI-842's AC ("#1232 main green") is unreachable by GC1 alone. Also: claude-review left a Pattern-A should-fix on #1233 (CHANGES_REQUESTED, advisory; gc1-allow is orch-006-acceptable + Codex-cleared) — moot for "green" until C5/C6 clear; Pattern-A conversion is viable per advisor if wanted.
- **WI-842 commit state:** `97cb6f1` (initial, prettier-broke) → `6d548c1` (multiline gc1-allow, prettier-stable) pushed to `new-llm-r2-gc1` / PR #1233. NOT merged. WI-842 still Stage=Executing (claimed). Did NOT run cosmo:execute complete (blocked).
- **RESOLVED by orch-008:** C5/C6 routed → WI-843 (page `3838bce9-1f7c-8153-b774-f47c568f0f39`) + WI-844 (page `3838bce9-1f7c-8107-bd3b-f3862964a6b9`), both WS-13/MentoMate/Task/P2/Assisted/Item/Ready/State=Active, owner=ours/V2-shell in body, AC = the named test green. WI-842 stays claimed/open (no complete; gc1-allow as-is; Pattern-A skipped). Reported `prg17r2-008`. **NOW HOLDING for operator remediation-go** — do NOT start fixing WI-843/844; await the go + execution-mechanism directive.
- **Key memory:** `feedback_allowed_red_lane_masks_integration_test_reds.md` (ic-116), `feedback_forward_ratchets_not_in_prepush.md`, `project_prg11_arch_cleanout_lane.md`.
