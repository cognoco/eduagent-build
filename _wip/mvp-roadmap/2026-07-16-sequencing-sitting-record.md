# Roadmap sequencing sitting — ruling record (2026-07-16)

**Status:** RULED (thin sitting, SEQ-1..6). Companion to `2026-07-15-lockdown-lane-load-preread.md` (LD record) and `2026-07-15-full-backlog-disposition-sweep.md`.

**⚠ STANDING ORDERS (operator, ruled this sitting):**
1. **Comprehension, not commitment.** This sequencing and the visualization built from it are comprehension artifacts — they carry NO plan authority unless explicitly promoted to plan-of-record as a separate, deliberate decision. Possibly throwaway; that is fine.
2. **The map is not the terrain.** Whatever roadmap exists is a snapshot of intended shape as of its date — never a detailed execution plan followed slavishly. **Before EVERY batch kickoff: re-base** — verify premises (gate status, human clocks, QA arrivals, parallel-session landings, lane realities) and reorganize remaining work if warranted. A kickoff without the premise-check is a process violation.

## Rulings

**SEQ-1 — Phase frame: gate-bounded phases** (no Greek letters). **Phase 1 "Full Parallel"** — everything unblocked runs, all human clocks burn; *ends when the WI-1577 FINAL GATE re-run passes*. **Phase 2 "Submission"** — WI-1114/1115 finalized, WI-1335 submitted, store review cycles; *ends at store approval*. **Phase 3 "Beta & Launch"** — WI-1506 closed beta (needs WI-1764 helplines) → public-launch decision. Phases are bounded ONLY by the gates (the LD2 spine restated); no invented boundaries.

**SEQ-2 — Phase-1 opening: two-wave hypothesis** (explicitly re-decidable at each premise-check). Rationale accepted: the full feasible set (~80 unblocked items) would flood the non-elastic resource — human review/QA/ruling bandwidth (review pile-ups are a documented failure mode). **Wave 1 (now):** Dev-Infra unblockers (WI-1809/1813/1862/1874 + preview-build fixes from intake) · full Compliance-Eng chain (paces the Phase-1 exit) · Identity Cutover · Safety & Eval floor (WI-1986, WI-1898-floor) · QA-factory first drain. **Wave 2 (at first premise-check):** Core Learning Loop fixes · Mobile UX · Supporter & Linking · Four Strands · Store Billing & Release agent-side · trust builds when OPQ-40 design lands. Launch Readiness's ~11 in-flight items simply continue. Waves are *pools batches draw from*, not batches. The unconstrained feasible set stays visible in the visualization (Phase-1 column = feasible set; waves = capacity overlay).

**SEQ-3 — Human duty cycle: event-coupled** (no fixed calendar). Sittings triggered by: (a) batch/wave boundary → premise-check + next-batch ruling + OPQ sweep, co-located; (b) gate approach → focused gate sitting; (c) deadline backstop — an OPQ deadline within 3 days with no sitting scheduled forces one. **Amendment: two committed KICKOFF sittings before execution opens in earnest** — Jørn: clock-starts (OPQ-22 dispatch, OPQ-102 DPO, OPQ-110 DPA loops) + operator-row burn-down; Zuzka: OPQ-40 (DoR-blocks 5 trust builds), OPQ-117 (cap-hit scope), standing queue. Close out everything closable up front.

**SEQ-4 — Gated tails are parallel tracks, not phases.** Tracks alongside the phase axis, each with its own gates: **Stream 2** (autonomous, D-gate cadence) · **V2-finalization/soak tail** (10 items; S5/S6 + soak gates; not launch-blocking — its one launch coupling is the existing WI-2059 release-ADR edge) · **QA factory** (SEQ-5). Pen off-axis, unsequenced.

**SEQ-5 — QA-findings intake: severity-gated hybrid.** Standing model for dogfooding/emulator/beta findings: fast two-question intake — **Q1:** breaks a tree-IN capability or blocks the launch chain? YES → *inherits MVP status*, embeds in owning lane; NO → **QA fix factory** (holding lane, rolling agent drain batches, off the critical path). **Q2:** scope proposal in disguise? → scope authority (operator/Zuzka), never the factory. Coupling mechanism: dogfood (WI-1503) and beta (WI-1506) gates get a checklist line — *no open launch-blocking QA findings*. Factory items unfixed at beta: one severity re-look, then pen. **First intake batch = the 33 parallel-session arrivals (WI-2082+)**; pre-flagged: family-join cluster (WI-2123/2125/2126/2127/2128) = capability regression on tree-IN WI-1753 → embed; WI-2128 = live identity-correctness bug, urgent look; WI-2118/2119/2120 (preview-build breakage) → Dev-Infra embed (preview builds are the QA vehicle); WI-2112 (Challenge redefinition) = scope-in-disguise → Zuzka; WI-2103/2104 look like duplicate captures — dedup at intake.

**SEQ-6 — Batch formation: rule the principle, defer the instances.** Batches are formed at each kickoff/premise-check sitting by **co-execution affinity** (shared surfaces / one verification pass / dependency chain / one agent context), drawing from the current wave's pool. Workstream = taxonomy ONLY, never execution order; when a batch equals a lane, that's an outcome, not a policy. In-batch sequencing = `Blocked by` edges (hard) + batch brief (soft); no order numbers.

> **⚠ HOLD — Delivery Batch productionization awaits the ZDX ruling.** The schema proposal (Delivery Batch as relation to a minimal Batches DB — Name/Status/Formed/`Gated by`/Brief; universal planned-dispatch rule with single-item escape hatch; ordering principle) is filed as a finding in the ZDX stream: `nexus:_WIP/zdx-reboot/findings/delivery-batch-grouping-proposal-2026-07-16.md` (ledger: inbox; operator requested fast-track). **Until ZDX rules, do NOT create a Batches DB or new properties — interim mechanism is the Cosmo `Sprint` property as "pseudo-batches".**

## Next actions (post-sitting)
1. QA-factory lane creation + intake of the 33 (two-question proposals → operator batch-accept).
2. Visualization (comprehension artifact — carries the standing-order banner): View A = HITL impact flow (3 buckets → OPQ/WI → gates → collapsed downstream, with the "runs regardless" negative space); View B = phase×swimlane roadmap, tracks as bands, wave overlay on the Phase-1 feasible set. Interactive HTML in `_wip/mvp-roadmap/` from a re-runnable export + mermaid fallback doc.
3. Kickoff sittings (Jørn, Zuzka) — then Wave 1 batch formation via Sprint pseudo-batches.
