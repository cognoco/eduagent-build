**What was done:** Preserved the V2 freeform Mentor route opener as the first real persisted learner/Mentor exchange, including deterministic outbox idempotency, transcript-aware recovery, and opener subject/topic context across the internally allocated session-ID route backfill. Detailed historical and reviewer-rework evidence is in `docs/evidence/WI-2099/report.md`.
**What changed:** Retained the original opener pipeline changes in `apps/mobile/src/components/session/session-types.ts`, `apps/mobile/src/components/session/use-session-streaming.ts`, and `apps/mobile/src/components/session/use-subject-classification.ts`. Narrowly guarded the internal route backfill in `apps/mobile/src/app/(app)/session/index.tsx` from the full focused-screen reset and added real screen-boundary coverage in `apps/mobile/src/app/(app)/session/index.test.tsx`; the six persisted-session variants remain in `tests/integration/learning-session.integration.test.ts`. No backend wire contract, shared schema, database schema, migration, or non-Mentor behavior changed.
**Verification:** The focused-screen regression passed 1 selected case with 49 skipped; all six persisted-session cases passed with 16 skipped; the full impacted integration file passed 22 of 22; and the three affected mobile suites passed 140 of 140. Fresh mobile typecheck and lint, pre-push checks, formatting, diff hygiene, complete validation, and immutable RED, GREEN, production-only REVERT, and RESTORE proof are recorded in `docs/evidence/WI-2099/report.md`.
**Caveats / Follow-ups:** A replay that finds only the learner event remains pending until the server reports an assistant turn; repairing a permanently interrupted server-side half-turn would require the backend contract change excluded by this item. The integration and focused-screen Jest commands retain the repository's existing open handles and therefore use `--forceExit`; verification ran under the only available Node runtime, version 24, while the repository declares Node 22.
