**What was done:** Repaired the three bounced sibling session boundaries while preserving the five independently green boundaries from PR #2774. Summary submission gates at `apps/api/src/services/session/session-summary.ts:217`; summary retry gates at `apps/api/src/services/session/session-summary.ts:569` and releases an acquired lease on refusal; Recall Bridge gates at `apps/api/src/services/recall-bridge.ts:116` immediately before dispatch at `:117`.

**What changed:** Removed the three route-entry assertions while retaining service delegation at `apps/api/src/routes/sessions.ts:1061`, `:1083`, and `:1181`. Positive and negative service controls are at `apps/api/src/services/session/session-summary.test.ts:571`, `:608`, `:645`, `:670`, `:708` and `apps/api/src/services/recall-bridge.test.ts:128`, `:152`, `:168`, `:194`; the exhaustive ownership and under-gating guard is at `apps/api/src/middleware/metering.coverage.guard.test.ts:257`, `:302`, and `:324`.

**Verification:** The authoritative repeated-command RED/GREEN/production-revert/exact-restore record is `.workitem-artifacts/WI-2543-r2/red-green-revert.md`. The focused run passed 13/13 suites and 589/589 cases. `bash scripts/check-change-class.sh --run --fast` exited 0 with 506/506 API suites, 10,113 passed, 11 skipped, and 10,124 total cases, plus clean TypeScript and structural guards.

**Caveats / Follow-ups:** Four independently deliverable residues remain explicit: `apps/api/src/routes/dictation.ts:242`, `apps/api/src/routes/homework.ts:65`, `apps/api/src/routes/retention.ts:128` with deterministic returns at `apps/api/src/services/retention-data.ts:1128` and `:1189`, and `apps/api/src/routes/assessments.ts:264` before `:267`. The detailed authoritative summary is `.workitem-artifacts/WI-2543-r2/completion-summary.md`.
