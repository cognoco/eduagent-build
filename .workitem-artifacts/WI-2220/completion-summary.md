**What was done:** Threaded an authoritative app-nav shell value from the client into production app-help prompt composition, so a V2-shell request answers from the V2 destination map instead of always defaulting to V0.
**What changed:** Added `appShellSchema` to `@eduagent/schemas` and a `shell` field on `sessionMessageSchema`; added `ExchangeContext.shell?: AppShell` (threaded the same way as the existing `effectiveMode` field); passed `context.shell` into `buildAppHelpPromptBlock` at the exchange-prompts.ts assembly site; threaded `shell` through `prepareExchangeContext`'s options and both the non-streaming and streaming exchange call sites; and updated the mobile `useStreamMessage` send path to source `shell` from `FEATURE_FLAGS.MODE_NAV_V2_ENABLED`. Also unified the pre-existing `AppShell` type in `app-help-map.ts` to re-export the shared schema type instead of maintaining a second, driftable union.
**Verification:** Added co-located unit coverage for the plumbing (`buildSystemPromptSegments` emitting the correct destination map per shell value), the production composition boundary (`processExchange` never emitting retired V0 labels for a V2-shell request), and the safe-default and mid-session-change negative cases. Executed the red-green-revert regression pattern on the production-boundary test — fix in place, plumbing fix reverted, fix restored — to confirm the guard genuinely depends on the change. Ran the full API and mobile unit suites, a full-workspace typecheck, both projects' lint, the i18n gates, and the Tier-1 LLM eval harness, which reported no prompt-output drift.
**Caveats / Follow-ups:** The mobile `useStreamMessage` hook has no pre-existing co-located test file; one existing exact-body assertion in `use-sessions.test.ts` was updated to expect the new field rather than adding new hook-level test scaffolding. `buildAppHelpDirectReply` (a separate, unrelated assessments-feedback path) and the non-streaming `useSendMessage` hook (currently unused by any screen) were left untouched as out of this item's scope. Broader capability-policy consolidation remains deferred to WI-2221 per the ratified acceptance criteria.
