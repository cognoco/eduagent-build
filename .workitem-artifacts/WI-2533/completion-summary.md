**What was done:** Implemented the guardian-attachment and jurisdictional-consent path for credentialed learners whose live country-policy decision requires guardian consent. The learner retains the same Person and Login throughout. A distinct authenticated-adult ceremony validates a short-lived signed authority assertion and current jurisdiction policy before creating or confirming consent authority.

**What changed:** Added a shared strict request/result contract, an authenticated consent route, a domain-separated guardian-authority assertion signer/verifier backed by its own production-required HMAC secret, and one charge-serialized transaction that creates or confirms the global Guardianship edge and appends both consent-purpose grants. Each grant records jurisdiction, exact age, purpose, lawful basis, assurance method, tokenised evidence, policy version, authorization form and optional learner assent. An existing exact attachment is idempotently confirmed; divergent current history fails closed. Invitation remains separate, Supportership is untouched, and ordinary email-consent approval still never creates Guardianship. Mobile receives only the opaque assertion and learner id through a non-retrying mutation.

**Verification:** Focused authority-token and production-config API unit suites passed, as did the mobile mutation suite. A disposable local PostgreSQL integration suite passed the atomic success, idempotent retry, no-supporter-visibility, Person/Login preservation, learning-gate transition, wrong-adult, residence-change, withdrawn, restored, minor-caller, different-existing-guardian and forced-rollback cases. API, shared-schema and mobile typechecks passed. The repository prepush TypeScript build and repository-wide lint completed with no errors; lint reported only the repository's pre-existing warning baseline.

**Caveats / Follow-ups:** The authority token is the server-side boundary after the configured VPC or parental-responsibility verifier has produced a pass/fail assertion; selecting or integrating a specific external VPC provider is not introduced here. Live WI-2037, the named design predecessor, remains Backlog/Active with no reviewed outcome recorded, so the shepherd has ruled it a hard landing dependency even though this implementation follows WI-2533's literal acceptance criteria. Separate residue was reported for the predecessor-state/canon wording mismatch and for createIdentityGraph wiring login_id without setting has_own_account. No merge, execute-complete, review or close is performed by this executor.
