**What was done:** Implemented the executable guardian-authority/VPC ceremony and jurisdictional-consent path for credentialed learners whose live country-policy decision requires guardian consent. The learner retains the same Person and Login throughout. An authenticated mobile callback sends a single-use provider handle to a server-to-server verifier; an approved, fully bound result mints a short-lived authority assertion that redemption revalidates against current organization and country policy.

**What changed:** Added strict initiation/redemption contracts, authenticated Hono routes, production-required verifier URL/key plus an independent HMAC secret, and an external VPC verifier adapter. Authority assertions now bind guardian, learner, destination organization, jurisdiction and policy and carry issued-at/not-before/expiry with a 15-minute maximum TTL and clock-skew checks. Redemption uses the canonical consent-person advisory-lock namespace, canonical exact-date age brackets and live policy reads. Its single transaction creates or confirms Guardianship, appends both stamped consent grants, then terminalizes and back-links the complete consent-request purpose set while invalidating every prior email token. Exact retry is idempotent only when edge, grants and terminal request links all match. Invitation remains separate, Supportership is untouched, and ordinary email approval still never creates Guardianship.

**Verification:** Authority-token and production-config API unit suites passed 100/100. Mobile initiation/redemption hooks passed 3/3 and the production callback consumer passed 1/1. A disposable local PostgreSQL suite passed 10/10, including the real signed-JWT HTTP stack, external verifier boundary, denied VPC, complete request terminalization/back-links, stale email approval, organization move, two-connection canonical locking, exact retry, rollback, no supporter visibility and Person/Login preservation. API/schema/mobile typechecks, repository prepush, the hardcoded-JSX ratchet and changed-file lint passed. Repository-wide lint completed with zero errors and only the pre-existing warning baseline.

**Caveats / Follow-ups:** The external provider remains configuration-selected, matching the identity canon's buy-not-build/provider-open ruling; the application contract is executable through `GUARDIAN_AUTHORITY_VERIFIER_URL` and its private key without embedding vendor facts in mobile. WI-2037, the named design predecessor, is live Ready/Active and remains the shepherd's hard landing dependency. Separate residue was reported for createIdentityGraph wiring `login_id` without setting `has_own_account`. No merge, execute-complete, review or close is performed by this executor.
