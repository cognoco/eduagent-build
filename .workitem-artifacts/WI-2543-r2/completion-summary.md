**What was done:** Repaired the three bounced sibling session boundaries while preserving the five independently green boundaries from PR #2774. Summary submission now gates only after its saved-summary return (`apps/api/src/services/session/session-summary.ts:156`, gate at `:217`); retry feedback returns available/no-claim outcomes first and releases an acquired lease on refusal (`apps/api/src/services/session/session-summary.ts:515`, gate at `:569`); Recall Bridge returns missing-session/topic outcomes before its final gate (`apps/api/src/services/recall-bridge.ts:54`, gate at `:116`, dispatch at `:117`).

**What changed:** Removed the three unconditional route-entry assertions while retaining delegation at `apps/api/src/routes/sessions.ts:1061`, `:1083`, and `:1181`. Added withdrawn-consent positive and pre-dispatch negative controls at `apps/api/src/services/session/session-summary.test.ts:571`, `:608`, `:645`, `:670`, `:708` and `apps/api/src/services/recall-bridge.test.ts:128`, `:152`, `:168`, `:194`; route mapping/delegation controls sit at `apps/api/src/routes/sessions.test.ts:870`, `:990`, `:1011`, `:2637`, `:2666`, `:2686`, `:2708`. The exhaustive guard scans every production route assertion and requires exactly one classified manifest owner at `apps/api/src/middleware/metering.coverage.guard.test.ts:257`, then proves every granular service gate precedes its LLM dispatch at `:302` and `:324`.

**Verification:** Durable tests-before-code RED, candidate GREEN, exact production-revert RED, byte-identical restore GREEN, case names, commands, exit codes, and failure parity are recorded at `.workitem-artifacts/WI-2543-r2/red-green-revert.md`. The final focused run passed 13/13 suites and 589/589 cases. `bash scripts/check-change-class.sh --run --fast` completed with exit code 0: TypeScript and all structural guards passed; API units passed 506/506 suites with 10,113 passed, 11 skipped, and 10,124 total cases.

**Caveats / Follow-ups:** The exhaustive inventory made four independently deliverable mixed-route residues explicit rather than silently classifying them as uniformly LLM-backed: dictation review gates before rate-limit/payload returns (`apps/api/src/routes/dictation.ts:242`); homework OCR gates before content-length/multipart/MIME/file-size validation (`apps/api/src/routes/homework.ts:65`); standard retention recall gates before cooldown and lost-claim returns (`apps/api/src/routes/retention.ts:128`, `apps/api/src/services/retention-data.ts:1128`, `:1189`); quick-check gates before its missing-session return (`apps/api/src/routes/assessments.ts:264`, `:267`). These require separate service-contract work and were not broadened into this rework.
