**What was done:** Traced the joined-family credential path from the authenticated Clerk subject through caller-Person resolution, organization membership, omitted and explicit profile selection, mobile shell restoration, and cached profile state. Repaired the first authority substitution so each credential remains bound to its own Person while guardians retain only the operation authority granted by an active guardianship over an uncredentialed charge.
**What changed:** API profile-scope resolution now starts from the login-bound caller Person, centralizes self-versus-managed-charge authority, filters profile listing and explicit selection through that authority, denies credentialed-sibling and unrelated-person selection, preserves owner behavior, and emits privacy-safe categorical mismatch telemetry. Mobile profile restoration clears stale authority headers and proxy state, revalidates on mount and resume, withholds cached authority during refresh failures, and repairs invalid active selections without changing the learner shell contract.
**Verification:** The mandatory production-only revert reproduced the owner-substitution failure and the exact restore returned the named regression to green. The complete real-database credential-authority suite, every touched API and mobile suite, API and mobile type checks, targeted lint, the routed TypeScript/i18n/prompt-marker/runtime/test-export guards, and diff hygiene succeeded; exact commands and baseline-only exceptions are recorded in `.workitem-artifacts/WI-2128/verification.md`.
**Caveats / Follow-ups:** No migration, documentation correction, or generalized observability work was added. Captured residues remain separate: WI-2892 (correct headerless profile-resolution documentation to caller Person), WI-2893 (initialize vector and pg_trgm extensions in the disposable test database), WI-2894 (make the API integration runner resolve Corepack on Windows), and WI-2896 (restore memory-dedup integration typecheck after the provider contract change); all are outside WI-2128 scope, and WI-2896 is formally outside BID-49.
