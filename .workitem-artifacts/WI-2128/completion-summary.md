**What was done:** Traced the joined-family credential path from the authenticated Clerk subject through caller-Person resolution, organization membership, omitted and explicit profile selection, mobile shell restoration, and cached profile state. Repaired the first authority substitution so each credential remains bound to its own Person while guardians retain only the operation authority granted by an active guardianship over an uncredentialed charge.
**What changed:** API profile-scope resolution now starts from the login-bound caller Person, centralizes self-versus-managed-charge authority, filters profile listing and explicit selection through that authority, denies credentialed-sibling and unrelated-person selection, preserves owner behavior, and emits privacy-safe categorical mismatch telemetry. Profile switching applies the same caller-Person constraint before considering fresh-factor elevation, so factor proof cannot substitute one Person for another. Mobile profile restoration revalidates through a request-local `profileContext: 'omit'` client without clearing shared active-profile or proxy context, then repairs an invalid saved selection or stale proxy state after the authoritative response. It refreshes once per QueryClient/Clerk-session authority boundary plus reconnect and resume boundaries, withholds cached authority during refresh failures, and preserves the learner shell contract.
**Verification:** The mandatory production-only revert reproduced the owner-substitution failure and the exact restore returned the named regression to green. A second real-database RED proved that a fresh-factor learner could switch to the family owner when the caller constraint was omitted; the fix made that case and the complete 16-case credential-authority suite green. Every touched API and mobile suite, API and mobile type checks, targeted lint, the routed TypeScript/i18n/prompt-marker/runtime/test-export guards, and diff hygiene succeeded; exact commands and baseline-only exceptions are recorded in `.workitem-artifacts/WI-2128/verification.md`.
**Caveats / Follow-ups:** No migration, documentation correction, or generalized observability work was added. Captured residues remain separate: WI-2892 (correct headerless profile-resolution documentation to caller Person), WI-2893 (initialize vector and pg_trgm extensions in the disposable test database), WI-2894 (make the API integration runner resolve Corepack on Windows), WI-2896 (restore memory-dedup integration typecheck after the provider contract change), WI-2899 (collapse profile-operation authority listing to one bounded query), WI-2900 (keep cached-profile sessions usable after transient authority refetch failures), and WI-2901 (scope authority-refresh loading to profiles that can confer proxy capability). WI-2899, WI-2900, and WI-2901 are admitted BID-49 members; WI-2896 is formally outside BID-49.
