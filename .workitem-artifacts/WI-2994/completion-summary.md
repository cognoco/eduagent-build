**What was done:** Made terminal account-deletion and subscription-store teardown signals survive immediate rejection and timeout through a first-party durable outbox, immediate observer replay, and a scheduled recovery sweep with deterministic event ids.
**What changed:** Added migration 0164 and its Drizzle schema metadata; added the outbox service and recovery function; made the two terminal handlers persist before dispatch and acknowledge only confirmed sends; made the function-failure observer upsert/replay with five retries; preserved the existing PII-minimized event schemas and manual provider-remediation path; corrected the retention evidence and launch-health runbook ownership language.
**Verification:** Exact commands, exit codes, focused counts, all four transport/recovery variants, production-revert failures, exact-restore SHA-256 hashes, migration guards, full change-class/API unit results, lint/format, database package results, and the guarded deletion-v2 integration result are recorded in `.workitem-artifacts/WI-2994/red-green-revert.md` and `docs/evidence/WI-2994/report.md`.
**Caveats / Follow-ups:** Migration 0164 was generated but never applied; deployment must run it only through the deploy migrator before code that reads the outbox is released. WI-1916 remains responsible only for downstream chat/pager delivery and production-console routing. No environment, provider-console, or alert-routing mutation was performed.
