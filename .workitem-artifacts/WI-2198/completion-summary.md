**What was done:** Bounce-3 rework after reviewer rejection found the second rework still under-tagged AC-4 (233 of 288 rows carried no legacy-* tag, exempted by the legend's own tagging test) and left the Bug DoD's red-green-revert as an uncommitted prose claim. This pass: (1) removed the legend's shell-independent exemption and tagged all 227 previously-untagged rows across every section of the doc, so all 288 row IDs now carry exactly one legacy-* tag except the 6 new V2-only rows; (2) replaced the prior prose-only RGR claim with a committed, re-runnable red-green-revert capture. AC-7's finish (extending `checkFlagTokens` to compare a row's declared flag/shell state against the canonical navigation contract, not just token spelling) is scoped and designed but intentionally not yet implemented this dispatch — see Caveats/Follow-ups.

**What changed:** `docs/flows/mobile-app-flow-inventory.md`'s Status legend (the "Which rows get one of these three tags" paragraph) no longer exempts shell-independent rows from tagging — every retained row gets `legacy-current`, `legacy-superseded`, or `legacy-historical`; genuinely shell-independent rows get `legacy-current` plus a short accurate note (renders pre-shell, reached via equivalent surfaces regardless of shell, orphaned/dormant in every flag state, etc.) instead of being exempted. Applied to all 227 previously-untagged rows: Auth and Access (18), Profiles/Family/Consent/Account (49), Home/Navigation/Subject Setup (17), Learning/Chat/Library/Retention/Progress (38), Practice Hub incl. Quiz/Dictation (35), Homework and Parent Experience (12), Billing and Monetization (16), Regression and System Flows/QA-* (15), and Cross-Cutting Behaviors/CC-* (19, of which CC-19/CC-20/CC-22 were identified as genuinely shell-*specific* and tagged with a note naming the V0/V1-only mechanism instead of the generic shell-independent note). A new `docs/evidence/wi2198-rgr-crosslink-guard.md` captures the guard's row-ID cross-link check (`scripts/check-flow-inventory-cite-rot.ts`'s bare-`V2-NN` rule) failing when the exact reviewer-named "V2-05" bug is reintroduced and passing clean once restored — replacing `evidence.json`'s prior unsupported prose claim with a committed, reproducible capture; `evidence.json`'s `redGreenPointer` now points at this file.

**Verification:** `pnpm tsx scripts/check-flow-inventory-cite-rot.ts` reports clean (449 citations, 288 row IDs, row-id links, flag tokens, and legacy tags all resolve) after the AC-4 tagging pass. The co-located unit suite (`pnpm exec jest --config scripts/jest.config.cjs scripts/check-flow-inventory-cite-rot.test.ts --runInBand --no-coverage`) stays green — 29/29 — before and after. The RGR capture in `docs/evidence/wi2198-rgr-crosslink-guard.md` was executed directly (not transcribed): the guard was run against the doc with `V2-SCOPE-01` in the V2 Shell intro sentence temporarily replaced by the bare `V2-05` (RED, 1 problem reported, exit 1), then restored (GREEN, clean, exit 0) — `git diff` on that line confirms no net change. Every markdown table row was checked programmatically for a trailing pipe (no corruption from the bulk tagging edit).

**Caveats / Follow-ups:** AC-7's second finding (extending `checkFlagTokens` to compare a row's declared flag/shell state against `navigation-contract.ts`/`legacy-navigation-contract.ts`, not just token spelling) is scoped/designed but not implemented in this dispatch — reported separately to the shepherd for a design greenlight before building, per dispatch instructions. This is the one item still open before the bundle is complete. A judgment call surfaced during AC-4 tagging: whether the two audience-agnostic sections (QA-*, CC-*) should carry the V0/V1-legacy tag at all was not explicit in the reviewer's cited examples (all ACCOUNT-*) — resolved in favor of the AC's literal "every retained V0/V1 row" text rather than narrowing it, and flagged to the shepherd as a scope call they may want to revisit before merge. Reconciling AC-5 (unchanged this pass) still leaves the undocumented V2 `subject-hub-screen` gap noted in the prior rework's evidence — out of this docs-only item's scope, tracked in the Gaps table.
