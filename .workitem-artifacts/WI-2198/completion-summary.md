**What was done:** Bounce-3 rework after reviewer rejection found the second rework still under-tagged AC-4 (233 of 288 rows carried no legacy-* tag, exempted by the legend's own tagging rule), left the Bug DoD's red-green-revert as an uncommitted prose claim, and left AC-7's `checkFlagTokens` checking only flag-name spelling, never whether a row's declared shell state matches the canonical navigation contract. This pass: (1) removed the legend's shell-independent exemption and tagged all 227 previously-untagged rows across every section of the doc, so all 288 row IDs now carry exactly one legacy-* tag except the 6 new V2-only rows; (2) replaced the prior prose-only RGR claim with a committed, re-runnable red-green-revert capture; (3) added a fourth mechanical check, `checkNavShellMatrixTabShapes`, that compares the Navigation shell matrix's cited tab-set constants against their real `new Set([...])` literals in source (design pre-approved by the shepherd before implementation).

**What changed:** `docs/flows/mobile-app-flow-inventory.md`'s Status legend (the "Which rows get one of these three tags" paragraph) no longer exempts shell-independent rows from tagging — every retained row gets `legacy-current`, `legacy-superseded`, or `legacy-historical`; genuinely shell-independent rows get `legacy-current` plus a short accurate note (renders pre-shell, reached via equivalent surfaces regardless of shell, orphaned/dormant in every flag state, etc.) instead of being exempted. Applied to all 227 previously-untagged rows: Auth and Access (18), Profiles/Family/Consent/Account (49), Home/Navigation/Subject Setup (17), Learning/Chat/Library/Retention/Progress (38), Practice Hub incl. Quiz/Dictation (35), Homework and Parent Experience (12), Billing and Monetization (16), Regression and System Flows/QA-* (15), and Cross-Cutting Behaviors/CC-* (19, of which CC-19/CC-20/CC-22 were identified as genuinely shell-*specific* and tagged with a note naming the V0/V1-only mechanism instead of the generic shell-independent note). A new `docs/evidence/wi2198-rgr-crosslink-guard.md` captures the guard's row-ID cross-link check (`scripts/check-flow-inventory-cite-rot.ts`'s bare-`V2-NN` rule) failing when the exact reviewer-named "V2-05" bug is reintroduced and passing clean once restored — replacing `evidence.json`'s prior unsupported prose claim with a committed, reproducible capture; `evidence.json`'s `redGreenPointer` now points at this file. `scripts/check-flow-inventory-cite-rot.ts` gains a fourth check, `checkNavShellMatrixTabShapes`, bounded to the doc's "Navigation shell matrix" table: for every cell that cites a known tab-set constant (`LEARNER_TABS`, `GUARDIAN_TABS`, `PARENT_PROXY_TABS`, `FAMILY_MODE_TABS`, `STUDY_MODE_TABS`, `STUDY_TABS`, `FAMILY_TABS`, `PROXY_TABS`, `V2_TABS`) AND spells out the tab names it claims (shorthand cells like "same" or "4 (`STUDY_TABS`)" make no checkable claim and are skipped, not failed), it locates the citation's own enclosing parenthetical group (via a balanced top-level-paren scan, so trailing commentary after the citation and a nested citation like `` `(app)/_layout.tsx:827` `` inside one group never corrupt the claim span) and compares the claimed tabs against the constant's real `new Set([...])` literal, regex-extracted from source — no import/execution of the RN modules. A cited constant whose literal can't be resolved (non-literal declaration) is reported as a failure, never silently skipped.

**Verification:** `pnpm tsx scripts/check-flow-inventory-cite-rot.ts` reports clean (449 citations, 288 row IDs, row-id links, flag tokens, legacy tags, and nav-shell-matrix tab shapes all resolve). The co-located unit suite (`pnpm exec jest --config scripts/jest.config.cjs scripts/check-flow-inventory-cite-rot.test.ts --runInBand --no-coverage`) is green — 40/40 (29 pre-existing + 11 new: 3 for `extractTabSetLiteral`, 8 for `checkNavShellMatrixTabShapes` covering pass/fail/skip/H1-unresolvable/order-independence/trailing-comment-exclusion/clause-scoping). The RGR capture in `docs/evidence/wi2198-rgr-crosslink-guard.md` was executed directly (not transcribed): the guard was run against the doc with `V2-SCOPE-01` in the V2 Shell intro sentence temporarily replaced by the bare `V2-05` (RED, 1 problem reported, exit 1), then restored (GREEN, clean, exit 0) — `git diff` on that line confirms no net change. Ran the new check against the current doc directly (not just via the pass/fail unit tests): all 9 whitelisted tab-set constants resolve to literal Sets (no H1 gaps today), and all 6 currently machine-checkable matrix claims (`LEARNER_TABS`, `V2_TABS`, `GUARDIAN_TABS`, `FAMILY_MODE_TABS`, `FAMILY_TABS`, `PARENT_PROXY_TABS`) match the contract exactly. Standalone strict `tsc --noEmit` and `eslint` on both changed script files are clean. Every markdown table row was re-checked programmatically for a trailing pipe (no corruption; the doc itself is unchanged by this AC-7 rework — only `scripts/check-flow-inventory-cite-rot.ts` and its test file changed).

**Caveats / Follow-ups:** `STUDY_MODE_TABS`, `STUDY_TABS`, and `PROXY_TABS` are cited in the Navigation shell matrix but never spelled out with explicit tab names in any cell (only "same"/count-only shorthand) — the new check correctly has nothing to verify for them today; this is a pre-existing doc-authoring choice, not a gap this check introduces, but if a future edit ever DOES spell out one of those constants' tabs, the check will start verifying it automatically. A judgment call surfaced during AC-4 tagging: whether the two audience-agnostic sections (QA-*, CC-*) should carry the V0/V1-legacy tag at all was not explicit in the reviewer's cited examples (all ACCOUNT-*) — resolved in favor of the AC's literal "every retained V0/V1 row" text rather than narrowing it; the shepherd ruled to keep them tagged (non-narrowing reading), so no further action needed here. Reconciling AC-5 (unchanged this pass) still leaves the undocumented V2 `subject-hub-screen` gap noted in the prior rework's evidence — out of this docs-only item's scope, tracked in the Gaps table.
