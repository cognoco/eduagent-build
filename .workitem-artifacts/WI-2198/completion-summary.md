**What was done:** Reworked WI-2198 after reviewer rejection for under-scoping three Acceptance Criteria against their literal text. AC-4 now tags every row whose own described behavior differs across V0/V1/V2 flag states (not just the four the prior pass tagged), AC-5 now reconciles every distinct cited Playwright/Maestro/Jest manifest in the doc against the maintained file and the CI trigger it runs under (not a subset scoped to batch-touched rows), and AC-7's guard now also flags rows whose declared flag/shell state or row-ID cross-references drift from the canonical navigation contract, not just missing files — including the concrete cross-link bug (a nonexistent "V2-05" reference) the reviewer named.

**What changed:** `docs/flows/mobile-app-flow-inventory.md`'s Status legend states the AC-4 tagging discriminator explicitly (a row is legacy-tagged when its own behavior is tab/hub/mode-specific and V2 removes or replaces it wholesale — Home tab, Library tab, More hub, Progress tab, Recaps tab, ModeSwitcher, the parent-dashboard/child-drill-down route family) and applies it to 51 additional rows: the 14 Home-tab-paradigm rows, the More-hub row (its individual settings children left untagged since V2's AccountAdminSheet still reaches them), the Library tab and its five hosted rows (search, coach card, degraded states), the Progress tab and its seven hosted rows (subject-progress detail, streak, vocabulary browser, bookmarks, session archive, reports — added after a self-check caught that the legend named these two tabs as tagged surfaces while their own defining rows were still untagged, the same under-scoping shape the reviewer's original bounce named), and all 24 live rows in the parent-dashboard/child-drill-down family (PARENT-07 was already removed in a prior refresh, tracked in "Removed in this refresh") — the parent-dashboard verification was done by grepping `mentor.tsx`, `subjects.tsx`, and `journal/index.tsx` for any push to a child-profile route (none exist under V2). 55 rows carry a legacy-* tag in total. A new "Manifest Reconciliation (AC-5)" section adds a per-manifest table (path exists / runs-under-CI-trigger / proves-flow) covering all 187 distinct manifest files the doc cites, with the CI-trigger column derived mechanically from each Maestro flow's real `tags:` frontmatter against `ci-maestro-plan.mjs`'s suite-selection logic and each Playwright spec against its `playwright.config.ts` project. Reconciling this surfaced two concrete drift findings, both corrected in the affected rows: the V2 Shell section's intro falsely claimed no dedicated Maestro/Playwright flow targets the 3-tab shell (three already-landed native Maestro flows do, now cited against the correct rows) and mis-cited the scope-switching journey as "V2-05" instead of V2-SCOPE-01; and BILLING-06's "PARKED / out-of-manifest" claim for `child-paywall.yaml` is stale — the file's live tags carry `nightly` (so it is selected by the scheduled cron despite its own header comment) and its own "KNOWN BREAK" comment documents that it currently times out rather than passing (marked, not fixed — a test-flow/CI-config change is outside this docs-only item's scope). `scripts/check-flow-inventory-cite-rot.ts` gains three checks: row-ID cross-links (a referenced row ID must resolve to a defined or explicitly-removed row, with a dedicated rule for bare "V2-NN" tokens, which are never a real row-ID shape), flag-token existence (every `MODE_NAV_V*_ENABLED` or `EXPO_PUBLIC_ENABLE_MODE_NAV*` token cited must be a real symbol in `feature-flags.ts`), and legacy-tag validity (every inline tag must be one of the three the Status legend defines).

**Verification:** Ran the guard against the doc before fixing the V2-05 reference and confirmed it reported exactly that one problem; fixed the reference and confirmed the guard reports clean across every check (citations, row IDs, flag tokens, legacy tags). The guard's co-located unit suite covers each new check's pass and fail paths, plus a false-positive guard confirming bug-tracker and work-item ID shapes (which share the same row-ID-like pattern but are never real row IDs) are correctly ignored. Ran a standalone strict typecheck and `eslint` on both changed script files (clean), the full incremental workspace typecheck (`tsc --build`, clean), and the branch's change-class router (`check-change-class.sh --branch` against `origin/main`), which classified only the `typescript` fast path for the two script files — already covered above. No product code changed, so no additional test/lint surface applies.

**Caveats / Follow-ups:** AC-4's tagging criterion — legacy-tagged when a row's own behavior is tab/hub/mode-specific — is stated in the doc and applied exhaustively under that stated line; a reviewer could reasonably draw the shell/content boundary elsewhere for a specific row (e.g. whether Library-tab downstream content like shelf/book/topic screens should also carry a tag), and this pass left content-mechanics rows (quiz, dictation, session mechanics, most settings/auth/billing) untagged on the basis that they behave identically regardless of entry tab. Reconciling AC-5 surfaced an undocumented V2 screen: `apps/mobile/e2e/flows/v2/v2-shell-navigation.yaml` asserts a `subject-hub-screen` testID (`(app)/subject-hub/[subjectId]`, `components/subject-hub/*`) distinct from the legacy shelf/book pair — flagged as a Gap, not documented here, since fully describing a new V2 journey is AC-3 territory and AC-3 was verified as already satisfied by the prior landed pass, not reworked in this pass. `child-paywall.yaml`'s nightly-tag/KNOWN-BREAK contradiction is marked in both the row and the reconciliation table but not fixed (test code, outside this docs-only item's Effort=M scope) — flagged as a Gap with the two possible fixes named. The guard's row-ID cross-link check resolves bare ID-shaped tokens to defined-or-removed rows; it does not (and per the AC's "not a new harness" refinement, should not) semantically diff a row's prose claim against `navigation-contract.ts`'s actual gating logic beyond the existing symbol/line-count check — a row could still assert an inaccurate behavioral claim about a real, existing gate without the guard catching it.
